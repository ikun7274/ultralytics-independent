# 背景切片（empty tile）配额机制 与 "原图回退" 答疑

> 场景（用户原话）：400 张图，`slice_prob=True` → 在线 SAHI 切片；`slice_ratio=0.25` → 每轮随机选 100 张切片，切出 400 张；`slice_all_tiles=True` → 这 400 张切片全进池；`slice_background_ratio=0.25` → 按有目标切片数保留背景，问：剩余背景切片能否删除或别的处理？

> ⚠️ **本文写于 `img_origin` 重构之前，文中的"池大小/未选中整图槽"是旧布局的数字。** 当前布局为
> `base = n_per*K_slice`（**未选中的图在 base 段不占位**）、`origin = N`（`img_origin=True`）。
> 因此本文所有含 `(N − K_slice)` 未选中整图槽的合计（如 700/800）需按新公式重算；
> **"275 张整图回填"这一核心现象本身不受影响**（它发生在 base 段内，与未选中图无关）。
> 另外 `slice_keep_origin` 已更名 `img_origin`（语义扩大为全局覆盖，段宽 `K_slice` → `N`）。

## 结论：你的模型基本对，三处修正

1. **`slice_prob=True` 是"总开关"（on/off），不是强度。** 强度由 `slice_ratio` 管。你这条链路本身没问题。
2. **算术更正**：400 张切片里 = **100 张正片 + 300 张背景片**（不是 100 张背景）。配额 `0.25 × 100 = 25` 张背景被保留 → **275 张被拒**（你写的"75"是算错了，300−25=275）。
3. **被拒的背景片既不能被"删除"，也不会留在池里当空片** —— 见下。

## 关键机制：被拒背景片变成"整图回填"

代码唯一真源（`ultralytics_ooo/pool/augment_setup.py`，`_get_slice`）：

```python
if len(idx) == 0:  # 背景（空）切片
    if self._allow_background():        # 配额没超 → 发空片（负样本）
        return sub, self._empty_label(sub, label)
    # 配额超了 → 返回【原图】，绝不会是空片
    return img, label                   # ← 整图回填（"原图直通"）
```

`slice_all_tiles=True` 时，每张被 `slice_ratio` 选中的图在 base 段**预分配了 `n_per`(=4) 个槽位**。当某个背景切片超出配额，槽位已经存在、不能凭空消失，于是代码把**整张原图**塞回这个槽位。换句话说：被拒的 275 个槽位 = 275 张整图，正是你担心的"原图激增"。

## 为什么不能"直接删除"这些槽位

池长 `len(dataset)` 由 `_segment_lengths()` **固定**（与 epoch、与"抽中了谁"无关）：

```
base = n_per*K_slice                        # 当前：只有被选中的图占位，且每图 n_per(=4) 槽
# 旧布局（本文其余算术仍按此）：base = n_per*K_slice + (N - K_slice)
```

DataLoader、分组采样器 `GroupedImageSampler`、以及 mosaic 的 `nb`/`nw` 全部依赖这个固定长度做 buffer 记账（`random.choices(dataset.buffer)` 取出的值直接当池索引用）。**中途删 275 个槽位 → 池长变短 → 旧索引越界 IndexError**，是结构性破坏，不是"换个内容"。所以代码选择"回填整图"让槽位保持合法，而不是删除。

## 那 275 张怎么处理？—— 两个已支持的旋钮

| 方案 | 配置 | 结果 | 取舍 |
|---|---|---|---|
| **A. 保留为空片负样本** | `slice_background_ratio = -1` | 配额 `_bg_count < _pos_count * neg_ratio` 永不满足 → 300 张背景片**全部作为空片保留**，切片侧不再产生整图回填 | 池里有 300 张近似重复的空片（稀疏数据友好、能当硬负样本；密集数据冗余） |
| **B. 从源头减少槽位** | `slice_all_tiles = False`（Plan A） | 每张选中图只产 **1 个 slot**，根本没有 300 个背景 slot 可拒；池缩到 ~N | 失去 4 切片密度，但"原图"最少 |

> B+ 布局（已落地）已经把**未选中图**的槽位从 4 降到 1，把"原图回退"占比从 91% 压到 ~0%。**残余的原图膨胀只剩 `slice_all_tiles=True` + `slice_background_ratio < 1` 这一处交互** —— 也就是你这次问的 275 张。

**直接回答"能否删除"**：不能靠删；要么用 `-1` 让它们当空片负样本留下（方案 A），要么用 `slice_all_tiles=False` 让它们根本不产生（方案 B）。

## 额外提醒：`slice_background_ratio` 是"运行配额"不是"固定比例"

判据 `_allow_background()`：`_bg_count < _pos_count * neg_ratio`，按**进程累计、每 epoch `reset_counters()` 重置**。所以早期正片少时，背景片更容易被替换——实际保留比例≈配置值，但不严格等于。多 worker 时每进程独立计数，是设计内的近似。

## 你的 400 图场景，池构成（解析值）

`N=400, K_slice=100, n_per=4, bg=0.25`：
- base = `4×100 + (400−100)` = **700** 槽
  - 其中 400 切片槽 = 100 正片 + **25 空片负样本** + **275 整图回填**
  - 另 300 槽 = 300 张未选中图的整图
- origin（`img_origin` 开）= `N` = **100 槽**（每张原图 1 个整图视图；旧文案写 `K_slice`，见顶部提示）
- **池总计 = 800**（`img_origin` 关则 700）

> 数字由布局公式解析得出（未跑实证脚本）。实证脚本在 `%TEMP%\ooo_audit\probe_bg_quota.py`，需要可跑。

## 上层参数解法矩阵（不改底层逻辑）

你问的"底层逻辑无法改动，能否从上层/参数下手"——**可以**。`_emit` 里 `return img, label` 那行不用动，下面这些都是**只改配置**就能改变 275 张的去向：

### 机制前提（读代码确认的）
- `slice_all_tiles=False` → `_n_per()=1`：每张被选中图在 base 段只占 **1 个槽**，且 `OnlineSlice.__call__` 走 `k = random.randrange(4)` —— **每轮每张图只随机取 1 片**（不是 4 片全发）。
- 背景配额判据 `_allow_background()` 在两种模式都生效；配额超了就回填整图（line 450）。`bg=-1` 时配额永不满足 → 空片一律作为**子图裁剪的空片负样本**发出（line 446，`sub = img[y0:y1,x0:x1]`，分辨率是 tile 不是整图），**绝不回填整图**。

### 你的 400 图场景（25% 切片密度）四条配置对比

| 配置 | 正片 | 空片负样本 | **整图回填(回退)** | 未选中整图 | 池大小 |
|---|---|---|---|---|---|
| `all_tiles=T, bg=0.25`（现状） | 100 | 25 | **275** | 300 | 700 |
| `all_tiles=T, bg=-1` | 100 | 300 | **0** | 300 | 700 |
| `all_tiles=F, bg=0.25` | ~25 | ~25 | **~50** | 300 | 400 |
| `all_tiles=F, bg=-1` | ~25 | ~75 | **0** | 300 | 400 |

（`img_origin` 关；开则额外 +`N` 个整图视图，段宽为原图数而非选中数。mode A 正片数随密度随机，25% 密度下约 25/100。）

### 逐个解法说明
1. **`slice_background_ratio=-1`（推荐，零逻辑改动，保住全部正片）**
   配额永不超限 → 300 张背景片**全部作为空片负样本保留**，切片侧不再产生整图回填。275 张"问题片"变成 275 张空片负样本，**正片 100 张一颗不丢**。代价：池里多了 300 张近似重复的空片（稀疏/小目标数据其实有益，密集数据冗余）。**这是"既要消灭原图激增、又要保住正片"的唯一纯参数解。**
2. **`slice_all_tiles=False`（你提到的旋钮，确实有效，但牺牲密度）**
   每张选中图只占 1 槽、只随机发 1 片 → 根本没有 4 个背景槽可拒，275 直接降到 **~50**（bg=0.25）或 **0**（bg=-1）。代价：正片从 100 掉到 **~25**（25% 密度下随机取片只有 1/4 命中含目标片）。**这是"最小池 + 零回退"的纯参数解，但小目标正片少 4 倍。**
3. **`slice_ratio` 调小**（降 magnitude，非根治）
   选中图更少 → 4 槽组更少 → 背景槽总数更少 → 275 随之缩小；但同时正片数也按比例降。
4. **`img_origin=False`**（旧名 `slice_keep_origin=False`；只去"额外"整图，不碰 275）
   关掉 origin 段的整图视图，但 base 段里的 275 回填整图**不受影响**，只能算辅助手段。

### 结论
- 想**消灭 275 且保住全部正片** → `slice_all_tiles=True` + `slice_background_ratio=-1`（纯参数，零底层改动）。
- 想**池最小且零回退、能接受正片减半再减半** → `slice_all_tiles=False` + `slice_background_ratio=-1`。
- 你直觉里的 `slice_all_tiles` 下手**方向正确**，但它本质是"减少可拒槽位"而非"改变回填行为"；要彻底不回填，必须配合 `bg=-1`，或接受 mode A 的正片损失。

> 注：表中 mode A 的 ~25 正片是随机取片的结果（25% 密度），不同密度会变。需要精确数字可跑 `tools/ooo_slice_knobs.py`（`--all-tiles off --prob 1.0 --grid` 扫 bg）。

## 顺手修的一处笔误
`dataset.py:871` docstring 写 `3*K_slice`，与 909 行代码 `n_per*K_slice` 不符（2×2 SAHI 下 `n_per=4`），已改为 `n_per*K_slice + (N - K_slice)`，与代码一致。
