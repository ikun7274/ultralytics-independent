# 背景切片（empty tile）配额机制 与 "原图回退" 答疑

> 场景（用户原话）：400 张图，`slice_prob=True` → 在线 SAHI 切片；`slice_ratio=0.25` → 每轮随机选 100 张切片，切出 400 张；`slice_all_tiles=True` → 这 400 张切片全进池；`slice_background_ratio=0.25` → 按有目标切片数保留背景，问：剩余背景切片能否删除或别的处理？

> ✅ **本文数字已实体化复核**（2026-09-15）：不再是解析推演。做法是造一个 400 图合成集（每图恰好 1 个
> 目标框、钉在某个象限中心，所以任意 100 张被选中的图各产出 1 张正片），再用 `tools/ooo_slice_knobs.py`
> 逐槽位统计池中各槽的**实际内容**。**实测与解析预测逐位吻合**：100 正片 / 25 空片 / 275 整图回填 /
> origin 400。复现命令见文末「复现命令」。
>
> 布局口径：当前为 `base = n_per*K_slice`（**未被选中的图在 base 段不占位**）、`origin = N`（`img_origin=True`，
> 默认开启）。旧文里的 `(N − K_slice)` 未选中整图槽已不存在；`slice_keep_origin` 已更名 `img_origin`
> （语义扩大为全局覆盖，段宽 `K_slice` → `N`）。

## 结论：你的模型基本对，两处需要修正

1. **`slice_prob=True` 是"总开关"（on/off），不是强度。** 强度由 `slice_ratio` 管。你这条链路本身没问题。
2. **算术确认**：400 个槽位里 = **100 张正片 + 300 张背景片**；配额 `0.25 × 100 = 25` 张背景被保留 → **275 张被拒**（300 − 25 = 275）。这三个数已实测坐实。
3. **被拒的背景片既不能被"删除"，也不会留在池里当空片** —— 见下。

## 关键机制：被拒背景片变成"整图回填"

代码唯一真源（`ultralytics_ooo/pool/augment_setup.py`，`OnlineSlice._emit`，第 474–484 行）：

```python
if len(idx) == 0:  # 背景（空）切片                      # 474
    if self._allow_background():        # 配额没超 → 发空片（负样本）
        return sub, self._empty_label(sub, label)      # 480
    # 配额超了 → 返回【原图】，绝不会是空片
    return img, label                   # ← 整图回填（"原图直通"）  # 484
```

`slice_all_tiles=True` 时，每张被 `slice_ratio` 选中的图在 base 段**预分配了 `n_per`(=4) 个槽位**。当某个背景切片超出配额，槽位已经存在、不能凭空消失，于是代码把**整张原图**塞回这个槽位。换句话说：被拒的 275 个槽位 = 275 张整图，正是你担心的"原图激增"。

## 为什么不能"直接删除"这些槽位

池长 `len(dataset)` 由 `_segment_lengths()` **固定**（与 epoch、与"抽中了谁"无关）：

```
base = n_per*K_slice                        # 只有被选中的图占位，且每图 n_per(=4) 槽
```

DataLoader、分组采样器 `GroupedImageSampler`、以及 mosaic 的 `nb`/`nw` 全部依赖这个固定长度做 buffer 记账（`random.choices(dataset.buffer)` 取出的值直接当池索引用）。**中途删 275 个槽位 → 池长变短 → 旧索引越界 IndexError**，是结构性破坏，不是"换个内容"。所以代码选择"回填整图"让槽位保持合法，而不是删除。

## 那 275 张怎么处理？—— 两个已支持的旋钮

| 方案 | 配置 | 结果 | 取舍 |
|---|---|---|---|
| **A. 保留为空片负样本** | `slice_background_ratio = -1` | 配额 `_bg_count < _pos_count * neg_ratio` 永不满足 → 300 张背景片**全部作为空片保留**，切片侧不再产生整图回填 | 池里有 300 张近似重复的空片（稀疏数据友好、能当硬负样本；密集数据冗余） |
| **B. 从源头减少槽位** | `slice_all_tiles = False`（Plan A） | 每张选中图只产 **1 个 slot**，根本没有 300 个背景 slot 可拒；池缩到 ~N | 失去 4 切片密度，但"原图"最少 |

> B+ 布局（已落地）移除了**未选中图占 4 槽**那一类膨胀（现在未选中的图在 base 段一个槽都不占）。
> **残余的原图膨胀只剩两处**：① `slice_all_tiles=True` + `slice_background_ratio < 1` 的这 275 张回填；
> ② `img_origin=True` 的 `N` 个整图视图（默认开启，是**有意**的覆盖保险，不是膨胀缺陷）。
> 你这次问的是 ①。

**直接回答"能否删除"**：不能靠删；要么用 `-1` 让它们当空片负样本留下（方案 A），要么用 `slice_all_tiles=False` 让它们根本不产生（方案 B）。

## 额外提醒：`slice_background_ratio` 是"运行配额"不是"固定比例"

判据 `_allow_background()`：`_bg_count < _pos_count * neg_ratio`，按**进程累计、每 epoch `reset_counters()` 重置**。这是个**令牌桶**，不是固定比例：每出现 1 张正片配额只涨 0.25，所以 `_bg_count` 的长期上限 ≈ `floor(正片数 × 0.25) + 1`。多 worker 时每进程独立计数，是设计内的近似。
**实测**：100 张正片 → 恰好保留 **25** 张空片（`bg=0.25`）；`bg=0` → 0 张；`bg=-1` → 全部 300 张。

## 你的 400 图场景：池构成（**实测值**）

`N=400, slice_ratio=0.25 → K_slice=100, slice_all_tiles=True, bg=0.25, img_origin=True`（默认开）：

| 段 | 槽数 | 内容 |
|---|---|---|
| base | **400** = `n_per × K_slice` = `4 × 100` | **100 正片** + **25 空片负样本** + **275 整图回填** |
| origin | **400** = `N` | 每张原图 1 个整图视图 |
| **池总计** | **800** | 675 张整图（400 origin + 275 回填）· 25 空片 · **100 真切片** |

最刺眼的一行：**池里真正是"切片"的只有 125 张（100 正 + 25 空），占 800 的 15.6%**；其余 84% 都是整图。
`img_origin=False` 时池降到 400（少掉 origin 那 400），base 段里的 275 回填**不变**。

> 实测复现见文末「复现命令」。这些数字是 `tools/ooo_slice_knobs.py` 逐槽位统计的真实内容，不是公式推演。

## 上层参数解法矩阵（不改底层逻辑）

你问的"底层逻辑无法改动，能否从上层/参数下手"——**可以**。`_emit` 里 `return img, label` 那行不用动，下面这些都是**只改配置**就能改变 275 张的去向：

### 机制前提（读代码确认的）
- `slice_all_tiles=False` → `_n_per()=1`：每张被选中图在 base 段只占 **1 个槽**，且 `OnlineSlice.__call__` 走 `k = random.randrange(4)` —— **每轮每张图只随机取 1 片**（不是 4 片全发）。
- 背景配额判据 `_allow_background()` 在两种模式都生效；配额超了就回填整图（`augment_setup.py:484`）。`bg=-1` 时配额永不满足 → 空片一律作为**子图裁剪的空片负样本**发出（`augment_setup.py:473` 的 `sub = img[y0:y1, x0:x1]`，分辨率是 tile 不是整图），**绝不回填整图**。

### 你的 400 图场景：六条配置对比（**实测**，同一份 400 图数据）

| 配置 | 正片 | 空片负样本 | **整图回填** | origin 整图 | 池大小 |
|---|---|---|---|---|---|
| `all_tiles=T, bg=0.25`（你的现状） | **100** | **25** | **275** | 400 | **800** |
| `all_tiles=T, bg=0` | **100** | 0 | 300 | 400 | 800 |
| `all_tiles=T, bg=-1` | **100** | 300 | **0** | 400 | 800 |
| `all_tiles=F, bg=0.25` | 19–24 | 5 | **76** | 400 | 500 |
| `all_tiles=F, bg=-1` | 19–24 | 76 | **0** | 400 | 500 |
| `all_tiles=T, bg=0.25, img_origin=F` | **100** | 25 | **275** | 0 | 400 |

读法：
- **只有 `bg=-1` 能把回填压到 0**。`all_tiles=F` 单独改做不到：它仍有 76 个回填槽（盲抽到的那 1 片若为空，
  配额一满照样回填整图）。
- `all_tiles=F` 的正片掉到 19–24 且**每次运行都不同**：它走 `k = random.randrange(4)` **盲抽 1 片**，另 3 片
  的内容直接丢弃。理论上限 ≈ `100 × 1/4 = 25`。
- **池大小与 `bg` 完全无关（恒为 800）** —— 这就是"不能靠删除"的实证：改 `bg` 只换槽位内容，不动长度。

### 逐个解法说明
1. **`slice_background_ratio=-1`（推荐，零逻辑改动，保住全部正片）**
   配额永不超限 → 300 张背景片**全部作为空片负样本保留**，切片侧不再产生整图回填。275 张"问题片"变成 275 张空片负样本，**正片 100 张一颗不丢**。代价：池里多了 300 张近似重复的空片（稀疏/小目标数据其实有益，密集数据冗余）。**这是"既要消灭原图激增、又要保住正片"的唯一纯参数解。**
2. **`slice_all_tiles=False`（你提到的旋钮，确实有效，但牺牲密度）**
   每张选中图只占 1 槽、只盲抽 1 片 → 可拒背景槽从 300 降到 0，回填从 275 降到 **76**（bg=0.25）或 **0**（bg=-1）。代价：正片从 100 掉到 **19–24**（盲抽 1/4 片）。**这是"最小池 + 零回退"的纯参数解，但小目标正片少约 5 倍——且它单独用并不能把回填清零，必须配上 `bg=-1`。**
3. **`slice_ratio` 调小**（降 magnitude，非根治）
   选中图更少 → 4 槽组更少 → 背景槽总数更少 → 275 随之缩小；但同时正片数也按比例降。
4. **`img_origin=False`**（旧名 `slice_keep_origin=False`；只去"额外"整图，不碰 275）
   关掉 origin 段的整图视图，但 base 段里的 275 回填整图**不受影响**，只能算辅助手段。

### 结论
- 想**消灭 275 且保住全部正片** → `slice_all_tiles=True` + `slice_background_ratio=-1`（纯参数，零底层改动；实测回填 0、正片 100）。
- 想**池最小且零回退** → `slice_all_tiles=False` + `slice_background_ratio=-1`（实测池 500、回填 0）。
  缺点原本是正片同时掉到 **19–24**（盲抽 1/4 片）——**该缺点已可消除**：加 `slice_target_tiles=True` 即得
  「1 图 1 槽 + 几乎全是正片」（实测 400 图集 100 槽 / 正片 24 → 100，池长不变）。见下文第四个解法。
- 你直觉里的 `slice_all_tiles` 下手**方向正确**，但它本质是"减少可拒槽位"而非"改变回填行为"；要彻底不回填，**必须配合 `bg=-1`**，或接受盲抽带来的正片损失（后者可用 `slice_target_tiles=True` 换成按目标挑片）。
- 如果**漏检（FN）是你的主要痛点**，别用 `bg=-1`（那会往池里塞 300 张空片）；用 `bg=0` 让 300 张全变整图，或直接用 `img_origin=True` 保底覆盖。反过来，**误检（FP）多**才值得留空片负样本。

## 稀释的真正来源：275 张里没有一张是"新内容"（逐槽追源，实测）

上面只说了 275 张会变成整图。**逐槽位追源**之后，比"整图多"更严重的一点是：**它们全是重复**。

| 配置 | 池 | 切片槽 | 正片 | 空片 | 整图槽 | 其中重复 | 每张被选中图以整图出现的次数 |
|---|---|---|---|---|---|---|---|
| A 现状 `r=.25 bg=.25 all_tiles origin` | 800 | 125 | 100 | 25 | 675 | **275** | **3.75×** |
| B `r=.25 bg=-1 all_tiles origin` | 800 | 400 | 100 | 300 | 400 | 0 | 1× |
| K `r=.25 bg=.25 all_tiles NO-origin` | 400 | 125 | 100 | 25 | 275 | **175** | 2.75× |

A 的 675 个整图槽 = 275（回填）+ 400（origin 段）。而 275 个回填槽只覆盖 **100 张不同的图**
（重复计数 `dup=175`），且这 100 张**全部已经在 origin 段各有一个整图槽**。

所以结论要换个说法：不是"整图稀释了切片"，而是**同一张整图把切片挤掉了** —— 每张被选中图每 epoch
以整图形式出现 3.75 次，其中 2.75 次纯属副本。由此得到一条硬规则：

> **`img_origin=True` 时不要配 `slice_background_ratio > 0`。**
> origin 已经保证"每张图每 epoch 恰好出现一次整图"；此时 bg 配额把空片换成的整图，
> **只能**是已在池中的图的副本。A → B：池大小不变（800），切片槽 125 → 400（15.6% → 50%），
> 重复整图 275 → 0，正片一颗不丢。

### 三个反直觉的实测结论

1. **正片数只由 `slice_ratio` 决定**，`bg` / `img_origin` 一点都不影响它：
   `r=.25 → 100`、`r=.5 → 200`、`r=1.0 → 400`（A/B/C 全 100；D/E/F 分别 100/200/400）。
   → 冲小目标召回就调 `slice_ratio`；`bg` / `img_origin` 只决定"剩下的槽位装什么"。
2. **低密度下 `bg` 几乎是二值的**：配额判据是 `空片 ≤ bg × 正片`，而稀疏场景正片只占全部切片的 1/4，
   要留住 4 片就得 `bg ≥ 3`。所以 `0.25 → 0.5` 只把切片占比从 15.6% 抬到 18.8%（A→C），
   而 `→ -1` 直接到 50.0%（B）。**微调 `bg` 不是通往"以切片为主"的路。**
3. **`all_tiles=False` 是省成本旋钮，不是召回旋钮**：`OnlineSlice.__call__` 用 `k = random.randrange(4)`
   **盲抽**（`augment_setup.py:547`，在算几何之前就定了片号），命中框概率 ≈ 1/4。
   池子缩小 4 倍、切片占比 100%，但**正片塌掉**：`r=.25` 100 → 19，`r=1` 400 → 86。

### 推荐配方（同一份 400 图，实测）

| 目标 | 配方 | 池 | 切片槽 | 正片 | 切片占比 | 重复整图 |
|---|---|---|---|---|---|---|
| 只让切片不再被稀释（最小改动） | `r=.25 bg=-1 all_tiles origin` | 800 | 400 | 100 | 50.0% | 0 |
| **保覆盖 + 切片为主（推荐起点）** | `r=.5 bg=-1 all_tiles origin` | 1200 | 800 | 200 | 66.7% | 0 |
| 同上，满覆盖 | `r=1.0 bg=-1 all_tiles origin` | 2000 | 1600 | 400 | 80.0% | 0 |
| 纯切片池（放弃整图视角） | `r=.5 bg=-1 all_tiles NO-origin` | 800 | 800 | 200 | 100% | 0 |

### 第四个解法：按目标挑片 —— `slice_target_tiles`（**已实现**，`slice_all_tiles=False` 专用）

`all_tiles=True` 的浪费是**结构性**的：它给每张被选中图先分 4 个槽，而稀疏场景 4 片里只有 1 片可能有目标
（实测正片密度 25%），剩下 3 个槽无论给空片还是给整图，都不可能是"新的正片"。也就是说：

> 在 `slice_ratio` 已经定死的池宽里，**正片数有硬上限 = `K_slice × 每图平均正片片数`**。
> 把空片换成整图、调 `bg`、开关 `img_origin` 都动不了这个上限。

`slice_all_tiles=False` 反而是对的结构（1 图 1 槽），但默认**挑片方式是盲抽**（`random.randrange(4)`，
实测只有 ~1/4 概率抽到含目标那片），白丢 3/4 的机会。`slice_target_tiles=True` 就是把这个盲抽换掉：

- **调度单位是"片"不是"图"**：先把全库所有"含目标的 (图, 片)"排成一个队列并打乱；
- **每轮取队列的下一段**（段长 = `K_slice`），所以**连着两轮不会有同一个单位**；
- **整个队列走完才回绕**，回绕时**重新洗牌**（= 你要的"重新开始选取"）；
- 段的划分只由 `(数据集文件表, epoch)` 决定，**不加任何全局随机**，所以主进程与每个 worker 各自算出的
  调度必然一致（实测两次独立进程输出逐字相同），也不会扰动其它分支的抽样。

实测（同一份 400 图合成集，每图 1 个 24×24 象限框 → 队列 400 单位）：

| 配置（`ratio=0.25`, `img_origin=False`, `bg=-1`） | base 槽 | 正片 | 空片 | 整图 | 池 |
|---|---|---|---|---|---|
| `slice_target_tiles=False`（默认，盲抽） | 100 | **24** | 76 | 0 | 100 |
| `slice_target_tiles=True` | 100 | **100** | 0 | 0 | 100 |

调度本身（`ratio=0.25` → 每轮 100 槽，队列 400 → 一轮 4 个 epoch）：

```
epoch 0..3  block 1..4/4  pass 1   每轮 100 个互不重复的单位，并集 = 全部 400
epoch 4     block 1/4     pass 2   重新洗牌后重新开始
日志：augment masks @ epoch 0: slice 100/400 (ratio 0.25) | target tiles: 100 units of 400 (block 1/4, pass 1)
```

**代价与注意**：
- 池里几乎**没有空片**了（全是正片会推高背景误检）→ 要负样本就把 `slice_ratio` 调小，或开 `img_origin`，
  或让其它分支（blur/weather/occlusion）供负样本。
- 只在 `slice_prob>0` **且** `slice_all_tiles=False` 时有效；`all_tiles=True` 或切片关闭时会**告警一次**
  （那两种形态下根本没有"挑哪一片"这回事）。
- 无任何框的图（纯背景图）**不进队列**，它们的入池只靠 `origin` 段 / 其它分支。
- `K_slice` 大于队列长度时（例如 N=8 但只有 2 张图有框），会在一轮内重复单位并**告警一次**，提示调小 `slice_ratio`。
- 目标位置跨切缝时，调度使用的"含目标片"是按**居中网格**算出的（这样不必解码图片，只读标注缓存）；
  真正发射时若 `center_bias` 移动了切缝，会**按实际网格重新挑一片仍含目标的片**兜底，不会退化成空片。
- 队列在首个 epoch 之前构建一次、之后整轮复用（每个进程各一份，只扫标注缓存、**不解码图片**；
  数千张图约 1 秒量级），所以它不是每个 epoch 的固定开销。

## 复现命令

```bash
# 造 400 图合成集：每图 1 个 24x24 框钉在象限中心 -> 任意 100 张被选中的图各产出 1 张正片
python make.py    # 脚本见下文「复现脚本」；产物：images/ + labels/ + data.yaml

# 逐槽位统计池内容（不加 --img-origin 则 origin 段为 0）
python tools/ooo_slice_knobs.py --data data.yaml --imgsz 320 \
    --prob True --ratio 0.25 --bg 0.25 --all-tiles --img-origin
# -> pool=800  base=400  origin=400  K_slice=100/400
#    slots=800  positive=100 (12.5%)  empty=25  whole=675

# 六条配置扫描
for bg in -1 0 0.25; do python tools/ooo_slice_knobs.py --data data.yaml --imgsz 320 \
    --prob True --ratio 0.25 --bg $bg --all-tiles --img-origin; done
python tools/ooo_slice_knobs.py --data data.yaml --imgsz 320 \
    --prob True --ratio 0.25 --bg -1 --one-tile --img-origin
python tools/ooo_slice_knobs.py --data data.yaml --imgsz 320 \
    --prob True --ratio 0.25 --bg 0.25 --all-tiles      # 不加 --img-origin
```

### 复现脚本（`make.py`）
```python
import pathlib, cv2, numpy as np
ROOT = pathlib.Path(__file__).parent
(ROOT / "images").mkdir(exist_ok=True); (ROOT / "labels").mkdir(exist_ok=True)
S, B = 96, 24                                       # 96x96 图 / 24x24 框
QUAD = [(24, 24), (72, 24), (24, 72), (72, 72)]     # 象限中心：避开 20% 重叠带，保证恰好落在 1 片里
for i in range(400):
    img = np.random.default_rng(i).integers(0, 255, size=(S, S, 3), dtype=np.uint8)
    cv2.imwrite(str(ROOT / "images" / f"img_{i:04d}.png"), img)
    cx, cy = QUAD[i % 4]
    (ROOT / "labels" / f"img_{i:04d}.txt").write_text(
        f"0 {(cx + .5) / S:.6f} {(cy + .5) / S:.6f} {B / S:.6f} {B / S:.6f}\n", encoding="utf-8")
(ROOT / "data.yaml").write_text(
    f"path: {ROOT.as_posix()}\ntrain: images\nval: images\nnames:\n  0: obj\n", encoding="utf-8")
```

> 注意：`tools/ooo_*.py` 原先只能通过 `python -m tools.xxx` 或设置 `PYTHONPATH` 才能跑（直接
> `python tools/xxx.py` 会因为 `sys.path[0]` 是 `tools/` 而 `ModuleNotFoundError: ultralytics`）。
> 现已给 4 个工具补上仓库根目录引导，**上面的命令可以原样复制执行**。

## 顺手修的一处笔误
`dataset.py:871` docstring 曾写 `3*K_slice`，与 `_segment_lengths()` 的实际实现 `n_per*K_slice` 不符
（2×2 SAHI 下 `n_per=4`），已改为与代码一致。`origin` 段的宽度也已按 `img_origin` 重构更新为 `N`。
