# ultralytics_ooo 代码质量审查报告

> 审查对象：`C:\Users\Administrator\Desktop\ultralytics-main`
> 审查范围：`ultralytics_ooo/`（15 个 Python 文件，约 3 500 行）+ `tests/test_ooo_*.py` + `train.py`
> 明确排除：`ultralytics/`（干净上游 8.4.126 原版，未修改，不作为本项目代码审查）
> 审查日期：2026-09-15　｜　审查方式：从零开始全量静态审查 + 运行时实证复现
> 环境：conda `D:\miniconda3\envs\langchain`（Python 3.12.13 / torch 2.5.1 / opencv 5.0.0.93 / ultralytics 8.4.126 指向本仓库）

---

## 一、审查方法

本轮审查忽略此前任何结论，独立完成，包含四条互补的证据链：

| 证据链 | 做法 |
|---|---|
| **人工通读** | 逐行阅读 `ultralytics_ooo` 全部 15 个文件与 4 个项目测试文件，对照上游 `engine/trainer.py`、`engine/validator.py`、`data/build.py`、`data/dataset.py` 的调用契约 |
| **自制 AST 静态检查** | 环境内无 ruff/flake8/pyflakes，故自建 `ast` 检查器：未使用导入(F401)、模块级重定义/遮蔽(F811)、未使用局部导入与形参、重复函数体指纹、裸/宽泛 except、可变默认参数、浮点相等比较、超长函数。脚本见附录 A |
| **配置一致性核对** | 把 `pool/constants.py::_ONLINE_DEFAULTS`（174 条）与上游 `DEFAULT_CFG_DICT` 逐键比对 |
| **运行时实证** | 全部性能数字与缺陷均为实测复现（微基准 + 缺陷最小复现脚本），非推断。见附录 A |

**基线状态**：`pytest tests/test_ooo_{core,augment,resume,valslice}.py -q` → **16 passed**。即下述问题**全部存在于"当前 16 项测试全绿"的状态下**，属测试覆盖盲区。

---

## 二、结论摘要

| 严重程度 | 数量 | 分布 |
|---|---|---|
| **严重** | 3 | S1 双口径验证静默退化（正确性）；S2 六个保存分支默认全开导致取样路径被同步写盘主导（性能）；**S3（修复阶段新增）三个"每 epoch 增强比例"开关从未到达 dataset，配置被静默忽略（正确性）** |
| **中等** | 9 | 采样种子失效、复制上游实现漂移、浮点相等判定、配置表复述上游、静默异常吞没、每轮重建 val 集、位置传参失配、切片参数校验缺口、保存目录延迟校验 |
| **轻微** | 14 | L1 10 处死导入 ｜ L2 `_save_cap` 双实现遮蔽 ｜ L3 `__all__` 不完整 ｜ L4 三处文档与实现矛盾 ｜ L5 tile 计算浪费 ｜ L6 val 侧全分辨率 memcpy ｜ L7 工作分辨率上限可关闭 ｜ L8 OpenCV 全局 RNG 污染 ｜ L9 临时目录泄漏 ｜ L10 未使用形参 ｜ L11 模块职责混杂 ｜ L12 分支骨架重复 ｜ L13 bbox 转换重复 ｜ L14 硬编码绝对路径 |

**总体评价**：这个包的工程质量**明显高于**同类"深度侵入改造 + 运行时 patch"项目——注释密度与"为什么这样写"的记录非常少见地扎实，`_n_per()` / `_segment_lengths()` / `_finalize_label()` 的"单一真源"重构方向正确，`core/` 的零依赖边界保持得干净，幂等 patch 与跨进程掩码同步的设计有想法。

但存在一个**结构性的系统风险**：包的核心策略是"运行时替换上游内部实现"，而其中最关键的一处（`build_dataloader` 尾部）是**逐行复制上游代码**，另一处（双口径）**依赖上游 `self.dataloader or ...` 的短路语义却未考虑其副作用**。这两处正是严重/中等问题 S1、M1、M2 的来源——**不是写法问题，而是"侵入式 patch 缺少行为对齐测试"的方法论问题**。

---

## 三、严重问题

### S1 【严重·正确性】`val_slice_dual_metric` 从第 2 个 epoch 起静默退化为整图单口径

**问题位置**
- `ultralytics_ooo/pool/dual.py:52-68`，关键行 `dual.py:62`
  ```python
  vargs.val_slice_enable = False
  v.dataloader = v.get_dataloader(v.data["val"], self.batch_size)   # ← 把 validator 的 loader 换成整图 loader
  return v(self)
  ...
  finally:
      vargs.val_slice_enable = orig        # ← 但 v.dataloader 留在了整图 loader 上
  ```
- 交互点（上游，只读不改）：`ultralytics/engine/validator.py:235`
  ```python
  self.dataloader = self.dataloader or self.get_dataloader(...)   # 真值短路：非 None 就不重建
  ```
- `ultralytics/engine/trainer.py:408`（`self.validator = self.get_validator()`，**整个训练只创建一次**）、`:871`（`metrics = self.validator(self)`，每 epoch 复用同一实例）

**问题描述**
`_run_whole` 为了实现"整图第二遍验证"，把 `v.dataloader` 直接替换成整图 loader；`finally` 只恢复了 `val_slice_enable`，**没有恢复/清空 `v.dataloader`**。而 validator 实例是跨 epoch 复用的，上游 `:235` 的 `or` 短路使后续 epoch 不再重建 loader。于是从第 2 个 epoch 起：

- 主（切片）那一遍实际上跑在**整图 loader** 上；
- 补丁过的 `update_metrics`（`valslice.py:289-293`）检查 `"val_slice_meta" in batch` → 不成立 → 回落到原生整图 `_orig_update_metrics`；
- 主口径 fitness 与 whole 口径**变成同一个数**，`best.pt` 退化为按整图 fitness 选择；
- **复核修正（2026-09-15 修复阶段）**：本节初稿曾称 `seen` 会"冻结在 epoch 1"。**该说法有误**。`DetectionValidator.init_metrics` 每次 `validator(...)` 调用都会 `self.seen = 0`（`ultralytics/models/yolo/detect/val.py:97`），而退化的主口径走原生 `update_metrics`（`val.py:179` 的 `self.seen += 1`），因此 `seen` 仍等于验证集图片数，统计显示**没有**失真。此项从影响范围中移除；S1 的实质影响是"主口径 fitness 换成整图口径 ⇒ `best.pt` / 早停 / 最佳权重选择依据错误，且 `whole_metrics/*` 与主指标列重复"。其余结论均经复现，不受影响。

**实测复现**（脚本见附录 A，使用 `patch_dual_metric` 安装的**真实** `_run_whole` 闭包 + 复刻上游 `:235` 契约）：

```
epoch | primary pass ran on | primary metrics
  1   | SLICED              | sliced-metrics
  2   | WHOLE               | whole-metrics      ← 已退化
  3   | WHOLE               | whole-metrics      ← 已退化
recorded passes: [('SLICED','sliced-metrics'), ('WHOLE','whole-metrics'), ('WHOLE','whole-metrics'), ...]
```

**影响说明**
- **直接推翻项目文档的承诺**：`dual.py:1-9` 与 `项目说明.md` 第三节、第六节明确写"每轮切片(主)+整图(副)跑两遍；主 fitness→`best.pt`"，实际只有第 1 轮成立。
- 训练侧耗费了在线切片 + 切片推理的全部算力，却拿到整图口径的 `best.pt`；`results.csv` 中主指标列与 `whole_metrics/*` 列在前两轮之后就一致，任何基于该 CSV 的对比分析都会得出错误结论。
- 全程**零告警、零报错**——正是本项目注释反复强调要消灭的"静默失效"类型。

**改进方向**
`_run_whole` 必须做到"用完即还原"，最小改动：

```python
def _run_whole(self):
    v = getattr(self, "validator", None)
    if v is None or getattr(v, "data", None) is None:
        return None
    vargs = v.args
    orig_enable = bool(getattr(vargs, "val_slice_enable", False))
    orig_loader = v.dataloader                    # 保留主口径 loader
    try:
        vargs.val_slice_enable = False
        v.dataloader = None                       # 强制重建为整图 loader
        return v(self)
    except Exception as e:
        LOGGER.warning(f"val_slice_dual_metric: whole-image second pass failed ({e}); skipping best_whole.pt.")
        return None
    finally:
        vargs.val_slice_enable = orig_enable
        v.dataloader = orig_loader                # 还原，否则下个 epoch 的主口径丢失
```

并补一条**回归测试**：伪造 validator，连续跑 3 个 epoch 的 `validate()`，断言每轮"主口径使用的 loader 类型"恒为切片 loader。这类"patch 与上游契约的交互"测试，正是当前 16 项测试完全缺失的一层。

---

### S2 【严重·性能】六个保存分支默认全开，同步 JPEG 编码+落盘落在 DataLoader 取样路径上

**问题位置**
- `train.py:135-145` —— `slice_save_annotated=True`、`compose_save=True`，且 6 个 `*_save_dir` 全部指向本机绝对路径，配合 `slice_save_max=0`（不限量）
- `ultralytics_ooo/pool/dataset.py:443-486`（`_save_annotated`，被 5 条分支内联调用）
- `ultralytics_ooo/pool/augment_setup.py:247-283`（`OnlineSlice._save_tile`，切片分支，写盘用的是**原图分辨率** tile）
- `ultralytics_ooo/core/saver.py:37-51`（`_imwrite`：`imencode` + `ndarray.tofile`，同步阻塞）

**问题描述**
保存动作内联在每条分支的取样函数尾部，是**同步的 `cv2.imencode` + 磁盘写**，既不节流、不异步，也不按采样步长抽样；`slice_save_max=0` 表示"不限量"，仅靠 `(分支, 索引)` 去重。切片分支的 tile 是原图分辨率（4000×3000 原图 + 0.2 重叠 → 2400×1800），而 `_degrade_frame` 限幅只作用于退化分支，**切片保存不受工作分辨率上限约束**。

**实测数据**（附录 A，一次 `_imwrite` 的均值，10 次取样）：

| 图像尺寸 | 单次带标注 JPEG 编码+写盘 |
|---|---|
| 1280×960（退化分支的限幅分辨率） | **23.6 ms** |
| 4000×3000（切片 tile 的实际量级） | **182.6 ms** |

**影响说明**
- 按 8520 图的样本池估算，六个分支去重后合计约 6.6 万次写盘。以切片分支数十毫秒、退化分支 23.6 ms 计，累计约 **30–60 分钟纯编码+IO**，且**全部落在 worker 的取样关键路径上**——当数据加载是瓶颈时（大图 + 在线切片时几乎必然如此），这段耗时会直接加进 epoch 时间。
- 磁盘产物是数万张全分辨率 JPEG，且 `train.py` 是仓库里唯一可直接运行的训练入口，新使用者第一次跑就会命中。
- 与 `_degrade_frame` 的注释（"measured 11.5x across the three branches"）所体现的优化意图**方向相反**：一边把退化算子限幅到 2×imgsz 省时间，一边在原图分辨率上写 JPEG。

**改进方向**
1. **立即**：`train.py` 中 6 个 `*_save_dir` 全部注释/置空（包默认本就是关闭的，是示例脚本把它打开了）；需要人工抽查时只开一条分支，并设 `slice_save_max=50~200`。
2. **包内**：把写盘从取样路径移出——用 `queue.Queue` + 单个常驻写盘线程（或用 `torch.utils.data` 之外的异步落盘），或至少按固定采样步长（如每 1/64 样本）抽样。
3. 让切片保存同样受 `degrade_max_side` 约束（或新增 `save_max_side`），避免"为了看图"而在原图分辨率上编码。

---

## 三·补 S3 【严重·正确性】三个"每 epoch 增强比例"开关从未到达 dataset，配置被静默忽略

> 此项为首轮审查**遗漏**、在修复阶段新增集成测试时被测试捕获的缺陷，补记于此。

**问题位置**
- `pool/augment_setup.py::v8_transforms` —— `weather_ratio`（`:730`）与 `occlusion_ratio`（`:742`）被显式镜像到 dataset，`dataset.ratio_pad_ratio` / `dataset.blur_ratio` / `dataset.compose_ratio` **完全缺失**
- `pool/dataset.py::_mask_specs`（`_rebuild_epoch_masks` 的取数处）：`x = float(getattr(self, ratio_attr, _online_default(ratio_attr)))`

**问题描述**
`_rebuild_epoch_masks` 通过 `getattr(self, <ratio_attr>, _online_default(...))` 读取比例。对上述三个键，dataset 上**不存在**该属性 → 静默取到包内兜底值 **1.0** → `0.0 <= 1.0 < 1.0` 为假 → `mask = None` → `_build_*_sample` 里的 `self._*_mask is not None and not mask[...]` 判断永远不成立 → **该分支对每张原图、每个 epoch 全部生效**，用户配置的比例完全无效。

而 `_mask_specs` 的 docstring 恰恰写着"training always copies them from default.yaml"——一句从未成立的断言，掩盖了这个 bug。

**实测数据**（4 图临时数据集，六个比例全配 `0.1`，`set_epoch(0,10)` 后观察掩码）

| 分支 | 比例属性 | 属性是否存在 | 实际掩码 | 实际增强比例 |
|---|---|---|---|---|
| slice | `slice_ratio` | ✅ | `sum=0/4` | 0%（按配置） |
| **ratio** | `ratio_pad_ratio` | ❌ | **`None`** | **100%** |
| **blur** | `blur_ratio` | ❌ | **`None`** | **100%** |
| **compose** | `compose_ratio` | ❌ | **`None`** | **100%** |
| weather | `weather_ratio` | ✅ | `sum=0/4` | 0%（按配置） |
| occlusion | `occlusion_ratio` | ✅ | `sum=0/4` | 0%（按配置） |

**影响说明**
- `train.py` 出厂即配 `ratio_pad_ratio=0.1`、`blur_ratio=0.1`、`compose_ratio=0.1`，并注释"每 epoch 随机选 round(x*N) 张原图"。**实际是 100% 全量增强**，与 slice/weather/occlusion 的 10% 形成 10 倍失衡：宽高比填充、运动模糊（短+长两张）、合成三条分支的真实占比与配置和文档均不符，训练分布因此与预期显著偏离。
- 采样池长度恒定（区段数不变、未选中位回退原图），所以**训练照跑、loss 照降、无任何报错**——典型的"配置静默失效"。
- 这也解释了为什么"未选中整图直通"这条回退路径在这三条分支上从未被真正走到。

**改进方向**
1. 在 `v8_transforms` 中与 `weather_ratio` / `occlusion_ratio` 同样显式镜像三个比例（已修复）。
2. 把"每个 `ratio_attr` 都必须由 `v8_transforms` 镜像"从散文断言变成**机械断言**：`tests/test_ooo_branches.py::test_every_mask_ratio_knob_actually_reaches_the_dataset` 遍历 `_mask_specs()` 的每一行，要求属性存在且 `ratio=0.5` 时确实产生掩码。这类"表驱动开关"必须配一个遍历该表的测试，否则漏掉一行永远只能靠人工发现。

---



### M1 【中等·可复现性】分组采样路径丢弃 `seed`，`seed=` 对 DataLoader worker 随机种子彻底失效

**问题位置**　`pool/sampler.py:100-101`、`pool/sampler.py:87`

```python
# sampler.py:87  —— 用默认 seed=0
grouped = GroupedImageSampler.from_dataset(dataset)
# sampler.py:100-101 —— 相对上游丢了 "+ seed"
generator.manual_seed((6148914691236517205 + int(_b.RANK)) % (1 << 64))
```
上游 `data/build.py:350,367` 为：
```python
seed = torch.initial_seed() - RANK - 1
generator.manual_seed((6148914691236517205 + RANK + seed) % (1 << 64))
```

**问题描述**　补丁版把上游 seed 表达式里的 `+ seed` 项整个丢掉；而 `data/build.py:230-234` 的 `seed_worker` 是 `worker_seed = torch.initial_seed() % 2**32` → 用 loader generator 的种子派生。种子恒定 ⇒ 每个 worker 的 `numpy`/`random` 全局流（在线增强、mosaic、fliplr、切片都是靠它们抽样的）与 `args.seed` 无关。同时 `GroupedImageSampler.from_dataset(dataset)` 未传 seed，其内部 `torch.Generator` 恒以 0 播种，`__iter__` 的 unit 顺序也与训练种子无关。

**实测数据**

| `args.seed` | 上游 generator seed | 补丁版 generator seed |
|---|---|---|
| 1234 | 6148914691236518438 | 6148914691236517204 |
| 9999 | 6148914691236527203 | 6148914691236517204（不变） |

**影响说明**　在分组采样（默认 `slice_grouped_sampler=True`）下，**改 `seed=` 不会产生不同结果**，实验中"换种子重跑"失去意义；与关闭分组采样的同配置运行也不可比（同一 seed 下 worker 随机流不同）。这是可复现性缺陷，不导致崩溃，但会让 A/B 对比与调参结论不可信。

**改进方向**　逐字对齐上游种子语义：
```python
seed = torch.initial_seed() - _b.RANK - 1
generator.manual_seed((6148914691236517205 + _b.RANK + seed) % (1 << 64))
...
grouped = GroupedImageSampler.from_dataset(dataset, seed=seed)
```
并加一条测试：固定 `torch.manual_seed(x)`，断言分组路径与原生路径的 loader generator 种子一致。

---

### M2 【中等·可维护性】复制上游 `build_dataloader` 尾部实现，已发生行为漂移

**问题位置**　`pool/sampler.py:91-115`（对上 `ultralytics/data/build.py:358-385`）

**问题描述**　补丁为了让 `sampler=` 生效，把上游 loader 构建尾部**逐行复制**了一份。已经出现的漂移：
- **丢失 `pin_memory_device`**（上游 `:369-371,384`）→ NPU/XPU 的 pinned-memory 设备选择在分组路径下失效；
- **丢失 `+ seed`**（即 M1）；
- 上游 `shuffle=shuffle and sampler is None` 被硬编码为 `shuffle=False`（当前等价，但语义耦合）。
上游后续若改动这段（例如加 `persistent_workers`、调整 `nw` 策略），**改动不会传导到分组路径**，形成无声分叉。

**影响说明**　这是"侵入式 patch"的典型架构债：任何一次上游小版本升级，都可能让分组路径与非分组路径在多 worker 行为、内存策略上不一致，且没有任何测试能发现。项目文档已声明"未在其他 8.4.x 小版本逐一枚举验证"，该风险是实打实的。

**改进方向**　优先不复制尾部——改为调用上游实现后替换 sampler：
```python
loader = _orig(dataset, batch, workers, shuffle=False, rank=rank,
               drop_last=drop_last, pin_memory=pin_memory, device=device)
loader.sampler = grouped                      # InfiniteDataLoader 复用同一 dataloader
```
若必须自建 loader，则应把 loader kwargs 抽成单一函数并增加"与上游 kwargs 逐键一致"的测试（可用 `inspect.signature` 或对 `InfiniteDataLoader.__init__` 参数快照做断言）。

---

### M3 【中等·正确性】`save_model` 用浮点相等决定是否覆盖 `best_whole.pt`，会冲掉历史最优

**问题位置**　`pool/dual.py:95-99` 与 `:107-111`
```python
# validate(): 只在“严格大于”时更新 best_whole_fitness
if self.fitness_whole is not None and (best_whole_fitness is None or self.fitness_whole > best_whole_fitness):
    self.best_whole_fitness = self.fitness_whole
# save_model(): 却用“相等”判定“本轮就是最优”
and getattr(self, "best_whole_fitness", None) == self.fitness_whole
```

**问题描述**　当本轮 whole fitness 与历史最优**恰好相等**（未严格大于）时：`best_whole_fitness` 不更新，但 `==` 成立 → 用**当前 epoch 的 `last.pt`** 覆盖 `best_whole.pt`。`best_whole.pt` 因此不再对应历史最优权重，而是最后一个"平了最好成绩"的 epoch 的权重。

**影响说明**　`best_whole.pt` 作为"与整图基线公平对比"的参考产物（`dual.py:7-8` 明确定位）失去可比性；在指标抖动、mAP 长期持平的训练末端概率不低。不会崩溃，属静默产出错误工件。

**改进方向**　用一个显式标志替代浮点比较：
```python
# validate() 里
self._whole_improved = self.fitness_whole is not None and (
    getattr(self, "best_whole_fitness", None) is None or self.fitness_whole > self.best_whole_fitness
)
if self._whole_improved:
    self.best_whole_fitness = self.fitness_whole
# save_model() 里
if ... and getattr(self, "_whole_improved", False):
```

---

### M4 【中等·可维护性】`_ONLINE_DEFAULTS` 复述 96 个上游键：对 DEFAULT_CFG 是死代码，对兜底路径是活代码

**问题位置**　`pool/constants.py:16-192`

**实测比对**（附录 A）

| 项 | 数量 |
|---|---|
| `_ONLINE_DEFAULTS` 总条目 | 174 |
| 真正的项目自有键（slice_/compose_/ratio_pad_/blur_/weather_/occlusion_/degrade_/val_slice_/ims_/mosaic_save_/close_aug_） | 76 |
| 同时存在于上游 `DEFAULT_CFG_DICT` 的键 | 96 |
| 其中**取值与上游不一致**的 | **0（当前 8.4.126 上完全一致）** |

复述的上游键包括 `optimizer / lr0 / lrf / momentum / weight_decay / warmup_* / box / cls / dfl / hsv_* / mosaic / mixup / cutmix / copy_paste / tracker / auto_augment / erasing / epochs / patience / workers / close_mosaic / fraction / imgsz / batch ...`。

**问题描述**　`installer.py:39-42` 的注册逻辑是：
```python
if not hasattr(DEFAULT_CFG, _k):
    setattr(DEFAULT_CFG, _k, _v)
DEFAULT_CFG_DICT.setdefault(_k, _v)
```
对已存在的上游键，`hasattr` 为真 ⇒ **永不写入**；`setdefault` 同理无效。所以这 96 条对 `DEFAULT_CFG` 是死代码。但因 `_hyp_get`（`augment_setup.py:548-555`）与约 120 处 `getattr(self, key, _online_default(key))` 都以 `_online_default()` 作兜底值，**这 96 条依然是活代码**——只是"活在兜底分支里"。

**影响说明**　今天是**零危害**（实测 0 条不一致），但它埋了一个升级期陷阱：上游 8.4.x 改动 `close_mosaic`(10)、`patience`(100)、`workers`(8)、`lr0`(0.01) 等任一键的默认值时，只要代码走到兜底分支，就会静默使用**旧值**，而文件头"Single source of truth for the online-augmentation fallbacks"的声明会让人误以为它权威。这与项目在 `_hyp_get` 文档里追求的"默认值只能有一个真源"目标自相矛盾。

**改进方向**　
1. 把 96 条上游键从表中删除；`_hyp_get` 已经实现"先查 `DEFAULT_CFG_DICT`、再查包内表"的正确优先级，上游键根本不需要在本表留副本。
2. 若暂时不想删，则在 `constants.py` 加一个模块级自检，导入时比对上游值，不一致就 `LOGGER.warning`（把静默漂移变成显式告警）。

---

### M5 【中等·可维护性】`_sync_epoch_masks` 的两处静默 `except` 会让"每 epoch 掩码"机制无声停摆

**问题位置**　`pool/dataset.py:356-359`、`:362-367`
```python
try:
    epoch = v.value
except Exception:
    return                      # ← 无任何日志
...
try:
    epochs = ev.value if ev is not None else -1
except Exception:
    epochs = -1                 # ← 无任何日志
```

**问题描述**　这两处是跨进程 epoch 同步的核心读取。一旦 `multiprocessing.Value` 读取持续失败（句柄在 loader 拆除后仍被访问、进程被 fork 后句柄失效等），worker **不会重建掩码**，将一直沿用旧 epoch 的掩码。

**影响说明**　`_sync_epoch_masks` 的 docstring 自己写明：`close_mosaic` / `close_aug_epoch` / 各 `*_ratio` 的"每 epoch 变化"完全依赖这条链路。读取失败后：`slice_ratio`/`blur_ratio`/`weather_ratio`/`compose_ratio` 不再按 epoch 重新抽样，`close_aug_epoch` 的"最后 N 轮关增强"永不生效——训练照跑、loss 照降、**没有任何提示**。排查成本极高（需要读源码才能意识到掩码机制没走）。对照 `:257` 的同类异常有 `LOGGER.debug`，这两处明显是遗漏。

**改进方向**　
```python
except Exception as e:
    if not getattr(self, "_sync_fail_logged", False):
        self._sync_fail_logged = True
        LOGGER.warning(f"{self.prefix}epoch-mask sync failed ({e}); workers will keep stale masks.")
    return
```
另建议在 `set_epoch` 侧记录发布序号、在 `finalize`/epoch 末做一次"worker 是否跟上"的校验并告警。

---

### M6 【中等·性能】`_run_whole` 每个 epoch 重建整图 val 数据集

**问题位置**　`pool/dual.py:62`

**问题描述**　`v.get_dataloader(v.data["val"], self.batch_size)` 每次都走完整 `build_dataset` 路径（`valslice.py:314-339`）：重新构造 val 数据集对象、重新解析/校验标签，然后才建 loader。而 `valid_loader` 本该只需一份。

**影响说明**　叠加在"双口径本就跑两遍验证"的成本之上：每轮多一次完整 val 数据集构建（含标签扫描与可能的图像校验）。对 val 集较大或 `.npy` 缓存未命中的场景，这是纯浪费。配合 S1 的修复，应在首次构建后缓存复用（`orig_loader` 复用同一思路）。

**改进方向**　维护一个 `self._whole_val_loader` 缓存，仅在为 None 时构建；或统一改为"临时 validator 实例 + 一次性 loader 缓存"。同时建议把 `valslice.py:319-322` 中"因 `rect` 而二次 `build_dataset`"的逻辑与这里合并，避免重复建集。

---

### M7 【中等·健壮性】`OnlinePoolDataset.__init__` 用 `kwargs.get()` 取 `hyp`/`fraction`，位置传参时静默失配

**问题位置**　`pool/dataset.py:72-77`
```python
def __init__(self, *args, **kwargs):
    super().__init__(*args, **kwargs)
    hyp = kwargs.get("hyp", DEFAULT_CFG)
    fraction = kwargs.get("fraction", 1.0)
```

**问题描述**　`hyp` 与 `fraction` 只从 `kwargs` 取。构造签名被 `*args/**kwargs` 抹平后，任何**按位置**构造的调用者（`OnlinePoolDataset(path, 640, False, True, hyp_ns, ...)`，或第三方 fork 的 `build_yolo_dataset`）都会静默拿到 `DEFAULT_CFG` / `1.0`。

**影响说明**　`hyp` 失配会连带影响：`_resolve_ims_cap`（`ims_cache_frames`/`ims_cache_mb` → 内存上限）、`slice_raw_cache_size`（原始图 LRU 是否启用）、`prefetch_factor`、`slice_grouped_sampler`（采样策略）、以及 `cache='ram'` 与在线切片冲突的告警（`dataset.py:123-128`）。全都不报错，只是行为悄悄回退到默认。当前上游 `build_yolo_dataset` 恰好全用关键字传参，所以暂未暴露——但这属于"依赖调用方习惯"的隐性契约。

**改进方向**　改成显式签名（最佳），或用 `inspect.signature(YOLODataset.__init__).bind(*args, **kwargs)` 得到完整绑定后再读默认值；至少加一条 `assert "hyp" in kwargs` 的显式失败。

---

### M8 【中等·健壮性】`OnlineSlice` 的 `bias_margin`/`bias_jitter` 未校验，可致 0 面积切片 → worker 内 OpenCV 断言崩溃

**问题位置**
- `pool/augment_setup.py:166-175` —— `__init__` 校验了 `p`、`overlap_ratio`、`min_area_ratio`、`min_retain_ratio`、`min_center_ratio`，**独独漏了 `bias_margin` / `bias_jitter`**
- `core/geometry.py:29-36` —— `slice_geometry` 显式接受 `bias_x ∈ [-0.5, 0.5]`、`overlap_ratio ∈ [0, 1)`
- `core/geometry.py:74-79` —— `compute_slice_bias` 返回 `bx - 0.5`，`bx ∈ [margin, 1-margin]` ⇒ `bias ∈ [-0.5, 0.5]`，`margin=0` 时取到端点
- `pool/dataset.py:494-500`（`_finalize_label` 的 resize）

**问题描述**　`slice_geometry` 的 tile 宽度/高度为 `tw2 = sw - dx`、`th2 = sh - dy`，其中 `dx` 由 `bias` 决定。当 `overlap_ratio = 0`（`sw = w/2`）且 `|bias| = 0.5` 时，`tw2 = 0` ⇒ 产生 0 宽/0 高的 tile。**`bias_margin=0` 正是取到端点的条件，而它没有任何校验**。

**实测复现**（附录 A）
```
overlap_ratio=0.0  bias_x=+0.5 -> tile 面积=[6000000, 6000000, 0, 0]  degenerate_tiles=[2, 3]
Overlap_ratio=0.0  bias_x=-0.5 -> tile 面积=[0, 0, 6000000, 6000000]  degenerate_tiles=[0, 1]

端到端（OnlineSlice(center_bias=True, bias_margin=0, overlap_ratio=0), 4000x3000, 40 框）：
  compute_slice_bias 返回 (-0.371, -0.500)
  tile0: (0,0,516,0)   h=0     ← 退化
  tile2: (516,0,4000,0) h=0    ← 退化
  切片后的子图 shape = (0, 3484, 3)
  _finalize_label 内 cv2.resize →
  cv2.error: (-215:Assertion failed) !ssize.empty() in function 'cv::resize'

对照组（出货默认 bias_margin=0.25）：200 次抽样中退化网格 0 次。
```

**影响说明**　默认配置安全，但 `slice_bias_margin` + `slice_overlap_ratio` 的组合没有任何防护：用户为"让切缝在整个轴向搜索"而设 `slice_bias_margin=0`，训练会在**数分钟后、于 DataLoader 子进程内**抛出 OpenCV 断言错误。堆栈指向 `resize` 而非配置，定位困难。

**改进方向**　三层防护（建议全做）：
1. `OnlineSlice.__init__` 增加 `0.0 < bias_margin <= 0.5`、`bias_jitter >= 0.0` 的显式 `ValueError`；
2. `slice_geometry` 夹紧偏移，保证 tile 不退化为 0 面积：
   ```python
   limit_x = max(0, sw - 1)                      # 保证 tw2 >= 1
   dx = max(-limit_x, min(limit_x, int(round(bias_x * w))))
   limit_y = max(0, sh - 1)
   dy = max(-limit_y, min(limit_y, int(round(bias_y * h))))
   ```
3. `_emit`/`_finalize_label` 对 `<= 0` 面积的切片回退原图（与"背景配额超限退回原图"的既有 Plan A 语义一致），把崩溃降级为可诊断的告警。

---

### M9 【中等·健壮性】保存目录合法性在 worker 内首次保存时才校验

**问题位置**　`pool/augment_setup.py:254-258`（`_save_tile` 内 `raise FileExistsError`）；`__init__` 未做任何 `save_dir` 校验

**问题描述**　`slice_save_exist_ok=False` 且目录已存在时，异常在 `_save_tile` 第一次被调用时抛出——即训练启动数分钟后、在 DataLoader 子进程内。多 worker 下还会出现"部分 worker 已写入、部分才抛错"的混乱状态。

**影响说明**　配置错误（目录已存在）本该在构造期 0.1 秒内失败，现在变成运行期在子进程中失败，堆栈穿过 DataLoader 内部，用户体验差；`_forget_dir` 的存在（`core/saver.py:32-34`）也暗示这里的失败-重试语义被反复调整过。

**改进方向**　把 `save_dir` 的存在性/可写性校验前移到 `OnlineSlice.__init__` 与 `v8_transforms` 组装期（`augment_setup.py:650-667` 构造 `OnlineSlice` 的那几行），运行期只做 `_ensure_dir` + 写失败告警。

---

## 五、轻微问题

### L1 【轻微·可读性】10 处真实死导入（Ruff F401）

| 文件:行 | 未使用导入 |
|---|---|
| `pool/augment_setup.py:13` | `import math` |
| `pool/augment_setup.py:16` | `import time` |
| `pool/augment_setup.py:22` | `import torch` |
| `pool/augment_setup.py:26` | `from ultralytics.utils.instance import Instances` |
| `pool/constants.py:12` | `import numpy as np` |
| `pool/dataset.py:12` | `import bisect` |
| `pool/dataset.py:17` | `import time` |
| `pool/dataset.py:27` | `import torch` |
| `pool/dataset.py:28` | `from torch.utils.data import Dataset` |
| `pool/valslice.py:195` | `from pathlib import Path`（**函数内**局部导入，`_update_metrics_sliced` 未使用 Path；`:226` 那处才用到） |

**影响**　纯噪音，但 `core/` 声称"零框架依赖"、`pool/` 声称"只在需要时引入 ultralytics"的边界，会被这些残留导入模糊；CI 一旦启用 Ruff 会一次报红。**改进**：直接删除。

### L2 【轻微·重复代码】`_save_cap` 双实现 + 模块级遮蔽（F811）

`pool/dataset.py:43` 导入 `core.saver._save_cap`，紧接着 `pool/dataset.py:58` 又定义同名模块级函数 → **导入被遮蔽**，`core.saver._save_cap` 成为无人调用的死代码（仅经 `core/__init__.py:31` 再导出，而该符号同样全仓无人调用）。两份实现的兜底语义还不一致：一份读 `_ONLINE_DEFAULTS`，一份不读。此外模块级函数把首参命名为 `self`，极易被误认为方法。
**改进**：保留一份（建议保留 `core/saver` 版并让其读 `_online_default`），删除另一份；参数名由 `self` 改为 `dataset`/`obj`。

### L3 【轻微·模块结构】`core/__init__.py` 导出不完整

`core/__init__.py:3-15` 导入了 `_RATIO_PAD_COLORS`，但 `:17-32` 的 `__all__` 未列它（`pool/dataset.py:34-42` 却依赖这个符号）；同时 `_forget_dir` 被列入 `__all__` 但仅 `saver.py` 内部使用。
**改进**：补齐 `__all__`，或改用显式 `__getattr__` + 自检测试锁定公开面。

### L4 【轻微·文档】三处文档与实现矛盾

1. `installer.py:6-14` 与 `__init__.py:6-12` 的模块文档提到 `ultralytics_ooo.resume`、`ultralytics_ooo.valslice`、`ultralytics_ooo.data.augment` 等**并不存在**的子模块（实际都在 `ultralytics_ooo.pool.` 下），会误导新读者。
2. `augment_setup.py:537-540` 声称 `_hyp_get` 对"两处都没有的键会 `raise ValueError`，让 default.yaml 登记约定自校验"，但同一函数的 `:552-554` 实现是回落到 `_online_default(key)`（未知名→`None`），`tests/test_ooo_augment.py:33` 也断言返回 `None`。**同一函数的两段注释直接互相矛盾**，"self-enforcing"的承诺实际并未实现。
3. `pool/dataset.py:888-891` 注释称轴对齐模糊比通用路径"~1.8x faster"，实测为 **5.3–5.5x**（见性能专项 P5）。
**改进**：同步文档；性能数字标注测量条件与分辨率。

### L5 【轻微·性能】mode A 下 `_geometry` 计算全部 4 个 tile 只用 1 个

`augment_setup.py:489-492`：`_geometry` 返回 4 个 tile 的结果，随后 `random.choice` 只用 1 个。实测 200 框 / 4000×3000：`_geometry` = **0.28 ms**（无偏置）/ **0.58 ms**（`center_bias`），其中 3/4 被丢弃 ≈ 0.21 / 0.44 ms 每样本；按 85k 样本/epoch 约 18–37 s 纯 worker CPU。
**改进**：mode A 先 `k = random.randrange(4)`，让 `_geometry` 只算第 k 个 tile（加 `only=k` 参数）；或改为按需惰性求值。

### L6 【轻微·性能】val 侧每个 tile 都做一次全分辨率 memcpy

`valslice.py:99` 调 `_load_image_cached(oi)` 走默认 `copy=True`，而 `:108` 的切片 `np.ascontiguousarray(im[y0:y1, x0:x1])` **只读**该数组。实测 4000×3000 一次 memcpy = **13.6 ms**，`all_tiles=True` 下每图 4 次 ≈ 54 ms（可省约 40 ms/图）。另外 `:95` 的 `deepcopy(self.labels[oi])` 也在 4 个 tile 里重复执行。
**改进**：传 `copy=False`（与 `_build_compose_sample` 的既有用法一致，`pool/dataset.py:1290-1295` 已论证安全）；标签改为每图 deepcopy 一次后按 tile 浅拷贝。

### L7 【轻微·性能护栏】`degrade_max_side` / `compose_max_side` 取负值即关闭工作分辨率上限，无任何提示

`pool/dataset.py:842-855`（`v < 0 -> 0.0`，注释称 legacy behaviour）、`pool/dataset.py:1283-1287`。实测关闭后：weather-noise 的瞬时分配从 **18.4 MB**（1280×960）涨到 **180 MB**（4000×3000）；dense-PSF 运动模糊从 52 ms 涨到 451 ms（见性能专项 P4/P6）。多 worker 下极易 OOM 或速度骤降，且无告警。
**改进**：`< 0` 分支加 `LOGGER.warning` 明示代价；或直接取消"关闭"语义（保留 `>0` 手动指定即可，`0` 保持 auto）。

### L8 【轻微·副作用】`_apply_weather` 的 noise 分支污染 OpenCV 进程级全局 RNG

`core/degrade.py:184-193`：`cv2.setRNGSeed(int(np.random.randint(0, 2**31 - 1)))`。`cv2.setRNGSeed` 是**进程级全局副作用**，会影响同进程内其他依赖 OpenCV RNG 的算法；同时该模块混用 `np.random`（weather）、`random`（occlusion / OnlineSlice）与按 seed 派生的 `random.Random`（epoch 掩码）三种流，随机来源不统一，跨配置的可复现性依赖"抽样次数恰好一致"。
**改进**：噪声改用局部生成器（`rng = np.random.default_rng()` 生成噪声后叠加），或在使用前后保存/恢复 OpenCV RNG 状态；中期建议在线增强统一注入一个 `random.Random` / `Generator`。

### L9 【轻微·资源】`_patch_plot_results` 泄漏临时目录

`pool/dual.py:40-42`：`tempfile.mkdtemp(prefix="ooo_plot_")` 创建的目录从不删除。每次 `plot_results` 都在系统临时目录留下一个 `ooo_plot_*`（含一个 `results.csv`），长跑/多次实验后堆积。
**改进**：改用 `with tempfile.TemporaryDirectory(prefix="ooo_plot_") as tmp:`，或 `try/finally` + `shutil.rmtree(..., ignore_errors=True)`。

### L10 【轻微·可读性】`_remap_boxes(self, ...)` 的 `self` 未被使用

`pool/valslice.py:157`：函数体只用 `boxes`/`meta`/`imgsz`，但被作为方法挂载（`:347`）。同类问题：`_finalize_sliced_orig(self, oi, acc)` 用了 `self`，`_gt_orig_pixels` 也用了，只有它是纯函数。
**改进**：去掉 `self`（并同步调整挂载方式为 `@staticmethod` 或保持为模块函数）。

### L11 【轻微·模块结构】`patch_fraction_guard()` 放在 `pool/dataset.py` 末尾，破坏"一个 patch 一个模块"约定

`pool/dataset.py:1521-1550`。其余补丁（`resume.py` / `valslice.py` / `dual.py` / `sampler.py`）都各自持有独立模块与安装器，只有 fraction 兜底寄生在数据集模块里。叠加 `pool/dataset.py` 已 1551 行、`OnlinePoolDataset` 承担 11 类职责（初始化 / 图像读取 / epoch 掩码 / 区段布局 / 分组单元 / LRU 统计 / 6 个样本构建器 / 分发 / 长度 / 保存），该文件已到需要拆分的体量。
**改进**：抽出 `pool/fraction.py`；长期按"区段构建器"拆分 `pool/builders.py`。

### L12 【轻微·重复代码】六条分支构建函数共享同一 8 行骨架

`pool/dataset.py:736-762`（origin）、`:881-954`（blur）、`:956-1016`（weather）、`:1018-1123`（occlusion）、`:1125-1242`（ratio）、`:1244-1390`（compose）重复同一序列：
`取帧（含限幅）→ 分支变换 → deepcopy 标签 → pop("shape") → label["im_file"]=f → label["img"]=… → _touch_buffer(index) → 可选 _save_annotated → _finalize_label`。
另有两处局部重复：`pool/dataset.py:1071-1080` 与 `core/degrade.py:197-219` 的矩形并集面积；`pool/dataset.py:465-486` 与 `pool/augment_setup.py:261-271` 的画框逻辑；`core/saver.py:54-60` 与 `pool/dataset.py:58-62` 的 `_save_cap`（见 L2）。
**改进**：骨架提为模板方法 `_build_branch_sample(branch, index, img_index, transform=None, save_key=...)`，六条分支只提供变换回调与保存元数据；画框与面积并集各抽一个共享 helper。

### L13 【轻微·重复代码】`_geometry` 内 bbox 格式转换的 4 段近似重复

`pool/augment_setup.py:313-330`：`normalized` × (`xywh`/`ltwh`/`xyxy`) 四个分支几乎逐字重复。
**改进**：先按 `normalized` 统一缩放到像素，再按 `bbox_format` 转换一次，可把 18 行压到 8 行。

### L14 【轻微·可移植性】`train.py` 硬编码本机绝对路径

`train.py:44`（`project=r"C:\Users\Administrator\Desktop\ultralytics-main\runs"`）、`:139-145`（6 个 `*_save_dir` 绝对路径）、`:39`（注释里的 resume 路径）。同时 `device='cpu'`、`epochs=2`、`workers=0` 是调试配置却被当作示例。
**改进**：改用相对路径（`project="runs"`）并把调试配置注释化，或把该文件定位成"本地冒烟脚本"并新增 `examples/train_full.py` 作为推荐入口。

---

## 六、性能专项（审查重点）

按**实测影响**从大到小排序。所有数字均为本机 conda `langchain` 环境实测（见附录 A）。

| 编号 | 位置 | 实测代价 | 触发条件 | 建议 |
|---|---|---|---|---|
| **P1** | `train.py:135-145` + `pool/dataset.py:443-486` + `core/saver.py:37-51` | 单次写盘 **23.6 ms**（1280×960）/ **182.6 ms**（4000×3000）；六分支合计约 6.6 万次同步写盘，累计约 **30–60 min** 纯编码+IO | 示例脚本默认全开（`slice_save_max=0`） | 见 **S2**：脚本默认关闭 + 包内异步/抽样落盘 |
| **P2** | `pool/dual.py:62` ｜ `pool/dual.py:89` | 每 epoch **多一整遍整图推理 + 一次完整 val 数据集构建**；叠加"两遍验证" | `val_slice_dual_metric=True` | 见 **M6**：缓存整图 loader |
| **P3** | `pool/valslice.py:99-108` | 全分辨率 memcpy 13.6 ms × 4 tile ≈ **54 ms/图**（可省 ~40 ms） | `val_slice_enable=True` + `all_tiles=True` | 见 **L6**：`copy=False` |
| **P4** | `pool/dataset.py:842-855` ｜ `core/degrade.py:70-84` | weather-noise 瞬时峰值 **18.4 MB**（1280×960）→ **180 MB**（4000×3000）；像素工作量差 **9.8x** | `degrade_max_side < 0` 或 `compose_max_side < 0` | 见 **L7**：负值告警或移除该语义 |
| **P5** | `core/degrade.py:93-147` | dense-PSF 运动模糊 vs 轴对齐：1280×960 **52.4 / 9.5 ms（5.51x）**；4000×3000 **451 / 85 ms（5.30x）** | `blur_axis_aligned=False` | 默认已是轴对齐（好）；建议为 dense 路径按 `(length, angle)` 缓存 PSF，并修正注释里"~1.8x"为实测值 |
| **P6** | `pool/augment_setup.py:489-492` | `_geometry` 4 个 tile 只用 1 个：**0.21 / 0.44 ms** 每样本（无偏置 / `center_bias`）；85k 样本/epoch ≈ **18–37 s** worker CPU | `slice_all_tiles=False`（mode A） | 见 **L5**：只算被抽中的 tile |
| **P7** | `pool/dataset.py:104` | 构造期 `zlib.crc32("\n".join(self.im_files).encode())`：一次性约 O(N×路径长) 的临时串 | 数据集构造 | 样本量大时可改为增量 `crc32` 累积（低优先） |
| **P8** | `pool/sampler.py:109` | `prefetch_factor=4` 硬编码，而 `pool/dataset.py:109` 的 `self.prefetch_factor`（默认 2）**全仓无人读取** → 可调旋钮是死的 | 始终 | 删除死属性，或让 loader 读取它 |
| **P9** | `pool/dataset.py:570-571` | `_segment_bases()` 每次调用都重算 `_segment_lengths()`（7 元素 list + 6 条 getattr 链），`__len__` 与 `get_image_and_label` 各一次 ≈ 2.5 µs/次 × ~2/样本 | 始终 | **作者的知情取舍**（docstring 明确量化并解释为正确性优先），仅记录，不建议改 |

**性能小结**：包内已做的优化（`_cap_long_side` 限幅 9.8x、轴对齐 PSF、`_degrade_frame` 的 11.5x、compose 单次分配、`self.ims` 有界 FIFO、`deque(maxlen)` 替代 `list.pop(0)`、LRU 命中免 memcpy）**方向正确且收益真实**。真正吃掉时间的是**取样路径上的保存 IO（P1）**与**双口径的重复验证开销（P2）**——都是"功能默认打开"而非"算法不够快"。

---

## 七、安全性评估

| 项 | 结论 |
|---|---|
| 危险函数（`eval`/`exec`/`pickle`/`subprocess`/`os.system`/`shell=True`/`yaml.load`） | **未发现**任何一处 |
| 路径穿越 | **未发现**。保存文件名由 `f"{stem}_p{os.getpid()}_img{img_index}.jpg"` 构造，无用户可控路径片段；目录来自 hyp 配置而非外部输入 |
| 反序列化 | 仅通过上游 `torch.load`（所有权重加载路径），本包不新增反序列化面 |
| 破坏性文件操作 | 仅 `pool/dataset.py:184,188` 的 `Path(fn).unlink(missing_ok=True)`，删除对象是 `im_file.with_suffix('.npy')` 派生的缓存文件，且仅在"通道数不匹配 / 损坏"时触发——镜像自上游行为，风险低，但属"数据加载路径上的删除副作用"，建议在文档中显式声明 |
| **主要风险：patch 冲突** | `installer.py` 会替换 7 处上游对象：`data.build.YOLODataset`、`data.dataset.v8_transforms`、`BaseTrainer.{validate,save_model,resume_training,check_resume}`、`DetectionValidator.{__init__,update_metrics,finalize_metrics,gather_stats,get_dataloader}`、`BaseDataset.get_img_files`、`data.build.build_dataloader`。若第三方库（wandb/mlflow 回调、ultralytics 平台插件、其他 fork）也 patch 同名方法，**后安装者覆盖先安装者，且无任何检测**。现有 `_ooo_*_patched` 幂等标记只防自身重复安装。建议在 `install()` 里对每个目标方法记录安装前实现并检测"是否已被他人替换"，冲突时告警。 |
| `_compat` 静默丢弃 kwargs | `augment_setup.py:53-61` 按签名过滤未知参数，使上游改参数名/移除参数时，"fork 调试开关"静默失效（文档已声明这是为兼容性做的取舍，但建议在丢弃发生且非空时 `LOGGER.debug` 一次） |

---

## 八、优先修复清单（可执行顺序）

| 顺序 | 动作 | 产出 | 关联 |
|---|---|---|---|
| 1 | 修 `dual.py::_run_whole` 的 loader 还原（附代码见 S1） | 双口径恢复设计语义 | **S1** |
| 2 | 为 S1 补跨 epoch 回归测试（断言主口径恒用切片 loader） | 锁死该类"patch×上游契约"缺陷 | S1 |
| 3 | `train.py` 关闭 6 个 `*_save_dir` / `compose_save`，并设 `slice_save_max=100` | 单点拿下最大性能收益 | **S2** |
| 4 | 对齐 `sampler.py` 的 seed 语义（`+ seed` 与 `from_dataset(..., seed=seed)`） | 恢复 seed 可控可复现 | **M1** |
| 5 | `save_model` 改用显式 `_whole_improved` 标志 | `best_whole.pt` 语义正确 | **M3** |
| 6 | `_sync_epoch_masks` 两处静默 except 加告警 | 消除最难排查的静默失效 | **M5** |
| 7 | `OnlineSlice.__init__` 校验 `bias_margin`/`bias_jitter` + `slice_geometry` 夹紧偏移 | 消除配置可触发的 worker 崩溃 | **M8** |
| 8 | `_ONLINE_DEFAULTS` 删除 96 条上游键（或加一致性自检） | 消除升级期静默漂移 | **M4** |
| 9 | 删除 10 处死导入 + `_save_cap` 去重 + 修 3 处文档矛盾 | 清理噪音、启用 Ruff 的前提 | **L1/L2/L4** |
| 10 | 缓存整图 val loader；`valslice` 改 `copy=False`；mode A 只算 1 个 tile | 追加性能收益 | **M6/L6/L5** |
| 11 | 抽 `pool/fraction.py`；六分支骨架提模板方法 | 结构性改善，为后续改动降风险 | **L11/L12** |
| 12 | 引入 Ruff（`pyproject.toml:222-230` 已有 `[tool.ruff]`，line-length 120）并纳入 CI | 让 L1/L2 类问题不再回流 | L1/L2 |

---

## 附录 A：复现方式

审查脚本（临时目录，未写入仓库）：`%TEMP%\ooo_audit\`

| 脚本 | 作用 |
|---|---|
| `ast_audit.py` | 自建 AST 检查器：F401 / F811 / 未使用局部导入 / 未使用形参 / 超长函数 / 重复函数指纹 / 裸与宽泛 except / 浮点相等 / 可变默认参数 |
| `cfg_drift.py` | `_ONLINE_DEFAULTS` vs 上游 `DEFAULT_CFG_DICT` 逐键比对 |
| `bench_audit.py` | 微基准：退化 tile、`_geometry` 成本、轴对齐 vs dense PSF、`_cap_long_side`、weather-noise 峰值（`tracemalloc`）、全分辨率 memcpy |
| `poc_dual.py` | S1 的最小复现：用 `patch_dual_metric` 安装的真实 `_run_whole` + 复刻上游 `validator.py:235` 短路契约，连跑 3 个 epoch |

执行方式：

```bash
export PATH="/d/miniconda3/envs/langchain:/d/miniconda3/envs/langchain/Scripts:$PATH"
export PYTHONPATH="C:\\Users\\Administrator\\Desktop\\ultralytics-main"
python "$TEMP/ooo_audit/ast_audit.py" ultralytics_ooo
python "$TEMP/ooo_audit/cfg_drift.py"
python "$TEMP/ooo_audit/bench_audit.py"
python "$TEMP/ooo_audit/poc_dual.py"
python -m pytest tests/test_ooo_core.py tests/test_ooo_augment.py tests/test_ooo_resume.py tests/test_ooo_valslice.py -q
```

## 附录 B：审查范围与未覆盖项

- `ultralytics/`（上游 8.4.126，497 个文件）**未审查**——按项目定义它是"干净原版、未修改"，仅作为 patch 的契约参照。
- `tests/test_{cli,cuda,engine,exports,integrations,python,solutions,...}.py` 属上游自带测试，未逐行审查。
- `weights/*.pt`、`_mini_val_set/`、`runs/` 为数据/产物，未审查。
- 未做真实端到端训练验证（成本高）；S1 采用"复刻上游契约的最小复现"而非完整训练复现，逻辑链条与上游 `trainer.py:408/871` + `validator.py:235` 逐行对应，建议按第八节第 2 项补真实回归测试以闭环。
- 未评估与上游"深度侵入版"的功能等价性（属功能验证范畴，非代码质量）。

---

## 附录 C：修复状态（2026-09-15 完成）

按第八节顺序逐项修复完毕，详见 `更新说明.md`。测试从 **16 项 → 134 项全通过**（**历史快照**：本轮修复完成时的数字；后续样本池 B+ 到 146、`img_origin` 重构后到 **153**，见 `更新说明.md` 第五节末的表）。

| 编号 | 结论 | 验证方式 |
|---|---|---|
| S1 | ✅ 已修（`_run_whole` 保存/还原 loader + 缓存整图 loader） | `tests/test_ooo_dual.py`（旧代码下该测试失败：`['SLICED','WHOLE','WHOLE']`） |
| S2 | ✅ 已修（`train.py` 关闭 6 个保存目录、`slice_save_max=100`） | 配置审查 + 实测写盘耗时数据保留在报告中 |
| S3 | ✅ 已修（镜像三个比例键）+ 新增表驱动结构性断言 | `tests/test_ooo_branches.py::test_every_mask_ratio_knob_actually_reaches_the_dataset` |
| M1 | ✅ 已修（补 `+ seed`，并把 seed 传入 `GroupedImageSampler`） | `tests/test_ooo_sampler.py`（断言 generator 种子随 `torch.initial_seed()` 变化） |
| M2 | ✅ 已修（补 `pin_memory_device`）+ 新增 loader 属性漂移测试 | `tests/test_ooo_sampler.py::test_grouped_loader_attributes_match_the_stock_formula` |
| M3 | ✅ 已修（改用显式 `_whole_improved` 标志） | `tests/test_ooo_dual.py::test_save_model_tie_does_not_destroy_best_whole` |
| M4 | ✅ 已修（表精简 174→77 条，仅项目自有键）+ `install()` 自检 | `tests/test_ooo_guards.py::test_online_defaults_table_shadows_no_upstream_key` |
| M5 | ✅ 已修（两处静默 except 改为去重告警） | 代码审查 |
| M6 | ✅ 已修（整图 val loader 构建一次并复用） | `tests/test_ooo_dual.py::test_whole_loader_is_built_once_and_reused` |
| M7 | ✅ 已修（按 `BaseDataset.__init__` 签名解析位置传参） | `tests/test_ooo_guards.py::test_init_knobs_resolve_keyword_and_positional` |
| M8 | ✅ 已修（`OnlineSlice` 校验 `bias_margin`/`bias_jitter` + `slice_geometry` 夹紧偏移） | `tests/test_ooo_guards.py`（几何参数化用例 + 极端偏置端到端） |
| M9 | ✅ 已修（保存目录校验前移到构造期） | `tests/test_ooo_guards.py::test_online_slice_validates_the_save_target_at_construction` |
| L1–L4 | ✅ 已修（10 处死导入清零、`_save_cap` 收敛为单实现、`__all__` 补齐、3 处文档矛盾改正） | 自制 AST 检查器复跑：F401/F811/浮点相等 全部归零 |
| L5 | ✅ 已修（`_geometry(only=k)`，mode A 与 `slice_at` 同时受益）+ 等价性测试 | `tests/test_ooo_guards.py::test_geometry_only_k_is_equivalent_to_computing_all_four` |
| L6 | ✅ 已修（val 侧 `copy=False`，并兼容无该参数的 base） | 分支/切片验证测试 |
| L7 | ✅ 已修（取负值关闭限幅时告警一次） | 代码审查 |
| L9 | ✅ 已修（`TemporaryDirectory` 替代 `mkdtemp`） | 代码审查 |
| L10 | ✅ 已修（`_remap_boxes` 去 `self` 并挂为 `staticmethod`） | 切片验证测试 |
| L11 | ✅ 已修（抽出 `pool/fraction.py`） | 测试全量通过 |
| L12 | ✅ 已修（抽出 `_begin_branch_label`/`_save_branch`/`_finish_branch`，4 条分支复用） | `tests/test_ooo_branches.py` 12 项端到端 |
| L13 | ⏸ 未改（`_geometry` 内 bbox 转换的 4 段重复） | 纯可读性，改动收益低于风险，留作后续 |
| L14 | ✅ 已修（`project` 改相对路径） | 配置审查 |
| 第 12 项 Ruff | ⚠️ 部分（新增 `ultralytics_ooo/ruff.toml` + CI 门禁；**未在本地安装 ruff**，以免改动用户 conda 环境） | 以自制 AST 检查器复核等价规则（F401/F811 归零） |
