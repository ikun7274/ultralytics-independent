# 样本池"原图回退"膨胀问题：可行性分析（含实施结果）

> 问题：「`*_ratio` 未选中的位内容退回『原图直通』，导致原图激增」——能否删除这些位，或其他处理方式？
> 分析日期：2026-09-15　｜　**实施日期：2026-09-15**　｜　基线：Ultralytics 8.4.126 + 修复后的 `ultralytics_ooo`
> 量化工具：`tools/ooo_pool_measure.py`（可复现本文全部数字）
> 布局回归工具：`tools/ooo_layout_snapshot.py`（`ratio=1.0` 的"索引→样本"逐位快照比对）
>
> **状态：方案 B+ 已实施并验证；§7.1（`keep_origin` 收窄）作为独立改动也已落地 —— 见 §九。**

---

## 一、结论摘要

| # | 方案 | 可行性 | 风险 | 工作量 | 建议 |
|---|---|---|---|---|---|
| **A** | 每 epoch 按实际选中数**变长**池（真正删除位） | ✅ 技术可行 | **高**：`len(dataset)` 每轮变化 → 需处理 mosaic buffer 失效、`nb`/`nw` 陈旧、sampler units 重建 | 大 | 不推荐 |
| **B+** | **按 ratio 定长切分**：段长由 ratio 决定（恒定），未选中图只保留 **1** 个原图位 | ✅ 完全可行 | **低**：`len` 全程恒定，不触碰 buffer/调度/warmup | 中 | ★ **已采用** |
| **G** | 池结构不变，只让 **sampler 过滤**掉回退位 | ✅ 可行 | 低-中：仅一条链路改动，但 `len(dataset)` 与实际访问集不一致 | 小 | 可作为过渡/备选 |
| **C** | 保留位，但把回退内容换成**另一种真实增强** | ⚠️ 语义可行、收益有限 | 中 | 中 | 不推荐（见 §6.3） |

**结论：可以做，而且比现有注释断言的更安全；但正确做法不是"删除"（方案 A），而是"一开始就不分配这些位"（方案 B+）。**

方案 B+ 的关键收益与不变量：

- 池中"原图回退"占比：**91.0% → 0%**（常态 epoch）；
- 池规模（N=40，六 ratio 全 0.1，实测）：**410 → 113**（3.63×）；**§7.1 落地后进一步到 77（5.32×）**；
- **`*_ratio >= 1` 时布局与今天逐位一致** —— 方案可增量采用，历史 A/B 的 legacy arm 不受影响；
- `len(dataset)` **全程恒定**，`close_aug_epoch` 也不需要中途变长（只需让少量"额外的位"回退）。

---

## 二、现象量化（实测）

`python tools/ooo_pool_measure.py --n 40 --ratio 0.1`（六条分支全开，`slice_all_tiles=True`）：

```
segment       slots   really augmented   plain-original fallback   ratio
base            160                 16                       144     0.1
origin           40                  0                        40       -
ratio            40                  4                        36     0.1
blur             80                  8                        72     0.1
compose          10                  1                         9     0.1
weather          40                  4                        36     0.1
occlusion        40                  4                        36     0.1
TOTAL           410                 37                       373
  -> plain-original slots: 373 = 91.0% of the pool
```

三点值得注意：

1. **91% 的池位是"原图直通"**，只有 9% 带任何增强。用户配置的是 `ratio=0.1`，直觉上以为"池里 10% 是增强样本"，实际是"9% 增强 + 91% 重复原图"。
2. **base（切片）段最严重**：160 位里只有 16 位是真切片，144 位是重复原图。因为 `n_per=4`（`slice_all_tiles`），**未被切片选中的图会占 4 个位**。已用内容哈希实测确认这 4 位**逐字节相同**：
   ```
   base: un-selected image #0 owns 4 slots -> 1 distinct content (4x duplication)
   ```
3. `origin` 段的 40 个原图位是 `slice_keep_origin` 的**设计语义**（每图一张未切片原图），不算缺陷，但见 §7.1 的重叠问题。
   > 命名已变更（本文写于变更前）：`slice_keep_origin` 现已由 **`img_origin`** 取代，且语义扩大为"每张原图恒定 1 个整图槽、对**所有**增强分支生效"（`origin` 段宽 `K_slice` → `N`）。下文凡出现 `slice_keep_origin` 处均按此对应。

**危害有两层**，而且**独立的**：

- **算力浪费**：373 个位要解码（LRU 可命中）、resize、参与 mosaic 拼图、走完仿射/HSV/翻转，产生的信息增量接近 0。池越大 → 每个 epoch 的步数越多、每步更慢，但模型见到的有效内容没有增加。
- **内容稀释**：`Mosaic.get_indexes()` 从 `dataset.buffer` 抽邻居（`ultralytics/data/augment.py:497`），而 buffer 里堆满同一批原图 → 拼图邻居多样性下降。同时"原图内容"在梯度里的权重被显著放大（每个原图被复制 4+6 次），**实际等效增强强度远低于配置值**。

---

## 三、这个设计是怎么来的

`train.py` 里 `slice_background_ratio` 那段注释给出了当初的理由（原文）：

> 空片占的位置不能消失，只能有两种处理：
> 1. 位置消失（真・丢弃）→ `__len__` 从 4N 变成不确定值 → "预计算 + len 不恒定"：mosaic buffer 记账错乱、epoch 内 batch 数漂移、进度条和训练循环不同步。要修得改 sampler + buffer + 训练循环，工程量大且容易出隐蔽 bug。
> 2. 位置保留、内容替换（丢弃空片，用别的样本补位）→ len 恒定为 4N，无副作用。

**方向判断是对的**（恒长确实是低成本换稳健），但有两个问题：

1. 它把"恒长"实现成了"**按最大值预分配 + 回退填充**"，而不是"**按实际需要定长**"。前者在 `ratio=0.1` 下浪费 91%，后者不会。
2. 它的推理只覆盖了"位置消失"，没有考虑**第三种做法：位置按 ratio 预先定长**——这既避免了变长，也避免了回退。

---

## 四、那条注释的论据，逐条核实

我把依赖 `len(dataset)` 的地方全部查了一遍，结论与注释**部分不一致**：

| 注释声称 | 核实结果 | 证据 |
|---|---|---|
| "改 sampler" | ✅ 属实。`GroupedImageSampler` 的 units 在 loader 构建时算一次；布局变了会越界 | `pool/sampler.py:46-53`；`build_dataloader` 只调用一次 |
| "buffer 记账错乱" | ✅ **属实且是真正的硬约束**。buffer 里存的是**池索引**，被当作索引直接喂给 `get_image_and_label`；池缩短后旧索引越界 | `augment.py:497-498`（`random.choices(list(dataset.buffer))`）→ `augment.py:365`（`self.dataset.get_image_and_label(i)`） |
| "训练循环不同步" | ⚠️ **基本不成立**。LR 调度是 **epoch 驱动**的，与 batch 数无关 | `trainer.py:250-256`（`LambdaLR(lambda x: max(1 - x/self.epochs,0)*(1-lrf)+lrf)`）+ `trainer.py:452`（每 epoch 一次 `scheduler.step()`） |
| "epoch 内 batch 数漂移" | ⚠️ 部分成立，且**可控**。`nb`/`nw` 只在训练开始时算一次 | `trainer.py:428-429`；`ni = i + nb*epoch`、`if ni < nw`（`:471-472`） |
| "进度条不同步" | ⚠️ 属实但**纯外观** | `TQDM(self.train_loader, total=nb)` |

**精确结论**：`len(dataset)` 变化时真正会出问题的只有两处 ——

1. **mosaic buffer 的陈旧索引**（会 `IndexError`，响亮崩溃，不是静默错误）；清除 buffer 即可解决，且上游已有同类时机（`close_mosaic` 会在边界重建 transforms）。
2. `nw`（warmup 迭代数）与 `ni` 的基准是**训练开始时**的 `nb`：若中途变长，warmup 的插值分母会与实际步数不符。**影响仅限 warmup 期**（默认前 3 个 epoch），而中途变长只会发生在 `close_aug_epoch` 边界（训练末段）。

其余（pbar total、`plot_idx`）是纯外观。

**所以：变长池并不是"不能做"，而是"需要处理两个已知点"。但既然有更好的做法，就不必承担这个风险。**

另外核实到一条**与方案无关但值得一提**的既有事实：`trainer.py:456` 的 `self.train_loader.sampler.set_epoch(epoch)` 只在 `RANK != -1`（DDP）时调用，而分组采样补丁只在 `rank == -1` 时生效，所以 `GroupedImageSampler` 缺少 `set_epoch` 目前不会触发（详见 §7.2）。

---

## 五、推荐方案 B+：按 ratio 定长切分

### 5.1 核心思想

把「段长」从"最大值（`4N` / `2N` / `N`）"改成"**由 ratio 算出的实际需要**"，并且——

> **每张图在每个 epoch 里恰好出现 1 次原图位；增强位只属于被选中的图。**

这样**根本不需要"回退"**：没有被选中的图，本来就没有额外的位可占。

### 5.2 布局定义（N 张原图，`sel(ratio) = round(ratio × N)`）

| 段 | 今天 | 方案 B+ | 说明 |
|---|---|---|---|
| base（切片） | `4N` | `4·K_slice + (N − K_slice)` | 选中图 → 4 片 tile；未选中图 → **恰好 1** 个原图位（不再是 4 个） |
| origin | `N` | `K_slice`（§7.1 实施后） | 只为**被切片选中**的图补回整图视角；未选中的图在 base 段已是整图 |
| ratio（比例填充） | `N` | `K_ratio` | 只有选中图获得额外的填充位 |
| blur | `2N` | `2·K_blur` | 短+长 |
| compose | `ceil(N/4)` | `K_compose`（组级） | 只有选中的组 |
| weather | `N` | `K_weather` | |
| occlusion | `N` | `K_occlusion` | |

**关键性质**：`K_*` 只由「ratio × N」决定，**与具体选中了哪些图无关** → **池长在整个训练过程中恒定**。

两个边界自洽性（已作为测试用例）：

- `ratio = 0` → `K = 0` → base 段退化为 `N`（每图恰好 1 个原图位），**与"关闭切片"完全等价**；今天则会产生 `4N` 个重复原图。
- `ratio = 1` → `K = N` → 布局**逐位退化为今天的形状**（见 §5.5 兼容性不变量）。

实测对比（N=40, ratio=0.1，`tools/ooo_pool_measure.py --n 40 --ratio 0.1`）：

```
segment       current  proposed after-B+  after-§7.1   note
base              160        52            52         selected -> 4 tiles, un-selected -> exactly 1 plain original
origin             40        40             4         one whole frame per SLICED image (was: per image)
ratio              40         4             4         only selected images get a slot
blur               80         8             8         2 x selected (short+long)
compose            10         1             1         selected groups only
weather            40         4             4         only selected images get a slot
occlusion          40         4             4         only selected images get a slot
TOTAL             410       113            77         3.63x / 5.32x smaller
```

N=8520 全开（ratio 0.1）：`87 330 → 24 069`（3.63×），§7.1 后 `16 401`（**5.32×**）。

### 5.3 为什么这是"低风险"的

| 风险点 | B+ 下的状态 |
|---|---|
| mosaic buffer 陈旧索引 | **不存在**。池长恒定，索引空间固定；buffer 里的索引始终有效 |
| `nb` / `nw` / `ni` | **不存在**。`len(dataset)` 全程不变 |
| 进度条 / `plot_idx` | 与实际一致（反而更准） |
| 分组 sampler units | 布局变了但**同样恒定**，构建一次依然有效 |
| `close_aug_epoch` | **不需中途变长**：只在收尾 epoch 让"额外的位"回退原图即可（约 29% 的位，见 §5.4 第 6 条；这些位本来就应该输出原图） |
| 跨进程一致性 | 沿用现有机制：`K_*` 由 ratio 决定、选中集合由 `(_mask_seed, epoch, epochs)` 播种的 rng 决定，**主进程与 worker 各自重建、结果一致**（无需传输） |

### 5.4 实施要点

1. **`pool/dataset.py::_segment_lengths`** —— 把 `n * mult` 改成 `len(sel) * mult + (n - len(sel))`（仅 base 段有第二项）。这是"单一真源"设计的好处：段长只在这里定义。
2. **选择集**：把现在的布尔掩码 `_slice_mask` 等升级为**有序选择数组** `_sel_*`（`rng.sample(range(n), K)`），并保证**与今天的掩码在同一 epoch 下选中同一批图**（`K` 由 ratio 决定，抽样方式不变 → 可保持逐位兼容，见 5.5）。
3. **索引映射**：`get_image_and_label` 里 `img_index = index // n_per` 改成"按段内偏移查选择集 + 累积偏移"：
   - `sel = self._sel_blur; k = index - blur_base; img_index = sel[k // 2]; tier = k % 2`
   - base 段因**每图位数为 4 或 1 两种**，需要累积偏移表（`cum`），实现方式**本项目已有先例**：`SliceValDataset._build`（`pool/valslice.py:57-81`）就是用 `counts / _cum / _metas` 做逐图变长布局 + `bisect` 定位。
4. **分支构建器简化**：`_build_blur_sample` 等里"`if self._blur_mask is not None and not self._blur_mask[img_index]`"这类**回退判断可以删掉**（未选中图不再进入该段），代码更短，也少一类分支。
5. **`_extended_pool_on()` / `grouped_sample_units()`** 需同步按新布局改写（后者已有 `flat == list(range(total))` 的自校验，会把不一致变成告警而不是静默丢样本）。
6. **`close_aug_epoch`**：`_rebuild_epoch_masks` 在收尾 epoch 把 `_sel_*` 置空 → 段长退化为 `0`（而非 `len(sel)`）会**改变池长**，所以这里必须反过来：**保持段长不变，让那些位回退原图**。代价是收尾 epoch 有"额外位"数量的重复原图（ratio=0.1、N=8520 时约 **7 029** 个位，占新池 24 069 的 **≈29%**）。注意这是相对**新池**的占比；且收尾 epoch 本来就应该"全是真实原图"，这些位只是同一批原图多出现几次，语义上无害。全局看仍是 3.6× 的池缩减。
7. **`_recompute` 时机**：`set_epoch` 重建选择集时必须**同时刷新 `_segment_bases` 缓存**（现有 version-stamp 机制已按 `tuple(_segment_lengths())` 做戳，段长不变则缓存不失效 —— 但 base 段的**逐图位表**（`cum`）必须显式重建）。

### 5.5 兼容性不变量（强烈建议作为测试锁定）

> **`*_ratio >= 1` 时，池布局与索引→样本映射必须与今天逐位一致。**

因为 `sel(1.0) = round(1.0 N) = N`，此时 `base = 4N + 0`、`ratio/blur/weather/occlusion = N/2N/…`，**布局自动退化为今天的形状**。这意味着：

- `tests/test_ooo_branches.py::test_pool_length_matches_the_segment_layout`（断言全开 ratio=1.0 时 `[16,4,4,8,1,4,4]`）**无需修改**，并直接成为兼容性守卫；
- 历史 A/B 的 legacy arm（ratio 全 1.0）**逐位不受影响**；
- 可以安全增量采用：先让默认 ratio 保持 1.0 上线，确认无回归后再把 `train.py` 的 0.1 打开。

### 5.6 工作量与验证

| 项 | 规模 |
|---|---|
| 改动文件 | `pool/dataset.py`（布局/映射/分支简化/close 语义）、`pool/sampler.py`（units 同步）、`train.py` + 文档注释 |
| 预计改动量 | 中等（约 200–300 行，含注释），**不新增文件、不改上游** |
| 现有测试保护 | 134 项（本文写作时点；后续为 146 → 157 → 171 → 179 → **183**）；其中 `test_pool_length_matches_the_segment_layout`、`test_get_image_and_label_covers_the_whole_index_range`、`test_every_segment_produces_a_usable_label`、`test_unselected_entries_fall_back_...`、`test_close_aug_epoch_...` 直接覆盖本次改动的核心不变量 |
| 新增测试建议 | ① `ratio >= 1` 布局逐位不变；② `ratio < 1` 时"每图恰好 1 个原图位"（用内容哈希去重计数）；③ 池长与 `set_epoch(N)` 无关（多轮锁长）；④ `extras=0` 与 `*_keep=False` 的边界 |
| 未覆盖 | 真实端到端训练（建议改完后跑 1–2 epoch 冒烟 + `tools/ooo_ab_ratios.py` 对照） |

---

## 六、其他方案评估

### 6.1 方案 A：真正变长（每 epoch 按实际选中数删位）
技术可行，但要承担 §4 的两个已知点（buffer 清理、`nw`/`ni` 基准）。**收益与 B+ 相同，风险显著更高**，没有理由选它。除非将来要在 epoch 内部动态改变样本量。

### 6.2 方案 G：池结构不变，sampler 过滤
保持数据集索引空间恒定，让 sampler 每 epoch 只产出"值得访问"的索引（每图 1 个原图位 ∪ 增强位）。
- 优点：改动最小（只动 sampler），**buffer 绝对安全**（索引空间不变），分支代码零改动。
- 缺点：`len(dataset)` 与实际访问集不一致（日志/统计误导，正是本次审查反复要消灭的"配置与行为不一致"）；`len(train_loader)` 每 epoch 变化（只有 `close_aug_epoch` 边界才会变，影响同 §4）；`dataset` 仍分配那 91% 的虚位（虽不访问，但 `_segment_bases` 等仍按大数计算）。
- 适合作为**临时过渡**（想快速止血、不想动布局），但长期应回到 B+。

### 6.3 方案 C：保留这些位，但把回退内容换成"另一种真实增强"
即"未选中的位不输出原图，而输出另一种增强（例如仅做 HSV/翻转）"。不可行的原因：
- 池位存在的意义是"这个图这一 epoch 参与训练"；未选中图的 1 个原图位**必须有**（否则该图整个 epoch 不出现）。给它的额外位再做一遍轻量增强，与下游必然执行的 mosaic/仿射/HSV **重复**，信息增量为 0。
- 若改成"给其他图的增强样本"（把 ratio 当**配额**而非**开关**），语义就变成"随机挑 10% 的**位**做模糊"，会破坏"短+长同命运""同一图 4 片同命运"等既有约束。
- **真正有效的"其他处理"只有一条：让未选中的图只占 1 个位** —— 那就是 B+。

### 6.4 方案 D（不推荐）：直接调大 ratio
把 `ratio` 调到 1.0 就没有回退位了，但那是"全量增强"，与"我只想要 10%"的诉求相反。不过**如果实际目标只是"不要浪费算力"而增强强度无所谓**，这确实是最省事的做法——只是它改变的是增强强度，不是池效率。

---

## 七、附带发现（与本次问题相邻）

### 7.1 `slice_keep_origin` 在 B+ 下会与 base 段的原图位重叠（已实施；后被 `img_origin` 统一取代）
`keep_origin` 原先为**每张图**（含未被切片选中的图）再加 1 个未切片原图位。而在 B+ 下，未被切片选中的图在 base 段**已经是**原图位 → 该图每 epoch 出现 2 次逐字节相同的原图。
**曾实施的收窄**：把 `keep_origin` 语义收窄为"为**被切片选中**的图补一张未切片原图"，段宽由 `N` 变为 `K_slice`（见 §9.2）。

> **最终形态（已取代上述收窄）**：`slice_keep_origin` 已被 **`img_origin`** 取代，语义变为**全局统一覆盖** —— `origin` 段宽回到 **`N`**（每张原图恒定 1 个整图槽，对所有增强分支生效），而 `base` 段**不再为未选中的图保留任何位**（`base = n_per*K_slice`）。因此本节描述的"重叠"不再存在：一张原图要么在 base 段以切片形式出现，要么只在 origin 段以整图出现，不会两个区段各占一份（`img_origin=True` 时切片图确实同时在 origin 段有一份整图，但**这是刻意的"整图视角"覆盖，不是重复膨胀**）。`img_origin=False` 时未被任何分支选中的图**真正从池中丢弃**。当前语义以 `ultralytics_ooo/README.md` 的「样本池布局」与「切片侧 4 旋钮 + 1 全局统一覆盖旋钮」两节为准。

### 7.2 `GroupedImageSampler` 缺 `set_epoch`（已补）
`trainer.py:456` 在 `RANK != -1`（DDP）时调用 `self.train_loader.sampler.set_epoch(epoch)`。分组采样补丁仅在 `rank == -1` 生效，所以 `GroupedImageSampler` 不会被这样调用（`DistributedSampler` 走的是原生路径）。**已补上 `set_epoch`**（记录 epoch 并折进顺序种子），避免将来支持 DDP + 分组采样时在 epoch 循环里 `AttributeError`。

---

## 八、建议的推进顺序

1. **先跑量化工具确认你的实际配置**：`python tools/ooo_pool_measure.py --n 8520 --ratio 0.1`（N 可填任意值，只影响算术）。若回退占比很高（预期 >85%），B+ 的收益就是实质性的。
2. **决定是否采用 B+**。若采用，按 §5.4 实施，并用 §5.5 的不变量作为第一道测试。
3. **`ratio=1.0` 先行**：B+ 落地后先保持 `ratio=1.0`（布局与今天逐位一致）跑通一轮，确认零回归。
4. **再打开 0.1**：配合新的"每 epoch 分支选中数"日志与 `tools/ooo_ab_ratios.py`，确认实际构成符合预期。
5. **最后收尾**：`keep_origin` 收窄（§7.1）作为独立改动。

> 本文的可行性分析与方案设计**已全部落地**（§9 记录了实施过程，含后续的 `img_origin` 统一覆盖重构）。文中"尚未改动生产代码"之类的时间点措辞请一律以 §9 与 `项目说明.md` 的当前描述为准。

---

## 九、实施记录（2026-09-15）

### 9.1 方案 B+（§5.1–5.6）已落地

按 §5.4 的顺序实施，改动集中在 `ultralytics_ooo/pool/dataset.py`：

| §5.4 | 实施内容 |
|---|---|
| 1 | `_segment_lengths` 改为按 ratio 定长；新增 `_ratio_K()` 作为"布局 K"的唯一来源（只读配置的 ratio，不读抽样结果） |
| 2 | 布尔掩码 `_*_mask` → **有序选择集** `_sel_*`；抽样方式、顺序与触发条件与旧实现逐字保持一致（仍只在 `0<x<1` 时 `rng.sample`），因此"哪几张图被增强"与改造前相同，变的只是"未选中的图不再占位" |
| 3 | base 段改为"选中图 `n_per` 个连续位 + 未选中图各 1 位"，新增 `_base_slot()`；其余段 `index - base` 现在查的是**选择集**而不是原图号 |
| 4 | 各分支构建器删除"未选中则回退原图"的判断，改为只判断 `close_aug_epoch`（`self._closing`） |
| 5 | `grouped_sample_units()` 按新布局重写；`_extended_pool_on()` 改为直接比较 `total != N`（不再维护第二份开关清单）。分组收益的判据从"总样本数 ≥ 4N"改为"存在某张图被读取次数 ≥ 原始 LRU 容量"，否则 ratio 定长后平均复用率下降会让分组被静默关掉 |
| 6 | `close_aug_epoch` 不再改段长：选择集变成恒等序列，构建器直接输出原图（段宽全程恒定） |
| 7 | `_ensure_epoch_sel()` 统一了懒初始化，并**移到分支分发之前**（旧实现放在分发之后，各构建器其实先读到了还没建的选择集） |

### 9.2 §7.1 `keep_origin` 收窄已落地（独立改动）

`_keep_origin_on()` 的语义收窄为"为被切片选中的图补回整图视角"，段宽 `N → K_slice`（`_origin_len()`）。未选中的图不再有第二份逐字节相同的整图。`ratio >= 1` → `K_slice = N` → 布局不变。

### 9.3 顺手修掉的严重缺陷：epoch 回调从未生效（S4）

实施过程中为了验证"每轮抽样真的换了"而加日志时发现：`installer._ooo_set_epoch` 读的是 `trainer.train_dataloader`，而**上游 `BaseTrainer` 从来没有这个属性**（它叫 `train_loader`）。后果是回调静默空转 →

- `set_epoch` 从未被 trainer 调用；
- 每 epoch 的 `*_ratio` 抽样与 `close_aug_epoch` **全程冻结在第 0 轮**；
- 没有任何报错，只在 `workers>0` 时由 `_mp_epoch`（也从未被写过）连带失效。

修复：改读 `train_loader`（保留 `train_dataloader` 兜底 + 找不到时告警），并按"标记"而非"函数身份"做幂等注册。已用真实训练验证：修复前 trace 里 `set_epoch` 一次都没有，修复后每轮都有，且 `augment masks @ epoch N` 开始出现在训练日志里。

### 9.4 验证

| 项 | 结果 |
|---|---|
| 测试 | `pytest tests/ -k ooo -q` → **146 项通过**（本文写作时点的实测值；后续 `img_origin` 重构、审查修复与 `slice_target_tiles` 之后为 **179 → 183 项**，以 `项目说明.md` 的「验证状态」为准）（新增：ratio=1 逐位兼容、ratio 定长、池长跨 epoch 恒定、`ratio=0` 边界、布局与选择集一致性、`keep_origin` 收窄、epoch 回调真的发布、上游属性名、**零侵入（全开关默认关闭时池 == 原图集）**） |
| 兼容性 | `tools/ooo_layout_snapshot.py --ratio 1.0 --compare` → **逐位 IDENTICAL**（82 索引、段长 `[32,8,8,16,2,8,8]`） |
| 池规模 | N=40 / ratio 0.1：`410 → 113`（B+）→ `77`（+§7.1） |
| 真实训练 | `_mini_val_set` 8 图 / 320px / 2 epoch / CPU，三个 arm（ratio 1.0 workers=0；ratio 0.1 workers=0 close=1；ratio 0.5 workers=2 close=1）全部跑通；`augment masks` 与 `raw-image LRU … % hit`（82–84%）正常，`close_aug_epoch` 在末轮生效 |
| 布局时间戳开销 | `_segment_bases()` 6.0 µs/次（改造前 2.5 µs）→ 24k 样本池约 0.15 s/epoch，可忽略 |

### 9.5 残留（非缺陷，供后续参考）

- `blur_axis_aligned`/`degrade_max_side` 等与本次无关的旋钮未动。
- `ratio >= 1` 时布局与旧形状一致，但池子仍然很大（`~10.25N`）；这是"全量增强"的固有代价，不是回退位。
- `close_aug_epoch` 的收尾 epoch 仍有 `ΣK_*` 个重复原图（ratio=0.1、N=8520 时约 4.9k 位，占新池 16.4k 的 30%）——语义上无害（收尾期本来就该全是真实原图），若将来要让收尾期也精确无重复，需要在 `close_aug_epoch` 边界重建 `nb` 与 sampler，代价大于收益。
