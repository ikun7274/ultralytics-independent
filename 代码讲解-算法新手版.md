# ultralytics_ooo 代码讲解（算法新手版）

> 这份文档带你看懂 `ultralytics_ooo/` 和 `tools/` 里每个文件是干什么的、怎么实现的。
> 假设你会 Python、知道什么是 YOLO 训练，但没读过 Ultralytics 源码。

---

## 0. 一句话总览

`ultralytics_ooo` 是一个**插件包**。它把原本要改 Ultralytics 源码才能实现的 5 个高级训练技巧，做成了"调用一行 `install()` 就自动挂上"的外挂：

1. **在线数据增强**：训练时实时把一张原图切成 4 片、加模糊、加雨雾、加遮挡……生成各种"虚拟样本"
2. **混合样本池**：把上面这些虚拟样本和原图按固定区段拼进一个大池子，DataLoader 从池子里按顺序取
3. **修补续训**：一个已经训练完的 `last.pt`，可以接着往更多轮数训
4. **切片验证（SAHI eval）**：验证时把大图切片推理再拼回来，专门解决小目标漏检
5. **双口径 mAP**：每轮同时算"切片指标"和"整图指标"，各存一个 best 权重

核心原则是**零侵入**：不改 Ultralytics 一行源码，只在程序运行时把某些函数"偷梁换柱"。

---

## 1. 先理解一个关键概念：什么是"零侵入 / monkey-patch"

正常做法：打开 `ultralytics/data/build.py`，把里面的数据集类改成自己的。
我们的做法：**不改文件**，而是在程序跑起来之后，在内存里把那个名字重新指向自己的类。

打个比方：

> Ultralytics 是一家餐厅，菜单上写着"数据集 = YOLODataset"。
> `install()` 做的事，就是趁餐厅营业时，偷偷把菜单上那道菜的后厨换成我们的厨房 `InstalledYOLODataset`。
> 菜单（源码文件）一个字都没改，但客人（训练流程）点到的已经是我们做的菜。

好处：
- 升级 Ultralytics 不会冲突（我们没改它的文件）
- 不想用了？删掉 `install()` 那一行，立刻回到原版
- 传任何开关都不设置时，行为和原版**逐字节一致**

---

## 2. `ultralytics_ooo/` 目录逐个讲

### 2.1 根入口文件

| 文件 | 作用 |
|------|------|
| `__init__.py` | 包入口。只做一件事：把 `install` 暴露出去。所以你能写 `from ultralytics_ooo import install` |
| `installer.py` | **整个包的总装车间**。`install()` 函数在这里把所有补丁打到原版上 |
| `dataset_class.py` | 定义最终那个"合体"数据集类 |
| `ruff.toml` | 代码风格检查配置（和功能无关） |

**`installer.py` 在干嘛（按顺序）：**

```python
from ultralytics_ooo import install
install()
```

执行这一行后，`install()` 依次做了 7 件事：

1. **注册新超参**：把 `slice_prob`、`blur_keep` 这些我们自己的开关，加到原版的配置对象 `DEFAULT_CFG` 上。这样 `model.train(slice_prob=1.0, ...)` 才不会报"未知参数"。
2. **换掉数据集工厂**：`build.YOLODataset = InstalledYOLODataset`——训练时建数据集，用我们的类。
3. **换掉数据增强函数**：把原版的 `v8_transforms` 换成我们的"在线增强版"（里面装了在线切片）。
4. **挂每个 epoch 的回调**：在每个 epoch 开始时，告诉数据集"现在是第几轮"，数据集据此重新随机抽一轮增强样本。
5. **打续训补丁**（`resume`）。
6. **打切片验证补丁**（`valslice`）。
7. **打双口径补丁**（`dual`）+ 分组采样 + fraction 兜底。

> 设计细节：`install()` 有个 `_INSTALLED` 标志，重复调用不会重复打补丁（幂等）。

**为什么 `dataset_class.py` 单独放一个文件？**
Windows 上 DataLoader 用多进程时，子进程要"反序列化"数据集对象。如果类定义写在 `install()` 函数里面，子进程找不到它就报错。所以这个"合体类"必须定义在模块顶层、独立成文件。

```python
class InstalledYOLODataset(OnlinePoolDataset, YOLODataset):
    # 多继承：左边是我们的"混合池"逻辑，右边是原版的数据集逻辑
    pass
```

---

### 2.2 `core/` —— 纯函数内核（不依赖 Ultralytics）

这个目录是"纯算子里"，只用到 numpy/cv2，不碰任何 Ultralytics 对象。好处：可以单独测试、单独复用。

| 文件 | 作用 |
|------|------|
| `geometry.py` | **切片几何**：给定原图宽高，算出 2×2 四张切片的坐标 `(x0,y0,x1,y1)`，支持重叠和"切缝偏移" |
| `degrade.py` | **退化算子**：运动模糊、雨/雾/噪声、遮挡、宽高比填充——所有"把图变差"的纯函数 |
| `saver.py` | **安全存图**：解决 OpenCV 在中文路径下静默失败的问题 |

**`geometry.py` 怎么切 4 片：**
- 把一张图横竖各一刀，分成左上、右上、左下、右下 4 块；
- `overlap_ratio=0.2` 表示相邻两块有 20% 重叠，避免目标正好被切缝劈成两半；
- `compute_slice_bias()` 会看目标框中心都集中在哪，把切缝挪到"目标最稀疏"的方向，减少目标被劈碎。

**`degrade.py` 有哪些算子：**
- `_apply_motion_blur`：运动模糊（模拟无人机飞太快拍虚）
- `_apply_weather`：rain（雨线）/ haze（雾）/ noise（高斯噪点）
- `_apply_occlusion`：rect（矩形遮挡）/ stripe（长条遮挡，模拟电线、树枝）
- `_ratio_pad_params`：把图 pad 成 4:3 或 16:9
- `_cap_long_side`：降采样到指定最长边，省内存

**`saver.py` 为什么重要：**
OpenCV 的 `cv2.imwrite` 遇到中文路径会**静默失败**（不报错但图没存上）。这里改用 `imencode` + `tofile`，并做了"每个进程只建一次目录"的优化。

---

### 2.3 `pool/` —— 混合样本池（核心业务逻辑）

这是最复杂的一层。每个文件负责一个能力，每个文件顶部都写着"给原版打哪个补丁"。

| 文件 | 一句话作用 |
|------|-----------|
| `constants.py` | 所有新超参的**默认值表**（单一事实来源） |
| `dataset.py` | **混合样本池本体**：把数据集索引空间扩成多个区段 |
| `augment_setup.py` | 在线切片 + 改写后的 `v8_transforms` |
| `sampler.py` | 分组采样器：同一张原图的所有子样本连续取 |
| `resume.py` | 修补续训 |
| `valslice.py` | 切片验证（SAHI eval） |
| `dual.py` | 双口径 mAP |
| `fraction.py` | 小数据集 fraction 兜底 |

**`constants.py`**
存放所有 `*_keep`、`slice_*`、`blur_*` 开关的默认值。原版 `default.yaml` 是用户可见默认的权威来源，这里只放"原版没有、我们新增"的键，避免两边漂移。

**`dataset.py`（最重要）**
原版 YOLO 数据集：索引 `0..N-1`，每个索引对应一张原图。
我们的 `OnlinePoolDataset`：把索引空间**拉长**成多个区段，每个区段放一类虚拟样本：

```
[0, 4N)        切片样本（每张原图切 4 片）
[4N, 5N)       原图
[5N, 6N)       宽高比填充图
[6N, 8N)       运动模糊图（短模糊+长模糊，×2）
[8N, 8N+N/4)   2×2 合成大图
[..., ...)     气象退化图
[..., ...)     遮挡图
```

DataLoader 调 `dataset[i]` 时，`get_image_and_label(index)` 根据 `index` 落在哪个区段，决定返回"切片 / 模糊 / 雨雾…"的样本。`set_epoch(epoch, total)` 每个 epoch 重新掷骰子：哪些图走增强、用什么随机参数。

**`augment_setup.py`**
原版 `v8_transforms` 负责"把一张图变成训练用张量"（Mosaic、翻转、HSV……）。我们包了一层，在前面插入 `OnlineSlice`——训练时在线把原图切成子块。

**`sampler.py`**
默认 DataLoader 是随机取索引。但我们的池子里，同一张原图可能衍生出切片、模糊、雨雾好几个样本。`GroupedImageSampler` 让它们**连续进出**，这样：
- 原图在内存缓存里只解码一次，多个增强样本共享（LRU 命中率高）
- Mosaic 拼 4 张图时更容易拿到同一张原图的不同版本

**`resume.py`（修补续训）**
问题：原版如果发现 ckpt 已经训练完了，就拒绝续训；而且你新传的 `epochs=200` 会被 ckpt 里的旧值覆盖。
解决：monkey-patch `check_resume` 和 `resume_training`，当你传 `resume_extend_epochs=200` 时，把 ckpt 的元数据（当前轮数、总轮数、patience）改掉，重建学习率调度表，让它接着训到 200 轮。

**`valslice.py`（切片验证）**
训练时我们在线切片，但验证如果还是整图推理，小目标会被降采样看不清，mAP 被低估。
这里：把每张验证图切成 2×2 子块 → 每块单独推理 → 把子块的预测框坐标**加回偏移量还原到原图** → 跨子块的重复框用类别级 NMS 合并 → 再和 GT 算 mAP。这就是 SAHI。

**`dual.py`（双口径）**
每轮验证跑两遍：
- 第一遍切片推理 → 指标驱动 `best.pt`
- 第二遍整图推理 → 指标存成 `whole_*` 列，驱动另存 `best_whole.pt`

这样你能公平对比"切片训练 vs 整图训练"的真实效果。还顺手修了一个 bug：双口径多出来的列会让原版绘图函数崩，这里包装了一下让绘图忽略这些列。

**`fraction.py`**
原版 `fraction` 参数在小数据集上 `round(8 * 0.01) = 0`，数据集变空，后面莫名其妙报 `IndexError`。这里兜底：如果切完是空的，就至少保留原图。

---

## 3. `tools/` 目录逐个讲（调试/测量工具，不参与训练）

这些是**开发期的测量脚本**，帮你搞清楚"池子里到底装了什么、性能怎么样、改坏没有"。

| 脚本 | 作用 | 什么时候用 |
|------|------|-----------|
| `ooo_pool_measure.py` | 打印当前配置下池子里每个区段占多少槽、覆盖多少张原图 | 改完 `*_ratio` 想确认"增强真的按比例生效了" |
| `ooo_ab_ratios.py` | 对比"旧版每图都增强"vs"新版按比例增强"的 A/B 模板 | 验证 `*_ratio` 开关有没有真的生效 |
| `ooo_slice_knobs.py` | 报告 4 个切片开关（`slice_prob`/`slice_all_tiles`/`slice_ratio`）实际往池子里放了多少样本 | 调切片参数时先跑这个看分布 |
| `ooo_layout_snapshot.py` | 给"索引→样本"映射打一个确定性指纹，改布局后可以逐位 diff | 重构后验证"输出和以前一模一样" |
| `ooo_compat_check.py` | 探针：检查这套补丁能不能在当前这个 Ultralytics 版本上装上、生效 | 换 Ultralytics 版本后第一件事跑它 |
| `ooo_perf_gen_data.py` | 生成拟真的合成检测数据集（平滑渐变+软形状，JPEG 大小接近真实） | 做性能测试时造数据 |
| `ooo_perf_bench.py` | 对每个算子（JPEG 解码、模糊、雨雾……）做微基准 | 想知道"哪个算子最吃 CPU" |
| `ooo_perf_run.py` | 端到端测量：通过真实 DataLoader 报池几何、采样器类型、Mosaic 放大倍数、解码数、缓存命中率、RSS 内存 | 评估一个配置的整体训练吞吐 |

**为什么需要这么多测量工具？**
因为混合样本池最大的坑是" silently wrong"——比如 `*_ratio` 没传到数据集，结果每张图都被 100% 增强，但日志看不出来。这些工具就是把"池子里到底有什么、采样器用了哪个、缓存命中多少"**显式打印出来**，让你一眼能发现配置和实际行为不一致。

---

## 4. 一次训练样本的完整旅程

```
原图 a.jpg
  │
  ├─ 区段1: OnlineSlice 切成 4 张子图 ──► 样本 idx 0..3
  ├─ 区段2: 原图直通                ──► 样本 idx 4
  ├─ 区段3: ratio_pad 加边框         ──► 样本 idx 5
  ├─ 区段4: 短模糊 + 长模糊          ──► 样本 idx 6,7
  ├─ 区段5: 和别的图拼成 2×2         ──► 样本 idx 8
  ├─ 区段6: 加雨/雾/噪声             ──► 样本 idx 9
  └─ 区段7: 加矩形/条带遮挡          ──► 样本 idx 10
          │
          ▼
  DataLoader 按 GroupedImageSampler 取一批
          │
          ▼
  Mosaic 再把 4 张拼成一张 → 网络训练
```

每个 epoch 开始时，`set_epoch` 告诉数据集"本轮随机选哪些图走增强"，所以同样一张图每轮增强的程度、参数都不同，起到正则化作用。

---

## 5. 新手常见疑问

**Q：我不想用某个功能怎么办？**
A：不传对应开关即可。所有开关默认关（或按 `constants.py` 默认），不传就和原版一样。比如不要切片，就别传 `slice_prob=1.0`。

**Q：`install()` 改了我的 Ultralytics 源码吗？**
A：没有。只在内存里换了名字指向，磁盘上的 `.py` 文件一个字节没动。删掉这行就恢复。

**Q：为什么有的脚本叫 `patch_xxx`？**
A：这就是 monkey-patch 的意思——运行时把原版的某个函数替换掉。每个文件一个补丁、一个幂等标志（`_ooo_xxx_patched`），重复 install 不会打两次。

**Q：core 和 pool 有什么区别？**
A：`core/` 是"纯数学"，不依赖 Ultralytics，好测试；`pool/` 是"业务编排"，知道 Ultralytics 的数据集/训练器长什么样，负责把纯算子组装进训练流程。

---

## 6. 想继续深入，建议的阅读顺序

1. `installer.py`（总览，知道都装了啥）
2. `core/geometry.py` + `core/degrade.py`（纯算子，最简单）
3. `pool/dataset.py` 的 `get_image_and_label`（看索引怎么分流到各个区段）
4. `pool/sampler.py`（看分组采样）
5. `pool/valslice.py` + `dual.py`（验证侧的 SAHI 和双口径）
6. `tools/ooo_pool_measure.py`（跑一下，对照它的输出读 `dataset.py` 最直观）
