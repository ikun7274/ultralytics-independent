# 更新说明

> 本次更新内容：依据 `代码质量审查报告.md` 的结论，**按优先级顺序完成全量修复**，并补齐对应的回归测试与工程化门禁（第一 ~ 八节）；随后在审查过程中立项的**样本池 ratio 定长切分（B+）**也按推荐顺序实施完毕（**第九节**）。
> 更新日期：2026-09-15　｜　目标版本：干净 Ultralytics **8.4.126**（未修改上游任何源码、未改上游 `default.yaml`）
> 验证环境：conda `D:\miniconda3\envs\langchain`（Python 3.12.13 / torch 2.5.1 / opencv 5.0.0.93）

---

## 〇、一句话总结

**测试基线从 16 项起点逐轮扩充**（各轮的数字与结果只维护在第七节的测试基线表里，本文档正文不写死测试数；第十三轮起有 6 条既有红测试，根因与本轮改动无关，证据见 §21.4）；第一轮修复 3 个严重缺陷（其中 1 个是修复过程中新增集成测试才发现的）、9 个中等缺陷、14 个轻微缺陷，并新增 lint 配置、CI 门禁与 A/B 对照模板；第二轮把样本池从"按最大值预分配 + 原图回退"改成"按 ratio 定长切分"，池缩减 **5.32×**（N=40 实测 `410 → 77`），并修掉一个让**每轮抽样与 `close_aug_epoch` 从未生效**的静默缺陷（S4）；第五轮新增 `slice_target_tiles`（按目标挑片 + 跨 epoch 不重复调度）；第七轮按性能审查修复三处缓存/采样失配（LRU 容量 4→16 + 分组门控 + 删 oversized 守卫），真实 loader 吞吐上限 **2.17×**；第八轮把切片段显示名 `base` → `sahi`；第九轮按 `性能审查报告-全面重审.md` 的优先级顺序落地 P0/P1 —— 训练切 GPU、`workers≤2` 与 `prefetch_factor` 可配置、修掉让自带工具**永远测不到分组采样器**的导入顺序缺陷、`origin` 段改走 LRU、切片 tile 由连续拷贝改为视图（4K 下整体 **2.93×**），并纠正了审查自身一处被机器状态污染的跨会话收益数字（**1.09× 才是真值**）；第十轮补齐双口径的 checkpoint 家族（`last_whole.pt`）并让收尾 stage 把两个整图检查点一并 strip（此前它们会以 fp32 + optimizer 状态存活）；第十一轮修掉 `close_aug_epoch` 收尾窗口的一个覆盖缺陷 —— 窗口里各分支原本都取固定前缀 `range(K)`，导致 N=8 时**只看得到 4 张图、另 4 张完全不出现**且每轮相同，删掉该短路后覆盖恢复 8/8 且子集随 epoch 轮换；**第十二轮换成外部视角**（从零独立验证：静态通读 4.7k 行 + 独立探针 + 真实训练），修掉两个"全绿门禁下静默存在"的缺陷 —— mosaic buffer 记账语义与上游不同（导致**不传任何开关时也与上游不等价**）与双口径下 `results.png` 丢失，并顺带修掉 `cache` 缓存旁路、舍入告警文案与 occlusion 标签长度不齐三处，详见第二十节；**第十三轮落地六模块性能修复 F1–F4**（噪声算术 / 比例填充 / 雨滴 `dst=` / 去掉退化分支白做的交接拷贝），四处改动**全部逐像素等价、无行为变更**，并在验收过程中发现三条**空转的**契约测试（注入缺陷时仍全绿）与验收工具自身的多处缺陷，详见第二十一节与第二十二节。

> ⚠️ **会改变训练行为的修复**：第一轮 5 处见第五节；第二轮 5 处见第九节 §9.8；**第十二轮的 S1 也算一处**（mosaic 混样伙伴池的内容变了，见 §20.2）。如果你的历史实验结论依赖旧行为，请先读这些章节再决定是否重跑。

---

## 一、严重缺陷修复

### S1 双口径验证自 epoch 2 起静默退化为整图单口径

**问题**：`pool/dual.py::_run_whole` 为跑"整图第二遍"把 `v.dataloader` 换成整图 loader，`finally` 只还原了 `val_slice_enable`，未还原 loader。而 validator 实例全训练只创建一次，上游 `validator.py:235` 又是 `self.dataloader = self.dataloader or ...` 的真值短路，于是**从第 2 个 epoch 起，主（切片）口径实际跑在整图 loader 上**，`update_metrics` 因批次里没有 `val_slice_meta` 而回落到原生整图路径。`best.pt` / 早停 / 续训所依据的"切片主口径"因此失效，全程零告警。

**修复**（`pool/dual.py`）
- `_run_whole` 在进入时保存 `orig_loader`，在 `finally` 中**还原**；
- 整图 loader 只构建一次并缓存到 `self._whole_val_loader`（顺带修掉 M6：此前每轮都重建 `build_dataset`）；
- 整图那一遍失败时也能正确还原（`finally` 覆盖异常路径）。

**回归测试**：`tests/test_ooo_dual.py` 复刻上游 `validator.py:235` 的短路契约，连跑 3 个 epoch 断言主口径恒为 `SLICED` / `sliced-metrics`。
在**未修复**的代码上该测试失败并给出 `['SLICED', 'WHOLE', 'WHOLE']` —— 证明它确实能拦住这个 bug。

### S2 六个保存分支默认全开，同步 JPEG 编码+落盘压在取样路径上

**问题**：`train.py` 出厂即把 `slice_save_annotated=True`、`compose_save=True` 与 6 个 `*_save_dir` 全部打开，且 `slice_save_max=0`（不限量）。保存是**同步**的 `cv2.imencode` + 磁盘写，内联在每条分支的取样函数里；切片分支保存用的是**原图分辨率** tile，不受 `degrade_max_side` 限幅约束。实测单次带标注写盘：1280×960 = **23.6 ms**、4000×3000 = **182.6 ms**。

**修复**（`train.py`）
- 6 个 `*_save_dir` 全部注释化（保留为可直接启用的示例）、`compose_save=False`；
- `slice_save_max=100` 作为默认上限；
- 附注释说明"需要抽查时只开一条分支并限量"的原因与实测代价。

> 包本身默认是关闭的（`_ONLINE_DEFAULTS` 里各 `*_save_dir` 为空），此次只收敛了示例脚本的默认值。

### S3 三个"每 epoch 增强比例"开关从未到达 dataset，配置被静默忽略 ★修复阶段新发现

**问题**：`_rebuild_epoch_masks` 用 `getattr(self, ratio_attr, _online_default(ratio_attr))` 读取比例。`v8_transforms` 镜像了 `slice_ratio` / `weather_ratio` / `occlusion_ratio`，却**漏掉了 `ratio_pad_ratio` / `blur_ratio` / `compose_ratio`** → 属性不存在 → 静默取到兜底值 `1.0` → 掩码恒为 `None` → **这三条分支对每张原图、每个 epoch 全部生效**。

实测（4 图，六个比例全配 `0.1`）：

| 分支 | 属性是否存在 | 实际增强比例 |
|---|---|---|
| slice / weather / occlusion | ✅ | 0%（按配置，`round(0.1×4)=0`） |
| **ratio / blur / compose** | ❌ | **100%（配置被忽略）** |

`train.py` 配的正是 `0.1`，注释也写着"每 epoch 随机选 round(x*N) 张原图"，实际是 100% 全量 —— 三条分支与另外三条形成 10 倍失衡，且因池长度恒定（未选中位回退原图）而**不报任何错**。

**修复**
- `pool/augment_setup.py`：与 `weather_ratio` / `occlusion_ratio` 同样显式镜像三个比例到 dataset；
- `pool/dataset.py`：修正 `_mask_specs` docstring 里"training always copies them from default.yaml"这句从未成立的断言。

**回归测试**：`tests/test_ooo_branches.py::test_every_mask_ratio_knob_actually_reaches_the_dataset` 遍历 `_mask_specs()` 的每一行，要求比例属性存在、且 `ratio=0.5` 时确实产出掩码。**表驱动的开关必须配一个遍历该表的测试**，否则漏一行只能靠人工发现。

---

## 二、中等问题修复

| 编号 | 修复内容 | 文件 |
|---|---|---|
| **M1** | 补回上游 seed 表达式的 `+ seed` 项，并把 `seed` 传给 `GroupedImageSampler`。此前分组路径的 generator 种子恒为 `6148914691236517204`，`args.seed` 对 worker 的 numpy/random 随机流**完全失效** | `pool/sampler.py` |
| **M2** | 补回被丢掉的 npu/xpu `pin_memory_device` 选择；新增 loader 属性漂移测试，把 nw / prefetch_factor / pin_memory / drop_last / generator seed / worker_init_fn 全部与上游公式对齐断言 | `pool/sampler.py`、`tests/test_ooo_sampler.py` |
| **M3** | `best_whole.pt` 的写盘判定从浮点 `==` 改为显式 `_whole_improved` 标志。此前本轮 whole fitness 与历史最优**打平**时也会用当轮 `last.pt` 覆盖掉真正的历史最优 | `pool/dual.py` |
| **M4** | `_ONLINE_DEFAULTS` 从 174 条精简到 **77 条（仅项目自有键）**，删掉 96 条复述上游 `default.yaml` 的键（实测这些行对 `DEFAULT_CFG` 是死代码、对约 120 处 `getattr` 兜底却是活代码，上游升版即静默漂移）。同时在 `install()` 中新增自检 | `pool/constants.py`、`installer.py` |
| **M5** | `_sync_epoch_masks` 的两处静默 `except` 改为**每进程一次**的告警。此前掩码同步失败会让所有 `*_ratio` 与 `close_aug_epoch` 无声失效 | `pool/dataset.py` |
| **M6** | 整图 val loader 构建一次并复用（并入 S1 修复） | `pool/dual.py` |
| **M7** | `hyp` / `fraction` 改为按 `BaseDataset.__init__` 的真实签名解析，位置传参不再静默回退到默认值 | `pool/dataset.py` |
| **M8** | `OnlineSlice` 补齐 `bias_margin` / `bias_jitter` 校验；`slice_geometry` 夹紧偏移，**保证两个方向的 tile 宽/高 ≥ 1**。此前 `slice_bias_margin=0` + `slice_overlap_ratio=0` 会产生 0 面积切片，在 DataLoader 子进程内炸出 `cv2.resize (-215:Assertion failed) !ssize.empty()` | `pool/augment_setup.py`、`core/geometry.py` |
| **M9** | 保存目录校验前移到**构造期**（`v8_transforms` 组装时，主进程内），不再等到首次保存才在 worker 里抛错 | `pool/augment_setup.py` |

---

## 三、轻微问题修复

| 编号 | 修复内容 |
|---|---|
| **L1** | 删除 10 处真实死导入（`augment_setup.py` 的 `math`/`time`/`torch`/`Instances`；`constants.py` 的 `numpy`；`dataset.py` 的 `bisect`/`time`/`torch`/`Dataset`；`valslice.py` 的函数内 `Path`） |
| **L2** | `_save_cap` 双实现收敛为单实现（删除 `core/saver.py` 中无人调用的那份，保留 `pool/dataset.py` 中读包默认表的那份），并消除模块级对导入的遮蔽（F811）；首参由 `self` 改名 `dataset` |
| **L3** | `core/__init__.py` 的 `__all__` 补齐 `_RATIO_PAD_COLORS` |
| **L4** | 修正 3 处文档与实现矛盾：`installer.py`/`__init__.py` 引用不存在的子模块名；`_hyp_get` docstring 自称会 `raise ValueError`（实际回落默认表）；运动模糊"~1.8x faster"改为实测 **5.3–5.5x**（1280×960：9.5 vs 52.4 ms） |
| **L5** | `_geometry` 支持 `only=k`，mode A 与 `slice_at` 只计算被使用的那个 tile。此前 mode A 白算 3/4；`slice_all_tiles` 下 4 次 `slice_at` 各算 4 个 tile（16 个顶 4 个用） |
| **L6** | val 侧 `_load_image_cached(copy=False)`：切片只读该数组，省掉每个 tile 一次全分辨率 memcpy（4000×3000 实测 13.6 ms，`all_tiles` 下每图约 54 ms）；对不支持该参数的 base 自动降级 |
| **L7** | `degrade_max_side` / `compose_max_side` 取负值关闭工作分辨率上限时告警一次（关闭后 weather-noise 峰值 18.4 MB → 180 MB，dense-PSF 模糊 52 → 451 ms） |
| **L9** | `_patch_plot_results` 改用 `TemporaryDirectory`，不再每次 `plot_results` 泄漏一个 `ooo_plot_*` 临时目录 |
| **L10** | `_remap_boxes` 去掉未使用的 `self`，按 `staticmethod` 挂载（与 `_class_wise_nms` 一致） |
| **L11** | 抽出 `pool/fraction.py`，`patch_fraction_guard` 归位，恢复"一个 patch 一个模块"约定 |
| **L12** | 抽出三条共享 helper（`_begin_branch_label` / `_save_branch` / `_finish_branch`），`blur` / `weather` / `occlusion` / `ratio` / `compose` 五条分支复用，消除每条约 8 行的重复骨架 |
| **L14** | `train.py` 的 `project` 改为相对路径，仓库可搬迁 |
| **L13** | ⏸ **未改**：`_geometry` 内 bbox 格式转换的 4 段重复。属纯可读性，改动收益低于回归风险，留作后续 |
| — | 顺带移除 `prefetch_factor` 配置键（读入 `dataset.prefetch_factor` 但**从未被任何 loader 使用**，上游硬编码 4），详见第五节 |

---

## 四、工程化（原清单第 12 项）

| 项 | 内容 |
|---|---|
| **Lint 配置** | 新增 `ultralytics_ooo/ruff.toml`：只作用于本包（ruff 就近发现配置），规则针对本包**真实出现过**的缺陷类别选择 —— `F401`（死导入）、`F811`（重定义/遮蔽）、`B009`（`getattr` 常量属性访问，即 `_hyp_get` 存在的理由）、`BLE`（盲 `except`，正是 M5 的病根）、`B`、`E/W`、`I`、`UP`；`E501` 与 `UP031/032` 显式忽略并注明原因 |
| **CI 门禁** | 新增 `.github/workflows/ooo-quality.yml`：`ruff check ultralytics_ooo` + `pytest tests/ -k ooo`（Python 3.10 / 3.12 矩阵），仅在本包与 `tests/test_ooo_*.py` 变更时触发 |
| **不改上游文件** | 完全未触碰 `pyproject.toml`（属上游 Ultralytics），lint 配置放在包内，符合"零侵入"契约 |
| **未安装 ruff** | 为**不改动用户 conda 环境**，未在 `D:\miniconda3\envs\langchain` 中安装 ruff。等价规则已用自制 AST 检查器复核：`F401` / `F811` / 浮点相等比较**全部归零**。首次 CI 运行如报 `I`(import 顺序) / `UP` 类提示，可用 `ruff check --fix ultralytics_ooo` 自动修复后移除 workflow 里对应的 `continue-on-error` |

---

## 五、⚠️ 行为变更（需要你确认的部分）

以下 5 处修复会让**相同配置产生不同的训练结果**。历史实验若依赖旧行为，建议重跑或至少重新评估结论。

| # | 变更 | 影响 |
|---|---|---|
| 1 | **S3：`ratio_pad_ratio` / `blur_ratio` / `compose_ratio` 现在真正生效** | `train.py` 配的 `0.1` 从"实际 100%"变为真正的 10%。这三条分支的样本占比大幅下降，"未选中回退原图"的比例大幅上升 —— **训练数据的有效构成变化最大的一处**。若此前按旧行为调过参，需重新评估 |
| 2 | **S1：双口径自 epoch 2 起真正走切片口径** | `best.pt` 的选择依据从"整图 fitness"变为"切片 fitness"。**修复后的 `best.pt` 会与历史实验里的不同**，`results.csv` 的主指标列也不再与 `whole_metrics/*` 相同 |
| 3 | **M1：`seed=` 现在真正影响分组采样路径的随机流** | 同一 `seed` 下，增强随机流与修复前**不同**；好处是换 seed 终于能复现出不同结果。与关闭 `slice_grouped_sampler` 的同配置运行也不再可比 |
| 4 | **M8：`slice_bias_margin` 必须 > 0** | 出货默认 `0.25` 不受影响；仅当显式设为 `0` 时现在会在构造期报错（此前会生成退化切片并在训练中途崩溃）。`slice_overlap_ratio=0` 仍合法 |
| 5 | **`prefetch_factor` 不再是合法参数** | 此前传入被静默忽略（读入一个从未被使用的属性）；现在传入会报"未知参数"。同时 `OnlineSlice` 在 `slice_save_exist_ok=False` 且目录已存在时改为**构造期**报错（默认 `True`，不受影响） |

---

## 五·补 行为变更处置建议（推荐方案）

### 总览：建议怎么处理

| # | 变更 | 推荐方案 | 需要重跑历史实验？ |
|---|---|---|---|
| 1 | S3 三个比例开关生效 | **接受，并重新标定三个比例**；若要做修复前后 A/B，把三个比例显式设为 `1.0` 即可**逐位复现旧行为** | 若旧结论依赖"这三条分支全量增强"→ 是 |
| 2 | S1 双口径自 epoch 2 起生效 | **接受**；按"线上推理是整图还是切片"决定部署产物与验证口径 | 是（旧的 `best.pt` 口径本身就是混合的，不可信） |
| 3 | M1 `seed=` 生效 | **接受**，当作实验纪律改善；**不要试图逐位复现旧运行** | 否（但旧运行的"可复现"是假象） |
| 4 | M8 `slice_bias_margin` 必须 > 0 | **默认配置无需动作**；若曾设为 `0`，改 `0.05` | 否 |
| 5 | `prefetch_factor` 移除 / 保存目录校验前移 | **接受**；已实测确认**旧 checkpoint 续训不受影响** | 否 |

**本轮据此落地的配套改动**（推荐方案中需要动代码/配置的部分）：

| 项 | 落地内容 |
|---|---|
| #1 可观测性 | `pool/dataset.py` 新增每 epoch 一行"各分支实际选中数"日志（`_log_mask_summary`），并在 `ratio > 0` 但取整为 0 时告警（`_warn_ratio_selects_nothing`）。**这是修复前最缺的一行**——当时配置写 0.1、实际跑 100%，日志里没有任何东西戳破它 |
| #1 语义说明 | `train.py` 顶部补充 `*_keep`（占不占池位）与 `*_ratio`（强度）的分工、取整陷阱、以及"日志怎么看" |
| #1/#3 A/B 模板 | 新增 `tools/ooo_ab_ratios.py`：默认**干跑**（不训练，直接打印两个 arm 的实际选中数），`--train` 时额外跑两次训练并汇总 `results.csv` 对比 |
| #2 场景配置 | `train.py` 的 `val_slice_*` 段补充三种场景的收敛建议（调参 / 整图部署 / 切片部署）与"口径对齐才能与历史对比" |
| #5 不做 | 未加回 `prefetch_factor` 旋钮（避免再加一处与上游的偏差），仅在文档说明如何按需加回 |

---

### #1 S3（影响最大）—— 接受修复，但要理解 `*_ratio` 的真实语义

**推荐**：接受修复，并把这三个比例当作"新引入的真实旋钮"重新标定。`train.py` 现在的 `0.1` 就是文档原本承诺的语义。

**关键点：`*_ratio` 控制的是"这条分支的强度"，不是"这条分支占多少池位"。**
池长度是恒定的（区段长度只由 `*_keep` 决定）：比例之外的位置不会消失，而是**内容被替换成"原图直通"**。所以：

- 想让池子**变小**、这条分支**彻底不参与** → 关 `*_keep`（池减少 N / 2N / ceil(N/4)）；
- 想**降低这条增强的强度**（仍保留它的池位） → 降 `*_ratio`。

**小数据集上的取整陷阱（实测，`tools/ooo_ab_ratios.py` 可直接复现）**：选中数 = `round(ratio × N)`。

出货的迷你冒烟集 `_mini_val_set` 有 **8 张**训练图，compose 是**组级**的（组数 = `ceil(8/4)` = 2），所以三个键在 `0.1` 下的实际行为**并不一致**：

| 分支 | count | `ratio=0.1` 时选中 | 结果 |
|---|---|---|---|
| `ratio_pad_ratio` | 8（原图级） | `round(0.8)` = 1 | 1 张真正做比例填充，其余 7 张退化为原图直通 |
| `blur_ratio` | 8（原图级） | `round(0.8)` = 1 | 1 张做模糊（短+长同命运），其余 7 张退化 |
| `compose_ratio` | **2（组级）** | `round(0.2)` = **0** | **完全不合成** —— 日志打 `<-- NONE` 并告警 |

一般规律（以 N=4 为例）：

| ratio | N=4 时的增强位数 | 含义 |
|---|---|---|
| `1.0` | 4 / 4 | 全量增强（= 修复前行为） |
| `0.5` | 2 / 4 | 一半 |
| `0.25` | 1 / 4 | 一条 |
| `0.1` | **0 / 4** | **该分支不产生任何增强样本**（全部退化为原图直通，**池位仍保留**） |

**注意 compose 更容易归零**：它的 count 是 `ceil(N/4)`，只有原图数的四分之一，所以 `compose_ratio` 需要 `>= 0.5/ceil(N/4)` 才能选到至少一组（N=8 时需 `>= 0.25`）。想在小数据集上看到 compose 生效，用 `1.0`；真实数据集（如 8520 图 → 2130 组）上 `0.1` → 213 组，正常。

两个 arm 的**池长度完全相同**（实测迷你集两种配置都是 82 样本，layout `[32, 8, 8, 16, 2, 8, 8]`）——比例只把增强位换成原图直通位。

**具体做法**
```python
# (A) 复现修复前的行为，用于做修复前后 A/B 对照（与旧代码逐位一致）
ratio_pad_ratio=1.0, blur_ratio=1.0, compose_ratio=1.0,
weather_ratio=1.0, occlusion_ratio=1.0, slice_ratio=1.0,

# (B) 按文档意图（推荐）
ratio_pad_ratio=0.1, blur_ratio=0.1, compose_ratio=0.1,   # 需满足 ratio x count >= 0.5
```

**可观测性（本次新增）**：每条分支每 epoch 的**实际选中数**现在会打印在主进程日志里，一行就能看出配置是否真的生效：

```
augment masks @ epoch 0: slice all/8 (ratio 1) | ratio 1/8 (ratio 0.1) | blur 1/8 (ratio 0.1) \
                       | compose 0/2 (ratio 0.1)  <-- NONE | weather all/8 (ratio 1) | occlusion all/8 (ratio 1)
```
`all/N` = 全量增强（ratio ≥ 1），`sel/N` = 只有 sel 个位真正增强，`<-- NONE` = 该分支本次一个都没选到。**这正是修复前缺失的那一行** —— 当时无论配置写多少，日志都只会显示 `all/N`。同时，`ratio > 0` 但取整为 0 时会额外发一次告警。

---

### #2 S1 —— 接受，并按"部署口径"选择产物

**推荐**：接受修复。修复前 epoch 2 之后 `best.pt` 与 `best_whole.pt` 其实是**同一个（整图）口径**选出来的，两者区别是假的；修复后 `best.pt` = 切片口径、`best_whole.pt` = 整图口径，才真正有意义。

**历史对比的正确做法**：旧运行的 `best.pt` 口径本身是**混合**的（epoch 1 按切片选、epoch 2 起按整图选），不可直接用来对比。要比就**口径对齐比**：
> 新运行的 `best_whole.pt` ↔ 旧运行的 `best.pt`（两者都是整图口径）。

**按场景收敛验证口径**（双口径每轮多跑一整遍验证，是报告里 P2 的耗时来源）：

| 场景 | 建议配置 | 说明 |
|---|---|---|
| 调参 / 摸清两个口径的差距 | `val_slice_enable=True, val_slice_dual_metric=True` | 两个都看，代价是验证跑两遍 |
| 线上是**整图**推理 | `val_slice_enable=False` | 回到纯整图，验证最省时，`best.pt` 即部署产物 |
| 线上是**切片（SAHI）**推理 | `val_slice_enable=True, val_slice_dual_metric=False` | 只跑切片主口径，省掉第二遍整图验证 |

---

### #3 M1 —— 接受，当作实验纪律的改善

**推荐**：接受。旧行为看着"可复现"，实则是**对 seed 不敏感**——同 seed 恒定、换 seed 也一样，等于你拿不到独立的随机重复。修复后 `seed` 才真正有效。

**建议的实验纪律**
- 每个实验**记录所用 `seed`**；
- A/B 对照**固定同一 seed**（现在跨 arm 同 seed 才真正可比，修复前这点是无效的）；
- 要估计种子方差就跑 3 个不同 seed，不要指望靠"换 seed 得到不同结果"以外的办法；
- 配合默认的 `deterministic=True`，同 seed 运行现在才**逐位可比**。

**不要**为了复现某次旧运行去把 loader generator 种子钉死（那是恢复到"seed 无效"的旧状态），不值得。

---

### #4 M8 —— 默认配置无需动作

出货默认 `slice_bias_margin=0.25`（合法），`train.py` 也没改，所以**不设该参数就没有任何影响**。

若某处配置写了 `slice_bias_margin=0`：
```python
slice_bias_margin=0.05,   # 替代 0：仍然允许切缝大范围搜索，但不会贴到图像边缘
```
理由：`0` 允许切缝落在图像边界，即使几何已被夹紧到 ≥1 px，那一侧也会退化成 **1 px 的"废切片"**——不再崩溃，但等于白丢一个样本。

推荐组合（开启目标感知切缝时）：
```python
slice_center_bias=True, slice_bias_margin=0.25, slice_bias_jitter=0.05,
slice_overlap_ratio=0.2,        # 与 center_bias 同开时建议 >= 0.1，给切缝留余地
```

---

### #5 `prefetch_factor` / 保存目录校验 —— 接受，续训安全（已实测）

**`prefetch_factor` 的移除是否影响旧 checkpoint 续训？→ 不影响。** 实测：

- 新增训练时传 `model.train(prefetch_factor=2)` → 报清晰错误
  `SyntaxError: 'prefetch_factor' is not a valid YOLO argument.`
- 但**旧 checkpoint 的 `train_args` 里带着 `prefetch_factor` 续训正常**：`trainer.check_resume` 走的是
  `self.args = get_cfg(ckpt_args)`，ckpt 的 train_args 是**基准（base）而不是 overrides**，而
  `check_dict_alignment` 只校验 overrides 中不存在的键、从不校验 base 的多余键。实测 `get_cfg(ckpt_args)` 正常通过，`prefetch_factor` 只是变成一个无人读取的惰性属性。

所以这是安全清理：**已有 checkpoint 可以照常 `resume`，只是别再在新训练里传这个参数。**

若确实需要调 `prefetch_factor`：目前上游硬编码 4，包内没有入口。需要的话可以按需加回一个"真正生效"的版本（读 `dataset` 上的值并保证与上游默认一致），当前不做是为了避免再加一处与上游的偏差。

**保存目录校验前移**：`slice_save_exist_ok` 默认 `True`，**无影响**。仅当显式设 `False` 且目录已存在时，现在在**数据集构建期（主进程）** 报错，而不是训练数分钟后在 DataLoader 子进程里抛错——这是期望中的改进。显式设 `False` 时请先清空或更换目录。

---

## 六、文件清单

### 新增

| 文件 | 说明 |
|---|---|
| `ultralytics_ooo/pool/fraction.py` | fraction 兜底 patch 独立成模块（L11） |
| `ultralytics_ooo/ruff.toml` | 本包专属 lint 配置 |
| `tools/ooo_ab_ratios.py` | S3 行为变更的 A/B 对照模板：干跑打印两 arm 的实际选中数，`--train` 汇总 `results.csv` 对比 |
| `tools/ooo_pool_measure.py` | 池膨胀量化工具：逐段统计"真增强位 vs 原图回退位"，并给出按 ratio 定长切分的对照布局 |
| `.github/workflows/ooo-quality.yml` | lint + 测试的 CI 门禁 |
| `tests/test_ooo_dual.py` | 双口径跨 epoch 不退化为整图口径；`best_whole.pt` 判定（9 项） |
| `tests/test_ooo_branches.py` | 临时数据集端到端：7 区段总长、各分支标签结构、掩码回退、遮挡剔除、`close_aug_epoch`、表驱动比例开关、每 epoch 选中数日志（16 项） |
| `tests/test_ooo_sampler.py` | 分组采样种子对齐上游、loader 属性漂移（7 项） |
| `tests/test_ooo_guards.py` | 切片几何不退化、构造期校验、`only=k` 等价性、位置传参解析、配置表不撞上游（77 项参数化） |
| `代码质量审查报告.md` | 本轮审查报告（含附录 C 修复状态） |
| `样本池膨胀分析与方案.md` | 后续议题：「未选中位退回原图直通」的池膨胀可行性分析（含实测量化与推荐方案 B+） |
| `更新说明.md` | 本文件 |

### 修改

| 文件 | 改动摘要 |
|---|---|
| `train.py` | 关闭 6 个保存目录与 `compose_save`、`slice_save_max=100`、`project` 改相对路径；补充 `*_keep`/`*_ratio` 语义与取整陷阱说明、验证口径的三场景收敛建议 |
| `ultralytics_ooo/installer.py` | 配置表自检（在注册前取上游快照）；修正文档中不存在的子模块名 |
| `ultralytics_ooo/__init__.py` | 修正子模块清单（`resume`/`valslice` 实际在 `pool.` 下） |
| `ultralytics_ooo/pool/dual.py` | S1（loader 还原 + 缓存）、M3（显式改进标志）、L9（临时目录） |
| `ultralytics_ooo/pool/dataset.py` | M5（告警）、M7（签名解析）、L1（删死导入）、L2（`_save_cap` 去重）、L5（`only=k`）、L7（限幅告警）、L11（移出 fraction）、L12（三条共享 helper）、移除哑开关 `prefetch_factor`；**新增每 epoch 各分支实际选中数日志 + `ratio` 取整为 0 的告警** |
| `ultralytics_ooo/pool/augment_setup.py` | S3（镜像三个比例键）、M8（参数校验）、M9（构造期校验）、L1（删死导入）、L4（`_hyp_get` docstring） |
| `ultralytics_ooo/pool/sampler.py` | M1（种子）、M2（`pin_memory_device`）、漂移说明 |
| `ultralytics_ooo/pool/valslice.py` | L6（`copy=False` + `_read_orig`）、L10（`staticmethod`）、L1（删局部导入） |
| `ultralytics_ooo/pool/constants.py` | M4（表精简至 77 条项目自有键）、新增 `check_online_defaults_are_project_only` |
| `ultralytics_ooo/core/geometry.py` | M8（夹紧偏移，保证 tile 不退化） |
| `ultralytics_ooo/core/saver.py` | L2（删除重复的 `_save_cap`） |
| `ultralytics_ooo/core/__init__.py` | L3（补齐 `__all__`）、移除 `_save_cap` 再导出 |
| `ultralytics_ooo/README.md`、`项目说明.md` | 目录结构、测试说明、验证状态、兼容性说明同步 |

### 未改动
- `ultralytics/` 全部上游源码（含 `cfg/default.yaml`）—— 零侵入契约不变
- `pyproject.toml`（属上游）：lint 配置放在包内

---

## 七、验证方式与结果

```bash
export PYTHONPATH="<repo root>"
python -m pytest tests/ -k ooo -q
```

| 阶段 | 测试数 | 结果 |
|---|---|---|
| 修复前基线 | 16 | 全通过（严重缺陷在上述 16 项全绿的状态下静默存在） |
| 第一轮（质量修复） | **134** | **全通过**（约 2.9 s，全部离线：不下载数据与权重） |
| 第二轮（样本池 B+ + §7.1，见第九节） | **146** | **全通过**（约 2.8 s，全部离线） |
| 第三轮（`img_origin` 统一覆盖 + lint/文档收敛） | **153** | **全通过**（约 4.1 s，全部离线） |
| 第四轮（审查发现修复：lint 归零 + 门禁扩容 / LRU 实测纠偏 / 死配置告警 / 切片 memcpy 优化，见第十节） | **157** | **全通过**（约 3.3 s，全部离线） |
| 第五轮（新功能 `slice_target_tiles`：按目标挑片 + 跨 epoch 不重复调度，见第十一节） | **171** | **全通过**（约 2.0 s，全部离线） |
| 第六轮（修复验证集构建崩溃 `batch_size=0` + `img_origin` 默认值回归，见第十二节） | **179** | **全通过**（约 4.5 s，全部离线） |
| 第七轮（性能修复：LRU 容量 4→16 + 分组门控 + 删 oversized 守卫 + 字节预算，见第十三节） | **183** | **全通过**（约 5.4 s，全部离线） |
| 第八轮（切片段显示名 `base` → `sahi`，见第十四节） | **184** | **全通过**（约 4.4 s，全部离线） |
| 第九轮（性能修复：审查报告 P0/P1 落地 —— GPU/内存/工具口径/origin 走 LRU/切片 tile 视图/noise int16，见第十五节与第十六节） | **192** | **全通过**（约 4.8 s，全部离线；`ruff check ultralytics_ooo` 与新增测试文件 lint 均 `All checks passed!`） |
| 第十轮（双口径 checkpoint 家族：补 `last_whole.pt` + 收尾把整图检查点一并 strip，见第十八节） | **197** | **全通过**（约 6.9 s，全部离线；包门禁与交付脚本的严格规则 lint 均通过，注入检查 5/5 咬住） |
| 第十一轮（`close_aug_epoch` 覆盖修复：删掉收尾窗口里的 `range(K)` 短路，改为每轮真实抽签，见第十九节） | **198** | **全通过**（约 4.5 s，全部离线；包门禁与交付脚本的严格规则 lint 均通过，注入检查 6/6 咬住） |
| 第十二轮（从零独立验证：mosaic buffer 记账语义与上游对齐 + 双口径 `results.png` 落盘 + `cache` 旁路告警 + 舍入文案 + occlusion 标签长度防御，见第二十节） | **200** | **全通过**（约 12.9 s，全部离线；`ruff check ultralytics_ooo` 通过，迁移自检 63/63，注入检查 8/8 咬住） |
| 第十三轮（六模块性能修复 F1–F4 落地：噪声算术 / 比例填充 / 雨滴 `dst=` / 去掉白做的交接拷贝，见第二十一节与第二十二节） | **207** | **201 通过 / 6 失败**（约 16 s，全部离线；6 条失败**均与本轮无关**，根因是既有容量钳位 `_legacy_ims_cap` 与几条老断言冲突，已用字节级回退取证，见 §21.4；本轮 6 条注入用例 6/6 咬住） |
| 第十四轮（F5「已缩放帧缓存」落地：`_cap_long_side` 产物按 `(图,cap,kernel)` FIFO 缓存，见第二十三节与第二十四节） | **220** | **214 通过 / 6 失败**（约 9.4 s，全部离线；6 条失败与上一轮**完全相同**，仍是容量钳位那几条，本轮未新增任何红；新增 13 条契约用例 + 9 条注入用例 9/9 咬住；4 组真实数据整池逐像素等价） |
| 第十五轮（`slice_decode_shadow_size` 影子 FIFO：把「喂入 mosaic 池」与「真实解码」解耦，抬容量不再改训练数据，见第二十五节与第二十六节） | **232** | **226 通过 / 6 失败**（全部离线；6 条失败与上一轮**完全相同**，仍是 `_legacy_ims_cap` 容量钳位那几条，本轮未新增任何红；新增 12 条契约用例 + 7 条注入用例 7/7 咬住；三臂探针证明解码下降而池喂入逐元素不变；P4 端到端 `mAP50-95` **逐位不变**） |

> 后续任何一轮新增/精简测试后，**请只更新本表最后一行与 `项目说明.md` 的「验证状态」**，不要在正文散落写死测试数——本轮之前正文里有 146/134 两个互相矛盾的数字，正是散落写死的后果；而第三轮留下的 153 也在第四轮新增 4 条用例后立刻过时，再次印证了这条纪律。

补充验证：
- **反向验证**：把 S1 的旧 `_run_whole` 在运行时换回去，`test_primary_pass_stays_sliced_across_epochs` 失败并输出 `['SLICED','WHOLE','WHOLE']` —— 确认该回归测试真实有效；
- **AST 复核**：自制检查器复跑，`F401`（排除 `__init__` 有意再导出与 `__future__`）、`F811`、浮点相等比较**全部归零**；
- **配置一致性**：`check_online_defaults_are_project_only` 对 `cfg/default.yaml` 返回空列表；
- **实测数据**：S2 写盘耗时、L5/L6 收益、L7 内存与耗时差异均为本机实测（见审查报告第六节）。

---

## 八、未处理事项与后续建议

0. **【已实施】样本池"原图回退"膨胀**（第二轮，见第九节）：实测 `ratio=0.1` 时**池中 91.0% 的位是"原图直通"回退位**（base 段未被选中的图各占 4 个**逐字节相同**的位）。按 `样本池膨胀分析与方案.md` 的方案 B+ 实施了 **ratio 定长切分**，并按该文 §7.1 收窄了 `keep_origin`：池缩减 **5.32×**（N=40 实测 `410 → 77`）、回退占比 → 0%、`ratio >= 1` 时布局**逐位不变**（已用快照工具比对）。工具：`tools/ooo_pool_measure.py`、`tools/ooo_layout_snapshot.py`。
1. **L13 未改**：`_geometry` 内 bbox 格式转换的 4 段重复（纯可读性）。
2. **真实端到端训练未重跑**：本次修复以离线测试与运行时复现为准。**强烈建议按第五节 + 五·补的建议确认行为变更后，用 `train.py` 跑一次 1–2 epoch 冒烟**，重点核对：`ratio/blur/compose` 三条分支的样本构成、`best.pt` 与 `best_whole.pt` 的指标关系、`results.csv` 中 `whole_metrics/*` 与主指标列的差异。
   - 冒烟前可先跑 **`python tools/ooo_ab_ratios.py`**（干跑，不训练，秒级）看清两种配置下每条分支的**实际选中数**；它已经显示出在出货的 8 图迷你集上 `compose_ratio=0.1` 会选到 **0/2 组**（`round(0.2)=0`），即 composition 分支在该数据集上完全不生效。
3. **首次 CI 运行需人工确认**：`ruff` 未在本地安装，`I`（import 顺序）/`UP` 类提示可能需要在 CI 上跑一次 `ruff check --fix ultralytics_ooo` 后固化。
4. **建议补的真实回归**：目前 S1 的验证是"复刻上游契约的最小复现"（逐行对应 `trainer.py:408/871` + `validator.py:235`）。若条件允许，建议再加一次真实小数据集的 `val_slice_dual_metric` 多 epoch 训练断言。
5. **本轮已落地的两项可观测性**（原"可选跟进"）：每 epoch 分支选中数日志 + `ratio` 取整为 0 的告警。二者都有测试锁定（`tests/test_ooo_branches.py`），因此**以后新增一条增强分支却忘了镜像它的 ratio，会在测试里立刻暴露**，而不是像 S3 那样潜伏到下一轮审查。
6. **仍然存在的结构性风险**（非缺陷，但值得记录）：
   - `pool/sampler.py` 仍复制了上游 `build_dataloader` 的 loader 尾部 —— 无法直接复用上游实现，因为 `InfiniteDataLoader` 会在构造时把 `batch_sampler` 包进 `_RepeatSampler` 并**当场创建 iterator**，事后替换 sampler 不会生效。现已用属性对齐测试兜住漂移，但上游若新增 loader 参数仍需同步。
   - patch 冲突无检测：`install()` 会替换 7 处上游对象，第三方库若 patch 同一方法会互相覆盖且无提示。`_ooo_*_patched` 只防自身重复安装。

---

## 九、第二轮：样本池 ratio 定长切分（B+）与 S4

> 起因：`*_ratio` 未选中的位"保留池位、内容退回原图"导致原图在池子里重复堆积。可行性分析与方案见 `样本池膨胀分析与方案.md`，本节记录**实施结果**。
> 实施日期：2026-09-15　｜　改动范围：`pool/dataset.py`、`pool/sampler.py`、`installer.py`、`train.py`（注释）、`tools/*`、测试与文档；**仍未改上游任何源码**。

### 9.1 S4 每 epoch 抽样从未生效（本题外发现，严重）★

为了验证"每轮真的换了选中的图"而加日志时发现：`installer._ooo_set_epoch` 读的是 `trainer.train_dataloader`，而**上游 `BaseTrainer` 从来没有这个属性**（它叫 `train_loader`，见 `trainer.py:286`）。后果：

- `on_train_epoch_start` 回调静默空转 → **`set_epoch` 从未被 trainer 调用**；
- 每 epoch 的 `*_ratio` 抽样、以及 `close_aug_epoch` **全程冻结在第 0 轮**（只有"没有 trainer 时"的懒初始化路径会把 epoch 0 的选择集建一次）；
- 没有任何异常或告警，`validate`/`resume` 等其它 patch 都正常，所以从现象上完全看不出来。

**修复**：改读 `train_loader`（保留 `train_dataloader` 兜底，两者都找不到时打印 WARNING 而不是静默返回），并把回调注册从"函数身份去重"改为"标记去重"，避免重复 `install()`（例如 `sys.modules` 被清理后重新导入）注册出两个回调。

**验证**（真实训练，2 epoch）：修复前用 trace 包装 `set_epoch` —— 一次都没被调用；修复后每轮都被调用，并且 `augment masks @ epoch 0/1` 开始出现在训练日志里。回归测试：
- `test_trainer_dataset_attribute_name_matches_upstream`（上游属性名仍在，且没有改名成 `train_dataloader`）；
- `test_epoch_callback_publishes_set_epoch_through_the_train_loader`（真实调用已注册的回调列表，断言 `set_epoch(epoch, epochs)` 恰好被调一次）；
- `test_epoch_callback_tolerates_an_unsupported_trainer_without_crashing`。

### 9.2 方案 B+：段长由 ratio 决定

| 段 | 改造前 | 改造后 |
|---|---|---|
| base | `4N` | `4·K_slice + (N − K_slice)`：选中图 4 片，未选中图**恰好 1** 个原图位 |
| origin | `N` | `K_slice`（§9.4 收窄后） |
| ratio / weather / occlusion | `N` | `K_x` |
| blur | `2N` | `2·K_blur` |
| compose | `ceil(N/4)` | `K_compose` |

`K_x = round(x_ratio × count_x)` **只由配置决定**，与"选中了哪几张图"无关 → **池长全程恒定**（mosaic buffer 索引、`nb`/`nw`、sampler units 都按训练开始时的池长预计算，不能中途变化）。

三条不变量（都有测试）：

1. **`ratio >= 1` 时布局与旧形状逐位一致**（`K == count` → 段宽退化）——由快照工具比对确认 `82` 索引逐位相同，并由 `test_ratio_1_layout_is_bit_identical_to_the_legacy_shape` 锁死（对照的是**旧的映射公式**，不是新实现，避免自证）；
2. **`ratio = 0` 等价于关闭切片**：base 退化为 `N`，每图恰好 1 个原图位（旧实现会在这里产生 `4N` 个重复原图）；
3. **池长与 `set_epoch` 无关**：`test_pool_length_is_constant_across_epochs` 连续 6 轮断言段长与池长不变，同时断言选择集确实在变（否则 ratio 是哑的）。

其它落地细节：

- 布尔掩码 `_*_mask` → **有序选择集** `_sel_*`，抽样条件、顺序与旧实现保持一致（仍只在 `0<x<1` 时 `rng.sample`），所以**"哪几张图被增强"不变，变的只是未选中的图不再占位**；
- `_extended_pool_on()` 改用 `total != N` 判断，不再维护第二份开关清单；
- 分组采样收益的判据从"总样本数 ≥ 4N"改为"存在某张图的读取次数 ≥ 原始 LRU 容量"——否则 ratio 定长后平均复用率下降会让分组被**静默关掉**（实测仍为 82–84% 命中）；
- `close_aug_epoch` 不改段长：选择集变恒等序列、构建器直接输出原图，代价是末几轮多出 `ΣK_*` 个重复原图（ratio=0.1、N=8520 时约占新池 30%，语义上无害）。

### 9.3 实测效果

| 场景 | 改造前 | B+ | +§9.4 收窄 | 缩减 |
|---|---|---|---|---|
| N=40，六 ratio 全 0.1 | 410 | 113 | **77** | **5.32×** |
| N=8520，六 ratio 全 0.1 | 87 330 | 24 069 | **16 401** | **5.32×** |
| N=8（出货迷你集），全分支 ratio 1.0 | 82 | 82 | 82 | 1.0×（兼容基线） |
| N=8，全分支 ratio 0.1 | 82 | 17 | **17** | 4.8× |

"原图回退"占比：**91.0% → 0%**（N=40 实测 `python tools/ooo_pool_measure.py --n 40 --ratio 0.1`）。

### 9.4 §7.1 `keep_origin` 收窄（独立改动）

`keep_origin` 原本为**每张图**补一张整图，未选中的图在 base 段已是整图 → 每图出现 2 次逐字节相同的原图。收窄为"只为**被切片选中**的图补整图"，段宽 `N → K_slice`。`ratio >= 1` 时 `K_slice = N` → 布局不变，兼容性不受影响。

### 9.5 §7.2 `GroupedImageSampler` 补 `set_epoch`

上游 `trainer.py:456` 在 DDP 下调用 `train_loader.sampler.set_epoch(epoch)`；分组采样补丁只在 `rank == -1` 生效，所以当前不会触发。补上该方法（记录 epoch 并折进顺序种子）以免将来 DDP + 分组采样在 epoch 循环里 `AttributeError`。

### 9.6 日志与工具

- **构造期日志改成按真实段长打印**：`Online augment: 17 training samples from 8 images (segment slots: sahi 11, origin 1, ratio 1, ...)`（本轮写作时该标签是 `base`，第八轮已改为 `sahi`，详见十四节）。旧文案是"每条每图 N 张"的静态描述，在 ratio < 1 时会与实际布局矛盾。
- `tools/ooo_pool_measure.py`：打印各段真实槽位、每图占位直方图、与旧布局的倍率。
- `tools/ooo_layout_snapshot.py`（新增）：存/比对"索引 → 样本"的逐位快照，布局改动前先存基线，`--compare` 有差异时退出码 1。
- `tools/ooo_ab_ratios.py`：更新为 ratio 定长语义（两条 arm 的池长现在不同）。

### 9.7 真实训练验证（`_mini_val_set`：8 训练图 / 3 验证图 / 320px / 2 epoch / CPU）

| arm | ratio | workers | close_aug_epoch | 池长（段长） | 关键日志 |
|---|---|---|---|---|---|
| legacy | 1.0 | 0 | 0 | 82 (`32,8,8,16,2,8,8`) | `slice all/8` …；LRU 82.8% |
| intended | 0.1 | 0 | 1 | 17 (`11,1,1,2,0,1,1`) | `slice 1/8 (ratio 0.1)` … `compose 0/2 <-- NONE`；末轮 `[close_aug_epoch: …]` |
| mw2 | 0.5 | 2 | 1 | 45 (`20,4,4,8,1,4,4`) | 同上；LRU 83.3% |

三个 arm 均训练完成并产出 `results.csv`；`augment masks` 与 `raw-image LRU … % hit` 每轮正常打印，Windows spawn 多 worker 下选择集跨进程同步正常。

### 9.8 ⚠️ 本轮行为变更

1. **`ratio < 1` 时池子变小**（这是本轮的**目的**）。例：出货迷你集全分支 `ratio=0.1` 从 82 → 17 样本。若历史实验结论依赖"每类增强样本数量 = N × 分支数"，需要重跑。
2. **`keep_origin=True` 且 `slice_ratio < 1` 时**，未被切片选中的图不再有第二张整图（少了一份重复样本）。
3. **`compose_ratio` 现在真的生效**（第一轮 S3 的延续）：`round(0.5 × ceil(N/4))` 在小数据集上常为 0，日志会告警。
4. **`set_epoch` 现在真的每轮被调用**（S4 修复）：每轮的选中集合会变、`close_aug_epoch` 会生效。**如果此前用 `close_aug_epoch>0` 训练过，那些运行的收尾期其实一直是开增强的**，结论不可比。
5. 复现旧布局的开关：把六个 `*_ratio` 全设为 **1.0**（段宽退化回旧形状；`keep_origin` 也回到 `N`）。

---

## 十、第四轮：审查发现的 P0–P2 修复

> 起因：一次**从零独立审查**（不依赖此前结论）的结论，全文见 `全面重新审查报告.md`。本节记录这些发现的**处置**。
> 全部结论都有可复跑的命令支撑；本节末尾 §10.12 列出命令与实测值。

### 10.1 P0-1 lint 归零：20 → 0

`ruff check ultralytics_ooo` 从 **20 errors** 变为 **All checks passed**：

| 规则 | 数量 | 处置 |
|---|---|---|
| BLE001 盲 `except` | 8 | 逐个判断后**加 `# noqa: BLE001` 并写明为什么必须盲捕**（共享内存已关闭、worker 内不可注册回调等），不是无脑放过 |
| I001 import 未排序 | 5 | `ruff check --fix` 机械修复 |
| UP037 引号注解 | 2 | `--fix` |
| B010 `setattr` 常量 | 2 | 改为普通属性赋值 |
| F841 未用变量 | 2 | 删除（其中 `f = self.im_files[img_index]` 确认确实无用） |
| E731 lambda 赋值 | 1 | 改为 `def` |

### 10.2 门禁扩容：本包自己的测试原先**完全未被 lint 覆盖**（28 → 0）

这是本轮顺带发现的一个覆盖漏洞。`tests/` 与 `ultralytics_ooo/` 的 lint 配置来源完全不同：

- `ultralytics_ooo/ruff.toml` 显式选了 `E,W,F,B,BLE,I,UP`（并逐条写了为什么）；
- 而 `tests/` 只能继承仓库根的 `pyproject.toml`，那份**属于上游、且没有 `[tool.ruff.lint] select`** —— 于是实际生效的是 **ruff 自己的默认规则集**：既不是本包的规则，也不随版本稳定。

处置：
1. 把 8 个 `tests/test_ooo_*.py` 的 **28 条**清零（16 条 `--fix` 机械修复，12 条手工）；
2. CI 增加一步 `ruff check --select E,W,F,B,BLE,I,UP tests/test_ooo_*.py`，**显式钉住规则集**，避免隐式默认值漂移；
3. 移除原先那个 `continue-on-error` 的 advisory 步骤——它的注释写着「如果首轮跑出 import-order/UP 的 nit，就 `--fix` 掉并把 continue-on-error 去掉」，而这件事现在真的做完了。

清零过程中**有两条不是「为了过 lint」，而是顺手把测试写强了**（原先是「变量取了不用」）：

- `_cap_long_side` 的 docstring 写着「不需要缩放时返回 **同一个对象**」，测试却只断言了 `scale == 1.0` → 补 `assert same is img`；
- 返回值 `scale` 是调用方用来缩放框的**长边比例**，原先没断言 → 补 `assert abs(scale - 640/4000) < 1e-9`；
- `resume` 的单测里 `called` 记录器取了不用 → 补 `assert called["orig"] is True`，锁住「补丁后的 `resume_training` 仍委托原始实现」。

三条新断言都做了**注入验证**（改坏 → 必须失败 → 字节级还原 → 复绿）：3/3 全部按预期失败。

### 10.3 P0-2 测试数统一为 157

数字链条：`134`（第一轮）→ `146`（第二轮）→ `153`（第三轮）→ **`157`**（本轮）。

之所以必须再改一次：上一轮刚把散落的 146/134 统一成 153，本轮又新增 4 条回归测试（见 §10.5–10.8），153 立刻过时。**这正是「不要在正文散落写死测试数」这条纪律的又一次实证**——第七节的测试基线表与 `项目说明.md` 的「验证状态」是唯一真源。

当前分布（`pytest tests/ -k ooo --collect-only -q`）：guards 96 / branches 27 / dual 9 / sampler 7 / augment 6 / valslice 5 / resume 4 / core 3 = **157**。

### 10.4 P0-3 `img_origin` 文档同步

`slice_keep_origin` 已在上一轮被 `img_origin` 取代（旧键只作为 deprecated 别名映射为 `img_origin=True`），但 5 个文档仍在用旧键，且 `origin` 段宽已在方案 B+ 下从 `K_slice` 变为 **N**。已全量同步：`项目说明.md`、`ultralytics_ooo/README.md`、`样本池膨胀分析与方案.md`、`背景切片配额与原图回退机制说明.md`、`代码质量审查报告.md`。

### 10.5 P1-1 ⚠️ 实测推翻了审查报告自己的建议（重要）

审查报告原本建议「把 `slice_raw_cache_size` 从有效默认 4 提到 8–16，以降低大图重复解码」。**实测结果是反的**，因此本轮没有采纳：

| 配置 | 顺序 | 分组 | 命中率 |
|---|---|---|---|
| cache=4（默认） | 分组采样顺序 | ON | **80.0%** = 强制失效下限 `1 − N/reads` |
| cache=8 | 分组采样顺序 | ON | 80.8%（同量级，纯噪声） |
| cache=64 | 分组采样顺序 | **OFF** | 60.0% |
| cache=4 | 随机打乱顺序 | ON | 60.0% |

机制：`_grouped_units` 只在「**某张图的槽位数 ≥ LRU 容量**」时才启用分组（用**最大值**而非平均值判断，因为 ratio 定长下一步未选中的图恒占 1 槽，平均值会被严重稀释）。把 cache 调过最忙图片的扇出，分组就被**静默关掉**——而打乱顺序会把同一张图的多个槽位与别人的解码交错开，LRU 再也留不住那张图，于是**解码更慢**。这是个没人会猜到的反转。

处置：**不调大**，改为在超容量时**告警一次**（说明「更大会更慢」并给出 `slice_raw_cache_size=N` 的实际值），并补回归测试 `test_default_lru_keeps_grouping_and_an_oversized_lru_warns`（同时锁住默认值下分组**必须真的产出**，避免告警写反）。

端到端坐实（`train.py` 实跑日志）：
```
train: raw-image LRU (slice_raw_cache_size=4, grouped sampler): 81.7% hit on 131 reads last epoch
```
81.7% 与该 8 图池的强制失效下限吻合。

> ❗ **本节结论已在第七轮被实测推翻，见第十三节。** 这里的证据全部来自 **8 图（及一个 24 图）池**——`max_buffer_length = min(ni, batch×8, 1000)` 在这种规模下**小于等于池本身**，整库都装得进缓存，容量当然无关紧要，所以"调大会关掉分组、更慢"的结论**不可外推到真实规模**。在 240 图池上重测，方向正好相反：`cap=16` 分组 41.0 vs 不分组 34.4 items/s、`cap=32` 分组 44.3 vs 37.7。第七轮据此**删除了 oversized 守卫、把默认容量提到 16，并新增字节预算 `slice_raw_cache_mb`**；`test_default_lru_keeps_grouping_and_an_oversized_lru_warns` 也随之改写成 `test_grouping_is_kept_whatever_the_cache_size` 等四项。**本节保留作为"实测条件不可外推"的反面教材。**

### 10.6 P2-1 `mosaic_save_*` 死配置改为告警

上游 `Mosaic.__init__` 签名是 `(dataset, imgsz, p, n)`，没有 `save_*`；`_compat` 用 `inspect.signature` **静默剔除**这 4 个键，所以 `mosaic_save_dir/max/annotated/exist_ok` **永不生效且无任何提示**。改为：只要用户显式设了其中之一，就**一次性告警**并指向真正有效的 `slice_save_dir`。新增 `test_mosaic_save_knobs_warn_that_they_are_inert`（含「默认值必须保持安静」与「只告警一次」两个方向）。

> 之所以保留这 4 个键而不是直接删除：删掉会让历史 `args.yaml` 在续训时静默失效，告警是更安全的过渡。

### 10.7 P2-2 切片热路径的 memcpy 优化（按身份判定）

`_load_image_cached(copy=True)` 在切片路径下每次多一次**全分辨率**拷贝（4000×3000 ≈ **20.7 ms**），而切片读取约 80% 命中 LRU，所以这是最贵的一笔。

关键约束：`OnlineSlice` 有**三条路径会把入参原样返回**——① `p` 抛硬币失败、② 退化尺寸守卫、③ `_emit` 的 Plan A 背景配额回退。此时返回的就是 LRU 里的那个数组，而下游会**原地写入**它，因此不能简单改成 `copy=False`。

处置：用**对象身份判定**（`im is shared`）精确识别这三条路径，只在这里才拷贝；其余路径直接复用 LRU 数组。这样无需给三个调用点各加标志，且三条路径任一新增加入都会被同一判据覆盖。

配套：
- 契约测试 `test_slice_branch_never_hands_the_lru_buffer_downstream`（强制走直通路径，断言交给下游的数组**从不**是 LRU 的条目）；
- **反向验证**：把 guard 临时禁用后，**16/16** 个样本直接别名了 LRU 缓冲 —— 证明这条测试真的能抓到回归，而不是永真。

### 10.8 P2-3 / P2-4 两处稳健性加固

- **P2-3 `valslice` 的 `shape` 依赖**：切片 tile 的边界来自 `label["shape"]`，但上游 `get_image_and_label` 会 `label.pop("shape")`。缺失时原代码退化为 `(0,0)` → 图块边界 `(0,0,0,0)` → `im[0:0,0:0]` 空数组，直到 DataLoader worker 里才以 `cv2.resize` 断言炸掉，**报错点离病因很远**。改为**显式整图退化 + 一次性告警**（否则「切片验证」会静默地什么都不切），附 `test_missing_shape_is_an_explicit_passthrough_not_an_empty_crop`。
- **P2-4 内存敏感用例**：`test_cap_long_side_self_check` 原先用 `(rng.random(...) * 255).astype(np.uint8)`，其 float64 中间量对 3000×4000×3 是 **288 MiB**（图像本身只有 36 MiB），在低内存机器上直接 `MemoryError`，把「全绿」结论变成看机器负载的运气。改为 `rng.integers(0, 255, size=..., dtype=np.uint8)`（**峰值降 8×**，覆盖等价）+ `pytest.skip` 环境门控。

### 10.9 ❗ 新发现（**未处理**）：门禁与其配置根本没进版本控制

`git status` 显示以下路径**未被跟踪**：

```
?? .github/                      ← 整个 CI 工作流
?? tools/                        ← 4 个诊断工具（ooo_pool_measure / ooo_slice_knobs / ...）
?? ultralytics_ooo/ruff.toml     ← lint 配置本身
?? ultralytics_ooo/pool/fraction.py
?? tests/test_ooo_{branches,dual,guards,sampler}.py   ← 承载 139/157 个用例
?? 代码质量审查报告.md / 更新说明.md / 样本池膨胀分析与方案.md / 背景切片配额与原图回退机制说明.md / 全面重新审查报告.md
```

含义很直接：**文档反复引用的 CI 门禁、它的规则配置、以及用来复现所有实测数字的诊断工具，在版本控制里都不存在**；一旦 clone，本包只剩 **18/157** 个测试用例、零门禁、零工具。§10.2 刚把门禁扩容到测试，但如果 `.github/` 本身不进仓，这次扩容同样不会被任何人执行到。

**建议动作**（不在本轮自动执行，因为涉及提交状态，需要你确认）：把上述路径 `git add` 并提交，或按需要写入 `.gitignore` 并调整文档表述。

### 10.10 本轮行为变更

对**配置正确的运行**：**无**——没有改动任何默认值、段长公式或数值语义。用户可见的变化只有两类：

1. **新增两条一次性告警**：LRU 容量超过最忙图片扇出时（§10.5）、设置了永不生效的 `mosaic_save_*` 时（§10.6）。两者原本都是静默的，因此把「安静地做错」改成「出声地提示」不算行为回归，但会让既有日志多出一行。
2. **两条测试断言变强**（`scale` 的长边比例、`cap<=0` 返回同一对象）——若有人依赖「`cap<=0` 会拷贝一份」这个**未被 docstring 承诺**的行为，现在会被测试拦住。这属于把隐含契约显式化。

**性能**：切片路径每次读取最多省一次全分辨率拷贝（4000×3000 ≈ 20.7 ms），实测 16/16 样本不再别名 LRU 缓冲；其余保持不变。

### 10.11 未处理

- §10.9 的未跟踪文件（等你的提交决策）；
- 审查报告 P3 项（`_geometry` bbox 重复等可读性问题，文档自认未改）；
- 审查报告提到但本轮未动的：OpenCV 全局 RNG 污染的进一步收敛（现状已有 `cv2.setRNGSeed` 处理，未再扩大改动面）。

### 10.12 验证方式与结果

```bash
# 1) 包本体 lint（CI 门禁步骤 1）
ruff check ultralytics_ooo
#    -> All checks passed!            （修复前：20 errors）

# 2) 本包测试 lint（CI 新增步骤，显式钉住规则集）
ruff check --select E,W,F,B,BLE,I,UP tests/test_ooo_*.py
#    -> All checks passed!            （修复前：28 errors）

# 3) 全量测试（CI 门禁步骤 2）
python -m pytest tests/ -k ooo -q
#    -> 157 passed, 339 deselected

# 4) 端到端训练（切片热路径 + 双口径 + valslice 都被本轮改动触及，必须实跑）
python train.py
#    -> 2 epochs completed；池 45 样本 [16,8,4,8,1,4,4]；
#       产出 best.pt / best_whole.pt / last.pt；
#       results.csv 含 7 个 whole_metrics/* 列；
#       "raw-image LRU (slice_raw_cache_size=4, grouped sampler): 81.7% hit on 131 reads"
```

补充验证：
- **注入验证**（§10.2、§10.7）：3/3 新断言按预期失败；禁用身份 guard 后 16/16 样本别名 LRU，证明契约测试非永真；
- **常量等价性**：`ALL_ON` 由 `dict(k=v)` 改写为字面量后，重新 `exec` 校验 15 个键与取值逐一相同，`SEG`/`TOTAL` 未变；
- **字节级还原**：所有注入实验都断言还原后哈希与原文一致（发现并修正了脚本自身的换行翻译问题——用 `read_text`/`write_text` 会把整个文件从 LF 改写成 CRLF，已改用 `newline=""` 并验证 `degrade.py` 与 HEAD 字节一致）。

---

## 十一、第五轮（新功能）：`slice_target_tiles` —— 按目标挑片 + 跨 epoch 不重复调度

### 11.1 需求（用户原话）

> 能否实现：当 `slice_all_tiles=False` 时，每个 epoch 挑"未被上个 epoch 选中，切含目标的那一片"，当所有含有目标的切片选中后，重新开始选取。

背景：`slice_all_tiles=False` 时 base 段是「1 张选中图占 1 个槽」，而槽里放哪一片由 `OnlineSlice.__call__` 的
`k = random.randrange(4)` **盲抽**决定——实测只有 ~1/4 概率抽到含目标那片，其余 3/4 落到空片。
上一轮（`背景切片配额与原图回退机制说明.md`）把它列为"要改代码才能拿到的第四个解法"，本轮落地。

### 11.2 实现

新增旋钮 **`slice_target_tiles`**（`pool/constants.py`，默认 `False`＝与旧行为完全一致）。

- **调度单位是"片"不是"图"**：先扫一遍**标注缓存**（只读 `labels[i]["bboxes"]` 与 `["shape"]`，**不解码任何图片**），
  用 `OnlineSlice.target_tiles()` 求出每张图中"经 `_geometry` 的盒过滤后仍保留 ≥1 个目标"的片号，
  把所有 `(图, 片)` 排成一个队列并按 `(mask_seed, pass)` 打乱。
- **每轮取队列的下一段**（段长 = `K_slice` = `round(slice_ratio × N)`）→ 连着两轮不会出现同一个单位。
- **队列走完才回绕，且回绕时重新洗牌**（`_target_tile_pass(pass_idx)`）→ 即"全部选完后重新开始选取"。
- **零新增状态**：段/趟编号是 `epoch` 的纯函数（`n_blocks = ceil(queue / K)`、`pass = epoch // n_blocks`、
  `block = epoch % n_blocks`），所以主进程与每个 DataLoader worker 各自重算的结果必然一致，**不需要跨进程传任何东西**；
  全程不碰全局 `random`，因此**不会扰动其它分支的抽样**（有测试锁死）。
- **池长不变**：`_segment_lengths()` 一个字都没改（base 仍是 `n_per × K_slice`），改的只是"这个槽装哪一片"。
- **兜底**：调度映射基于**居中网格**（为了免解码）；真正发射时 `slice_at(prefer_target=True)` 会按**实际网格**复查，
  若被指的片空了就换成第一片仍有目标的片。队列为空时告警一次并退回盲抽（而不是发布一个填不满的调度）。

关键实现点（**易踩的坑**）：`_sel_indices("slice")` 同时被 base 段映射和 `grouped_sample_units` 当作
"第 i 个槽属于哪张图"使用，所以开启调度后**必须同步改写 `_sel_slice`** 为队列段的图列，
否则分组采样会与实际内容错位。

### 11.3 涉及文件

| 文件 | 改动 |
|---|---|
| `ultralytics_ooo/pool/augment_setup.py` | 新增 `OnlineSlice.target_tiles()`；`slice_at()` 新增 `prefer_target` 参数（含按实际网格换片的兜底）；`v8_transforms` 镜像新键；新增 `_warn_if_target_tiles_are_inert()`（`all_tiles=True` 或切片关闭时告警一次） |
| `ultralytics_ooo/pool/dataset.py` | 新增 `_target_tile_schedule_on()` / `_target_tile_queue()` / `_target_tile_pass()` / `_apply_target_tile_schedule()` / `_warn_target_tiles_unusable()`；`_rebuild_epoch_masks` 末尾调用调度；`_has_partial_ratio` 纳入该旋钮（ratio≥1 也要每轮前移）；`_base_slot` 返回调度片号；base 分支改走 `slice_at`；`_log_mask_summary` 打印段/趟 |
| `ultralytics_ooo/pool/constants.py` | 新增 `slice_target_tiles: False` |
| `train.py` | 切片旋钮注释块加 ⑤ 层说明 + 实参 |
| `tools/ooo_slice_knobs.py` | 新增 `--target-tiles`；报告新增"含目标片数（队列长度）"与"本轮调度了几个"，并给出无重复所需的 `slice_ratio` 上限 |
| `tests/test_ooo_target_tiles.py` | **新增 12 项** |
| `tests/test_ooo_augment.py` | **新增 2 项**（inert 告警 / `prefer_target` 换片） |
| 文档 | `ultralytics_ooo/README.md`（旋钮表 4→5 + 要点）、`背景切片配额与原图回退机制说明.md`（第四个解法由"待评估"改为"已实现"）、`项目说明.md` / `更新说明.md`（测试数与本节） |

### 11.4 实测

**（a）400 图稀疏合成集**（96×96，每图 1 个 24×24 象限框 → 队列 400 单位；`ratio=0.25`，`img_origin=False`，`bg=-1`）：

| | base 槽 | 正片 | 空片 | 整图 | 池长 |
|---|---|---|---|---|---|
| `slice_target_tiles=False`（默认，盲抽） | 100 | **24** | 76 | 0 | 100 |
| `slice_target_tiles=True` | 100 | **100** | 0 | 0 | 100 |

调度本身：`epoch 0..3` 分别是 `block 1..4/4, pass 1`，每轮 100 个**互不重复**的单位，4 轮并集 = 全部 400；
`epoch 4` 进入 `block 1/4, pass 2`（重新洗牌后重新开始）。池长/段长在 `ratio ∈ {0.25, 0.5, 1.0}` 下与关闭时**完全相同**。

官方诊断工具输出（`python tools/ooo_slice_knobs.py --data … --one-tile --ratio 0.25 --bg -1 [--target-tiles]`）：

```
盲抽        slots=100  positive= 35 (35.0%)  empty= 65  whole=0   target-bearing tiles=400 (schedule OFF)
按目标挑片  slots=100  positive=100 (100.0%) empty=  0  whole=0   target-bearing tiles=400 (100 scheduled this epoch)
```

**（b）端到端真实训练**（`_mini_val_set`，320px，2 epoch，CPU，**`workers=2`**，`img_origin=True`）：
队列 28 单位；日志逐轮 `target tiles: 4 units of 28 (block 1/7 → 2/7 → 3/7, pass 1)`；
训练正常完成并产出 `best.pt` / `last.pt`。
**跨进程可复现**：同一脚本独立跑两次，打印的 `_tile_units` **逐字相同**（`diff` 为空）。

### 11.5 行为变更

- **默认 `False` 时，与第四轮行为逐字节一致**（唯一新增的是 `constants.py` 里一个默认 `False` 的键）。
- 开启后：该分支不再消耗那次 `random.randrange(4)`，因此**与"盲抽"运行的全局随机流不同**（自身仍完全可确定、可复现）。
  这是任何"确定性挑片"方案的必然代价。
- 开启后 base 段几乎不含空片 → **背景误检（FP）可能变多**；要负样本请调小 `slice_ratio`、开 `img_origin`，
  或让 blur/weather/occlusion 供负样本。

### 11.6 未处理

- `K_slice > 队列长度`（如 N=8 但只有 2 张图有框）时会在**同一轮内重复单位**，已告警一次并提示所需 `slice_ratio`；
  更精细的"跨轮预算调度"未做（当前按块切分，实现最简且可复现）。
- 调度队列用**居中网格**估算（为免解码）。`slice_center_bias=True` 且目标正好贴着切缝时，会走"按实际网格换片"的兜底，
  该轮的实际片号可能与调度记录不同（不影响"含目标"这一性质，也未额外告警）。

---

## 十二、验证集构建崩溃修复（`batch_size=0`）+ `img_origin` 默认值回归

### 12.1 现象

`python train.py` 在 `_build_train_pipeline` 构建 `test_loader` 时抛：

```
File "ultralytics/engine/trainer.py", line 296, in _build_train_pipeline
    self.test_loader = self.get_dataloader(
File "ultralytics/models/yolo/detect/train.py", line 97, in get_dataloader
    return build_dataloader(
...
ValueError: batch_size should be a positive integer value, but got batch_size=0
```

### 12.2 根因：val 数据集（`augment=False`）的 `len()` 是 0

`installer.py` 把 `build.YOLODataset` 换成 `InstalledYOLODataset`（= `OnlinePoolDataset` + `YOLODataset`），
**训练与验证共用同一个类**。而 `OnlinePoolDataset.__len__` 返回的是**混合样本池布局**的总长：

```python
return self._segment_bases().total   # base + origin + ratio + blur + compose + weather + occlusion
```

问题在于 **val 构建时 `v8_transforms` 根本不会运行**：上游 `YOLODataset.build_transforms` 在
`augment=False` 时直接返回 val 变换，于是 `slice_transform` / `img_origin` / `*_keep` 这些键
**一个都不会被镜像到对象上**，七个段全部为 0：

```
=== VAL (img_origin=False, 3 张图) ===
  augment    : False
  len(ds)    : 0
  segments   : base 0, origin 0, ratio 0, blur 0, compose 0, weather 0, occlusion 0
```

而 0 并不是"val 集为空"这么无害 —— `build_dataloader` 的第一行就是

```python
dataset_len = len(dataset)
batch = min(batch, dataset_len)      # ultralytics/data/build.py:348-349
```

**0 长的 val 集把自己的 batch 改写成 0**，torch 在 `BatchSampler` 里报 `batch_size=0`，
而栈里完全不提样本池、`img_origin` 或 `slice_prob` —— 排查方向被彻底带偏。
（detect 任务传的是 `batch_size * 2`，所以 `batch=4` 时 val 拿 8，再被 `min` 压成 0。）

### 12.3 为什么以前能跑

实测四种历史配置：

| 运行 | `val` | `img_origin` | 结果 |
|---|---|---|---|
| `runs/detect/runs/exp` | true | **true** | 正常 |
| `runs/detect/runs/exp-2` | true | **true** | 正常 |
| `ooo_final/*`、`ooo_smoke/*` | true | 键尚不存在（`slice_keep_origin` 时代） | 正常 |
| `runs/detect/runs/exp-3`（本次） | true | **false** | **崩** |

即 **val 一直是在"蹭" `img_origin`**：`_img_origin_on()` 用
`getattr(self, "img_origin", _online_default("img_origin"))` 读取，键没被镜像到对象上时落到默认值。
默认 `True` 时，val 靠 origin 段**歪打正着**地得到 `len == N`（而且恰好是正确的整图集）；
默认一旦变 `False`，同一次构建立刻变成 0。

### 12.4 修复（两处）

**（a）`pool/dataset.py` — val 构建完全交还上游（结构性修复）**

`__len__` 与 `get_image_and_label` 各加一道 `if not self.augment:` 前置守卫，分别委派
`super().__len__()`（= `len(self.labels)`）与 `super().get_image_and_label(index)`：

- `augment=False` 时 `index` 就是普通图片下标，**不是池内槽位**；继续走池分发会把它当成空布局的 base 槽，
  越界读空的 `_sel_*` 数组；
- `BaseDataset.get_image_and_label` 正是 val 需要的实现（含 rect 的 `batch_shapes` 查表），因此只委派、不重写；
- 训练路径（`augment=True`）**一行未动**。

**（b）`pool/constants.py` — 恢复 `img_origin` 默认 `True`**

`git diff` 显示该文件相对 HEAD 只有两处实质改动：新增 `slice_target_tiles`，以及把 `"img_origin"` 从
`True` 改成了 `False`（**没有任何注释说明这个默认值变更是有意的**）。这个翻转同时打破两件事：

1. **零侵入契约**（本项目第一类设计目标）：全开关默认关闭时池应当就是原图集；`img_origin=False`
   让七个段全为 0 → **训练集 `len` 也是 0** → 同一个 `batch=0` 崩溃；
2. `tests/test_ooo_branches.py::test_no_switches_means_no_expansion_at_all` —— 这条**已提交的**测试
   在改动后的工作区里是**红的**（它断言的正是 `origin == N` 的零侵入契约）。

现已恢复 `True` 并补上说明性注释；该测试转绿。

### 12.5 回归测试

新增 `tests/test_ooo_val_dataset.py`（8 项，含两组 `parametrize`）：

| 用例 | 锁定的不变量 |
|---|---|
| `test_val_build_reports_one_slot_per_image`（`img_origin` ∈ {False, True}） | `len(val) == N`，两种默认值下都成立 |
| `test_val_never_allocates_slicing_tiles`（同上） | val 无 `slice_transform`，base 段结构上为 0 |
| `test_val_items_are_whole_frames_with_their_ground_truth` | val 下标解析出**整图**（`ori_shape == 原图尺寸`），且框与标签文件逐值一致（切片 tile 会被重新归一化，可判别） |
| `test_the_val_dataloader_can_actually_be_built` | 真正建一次 `build_dataloader` 并取一个 batch —— 直接复现"`min(batch, len)` 变 0"这条崩溃路径 |
| `test_the_guard_does_not_leak_into_the_training_build` | `augment=True` 仍读池布局，且 `_tile_units` 仍按调度给出 |
| `test_a_train_build_with_img_origin_still_adds_the_whole_frame_segment` | `img_origin` 语义未变（训练 `+N`） |

### 12.6 验证

- 全量 `pytest tests/ -k ooo -q` → **179 项 0 失败**（含上述 8 项）；
- `ruff check ultralytics_ooo` 与测试文件 lint → 全部 `All checks passed`；
- **端到端真实训练**（`_mini_val_set`，即 `train.py` 原配置 `img_origin=False`、320px、2 epoch、CPU）：
  正常完成 2 epoch，产出 `best.pt` / `last.pt` / `best_whole.pt`，切片口径与整图口径均出指标；
  val 集 3 张 1920×1080 整图逐张可读、各 1 个框。

### 12.7 行为变更提醒

`img_origin` 默认值从 `False` 回到 `True`。**仅在用户未显式配置该键时才有差别**：不配置时，
每张原图会额外以整图形态进入池（宽度 `N`）—— 这也正是文档与已提交测试一直描述的默认语义。
`train.py` 显式写了 `img_origin=False`，因此示例脚本行为不变。

---

## 十三、第七轮（性能修复）：LRU 容量 × 分组门控 × 守卫判据

### 13.1 背景

`性能审查报告.md`（从零重做版）改用**真实 `build_dataloader` 层次**重测，测出三处确定性失配/缺陷。它们互相咬合：**只调容量会被守卫拦下（P0-3），只修分组会被容量拖住（P0-1）**。三项合起来的实测上限是 **20.4 → 44.3 items/s（2.17×）**，且只改变**访问顺序与缓存命中**，不改变**选中的图**、7 段布局与 `len()` 恒定性。本轮落地 P0-1/2/3。

### 13.2 P0-2 分组采样器的 compose 门控遗漏（功能缺陷，+29%）

**现象**：默认切片配置（`slice_prob=1` + `slice_all_tiles=True` + `slice_ratio=1.0`）下，`build_dataloader` 实际拿到的是 `RandomSampler` 而不是 `GroupedImageSampler`，并打出误导性警告：

```
WARNING train: grouped_sample_units produced 1260 indices but the pool holds 1200: the pool layout
and the grouping rules have drifted apart. Falling back to the plain shuffle; fix the grouping rules
```

**根因（两条叠加）**：

1. `grouped_sample_units()` 对 `slice / origin / ratio / blur / weather / occlusion` **六个分支都做了显式门控，唯独漏了 compose**。compose 关闭时该段宽度为 0，但 `compose_ratio` 默认 1.0 → `_sel_compose` 仍是 `None` 哨兵 → `_sel_indices` 返回 `range(ceil(N/4))` → 凭空造出 `ceil(N/4)` 个越界槽（1200…1259）→ 计数校验 `flat != range(total)` 失败 → `return None`。
2. `None` 哨兵**被重载了两种语义**（"ratio≥1 该用全部" 与 "分支关闭该段为 0"），而 `build_dataloader` 在**第一次 `set_epoch` 之前**就调用了本方法（trainer 先建 loader，再由 `on_train_epoch_start` 发 epoch）；唯一能校正哨兵的 `_ensure_epoch_sel()` 在 `_has_partial_ratio()` 为假时提前返回——而"所有启用分支 ratio 都 ≥1（含默认 1.0）"恰好为假。

**修复**：分支门控**不再手写**，改为从 `_mask_specs(n)` 这张**同一张表**（`_segment_lengths` 也用它定段宽）读出 `on[...]`，compose 自然一并门控；门控外不生成 `compose_slot`。一处表、一处门控，杜绝再次漂移。

**代价实测（cap=4，同缓存）**：分组生效 26.4 items/s / 37.84 ms vs 失效 **20.4 / 49.10** → **+29%**，且误导性警告消失。

### 13.3 P0-3 删除 oversized 守卫（判据错误）

旧守卫 `if fan_out < self._raw_cache_size: warn + return None`——**缓存比最忙图片的扇出还大就关掉分组**，还建议"把容量调小到 ≤ fan_out（默认 4 最好）"。它的依据是 24 图池实测（cap 16 vs 4：80.0% → 60.0%）。

**判据错在哪**：24 图池下 `max_buffer_length = min(24, 64, 1000) = 24`，**Mosaic 窗口比数据集还小**，整库装得进缓存，容量无关紧要。真实规模（240 图，窗口 63 槽 ≈ 13 张不同图）下方向相反：

| 组合 | 分组 | items/s |
|---|---|---|
| cap=16，被守卫关闭 | ❌ | 34.4 |
| cap=16，强制分组 | ✅ | **41.0** |
| cap=32，被守卫关闭 | ❌ | 37.7 |
| cap=32，强制分组 | ✅ | **44.3** |

**修复：删除守卫（而非反转）**。分组 units 只装 ≤4 张图并**轮询**访问，LRU 只要装下这 ≤4 张就能吸收全部重读，而容量地板 `max(4, v)` 恰好保证这一点——**不存在"关掉分组更好"的容量**。保留的唯一判据是 `fan_out < 2`（无图占两槽 → 无重读可省 → `None`）。

### 13.4 P0-1 默认容量 4 → 16 + 新增字节预算 `slice_raw_cache_mb`（+85%）

**默认容量**：`slice_raw_cache_size` 由 **4 → 16**。依据是 Mosaic 窗口而非单图扇出：窗口 63 槽 ÷ 每图 5 槽（4 片 base + 1 整图 origin）≈ 13 张不同图 → 取 16。命中率在 16–32 之间饱和，64 因 RSS 上升而**反弹变慢**（240 图实测 4→32：20.4 → 37.7 items/s = **+85%**，ms/样本 −46%）。

**字节预算**：LRU 存的是 **原始分辨率**帧，帧大小要到首次解码才知道，所以"条目数"无法预换算内存——4000×3000 一帧就是 36 MiB，16 帧 = 550 MiB/worker。新增 `slice_raw_cache_mb`（默认 **256**），在**每次插入时按字节驱逐**（`while ... len(cache) >= size or _raw_cache_bytes + im.nbytes > budget`），`>=1` 保证至少留下刚解码的那一帧（否则会立刻重解码，反而更慢）。`slice_raw_cache_size=0` 仍表示彻底关闭；`1–3` 仍地板到 4。

**语义保持不变**：显式设置任一键仍照常生效；默认关闭切片时零行为变更。

### 13.5 回归测试（4 项，`tests/test_ooo_branches.py`）

| 测试 | 锁住什么 |
|---|---|
| `test_grouping_is_kept_whatever_the_cache_size` | cap ∈ {4,16,64} 下 `grouped_sample_units()` **都必须非 None** 且是池的一个排列——直接钉死 P0-3，并断言**不再**出现"扇出/超容量"告警 |
| `test_grouping_survives_the_trainers_build_order` | **复刻真实 trainer 顺序**（`build_yolo_dataset` 后**不调 `set_epoch`**）：断言 `_mask_stamp == -1`（确认测试非永真）、units 是排列、且真实 `build_dataloader` 拿到的**就是** `GroupedImageSampler`——直接钉死 P0-2，补上"loader 构建早于 set_epoch"这个既有测试盲区 |
| `test_the_raw_cache_default_covers_the_reuse_window` | 默认 `_raw_cache_size == 16`、`_raw_cache_mb == 256`、`_raw_cache_budget == 256 MiB`；显式覆盖与 `0/1` 边界仍正确 |
| `test_the_raw_cache_byte_budget_caps_resident_frames` | 640×640 帧 + 3 MiB 预算 → 常驻 **正好 2 帧**（与 `slice_raw_cache_size=16` 无关）；字节计数与真实占用一致且不超预算 |

### 13.6 验证

- 全量 `pytest tests/ -k ooo -q` → **183 项 0 失败**（本轮 +4）；
- 改动文件 `py_compile` 全部通过（本机未装 `ruff`，改用编译 + 行长自检脚本代跑；`ruff` 默认规则集不含 `E501`，改动未引入新规则类问题）；
- `diag2.py`（复刻 trainer 构建顺序）重跑：分组在真实顺序下**已生效**，越界槽与误导性警告均消失。

### 13.7 行为变更

1. **默认 LRU 容量 4 → 16**：每 worker 常驻 +12 帧原图（720p ≈ +31 MiB/worker；大图由 256 MiB 预算上限兜住）。
2. **分组采样在默认配置下从"静默失效"变为"生效"**：**访问顺序变化，选中的图不变**——池布局、`len()`、每 epoch 随机种子语义均未改。
3. **不再出现**两条告警：`drifted apart` 与"把容量调小"。既有日志会少这两行。


---

## 十四、第八轮：切片段显示名 `base` → `sahi`

### 14.1 背景

构造期日志原来是：

```
Online augment: 25 training samples from 8 images (segment slots: base 4, origin 0, ratio 4, blur 8, compose 1, weather 4, occlusion 4)
```

`base` 一词读起来像"baseline / 未增强"，而它装的恰恰**不是**未增强内容 —— 该段是 `slice_transform` 切片管线的输出。真正未切的整帧在**另一个**段（`origin`，即 `img_origin` 的全图覆盖层）里。于是同一个名字同时暗示了"切片"和"不切片"，读日志的人只能去翻源码才能确认哪段是 SAHI 切片。

### 14.2 七段与显示名的对应（本轮确认）

| 日志显示名 | 代码内部名 | 段宽 | 装什么 |
|---|---|---|---|
| **`sahi`** | `base` / `_segment_bases().base` / `_base_slot()` | `n_per × K_slice` | **SAHI 切片**：只有 `slice_ratio` 选中的 `K_slice` 张图有槽位，每图 `n_per` 槽（`slice_all_tiles=True` 为 4，否则 1） |
| `origin` | `origin` | `N`（`img_origin` 开）或 `0` | **未切的整帧**，每张原图一槽，统一覆盖层 |
| `ratio` | `ratio` | `K_ratio` | 每张选中图 1 张 padding 到目标长宽比 |
| `blur` | `blur` | `2 × K_blur` | 每张选中图 短/长两档模糊 |
| `compose` | `compose` | `K_compose` | 每 4 张选中图合成 1 张 2×2 拼图 |
| `weather` | `weather` | `K_weather` | 每张选中图 1 张雨/雾/噪声退化图 |
| `occlusion` | `occlusion` | `K_occlusion` | 每张选中图 1 张矩形/条带遮挡图 |

以本节开头那行为例：8 张图、`slice_ratio=0.5` → 只有 4 张被选中切片，且 `slice_all_tiles=False`（每图 1 片）→ **`sahi 4`**；`origin 0` 说明 `img_origin=False`（未选中的另外 4 张本轮完全不出现在池里）；其余四段各自选中 4 张，`compose` 按 4 图一组取整得到 1。

### 14.3 改动范围

**只改显示名，槽位数一个没动。** 内部命名一律保留 `base`（`SegmentBases.base`、`_base_slot()`、`base_len`、`_segment_lengths()[0]`）—— 改内部名会波及布局缓存、`get_image_and_label` 分派与多个测试，收益为零。

改动点（全部是"给人看的字符串"）：

| 文件 | 改了什么 |
|---|---|
| `ultralytics_ooo/pool/dataset.py` | 构造期日志的 `_names` 元组首项 `base` → `sahi`（唯一一处训练日志） |
| `tools/ooo_pool_measure.py` | `SEG_ORDER` 首项、标签→属性名的反向映射 `"slice" if name == "sahi"`；顺带修掉该表两处**过期提示**（见下） |
| `tools/ooo_layout_snapshot.py` | `SEG_ORDER` 首项（打印 + JSON `segments` 的键） |
| `tools/ooo_perf_run.py` | `SEG` 首项（JSON `segments` 的键） |
| `tools/ooo_slice_knobs.py` | `bounds` 键、`report()` 的 `base=` 列、docstring |

三处**不受影响**的确认：

- **槽位数、段顺序、`len()` 全部未变**：`sahi 4` 与原来的 `base 4` 是同一个数；`tests/test_ooo_branches.py` 里 `SEG` / `SEG_ORDER` / 池长与逐位兼容测试**一条都不用改**（测试面对的是内部段名，本轮不动）；
- `augment masks @ epoch N` 那行用的是 `_mask_specs()` 的 `slice`/`ratio`/… 属性名（**不是** `base`），故不会出现 `sahi` 与 `slice` 两个名字打架：前者是**段**（槽位数），后者是**分支选择集**（`selects k of n images`），本来就是两个量；
- `ooo_layout_snapshot.py --compare` **只比对 `segment_lengths` / `total` / `entries`**，不比对 `segments` 的键名，因此已有的快照基线**无需重存**，重跑 `--compare` 仍应逐位 IDENTICAL。

顺带修掉的两处工具过期提示（`ooo_pool_measure.py` 的段表，与本轮标签无关但同表同屏）：

- `origin` 行原来把 `img_origin` 当成受切片选择集门控的分支，打印 `k_slice/N` 并写着 `keep_origin: one whole frame per SLICED image`。`img_origin` 实际是**全有或全无**的覆盖层（开启时 `N` 槽 = 每张原图一整帧，关闭时 `0`），与 `slice_ratio` 无关，现改为 `all/N` 并写明「N wide when on, 0 when off」；
- `sahi` 行的注记仍写着「未选中的图各占 1 个 plain slot」——那是 B+ 之前的行为；B+ 已删掉 plain tail，未选中的图在 `sahi` 段**没有槽位**（唯一回池路径是 `img_origin`），注记已改写。

### 14.4 新日志样例

```
Online augment: 25 training samples from 8 images (segment slots: sahi 4, origin 0, ratio 4, blur 8, compose 1, weather 4, occlusion 4)
```

### 14.5 回归测试（1 项）

| 用例 | 锁住什么 |
|---|---|
| `test_online_augment_header_names_the_slice_segment_sahi` | 构造期日志必须出现 `segment slots: sahi 16, origin 4, ratio 4, blur 8, compose 1, weather 4, occlusion 4`（N=4 全开），且**不得**再出现 ` base `。仅锁显示名，不锁内部属性名 |

### 14.6 验证

- 全量 `pytest tests/ -k ooo -q` → **184 项 0 失败**（本轮 +1）；
- **工具冒烟**：`tools/ooo_pool_measure.py --n 12 --ratio 0.5 --no-histogram`、`tools/ooo_layout_snapshot.py --n 8 --ratio 1.0` 实跑通过（exit 0），输出的段表首行均为 `sahi`，`segment_lengths` 与改名前一致（N=8/ratio=1 仍为 `[32, 8, 8, 16, 2, 8, 8]`，total 82）；
- 文档同步：`项目说明.md`「验证状态」、`更新说明.md` 测试基线表、`样本池膨胀分析与方案.md` 引用处；`更新说明.md` §9.6 那条历史日志引用加了更正标注。


---

## 十五、第九轮（性能修复）：`性能审查报告-全面重审.md` 的 P0/P1 落地

### 15.1 背景与顺序

`性能审查报告-全面重审.md`（从零重做的实测审查）给出按优先级排序的清单。本轮**按该顺序**落地 P0-1 → P0-2 → P0-3 → P1-1 → P1-2 → P1-3（可观测性部分）→ P1-4。

审查的两个环境级发现决定了顺序：**本机 7.9 GB 物理内存常态只剩 0.4~0.7 GB**，且 `train.py` 原本用 `device='cpu'`（实测 CPU 1853.9 ms/图 vs GPU 49.1 ms/图 = **37.7 倍**）。在 CPU 上整条数据管线只占训练步 **2.9%**，所以先用 P0-1/P0-2 把"优化是否可观测"这件事解决，再动数据侧代码。

### 15.2 改动清单

| 文件 | 改动 | 对应项 |
|---|---|---|
| `train.py` | `device='cpu'` → `device=0`；`workers=0` → `workers=2`；新增 `prefetch_factor=2`；补内存/算力注释块 | P0-1 / P0-2 |
| `ultralytics_ooo/pool/constants.py` | 新增 `prefetch_factor: 4`（= stock 硬编码值，默认零变更） | P0-2 |
| `ultralytics_ooo/pool/sampler.py` | 分组加载器尾部用可配置 `prefetch_factor`；分组未生效时**告警一次**而不是静默忽略 | P0-2 |
| `ultralytics_ooo/pool/augment_setup.py` | `v8_transforms` 镜像 `prefetch_factor`；`OnlineSlice._emit` 的 tile 由**连续拷贝**改为**视图**（保存路径自带 `ascontiguousarray`） | P0-2 / P1-2 |
| `ultralytics_ooo/pool/dataset.py` | `_build_origin_sample` 改走 `_load_image_cached` + `_finalize_label`；LRU 别名守卫由"身份判等"升级为"内存共享 + 是否需要 resize"；新增 LRU 有效容量一次性 INFO | P1-1 / P1-2 / P1-3 |
| `ultralytics_ooo/core/degrade.py` | 气象 `noise` 核由 float32 改为 int16 暂存 | P1-4 |
| `tools/ooo_perf_run.py` | **修口径缺陷**：`install()` 之后从模块属性取 `build_dataloader`；新增 `--prefetch`；输出 resolved `prefetch_factor` | P0-3 |
| `tools/ooo_perf_bench.py` | 同上调整导入顺序（统一约定，消除陷阱） | P0-3 |

### 15.3 P1-1 `origin` 段绕过 LRU（本轮最大收益）

**缺陷**：`_build_origin_sample` 调 `load_image`，而 `load_image` 的 `self.ims` 写入在
`getattr(self, "slice_transform", None) is None` 之后 —— **切片一开就完全不缓存**（实测 `ims_resident == 0`）。
于是 `origin` 段每一次访问都重新解码**并**重跑训练缩放，哪怕同一张图的其它子样本就在 LRU 里。

**修复**：改走 `_load_image_cached(img_index)`（`copy=True`，调用方独占）+ `_finalize_label`，与其它分支共用同一 LRU。
`_finalize_label` 施加与 `load_image` **完全相同**的 resize + imgsz clamp，因此 `resized_shape` / `ratio_pad` 不会漂移（§15.9 有逐像素证据）。

**代价（实测，必须知道）**：LRU **未命中**时比 `load_image` 慢一次全帧拷贝 —— 1280×720 为 2.52 ms，4000×3000 为 18.17 ms。
盈亏平衡命中率：720p **44%**、4K **22%**；而默认的分组采样器给出 **0.90~0.95**。所以关掉 `slice_grouped_sampler` 时这项会反向，
注释里写明了这一点。

**副作用（已记录）**：该路径不再走 `load_image` 的 `*.npy` 磁盘缓存分支；`_load_image_cached` 直接读 JPEG（其 docstring 已记录 npy 缓存实测无收益）。

### 15.4 P1-2 切片 tile 由"连续拷贝"改为"视图"

`OnlineSlice._emit` 原来做 `np.ascontiguousarray(img[y0:y1, x0:x1])`，而**紧接着**的 `_finalize_label` 又把它
`cv2.resize` 到 `imgsz` —— 这次全分辨率 memcpy 完全被下一次调用扔掉。

改为返回**跨步视图**。`cv2.resize` 能正确读取行跨步数组并自行分配输出（已验证逐像素一致）。

**契约变更（关键）**：`_emit` 现在可能把**视图**交给下游，而 LRU 别名守卫原来是按 `im is shared` 判**身份**的，
只能挡住"原样返回输入"的三条通路（`p` 硬币、退化尺寸守卫、Plan A 回退），挡不住视图。守卫升级为：

```python
if self._raw_cache_size > 0 and (
    im is shared or (max(im.shape[:2]) == self.imgsz and np.shares_memory(im, shared))
):
    im = im.copy()
```

即"**视图且其 resize 不会发生**"才复制。resize 真会跑时是安全的（`cv2.resize` 必然新分配），
因此不做无谓拷贝——否则又会把这次要省掉的 memcpy 加回来。这条边界由既有契约测试
`test_slice_branch_never_hands_the_lru_buffer_downstream` 与本轮新增的视图检查共同看住。

**收益集中在大图**：4000×3000 的 2400×1800 tile `10.79 ms → 1.84 ms（5.9×）`；1280×720 的 tile 只差 0.06 ms。

### 15.5 P1-4 气象 `noise` 核改 int16

结果本来就要量化回 uint8，float32 暂存只买到 2 倍写流量和两次整帧分配。改 `cv2.randn` → CV_16S +
`cv2.add(..., dtype=cv2.CV_16S)`。实测（`_perf_review/noisebench.py`，7 次中位数）：
1280×720 **29.59 → 25.31 ms（−14%）**，1280×960 36.43 → 34.31 ms，**实际 σ 完全一致（14.98 vs 14.98 @ σ=15）**。

**同一次横评否决了更"自然"的写法**：numpy `Generator.normal` 三种实现分别 47.90 / 78.46 / 50.56 ms，
比原实现**慢 1.6~2.7 倍**。这是本轮"先测再改"的直接收获——顺手把结论写进代码注释，防止后人再试。

### 15.6 P0-2 `prefetch_factor` 可配置 + `workers` 上限

**新增 `prefetch_factor` 键**（默认 4 = stock 硬编码值，默认行为零变更），在分组加载器尾部真正生效。
内存代价是显式的：`prefetch_factor × batch × 3 × imgsz² × 4B` = 4×8×3×640²×4 = **157 MiB/worker**，
叠在每 worker **固定 ≈340 MB**（spawn 重导入 torch+cv2 + 数据集副本）之上。

**分组未生效时不静默忽略**：stock `build_dataloader` 把它硬编码为 4 且不接受参数，所以那条路上无法生效——
改为**告警一次**并给出替代建议（降 `batch`）。这正是本项目一贯反对的"静默 no-op"。

`train.py`：`workers=2`、`prefetch_factor=2`。实测依据：`workers=4` 在本机**两次独立复现
`MemoryError in DataLoader worker`**（4 × (340+157) ≈ 2.0 GiB 仅 worker 一项，另加 240 MiB/worker 的 raw LRU，
而系统只剩 0.4 GB）。

### 15.7 P0-3 工具口径缺陷（会误导后续所有判断）

`tools/ooo_perf_run.py` 原来在 `install()` **之前**执行
`from ultralytics.data.build import build_dataloader, build_yolo_dataset`。`from ... import` 取的是**函数对象快照**，
所以补丁从未生效——**该工具在每一个配置上都报告 `RandomSampler`，永远测不到自家 `GroupedImageSampler`**，
也就是永远测不出本包的头号优化。修复前后同一命令：

```
run720_D_full.json         sampler=RandomSampler        item_ms=72.09   LRU hit=0.702  prefetch=None
after_run720_D_full.json   sampler=GroupedImageSampler  item_ms=33.93   LRU hit=0.958  prefetch=4
```

改为 `install()` 之后从模块属性读取，并新增 `--prefetch` 使新键可测。

### 15.8 P1-3 大图 LRU 预算：从"静默吞帧"改为"出声"

`slice_raw_cache_size`（帧数）与 `slice_raw_cache_mb`（字节）是两个独立上限，而只有字节上限知道真实帧大小：
4000×3000 单帧 34.3 MiB，默认 256 MiB 预算**只装得下 7 帧**，而此时把 `slice_raw_cache_size` 调到 32 **仍然是 7 帧**。
现于首次插入后打印一次 INFO，报出**有效常驻帧数**与所需字节预算。本轮**未改默认值**（本机内存不允许调大），只把不可见变可见。

### 15.9 等价性验证（本轮的验收前提）

两处修复都是**删除工作**，唯一可能出错的方式就是"产出的东西变了"。因此重建**修复前实现**并与现管线逐数组比对
（`_perf_review/verify_fixes.py`，重建源经断言校验，防止未来编辑把探针变成永真）：

| 检查 | 1280×720 | 4000×3000 |
|---|---|---|
| 切片槽位数 | 240 | 48 |
| **切片 `label["img"]` 逐像素差异** | **0** | **0** |
| 切片形状不一致 | 0 | 0 |
| origin 张数 | 60 | 12 |
| **origin `label["img"]` 逐像素差异** | **0** | **0** |
| origin `ori_shape`/`resized_shape` 不一致 | 0 | 0 |
| 交给 `_finalize_label` 的数组 | 300 | 60 |
| 其中别名 LRU（身份） | **0** | **0** |
| 其中别名 LRU（视图共享内存） | **0** | **0** |

即：**输出逐像素一致 + 契约（不得泄漏 LRU 缓冲）仍然成立**。

### 15.10 实测收益（同一进程内配对，唯一可信的口径）

本机跨会话方差达 ±40%，先把修复在**同一进程内回退**再配对测量（`_perf_review/revert_probe.py`），
每个变体都用一对基线夹住。**两次基线几乎重合**（4K B 例：46.270 / 46.202），说明这组数字可信：

| 场景 | 修复全开 | 全部回退 | **整体** | 仅 origin 修复 | 仅 emit 修复 |
|---|---|---|---|---|---|
| 720p `D_full` | 32.14 ms/项 | 35.09 | **1.09×** | 1.12× | 1.02× |
| 4K `B_slice_all` | 46.24 ms/项 | 135.28 | **2.93×** | 1.51× | 1.52× |
| 4K `D_full` | 93.08 ms/项 | 165.56 | **1.78×** | 1.55× | 1.10× |

与机理预测完全吻合：**origin 修复在两种尺寸上都有效**（720p ~12%、4K ~51~55%），
**emit 修复只在大图上有意义**（720p 2%，4K 切片密集配置 52%、全池配置 10%）——因为 720p 的 tile 太小。

⚠️ **同时纠正一个容易犯的错**：本文件夹里"跨会话"的前后对比给过 1.60× / 1.76× / 2.75× / 3.71×，
**那些数字被机器状态污染了**（内存占用在 0.4~3 GB 之间波动）。同场景的真实值是上表的 1.09×。
凡在本机做性能结论，**必须用配对法**，否则会把负载差异当成代码收益。

### 15.11 顺带量化的验证侧开销（审查报告标为"未覆盖"）

审查报告明确把 `val_slice_*` / `val_slice_dual_metric` 列为未实测项。本轮补了**结构倍数**
（`_perf_review/valslice_cost.py`，只读标注与几何，不解码）：

| 配置 | 每轮验证样本 | 相对整图验证 | ×2（双口径）|
|---|---|---|---|
| `all_tiles=True, ratio=1.0` | 12（整图 3） | **4.0×** | **8.0×** |
| `all_tiles=True, ratio=0.5`（`train.py` 当前值） | 9 | 3.0× | **6.0×** |
| `all_tiles=False`（任意 ratio） | 3 | 1.0× | 2.0× |

即 **`train.py` 当前的验证开销是朴素整图验证的 6 倍**。这是结构计数，不是墙钟计时；
验证轮的墙钟拆分仍未单独插桩（见 §15.13）。

### 15.12 端到端真实训练验证

`_perf_review/smoke_train.py`（复刻 `train.py` 的增强配置，写入 `_perf_review/runs`，不碰用户的 `runs/`）：

```
device=0  workers=2  prefetch_factor=2  imgsz=320  batch=4
-> 1 epochs completed in 0.002 hours；产出 best.pt / last.pt
-> Online augment: 25 training samples from 8 images (segment slots: sahi 4, origin 0, ratio 4, blur 8, compose 1, weather 4, occlusion 4)
-> augment masks @ epoch 0 [close_aug_epoch: every slot emits the original]: slice 4/8 | ratio 4/8 | blur 4/8 | compose 1/2 | weather 4/8 | occlusion 4/8
```

GPU 训练路径、`workers=2`、`prefetch_factor=2`、切片目标挑片调度、`close_aug_epoch`、切片验证 + 双口径
全部实跑通过。运行期无告警。

### 15.13 行为变更

1. **`train.py` 改到 GPU 训练**（`device=0`）。若 GPU 不可用请改回 `'cpu'`，并知悉此时数据侧优化的收益基本不可观测（2.9%）。
2. **`workers` 0 → 2**：多出 ≈692 MB 常驻（实测 2 worker 进程树 1466 MB vs 0 worker 581 MB）。内存不足时先降到 1 或 0。
3. **`origin` 段的读取路径变了**：不再走 `load_image` 与 `*.npy` 缓存；LRU 未命中时比原来慢一次全帧拷贝。
   **关掉 `slice_grouped_sampler` 会让这项反向**（盈亏平衡命中率 720p 44% / 4K 22%）。
4. **`_emit` 返回的 tile 可能是视图**（非连续内存）。任何**写入**该数组的新代码必须先自行 `ascontiguousarray`（保存路径已这么做）。
5. **`prefetch_factor` 默认仍是 4**，因此不显式配置时行为与之前完全一致；配置了但分组未生效时会**多一行告警**。
6. **大图 LRU 现在会打印一行 INFO**（有效常驻帧数），既有日志会多一行。

### 15.14 未处理

- **`P2-5` 池膨胀治理**（调 `*_ratio` / 关分支）：收益线性且最大，但**改变训练语义**，属效果取舍，留给你决定。
- **`P2-2` 让变换链与计算重叠**：已由 `workers=2` 部分实现，未再单独裁剪 HSV/Format。
- **`P2-4` 只读分支 `copy=False` 扩展**：未做，需先逐分支确认不写输入。
- **`val_slice_*` 的墙钟拆分**：只量了结构倍数（§15.11），未把两遍验证的耗时单独插桩。
- **`P1-3` 只做到"可观测"**，未调大默认预算——本机内存不支持，需在内存充裕的机器上重新定标。
- 审查报告 §3.9 列出的"非瓶颈"项（`_segment_bases` 缓存、分发开销、label deepcopy、`.npy` 磁盘缓存）**按结论未动**。

### 15.15 验证方式与结果

```bash
# 1) 全量测试（含本轮新增 8 项契约测试）
python -m pytest tests -k ooo -q
#    -> 192 passed, 339 deselected   （基线 184，本轮 +8，0 失败）

# 2) 包本体 lint
ruff check ultralytics_ooo
#    -> All checks passed!

# 3) 新增测试文件 lint（与 CI 第二步同规则集）
ruff check --select E,W,F,B,BLE,I,UP tests/test_ooo_perf_fixes.py
#    -> All checks passed!

# 4) 改动文件编译
python -m py_compile ultralytics_ooo/pool/dataset.py ultralytics_ooo/pool/augment_setup.py \
    ultralytics_ooo/pool/sampler.py ultralytics_ooo/pool/constants.py \
    ultralytics_ooo/core/degrade.py tools/ooo_perf_run.py tools/ooo_perf_bench.py train.py
#    -> OK (8 files)
```

补充验证（三层，细节见 §15.9 / §15.10）：

- **逐像素等价**：重建修复前实现并与现管线逐数组比对，两档分辨率全部 `B_origin_identical=True`、
  `A_slice_identical=True`、`C_no_lru_leak=True`；
- **配对收益**：修复在**同一进程内**回退后配对测量，两次基线几乎重合（4K 切片例 46.270 / 46.202 ms/项）；
- **注入检查**（`_perf_review/inject_check.py`）：把两个锚点改回修复前写法，对应测试**确实变红**（2/2），
  且源文件 sha256 与原文一致（字节级还原）；
- **端到端**：`_perf_review/smoke_train.py` 走真实 trainer（GPU + `workers=2` + `prefetch_factor=2`
  + 切片目标挑片调度 + 切片验证/双口径全开），1 epoch 完成并产出 `best.pt` / `last.pt`。

新增回归测试（`tests/test_ooo_perf_fixes.py`，8 项）：

| 用例 | 锁住的不变量 |
|---|---|
| `test_origin_segment_reads_through_the_raw_lru` | origin 段必须由 raw LRU 供给（第二次读必须命中），且 `self.ims` 保持为空——否则说明又走回 `load_image` |
| `test_origin_segment_reports_the_same_geometry_as_load_image` | LRU 读取路径的 `ori_shape` / `resized_shape` / 像素与 `load_image` 逐值一致 |
| `test_the_slice_tile_is_a_view_but_downstream_never_gets_the_lru_buffer` | tile 必须**是视图**（证明拷贝已移除），且 `_finalize_label` 的**输出**绝不与 LRU 共享内存 |
| `test_prefetch_factor_reaches_the_grouped_loader` | 配置值必须到达加载器（`num_workers=2` 时 `prefetch_factor=2`，且采样器为 `GroupedImageSampler`） |
| `test_prefetch_factor_defaults_to_the_stock_value` | 不显式配置时仍为 stock 的 4，本键纯 opt-in |
| `test_the_weather_noise_kernel_keeps_the_requested_sigma`（σ ∈ {5, 15, 30} 参数化） | int16 改写不得改变噪声强度（实测 σ 与无偏性） |

---

## 十六、答疑：P2-5「池膨胀治理（调 `*_ratio` / 关分支）」到底在动什么

### 16.1 问题（用户原话）

> 详细解释：P2-5 池膨胀治理（调 `*_ratio` / 关分支）

背景：`性能审查报告-全面重审.md` 把它列为 P2 级、**收益最大**的一项，同时是本轮唯一**未落地**的优化项
（见 §15.14）——因为它不是"修 bug"，而是"调配方"，必须与效果评估一起做。本节把它的代价量化下来，供决策。

### 16.2 它治理什么：池大小就是 epoch 时间的乘数

每个 epoch 训练用的**不是**那 N 张原图，而是一个**虚拟扩展池**：原图被在线展开成多个增强变体，
每个变体是池里的一个"槽"，**一个槽就是一个训练样本**。于是：

```
epoch 时间 ≈ 池大小 × 单样本耗时
```

`train.py` 当前配置（60 张图）实测：**池 = 188 槽 = 膨胀 3.13 倍**；若六个 `*_ratio` 全为 1.0 → **375 槽 = 6.25 倍**。

### 16.3 池大小的闭式公式（配置的纯函数，训练前即可算出）

```text
池 = n_per·K_slice + [img_origin ? N : 0] + K_ratio + 2·K_blur
     + K_compose + K_weather + K_occlusion

K_x        = round(x_ratio × N)      # compose 的 count 是 ceil(N/4)，不是 N
n_per      = slice_all_tiles ? 4 : 1
```

这不是推导，而是 `pool/dataset.py::_segment_lengths()` 的实际实现，而它是本项目的**唯一事实源**
（段边界 `_segment_bases`、`len(dataset)`、分组采样器的 units 全部由它派生）。

用该式算 `train.py` 现值（N=60，六个 ratio 均 0.5，`slice_all_tiles=False`，`img_origin=False`）：

```text
sahi 30 + origin 0 + ratio 30 + blur 60 + compose 8 + weather 30 + occlusion 30 = 188
```

与实测日志逐项一致：`Online augment: 188 training samples from 60 images (segment slots: sahi 30, origin 0, ratio 30, blur 60, compose 8, weather 30, occlusion 30)`。

### 16.4 关键认识：`*_ratio` 是**配方旋钮**，不是加速旋钮

采样器在整个池上打乱，所以**任何一个 batch 里各类样本的占比 = 该段宽度 ÷ 池大小**。
调低某个 ratio 不只是"跑得快"，而是**模型实际看到的那类样本真的变少了**。

当前配置的池内占比：

| 段 | 槽数 | 占池 | 备注 |
|---|---|---|---|
| `sahi`（切片） | 30 | 16.0% | `slice_all_tiles=False` → 1 槽/选中图 |
| `origin`（整图） | 0 | 0% | `img_origin=False`，唯一回池路径关闭 |
| `ratio`（比例填充） | 30 | 16.0% | 1 槽/选中图 |
| **`blur`（运动模糊）** | **60** | **31.9%** | **最大段**：短 + 长两档，2 槽/选中图 |
| `compose`（2×2 合成） | 8 | 4.3% | 组级：0.25 槽/图 |
| `weather`（气象退化） | 30 | 16.0% | 1 槽/选中图 |
| `occlusion`（遮挡） | 30 | 16.0% | 1 槽/选中图 |

即：每张被选中的图产出 `sahi 1 + ratio 1 + blur 2 + weather 1 + occlusion 1 = 6` 槽，**其中 blur 一人占 2/6**。

### 16.5 实测 what-if（60 图 / `imgsz=640` / `batch=8` / 同一台机 / 同一份数据）

| 配置 | 池 | 膨胀 | epoch | 相对基准 |
|---|---|---|---|---|
| 六个比率全 1.0 | 375 | 6.25× | 16.42 s | 1.82× |
| 六个比率全 0.7 | 262 | 4.37× | 12.94 s | 1.43× |
| `slice_all_tiles=True` | 278 | 4.63× | 10.39 s | 1.15× |
| `img_origin=True` | 248 | 4.13× | 9.53 s | 1.06× |
| **`train.py` 当前配置** | **188** | **3.13×** | **9.03 s** | **1.00×** |
| `blur_ratio=0.25` | 158 | 2.63× | 7.36 s | 0.82×（−18%） |
| 关 `weather`（`weather_keep=False`） | 158 | 2.63× | 6.90 s | 0.76×（−24%） |
| 关 `blur`（`blur_keep=False`） | 128 | 2.13× | 5.91 s | 0.65×（−35%） |
| 六个比率全 0.3 | 112 | 1.87× | 5.40 s | 0.60×（−40%） |
| **关 `mosaic`（池长不变）** | 188 | 3.13× | **4.64 s** | **0.51×（−49%）** |

单样本耗时在 37–49 ms 之间浮动（池的组成不同），但 epoch 时间**基本就是池大小的线性函数**——表中每一行都印证了这个关系。

### 16.6 三类杠杆：先分清哪些是"近无损"

**① 近无损（优先做）**

- **`slice_all_tiles=False` + `slice_target_tiles=True`**（当前已是）：切片槽 `4K → 1K`，**省 75%**。
  而且它比全片更**准**——项目第五轮实测正片率 **100% vs 盲抽 24%**（§11.4）。
  ⚠️ 反向也成立：全片模式会提供大量背景/空片作为负样本。**若你的问题是误检（FP）多，`all_tiles=True` 可能正是你需要的**，
  那时不要为了省时间关掉它。
- **`img_origin=False → True`**：只贵 **+5.5%**（origin 走 LRU 之后，修复前约 +13%），
  换来"每张图每个 epoch 必然出现一次"。当前 `ratio=0.5` 时各分支是**独立抽签**，未选中的图只靠并集概率覆盖
  （N=60 时约 97%），不是 100% 保证。

**② 有语义代价（需逐项判断）**

- **降 `*_ratio`**：各分支独立、对池大小线性。全 0.5→0.3 省 **40%**；0.5→0.7 多花 **43%**。
- **关 `*_keep`**：该段整段归零。关 `blur` 省 **35%**、关 `weather` 省 **24%**。

**③ 另一维度：不改池长，改单样本成本与样本混合**

- **`mosaic=0`**：epoch **9.03 → 4.64 s（−49%）**，池长一槽未变——因为 mosaic 的代价来自它把取样次数放大 ×3.9，
  而不是池变大了。代价是去掉核心增强。
- **`close_mosaic` / `close_aug_epoch`**：时间维衰减。⚠️ **它们不省时间**——段长必须全程恒定
  （mosaic buffer 索引、批次数、sampler units 都按它预计算），收尾轮只是把内容回退成原图，
  **槽位数不变、成本照付**。它们买的是"收敛质量"，不是"速度"。

### 16.7 为什么 `blur` 性价比最低、`compose` 最高

- `blur` 是**唯一"每张选中图产出 2 个槽"的分支**（短档 + 长档），所以天然吃掉池子的约三分之一；
  而它的长档（任意角 PSF + 失焦）又是全场最贵的单个核：1280×720 下 **67.25 ms vs 轴对齐 10.74 ms（6.3 倍）**。
  当前的 `blur_axis_aligned=True` 已经走在便宜那条路上，**不要为了"更通用"关掉它**。
- 相比之下 `compose` 每 4 张图才 1 槽（0.25 槽/图），只占池的 4.3%，是池里最划算的一段。

### 16.8 建议的决策顺序

1. 先定 `mosaic` / `close_mosaic` —— 时间与语义都是最大的杠杆（−49%）。
2. 确认 `slice_all_tiles=False` + `slice_target_tiles=True`，并顺带检查你是否**真的不需要**全片当负样本。
3. 再看 `blur`：先问"我的失效模式是运动模糊吗"。是 → 保留；否 → `blur_ratio` 0.5→0.25（−18%）或直接关（−35%）。
4. 再看 `weather`：不需要恶劣天气鲁棒性就关（−24%）。
5. 最后才是"整体降 ratio"（全 0.3 省 40%），因为这一步对配方的影响最大。

**每次只动一个分支，跑 A/B 看 mAP 与失效样本**；不要一次性把所有 ratio 降到 0.3——那样即使变快了，
也无法判断是哪个增强在起作用。

### 16.9 换算成与数据集规模无关的口径

§16.5 的数字来自 60 张图。真实数据集 N 更大时，池大小按 N **线性**放大，而单样本耗时大体不变，
因此 epoch 时间也按 N 线性放大。**真正可以在不同实验间比较、且与数据集规模无关的量是"膨胀倍数"**：

```text
epoch 估算 ≈ 膨胀倍数 × N × 单样本耗时(约 40~50 ms @ imgsz=640)
```

### 16.10 复现方式

```bash
# 一进程一配置；--variant 见文件顶部 V 字典
python _perf_review/pool_budget.py <dataset-root> --variant train_py_current
python _perf_review/pool_budget.py <dataset-root> --variant ratios_0.3
python _perf_review/pool_budget.py <dataset-root> --variant no_mosaic
```

工具输出池大小、逐段槽数、逐段占池比、epoch 秒数与 ms/项；11 个变体的汇总见 `_perf_review/pb_summary.txt`，
逐变体原始 JSON 为 `_perf_review/pb_<variant>.json`。换成自己的数据集可直接重跑。

### 16.11 本轮未落地（重申）

**没有改动任何默认配置**。本节只把"调 `*_ratio` / 关分支"的代价从定性变成可查的表。
是否落地取决于你的失效模式与效果评估结果——审查报告把它列为 P2 而非 P1，正是因为它改变训练语义。

## 十七、迁移到新的 Ultralytics：兼容性自检

### 17.1 问题（用户原话）

> 如果我需要在新的 ultralytics 中使用，需要怎么做

### 17.2 为什么这一项不能靠"跑一下试试"回答

本包**不编辑上游源码**，`install()` 的全部工作方式是**重绑上游已经存在的名字**（`build.YOLODataset`、`dataset.v8_transforms`、`build_dataloader`、`BaseTrainer.validate` …）。这个取舍有一个专属失效模式：

> 上游把符号改名或挪了模块 → **不抛 ImportError、不留 traceback，只是那个功能不再发生**。

本项目已经**被这一类缺陷真实伤过一次**（S4）：epoch 回调读的是 `trainer.train_dataloader`，而 `BaseTrainer` 从来没有这个属性。后果是 `set_epoch` 从不执行 → 每轮 `*_ratio` 抽签与 `close_aug_epoch` 在整个项目生命周期内**冻结在第 0 轮**，且**没有任何输出**。是后来补的守卫测试才发现的。

所以换环境时，"装上能跑"不等于"补丁生效"。必须**先自检**。

### 17.3 新增工具：`tools/ooo_compat_check.py`

```bash
python tools/ooo_compat_check.py                  # 人类可读报告；任一项 FAIL 退出码 1
python tools/ooo_compat_check.py --out res.json   # 同时落机器可读结果
python tools/ooo_compat_check.py --quiet          # 只看 FAIL / WARN
```

离线、不下载权重与数据、单进程（冒烟部分刻意用 `workers=0`，避免 spawn 的内存开销）。共 **63 项**（第十八节又加了 4 项），分四层：

| 层 | 项数 | 检查内容 | 失败意味着 |
|---|---|---|---|
| **A** 上游符号存在性 | 27 | `install()` 重绑的每个 `模块.属性` 是否仍可解析 | 上游改名/挪模块 → 该补丁静默失效 |
| **B** 签名 / 调用契约 | 12 | 被包装函数的**前导参数名与顺序**、`BaseDataset.__init__` 的 `hyp`/`fraction` 关键字、`BaseTrainer.train_loader` 属性名（读源码断言，因为它在 `_setup_train` 里才赋值）、`Mosaic.__init__` 是否带 `**kwargs` | 转发参数被错位解释，多半不报错 |
| **C** `install()` 生效性 | 13 | 打完补丁后逐个核对**幂等标记位**（`_ooo_sampler_patched` / `_ooo_fraction_guard` / `_ooo_resume_patched` / `_ooo_dual_patched` / `_ooo_valslice_patched`）与**身份判等**（`is`），在线超参是否真的写进 `DEFAULT_CFG`/`DEFAULT_CFG_DICT`，epoch 回调是否**恰好注册一个** | 补丁没落上、在线超参没进配置、或每轮抽签会跑两遍 |
| **D** 端到端冒烟 | 7 | 真实 4 图数据集走真实工厂：7 段池几何对硬编码契约、池层标签契约（`get_image_and_label`）、变换后样本（`ds[0]` 是 tensor）、**分组采样器类别**、epoch 回调真的调到 `set_epoch`、LRU 计数器 | 装得上但跑起来不对 |

两层分工是刻意的：**A/B 只能说明"名字还在"，C/D 才证明"补丁真的做了事"。** 一个布尔型的"属性存在"判断不出重绑有没有生效。

### 17.4 迁移时要搬的文件

| 路径 | 必要性 | 说明 |
|---|---|---|
| `ultralytics_ooo/` | **必需** | 15 个 `.py` + `ruff.toml`。目录名必须保持 `ultralytics_ooo`（spawn worker 靠重新 import 这个名字反序列化数据集） |
| `tools/ooo_compat_check.py` | **强烈建议** | 换环境后第一件事 |
| `tests/test_ooo_*.py`（11 个） | 可选 | **需放进新仓库的 `tests/`**，它们依赖上游的 `tests/conftest.py` |
| 上游 `ultralytics/` | **不要动** | 零侵入是设计前提；改上游会让后续升级无法合并 |

本包**没有独立构建配置**（`ultralytics_ooo/` 下只有 `ruff.toml`，无 `pyproject.toml`/`setup.py`），因此是随源码搬运而不是 `pip install` 的包。想 pip 安装需自行补 build 配置，且产物包名必须仍是 `ultralytics_ooo`。

### 17.5 三条时序契约

1. **`install()` 必须在任何 `YOLO(...).train(...)` 之前**——在线超参是 `install()` 时注册到运行时 `DEFAULT_CFG` 的；先 `train()` 会撞 `check_dict_alignment` 报 `not a valid YOLO argument`。
2. **`install()` 必须早于"按名字导入" `build_dataloader`**。`from ultralytics.data.build import build_dataloader` 会绑定**补丁前**的函数并永久失效；正确做法是 `install()` 之后 `from ultralytics.data import build as B` 再取 `B.build_dataloader`。**本项目自带工具 `tools/ooo_perf_run.py` 曾因此长期报 `RandomSampler`**，让人误判"分组采样没有效果"（已在第九轮修正）。
3. **不要依赖"多调一次没关系"**。`install()` 按标记幂等，但若发生 `sys.modules` 清理后重新 import、或环境里存在两份包副本，epoch 回调会被注册两次 → **每轮 `*_ratio` 抽签跑两遍**。C 层断言回调恰好一个。

### 17.6 非永真验证（本项目的既有要求）

新引入的门禁必须证明它**不是永真**。`_perf_review/compat_inject.py` 注入 4 类失败模式，逐项确认检查器变红：

| 注入 | 目标层 | 实测 |
|---|---|---|
| `delattr(build, "RANK")` | A：上游符号消失 | ✅ 捕获 `ultralytics.data.build.RANK` |
| 清掉 `_ooo_sampler_patched` | C：补丁未生效 | ✅ 捕获该标记 |
| 从 `DEFAULT_CFG` 删掉 `slice_prob` | C：在线超参未注册 | ✅ 捕获 |
| 往 `_ONLINE_DEFAULTS` 塞一个上游键（`mosaic`） | C：键表漂移 | ✅ 报 WARN |

6 项全部通过，且还原后基线复绿；注入全程**只改运行时对象**，`ultralytics_ooo/*.py` 逐文件 sha256 不变（本脚本不落盘改包）。

### 17.7 开发过程中被自检器抓到的两个自身缺陷（均已修）

1. **D3 查错了层**：最初用 `ds[0]` 断言池层标签契约，而 `ds[0]` 返回的是**变换后**的样本（`img` 已是 tensor、`instances` 已被 `Format` 拆成 `bboxes`/`cls`），于是误报 `missing=['instances'], bad_img=True`。改为断言 `get_image_and_label()`——这也正是 `tests/test_ooo_branches.py` 的既有约定。
2. **C8 撞上了它自己文档里警告过的假阳性**：`check_online_defaults_are_project_only()` 无参调用会读**活的** `DEFAULT_CFG_DICT`，而 `install()` 此时已把 80 个项目键写进去了，于是把每一个项目键都报成"上游重复"（实测报出 80 个）。改为**从 `cfg/default.yaml` 解析上游原始键集**后再比对——即该函数 docstring 推荐的"显式传入、与安装顺序无关"的用法。

### 17.8 文档同步

- `ultralytics_ooo/README.md` 新增「迁移到另一个 Ultralytics（新版本 / 新环境）」一节：自检用法、四层含义、要搬的文件、三种搬法、三条时序契约、Windows spawn 形态要求、按层定位的排障表。同时把该 README 末尾那条**已被第九轮改动推翻**的记述修正——它原先写着 `prefetch_factor` "已移除…传入会直接报未知参数"，而第九轮已把它变成**真实生效**的配置键（默认 4 = 上游硬编码值）。
- `_perf_review/doc_check.py` 的检查范围从 2 个文档扩到 3 个（纳入 `ultralytics_ooo/README.md`）。

### 17.9 验证方式与结果

```bash
python tools/ooo_compat_check.py                      # -> 63 项: PASS 63 / FAIL 0 / WARN 0, RC=0
python _perf_review/compat_inject.py                  # -> 注入检查 6 项: 全部通过, RC=0
python _perf_review/doc_check.py                      # -> format check: PASS（3 个文档）
ruff check tools/ooo_compat_check.py _perf_review/    # -> All checks passed
```

基线环境：`ultralytics 8.4.126`（干净原版，本仓库）、Python 3.12.13 / torch 2.5.1 / CUDA 12.4。
**已知验证版本就是 8.4.126**；每换一次上游版本都要重跑自检，把 README 里「第 4 步：按自检失败的那一层定位」的表当准入清单。

## 十八、双口径的 checkpoint 家族：补 `last_whole.pt` + 收尾 strip

### 18.1 两个缺陷

在梳理「从读数据集到产出 `.pt`」的完整流程时，对着 `final_eval` 读出来的两条：

| # | 缺陷 | 后果 |
|---|---|---|
| **①** | `best_whole.pt` **从未被 strip**。它是 `save_model` 里从**未 strip** 的 `last.pt` 复制字节，而 `final_eval` 只 strip `self.last` / `self.best` | 一个名叫"参考产物"的文件保留着 optimizer 状态与 **fp32** EMA，体积数倍于它本应对照的 `best.pt` |
| **②** | **没有 `last_whole.pt`**。`README` / `项目说明.md` / `train.py` 三处文档都写着"`best_whole.pt`/`last_whole.pt`"，实现里却只写了前者 | 文档与实现不符；整图家族比主家族少一个成员 |

用户决定：**① 修，② 实现（而不是改文档去迁就实现），③（收尾 epoch 退化为重复原图）不动。**

### 18.2 实现

改 `ultralytics_ooo/pool/dual.py` 两处：

**`save_model`（补 `last_whole.pt`）**

```python
ret = _orig_save_model(self)
if not _dual_enabled(self.args):
    return ret
written = self.last.read_bytes()                    # 复用上游刚序列化好的字节，不二次序列化
(self.wdir / "last_whole.pt").write_bytes(written)  # 每轮镜像 last.pt
if self._whole_improved:
    (self.wdir / "best_whole.pt").write_bytes(written)
```

**`final_eval`（新增包装，负责 strip）**

上游在这里把 `last.pt` / `best.pt` 变成推理产物（optimizer→None、权重→fp16 EMA、epoch→−1），它不知道我们这两个镜像的存在。新包装在 `_orig_final_eval` 之后把两个整图文件按同样方式 strip，并带上与 `best.pt` 相同的 `train_results` 更新（整图快照可能取自更早的 epoch，其内置曲线会被截断在自己的那一轮）。同时用 `RANK in {-1, 0}` 守住"只有 rank 0 写权重目录"。

配套改动：
- 抽出 `_dual_enabled(args)`，取代 `validate` 里内联的 `bool(...) and bool(...)`——"两个开关都开"这个判据现在有单一出处，`save_model` / `final_eval` / `validate` 共用。
- `final_eval` 在**打补丁时是可选的**（测试里的 stub trainer 只定义了 `validate`/`save_model`）。跳过它会静默复活缺陷①，所以**跳过时告警**而不是静默；同时在包装函数上打 `_ooo_final_eval_wrapper = True` 标记，由迁移自检断言——把一个只在日志里可见的降级变成硬失败。
- 新增两个上游耦合点，已登记进 `tools/ooo_compat_check.py`：`ultralytics.utils.RANK`、`ultralytics.utils.torch_utils.strip_optimizer`（A 层），`BaseTrainer.final_eval`（B 层），以及 C 层的 wrapper 标记断言。自检从 **59 项 → 63 项**。

### 18.3 语义说明（重要，别误解）

**`last_whole.pt` 与 `last.pt` 逐字节相同。**

权重不依赖"用哪个口径给它打分"，所以 `last_whole.pt` 在定义上就等于 `last.pt`。保留它**不是**为了存一份不同的模型，而是让整图家族与主家族**形状一致**：

| 文件 | 语义 | 与主家族的关系 |
|---|---|---|
| `last.pt` | 最新状态 | — |
| `best.pt` | 主口径（切片）fitness 最优 | — |
| `last_whole.pt` | 最新状态 | **内容等价于 `last.pt`**（非逐字节，见下） |
| `best_whole.pt` | 副口径（整图）fitness 最优 | 一般 **≠ `best.pt`**（取的 epoch 不同） |

也就是说 `{best, best_whole}` 这一对是有信息的，`{last, last_whole}` 是内容冗余的——这一点已写进 `dual.py` 的模块 docstring、`项目说明.md` §八 与 `train.py` 注释，避免下一个人以为是 bug。

**"内容等价"而不是"逐字节相同"——这一条是被实测纠正的。** 最初的注释写的是"byte-identical by construction"，理由看着很硬：`last_whole.pt` 就是 `last.pt` 的字节拷贝。但真实 2 epoch 跑完后四个文件的哈希两两不同，于是量了一遍（`_perf_review/ckpt_diff.py` → `ckpt_sizes.py` → `strip_determinism.py` → `ckpt_zip_diff.py`）：

| 观察 | 实测 |
|---|---|
| 四个文件的权重 | **逐元素相同**（`torch.equal` 全真），全部 fp16 |
| 四个文件的键集合 | 完全相同；`optimizer`/`ema`/`updates`/`scaler` 均为 `None`，`epoch = -1` |
| `strip_optimizer` 是否字节可复现 | **否**：同一份输入 strip 两次 → 长度相同（5,344,754）、哈希不同 |
| 上游自己的 `best.pt` vs `last.pt` | `save_model` 用**同一份** `serialized_ckpt` 字节写出两者，strip 后仍然哈希不同 |
| 尺寸差 35,848 的来源 | 全部在 zip 容器：716 个条目**条数与载荷总字节完全相同**（5,247,853），差异来自**条目前缀即归档名**（`last/` 比 `best_whole/` 短 6 字符）及其带来的对齐填充 |

结论：**strip 之后"两个 checkpoint 逐字节相同"根本不是可达性质**，与本次改动无关。因此四处文档里的"逐字节相同"全部改为"内容等价"，并注明要按内容或 `results.csv` 的指标比较、不要比哈希。

另一个刻意的选择：`last_whole.pt` **不**以"第二遍跑成功"为条件。它以"last 这个名字承诺的是最新状态"为准——若以成功为条件，一趟在最后一轮失败的双口径训练会留下一个**过期**的 `last_whole.pt`，对一个以"当前"为卖点的文件来说更糟。失败本身已经告警，且 `best_whole.pt` 仍被正确跳过。

### 18.4 验证

**真实端到端**（`_perf_review/dual_e2e.py`，2 epoch / 8 训练图 / imgsz 320 / CPU，写进 `_perf_review/_e2e_runs`，不碰用户的 `runs/`；1 分 7 秒）：

| 文件 | 字节 | `epoch` | `optimizer` | `ema` | dtype | `train_results` |
|---|---|---|---|---|---|---|
| `best.pt` | 5,346,238 | −1 | None | None | float16 | 有 |
| `last.pt` | 5,346,238 | −1 | None | None | float16 | 有 |
| `best_whole.pt` | 5,382,086 | −1 | None | None | float16 | 有 |
| `last_whole.pt` | 5,382,086 | −1 | None | None | float16 | 有 |

**四个都是 strip 过的推理快照**——这正是缺陷①的验收点（修复前 `best_whole.pt` 会以 fp32 + optimizer 状态存活）。探测脚本自身做了断言并按退出码返回，所以这次运行的 **RC=0 就是该结论的机器可读证据**。

**契约测试**（`tests/test_ooo_dual.py`，9 → **14** 项）：`last_whole.pt` 逐轮镜像；第二遍失败时仍写 `last_whole.pt` 但不写 `best_whole.pt`；双开关任一关掉时权重目录与 stock 运行完全一致；`final_eval` 确实把两个整图文件 strip（**用真 `strip_optimizer`**，断言 `epoch == -1`、`optimizer is None`、`ema is None`、`model.weight.dtype == torch.float16`、`requires_grad` 为假、`train_results` 已带上完整曲线）。

测试基座相应补齐：`_FakeTrainer` 增加 `final_eval` / `read_results_csv`，并新增 `_write_real_ckpt()`——写一个**真实的** ckpt（`model=None`、`ema=<nn.Module>`）。这个形状是刻意的：`strip_optimizer` 会断言 `"model" in x`，然后用 `x["ema"]` 覆盖它并调用 `.half()` / `.parameters()`，所以 `ema` 必须是一个 `nn.Module`——这正是 `save_model` 实际存的东西（`deepcopy(unwrap_model(ema.ema)).half()`，而不是没有这些方法的 `ModelEMA` 包装）。

**非永真验证**（`_perf_review/inject_check.py`，2 → **5** 条用例，全部咬住）：

| 注入 | 期望变红的测试 | 实测 |
|---|---|---|
| 删掉 `last_whole.pt` 的写入 | `test_last_whole_pt_mirrors_last_pt_every_epoch` | ✅ |
| 不包装 `final_eval` | `test_final_eval_strips_both_whole_mirrors` | ✅ |
| `save_model` 忽略双开关门控 | `test_no_whole_checkpoints_without_dual_mode` | ✅ |

三条注入的源文件字节级还原（sha256 一致）。

**门禁**：`pytest tests -k ooo` → **197 passed**；`ruff check ultralytics_ooo`、以及交付脚本按严格规则 `--select E,W,F,B,BLE,I,UP` 复查 → 全部 `All checks passed`；迁移自检 **63 项 PASS 63 / FAIL 0 / WARN 0**。

### 18.5 文档同步

- `ultralytics_ooo/README.md`：功能表、包结构、测试覆盖三处补 `last_whole.pt` 与 strip 说明。
- `项目说明.md`：包结构树、功能表、`BaseTrainer` 补丁清单、双口径验证条目、§八 边界说明；顺带把**上一轮新增却漏登记**的 `tools/ooo_compat_check.py` 补进工具清单。
- `train.py`：`val_slice_dual_metric` 注释（上一轮我把它改成"没有 `last_whole.pt`"，现在实现补齐了，注释随之改回并注明两者逐字节相同）。
- 测试基线表与「验证状态」：按本文档 §七 的约定，只更新该表最后一行与 `项目说明.md` 的「验证状态」。

## 十九、`close_aug_epoch` 窗口此前只覆盖前 `max(K)` 张图（已修复）

### 19.1 结论

上一轮把它记成"最后几轮池子里是重复的少量原图（约 3 倍重复）"。实测下来**说轻了**：以 `train.py` 的配置，进入收尾窗口后池子**只看得见 8 张图里的 4 张**，另外 4 张**完全不出现**，而看得见的那 4 张各出现 7/6/6/6 次。

**已按方案三修复**（用户采纳建议）。修复后同一场景覆盖恢复到 **8/8**。

### 19.2 机制

`_rebuild_epoch_masks` 里对每个分支：

```python
if self._closing:
    setattr(self, f"_sel_{attr}", list(range(self._ratio_K(ratio_attr, count, on))))
    continue
```

原注释写的是"the content is irrelevant（本轮没有 builder 做增强），只有长度要对，所以用 `range(K)` 而不是随机抽"。**长度那半是对的，内容无关紧要那半不对**：一个 epoch 能看见哪些图，恰好等于这些选择集的**并集**。而收尾窗口里每个图级分支都取同一个前缀 `range(K)`，于是并集就是**文件顺序的前 `max(K_x)` 张**——并且在整段窗口里**每个 epoch 都是同一个前缀**（`range(K)` 完全不碰 rng，连种子都改不动它）。

`train.py` 的 ratio 全是 0.5、N=8 → `K=4` → 并集 = `{0,1,2,3}`。

对照：正常 epoch 走 `rng.sample`，并集同样可能缺图（20 epoch 里 7 个 epoch 缺 1 张），但**每轮换一批**，长期会覆盖全部；前缀 `range(K)` 不会。

### 19.3 修复前实测（`_perf_review/closing_probe.py`，`_mini_val_set` 8 训练图 @ imgsz 320）

| 场景 | 池 | 不同图数 | 缺席图 | 单图最多槽 | 与真原图逐像素相同 | 不同 | mean\|diff\| | max\|diff\| |
|---|---|---|---|---|---|---|---|---|
| epoch 0（正常） | 25 | **8** | 0 | 5 | 0 | 25 | 18.09 | 255 |
| epoch 1（收尾窗口） | 25 | **4** | **4** | 7 | 5 | 20 | 5.51 | 191 |
| epoch 1 + `img_origin=True` | 33 | **8** | 0 | 8 | 13 | 20 | 4.17 | 191 |

（epoch 0 的 mean\|diff\| 是因为那些槽**本来就做过增强**，与"真原图"不同属预期——而且其中 blur 角度 / weather 类型 / 遮挡位置用的是**未播种**的 `random`，所以这个数逐次运行会变（18.09 → 16.67），这本身不是问题。收尾窗口的 ~5.5 才是"本该是原图却不是"的量，且逐次稳定。）

补充扫描：`epochs=20` 时 **8/20** 个 epoch 至少缺 1 张图；其中 epoch 19（唯一的收尾 epoch）缺的就是那 4 张。

### 19.4 附带发现：收尾窗口的"原图"有一半不是真原图

5 个槽（4 个 sahi + 1 个 compose）走 `_build_origin_sample` / `load_image`，是**真原图**；另外 20 个槽（ratio/blur/weather/occlusion）走 `_degrade_frame`，返回的是**被工作分辨率上限压过**的帧（`degrade_max_side=0` → auto = `2*imgsz`）。1920×1080 会先降到 640×360、再 resize 到 imgsz，即**二次重采样**，与 origin 段的一次重采样不同：平均差约 5.5/255（约 2%）、锐利边缘处最大 191/255，`ori_shape` 也报成压过的尺寸而非真原图。

这不是收尾窗口独有——那四条分支**平时也走同一个上限**（那是性能优化，见 `_degrade_frame` 的 docstring）。**这一条本轮未改**：它是有意的性能取舍，且收尾窗口里"原图"与"压过的原图"都属于真实分布，改成绕过上限会让收尾轮成为全流程最慢的几轮。

### 19.5 选定的方案与实现

三个候选：`img_origin=True`（0 行，覆盖 8/8 但不均衡仍在）、所有 `*_ratio=1.0`（0 行，池膨胀 6.25×）、**代码方案三（采纳）**。

实现比预想的更简单：**把那个 `if self._closing:` 短路块整块删掉**。

理由：正常路径本来就是 `sorted(rng.sample(range(count), k))`，且段长由 `_ratio_K`（配置的 ratio）决定、与抽到谁无关；分支关闭时给 `[]`、ratio ≥ 1 时给 `None` 哨兵。所以删掉短路块**恰好**得到方案三的全部效果，而且不留第二份重复逻辑——这是"短路块本来就是多余分支"的直接证据。

删掉的是 19 行（含上一轮我写的实测注释），替换为一段说明"为什么收尾窗口**不**跳过抽签"的注释，位置放在 `self._closing` 赋值处（读者看那里的地方）。`self._closing` 本身仍在三处做真正的工作：builder 短路输出原图、`_apply_target_tile_schedule` 不调度、base 段的切片门。

一个顺带的改善：短路块删除后，收尾窗口里 ratio ≥ 1 的分支重新拿到 `None` 哨兵，于是 `augment masks` 日志恢复打印 `all/<count>` 而不是 `count/count`，与非收尾 epoch 的写法一致了。

### 19.6 修复后实测（同一探针、同一数据集）

| 场景 | 池 | 不同图数 | 缺席图 | 单图最多槽 | 与真原图逐像素相同 |
|---|---|---|---|---|---|
| epoch 0（正常） | 25 | 8 | 0 | 5 | 0 |
| **epoch 1（收尾窗口）** | 25 | **8**（原 4） | **0**（原 4） | **5**（原 7） | 5 |
| epoch 1 + `img_origin=True` | 33 | 8 | 0 | 6 | 13 |

段长在三种场景下都**逐位不变**（`4/0/4/8/1/4/4`，`img_origin=True` 时 `4/8/4/8/1/4/4`）。

20-epoch 扫描：至少缺 1 张图的 epoch 从 **8/20 降到 7/20**，且**收尾 epoch 19 已从名单中消失**——那 7 个剩下的都是非收尾 epoch 的正常并集抖动（与前缀无关，是独立抽签的固有性质，长期轮换即覆盖全部）。

`max\|diff\|` 191 与 `ident=5` 都不变，符合预期：本轮只改"选哪些图"，没碰"怎么渲染"。

### 19.7 验证

- **新增回归测试** `test_close_aug_epoch_rotates_the_covered_subset`：`close_aug_epoch=6 / epochs=6` 让每个 epoch 都落在窗口内，断言 ①段长逐位不变、②每个分支的选择是**无重复且落在合法范围**的 K 元子集、③**各 epoch 的选择不完全相同**、④**至少有一个 epoch 触到前 `max(K)` 之外**。
  修复前 ③ 直接失败（每轮都是同一个 `range(K)`），所以这条测试不可能永真。
- **改写** `test_close_aug_epoch_keeps_the_ratio_sized_layout`：它原来断言 `_sel_slice == [0, 1]`，**恰好把这个 bug 锁成了契约**。改为断言"长度仍按 ratio + 选择合法"，并指向上面那条新测试。这也解释了为什么这个 bug 能长期存活：它被一条测试显式保护着。
- **注入检查**（`_perf_review/inject_check.py`，5 → **6** 条，全部咬住，源文件 sha256 还原）：把短路块插回去，新测试确实变红（第一版注入锚点与 `_log_mask_summary` 里的同形循环撞了 2 次而被跳过，已加长锚点）。
- **门禁**：`pytest tests -k ooo` **198 passed**；`ruff check ultralytics_ooo` 与交付脚本严格规则 lint 全过；三个文档格式自检 PASS。

### 19.8 影响与遗留

`close_aug_epoch` 越长，这个修复越有价值：`close_aug_epoch=1`（`train.py` 现值）原本只是最后一轮看一半数据；若是 100 轮里关 15 轮，原本是最后 15 轮都在同一个子集上收敛，与"收敛到真实分布"的目的相反。修复后覆盖子集每轮轮换，窗口 ≥ `N/K` 轮即覆盖全部。

**仍不是"每轮必然覆盖全部 N 张"**：每个分支只抽 K 张，一张图被 5 个图级分支全漏掉的概率是 `(1-K/N)^5`（N=8、K=4 时约 3.1%）。要硬保证全覆盖需另设计（例如让并集按轮次轮转），但那是另一档改动，不在本轮范围内。`img_origin=True` 仍是"保证出现在池中"的现成手段。

---

## 二十、从零独立验证发现的缺陷与修复（P0 两项已修）

### 20.1 本轮从哪里来

前面十九轮都是"实现者自查"。这一轮换成**外部视角**：不信任既有文档自述，一切以代码实跑为准，先静态通读 `ultralytics_ooo/**/*.py`（约 4.7k 行）+ 核对每个 patch 点在上游的真实存在性，再用**独立探针**收集证据（探针源码在 `_verify_probe/`，原始输出在 `_verify_out/`），最后跑一次真实的 `train.py`（2 epoch，配置与出厂一致）。

**同一个结论，两种到达方式**：本节的每一条都有"文件:行号 + 实测数值 + 复现命令"三件套，不引用任何文档里的自述。

自带门禁当时是**全绿**的（迁移自检 63/63、单测 198/198），也就是说下面这些缺陷全部活在"全绿"下面。

### 20.2 S1 零侵入契约不成立：mosaic buffer 的记账语义与上游不同 ★本轮修复

**问题**：不传任何 ooo 开关时，安装后仍与上游**不等价**，而文档与 `installer.py` 的注释承诺"byte-for-byte upstream"。

- 上游 `load_image` 只在**解码未命中**时写 `dataset.buffer`：`ultralytics/data/base.py:230 if im is None:` → `:270 self.buffer.append(i)`；命中路径在 `:278` 直接返回 `self.ims[i]`，**不 append**。
- 本包的样本（origin 段与全部分支，且 `img_origin` 默认 `True` 使**所有**样本都走 origin 段）读的是**原始分辨率的 raw LRU**，不走 `self.ims`，并且**无条件** append：
  - 原 `pool/dataset.py:1402 self._touch_buffer(index)`（origin 段）
  - 原 `pool/dataset.py:987 self._touch_buffer(index)`（`_begin_branch_label`，blur / weather / occlusion）
  - ratio / compose / base 切片段同理。
- `Mosaic.get_indexes()`（`augment.py:497-498`）正是从 `dataset.buffer` 抽混样伙伴，所以重复项不是"看起来脏"，而是**把不同的图挤出了混样池**。

实测（`_verify_probe/p1b_buffer_cause.py`，8 张图、逐样本 `random.seed(1000+i)`）：

| 采样点 | 上游 `buffer_before` | 修复前 | 修复后 |
|---|---|---|---|
| i=1 | `[0]` | `[0,0,0,0]` | `[0]` |
| i=4 | `[0,1,2,3]` | `[1,0,0,3,2,0,1]` | `[0,1,2,3]` |
| i=7 | `[0,1,2,3,4,5,6]` | `[5,4,5,6,0,0,6]` | `[0,1,2,3,4,5,6]` |

内容级后果（`p1_zero_intrusion.py`）：修复前 8 个样本 **7 个 sha256 与上游不同**（`n_box` 已变，i=3 为 4 vs 3、i=6 为 2 vs 1）；修复后 **8/8 摘要与上游完全一致**。

旁证：`self.ims` 常驻帧数 上游 7 / 本包 0 —— 这是**有意**的（origin 段走 LRU 是本项目第九轮的既定优化，见第十五节），保留不动；它与本缺陷无关，只是同一个"不走 `load_image`"事实的另一面。

**修复**（`pool/dataset.py`）：

1. 新增 `_load_image_cached_ex(...) -> (im, decoded)`，把"这次是否真的解码"从唯一知道答案的地方（LRU 命中判定处）报出来；`_load_image_cached` 降级为薄包装（签名不变，`valslice.py` 的调用与 `tests/test_ooo_valslice.py` 的假 reader 都不受影响）。
2. 新增 `_touch_buffer_for_decode(index, decoded)`：**只在解码时**喂 buffer。LRU 关闭（`slice_raw_cache_size=0`）时每次读都是真解码 → `decoded=True` → 历史行为逐字保留。
3. 新增 `_degrade_frame_ex(...) -> (img, scale, decoded)`，让 blur / weather / occlusion / ratio 四条分支拿到同一个标志（`_degrade_frame` 保持二元组签名）。
4. `_begin_branch_label(index, img_index, decoded)` 的 `decoded` 是**必填参数**——默认值 True 会让未来新增的分支静默复活这个缺陷。
5. compose 段：四个源**任一**解码即喂 buffer（它一个槽代表 4 张图）。
6. base 切片段：由"先喂后读"改为"读后按 `decoded` 喂"，一次读 4 个 tile 只贡献 1 条记录。
7. close_aug_epoch 下的 `load_image` 分支保持无条件喂：该状态下 `load_image` 既不入 `self.ims` 也不 append（两者都在 `slice_transform is None` 的判定后面），所以那次访问确实是一次真解码。

**回归测试**：

- `test_a_cache_hit_does_not_feed_the_mosaic_buffer`：4 图全分支池，一次全量 pass 后要求 `len(ds.buffer) <= ni` 且**无重复项**，并额外要求 `hits > 0`（否则说明命中路径没被走到，检查就是空的）。修复前该 pass 会 append 41 次（每个槽一次）。
- `test_no_switches_means_no_expansion_at_all` **补上 buffer 那一半**。原先它只断言 `_extended_pool_on() is False` 并写下"stock buffer bookkeeping"——**这个断言是假的**：该 flag 只影响 `load_image` 的内部分支，而那条路径在原点段根本没被走到。现在改为直接断言行为：同一个索引连读 3 次（都命中 LRU）后 `buffer` 必须不变。

**注入检查**（`_perf_review/inject_check.py` 6 → **8** 条）：把 `_touch_buffer_for_decode` 的门去掉（回到"每次访问都 append"），新测试确实变红；源文件 sha256 字节级还原。

### 20.3 S2 `val_slice_dual_metric=True` 时 `results.png` 静默丢失 ★本轮修复

**问题**：双口径运行时，训练曲线图 `results.png` 从来没有被写进 run 目录，且无任何告警。

- 上游 `plot_results` 把图写到 `Path(file).parent`（`utils/plotting.py:1064,1095`），并且会 **glob 同目录下所有 `results*.csv`**。
- 包装器为了让那个 glob 只看到"已剥掉 `whole_*` 列"的 CSV，必须把临时 CSV 放进**独立目录**；而那个目录同时也是图的落地点 → `TemporaryDirectory` 退出时把图一起删掉。
- 旁证（真实 run）：`runs/exp`、`exp-2`、`exp-3`、`exp-4` 都有 `Box*_curve.png` / `confusion_matrix*.png`，**唯独没有 `results.png`**。

**修复**（`pool/dual.py::_patch_plot_results`）：临时目录内画出图后，把 `results.png` 搬回真实 run 目录；`on_plot` **不**转发给内层调用（否则回调会登记一个马上就被删掉的临时路径），改由本包装器用最终路径调用。若内层没产出图，则回落上游原路径并告警。

**回归测试** `test_the_filtered_results_plot_still_lands_in_the_run_directory`：断言 **文件存在且非空**，并断言 `on_plot` 报的是 run 目录、原始 `results.csv` 未被改动、以及上游剥列后的路径本身仍能出图（证明包装器不是简单绕开原函数）。断言对象是文件，不是返回值——返回值检查在坏版本上也会通过。

**注入检查**：把 `final.write_bytes(...)` 换成 `pass`（即图留在临时目录），新测试变红。

**端到端验收**：修复后真实 `train.py`（2 epoch）产出 `runs/exp-5/results.png`（`runs/exp-4` 没有）。

### 20.4 M1 `cache='ram'` / `cache='disk'` 被静默旁路

**问题**：`self.ims` 存的是 **imgsz 缩略帧**，而池内所有样本都要 **原始分辨率** 帧，所以 `cache='ram'` 的预载被付了钱却用不上；而原来的降级告警只在 `slice_prob > 0` 时触发，**出厂配置（slicing 关、`img_origin` 开）完全静默**。

实测（4 图集，`cache='ram'`）：`self.ims` 4 帧全部预载，一次全量 pass 仍然 **4 次真实解码**。

**修复**（`pool/dataset.py::__init__`）：判据从 `slice_prob` 换成**布局**（`any(self._segment_lengths())`，即"确实有段会读图"）。`cache='ram'` 仍降级为 `cache=False` 并告警（否则 mosaic buffer 不生效，同一张图的每个子样本都要各解码一次）；`cache='disk'` 只给一条 `INFO`，因为关掉它还会连带停掉 `*.npy` 的写入。

> 未做：给池内样本做"原始分辨率 RAM 缓存"。它需要一张与 `self.ims` 独立、且带字节预算的表，与现有 raw LRU（16 帧 / 256 MiB，实测命中 91.8%）职责重叠，收益未实测前不动。

### 20.5 M5 banker's rounding 的告警文案在 `ratio*count == 0.5` 时是错的

**问题**：`_warn_ratio_selects_nothing` 建议"把比例抬到 `ratio x count >= 0.5`"，但抽取用的是 Python `round()`，**0.5 舍到偶数**：`round(0.5) == 0`、`round(1.5) == 2`。

可达形状：4 图数据集上 `compose_ratio=0.5` → 组数 `ceil(4/4)=1` → `round(0.5)=0` → **compose 段长为 0**（`_verify_probe/p5_contract_edges.py` §G 实测段长 `[2,4,2,4,0,2,2]`）。

**修复**：文案改为 `> 0.5`，并说明"抽签用的 `round()` 会把 0.5 这个平局向下取"；`_warn_ratio_selects_nothing` 的 docstring 补上这段推理与可达场景。

> 未改 `round()` 本身：布局（`_ratio_K`）与抽签用的是**同一个表达式**，两者不会漂移；单方面改成"四舍五入"会让"配置说 K 张、实际抽 K 张"这条不变量失去依据。舍入的单调性也实测过（1000 点扫描无回退）。

### 20.6 L1 `occlusion` 标签长度不齐时会 `IndexError`

**问题**：`label["cls"]` 的过滤没有长度校验，`len(cls) != len(bboxes)` 时布尔掩码索引直接抛 `IndexError`，把一次多小时训练打断；而同一个方法里 segments 路径早就有告警保护。

**修复**：过滤前先把 `boxes` / `cls` 对齐到公共前缀并告警，之后掩码索引不可能越界；`keep.all()` 为空掩码时自然跳过，坏标签保持原样而不是被改得更坏。

### 20.7 L3 resume 的 `patience` 修复措辞会误导

原日志写"自动修补 checkpoint 元数据: ... (patience=…)"，容易被读成"本轮早停阈值被改了"。实际上 `_setup_train` 在调用 `resume_training` **之前**就已按 `self.args.patience` 构造 `EarlyStopping`，而 `patience` 本身就在上游的 resume 白名单里，两侧天然一致；这段修复真正的作用是**改写 checkpoint 里保存的值，供下次续训读取**。日志与代码注释已按这个事实改写。

### 20.8 本轮**未改**（有意，附理由）

| 项 | 为什么不改 |
|---|---|
| `degrade_max_side` 默认 auto = `2*imgsz`（M4：分支耗时 weather 29.9% / occlusion 23.8% / blur 21.7%，而切片本体仅 0.7%） | 这是**增强强度**的参数，不是缺陷：改默认值会改变训练内容、让历史实验与新实验不可比。它已经可以用 `degrade_max_side` 显式设定。请把它当成一次需要你自己拍板的 A/B（建议先量 `1.25*imgsz` 对上 `2*imgsz` 的 mAP 差异），本节只把"瓶颈在这里"这件事说清楚。 |
| 双口径验证成本 = `(n_tiles + n_val) / n_val` 倍（本配置 6+3 vs 3 → **3×**） | 是特性不是缺陷（第二遍整图验证本来就要跑一遍）。已在 `项目说明.md` 标注倍数。 |
| 收尾窗口里 20/25 槽走 `_degrade_frame` 的工作分辨率上限（二次重采样，mean\|diff\| ~5.5/255） | 第十九节已确认是有意的性能取舍。 |
| 上游改名导致 patch 静默失效 | 零侵入设计的固有代价，已由 `tools/ooo_compat_check.py`（63 项）兜底；本轮未新增耦合点。 |
| 多 worker / GPU / 大规模数据集下的结论 | 本机 CPU 被上游强制 `workers=0`（`engine/trainer.py:164`），且只有 8 训练图；这些结论**本轮没有证据**，不下结论也不改代码。 |

### 20.9 一处对**验证报告自身**的更正

独立验证报告里把 `_save_state[branch]` 的去重键集合与 `OnlineSlice._saved_keys` 列为"无界增长"。复查后**这条是错的**：两者的键分别是 `(branch, tier, index)` 与 `(img_index, k)`（`augment_setup.py:320,351`），都按数据集规模有界（前者 `2 x 池长`，后者 `4 x N`）。本轮不为它改代码。保留这条更正，是为了说明"外部验证的结论同样需要被验证"。

### 20.10 验证方式与结果

```bash
# 1) 零侵入对照（2 进程：净化源码树 vs install()，同一随机序列）
python _verify_probe/p1b_buffer_cause.py upstream  _verify_out/ref_upstream.json
python _verify_probe/p1b_buffer_cause.py installed _verify_out/fix_installed.json
#    -> 8/8 样本摘要与 buffer 内容与上游一致（修复前：7/8 摘要不同）

# 2) 绘图 A/B（真实双口径 results.csv 复制进临时 run 目录）
python _verify_probe/p4_valremap_plot.py
#    -> results_png_present = true（修复前 false）；验证侧重映射 max err 仍为 1.1e-4 px

# 3) 全量单测
python -m pytest tests -k ooo -q
#    -> 200 passed（198 + 本轮新增 2），0 failed

# 4) 迁移自检 / 注入检查 / 格式自检 / lint
python tools/ooo_compat_check.py          # -> 63 项 PASS 63 / FAIL 0 / WARN 0
python _perf_review/inject_check.py       # -> 8/8 咬住，源文件 sha256 还原
python _perf_review/doc_check.py          # -> PASS
ruff check ultralytics_ooo                # -> All checks passed!

# 5) 端到端
python train.py
#    -> EXIT=0，2 epoch 完成；runs/exp-5 产出 results.png（exp-4 没有）；
#       best_whole.pt / last_whole.pt 照旧产出并被 strip
```

> **一件不能用来下结论的事**：修复后那次 `train.py` 的 epoch 墙钟（7.8 s）比上一次（19.6 s）快很多，但本机内存常态只剩 0.4–0.7 GB，同会话方差 ±13%、跨会话 ±40%，**这个差值不可归因于本轮改动**。本项目既有的测量纪律（第九节）依然适用：要谈收益，必须在同一进程内交替跑被测的两个版本。

本轮新增/改写的测试：

| 用例 | 锁住的不变量 |
|---|---|
| `test_a_cache_hit_does_not_feed_the_mosaic_buffer` | mosaic buffer 只记录**解码**，不记录**访问**；一次全量 pass 后条目数 `<= ni` 且无重复 |
| `test_no_switches_means_no_expansion_at_all`（补 buffer 半边） | 无开关时连读同一索引 3 次，`buffer` 必须纹丝不动 |
| `test_the_filtered_results_plot_still_lands_in_the_run_directory` | `whole_*` 剥离后 `results.png` 必须落在 run 目录、非空、`on_plot` 报最终路径 |

### 20.11 文档同步

- 本节（新增第二十节）；
- `项目说明.md`「验证状态」与该文件里"零侵入"相关表述；
- `ultralytics_ooo/README.md` 的功能表/测试覆盖；
- 本轮只更新第七节测试基线表的最后一行与 `项目说明.md` 的「验证状态」，正文不散落写死测试数。

---

## 二十一、第十三轮：六模块性能修复（F1–F4）落地

### 21.1 本轮从哪里来

用户指令：**「按照建议进行修复」**，依据是 `六模块性能与JPEG解码分析.md` 的 §5 优化项清单。

| 项 | 本轮 |
|---|---|
| F1 噪声算术（去冗余 `astype` + 饱和转换替代 `clip+astype`） | ✅ 实施 |
| F2 比例填充改 `cv2.copyMakeBorder` | ✅ 实施 |
| F3 雨滴 `addWeighted(dst=overlay)` | ✅ 实施 |
| F4 `_degrade_frame_ex` 去交接拷贝（+ 别名守卫） | ✅ 实施 |
| F5 缩放帧缓存 / F6 降采样解码 | ⬜ 未做（理由未变，报告 §5.3 / §5.4） |
| F7 `degrade_max_side` 默认值 / F8 两个默认键 | 📄 仅文档化，**不改默认值** |

> **2026-09-18 后续更新（覆盖本表 F5 一行）**：F5 当时"未做"的首要依据是**内存不足**，
> 该前提**已被推翻** —— 机器升到 16 GB（空闲 8.2 GB）。F5 经重新测量后结论改为 **做**：
> 收益为数据侧 epoch 的 **6.4%–15.3%**（推荐容量 W=128，占 84–338 MB/worker）。
> 同时修正一处作用范围错误：生产 `imgsz=320` 下 720p 源**会**触发 cap（原报告只在 `imgsz=640` 口径下成立）。
> 详见 `Instruction/六模块性能与JPEG解码分析.md` §5.3.1 与 `_perf_review/ooo7/F5-重新评估.md`。

### 21.2 改动清单（4 处，2 个文件）

全部是**逐像素等价**的算术/分配层改动，不动配置、不动段布局、不动 RNG 消费顺序。

| 文件 | 位置 | 改动 |
|---|---|---|
| `core/degrade.py` | `_apply_weather` 的 noise 尾 | `cv2.add(img, scratch, dst=scratch, dtype=cv2.CV_16S)`（`dtype=` 已做转换，省掉 `img.astype(np.int16)` 的整帧临时）；返回改 `_match_ndim(img, cv2.add(scratch, 0, dtype=cv2.CV_8U))`（饱和转换 ≡ `clip+astype`，一个 pass 替两个） |
| `core/degrade.py` | `_apply_weather` 的 rain 尾 | 3 通道帧走 `cv2.addWeighted(..., dst=overlay)` 直接写回 `overlay` 并返回它；非 3 通道仍走 `_match_ndim` 旧路（OpenCV 把 `(H,W,1)` 映射成 1 通道 Mat，返回 2-D） |
| `pool/dataset.py` | `_degrade_frame_ex` | `copy=False` 读取；并在 `if capped is im:` 时补回 `capped = im.copy()` |
| `pool/dataset.py` | `_build_ratio_sample` 的 pad 分支 | `np.full` 画布 + 块拷贝 → 一次 `cv2.copyMakeBorder`（少一个画布的峰值） |

两处必须留在代码里的警告（已写成注释，删了会静默改变像素）：

- 噪声尾**不能**「简化」成 `cv2.convertScaleAbs`：它把负偏移取**绝对值**而不是**钳位**。形状、dtype 全都对，只有像素不同。
- `copyMakeBorder` 的四参是 `(top, bottom, left, right)`，**far 侧是余数**（`new - size - offset`），而 `_ratio_pad_params` 返回的是**居中偏移**；差值偶数时两者相等，差值奇数时整体错位 1 像素且画布小 1 像素。

### 21.3 等价性验收（`_perf_review/ooo6/verify6.py`）

改前的实现**由当前源码反推重建**（`inspect.getsource` + 锚点断言，源码一移动就报错而不是静默跳过），因此"对拍"对的是真·改前逻辑，不是手抄的一份。

| 段 | 内容 | 结果 |
|---|---|---|
| A | `_apply_weather` / `_degrade_frame_ex` / `_build_ratio_sample` 改前 vs 改后逐像素 | 288 例气象（含 `(H,W,3)`/`(H,W,1)`/`(H,W)`）+ 51 例比例填充，**0 处不同** |
| B | 四种真实配置**整池**对拍：720p 无切片 / 720p+切片 / 720p `cap=-1` / 1080p | 槽位 50/82/50/32，**逐样本 0 差异**；epoch0 内核调用 16/8/8 次（`kernels_ran` 断言） |
| C | F4 所有权契约（cap 未触发时守卫必须 live） | 连读同一 `img_index` 3 次，**缓存逐字节不变**；无 id / 共享内存别名泄露下游 |
| D | 同进程交替计时（唯一可信口径） | 噪声 **1.41–1.57×**、雨 **1.34–1.77×**、比例填充 **4.93–5.38×** |

**顺带纠正的一个空转验收**：B 段第一版把 `_closing` 读在循环**外面**，拿到的是最后一个 epoch 的残留值，于是两个 epoch 都被记成「收尾窗口」——差异当然为 0，但那是"什么都没跑"的 0。改成逐 epoch 记录 + 断言内核确实被调用过（`kernels_ran`）之后才有效。**"没有差异"和"没有参与比较"必须能区分。**

**门禁复跑（本轮结束时）**

| 门禁 | 结果 |
|---|---|
| `ruff check ultralytics_ooo`（包内配置） | 干净 |
| `pytest tests/ -k ooo -q` | **207 项 / 201 通过 / 6 失败**（6 条详见 §21.4，均与本轮无关） |
| `python tools/ooo_compat_check.py` | **63/63 PASS，RC=0**（本轮改的是包**内部**调用与算子，未新增上游耦合点） |

### 21.4 那 6 条失败与本次改动无关（`_perf_review/ooo6/regress_check.py`）

全量 `pytest tests/ -k ooo -q` 会看到 **6 条红**：`tests/test_ooo_branches.py` 里 5 条
（`test_no_switches_means_no_expansion_at_all`、`test_a_cache_hit_does_not_feed_the_mosaic_buffer`、
`test_grouping_is_kept_whatever_the_cache_size`、`test_the_raw_cache_default_covers_the_reuse_window`、
`test_slice_branch_never_hands_the_lru_buffer_downstream`）+ `tests/test_ooo_perf_fixes.py::test_origin_segment_reads_through_the_raw_lru`。
根因**不在本轮**：

- `_raw_cache_size` 会被 `_legacy_ims_cap = min(ni, batch*8, 1000) - 1` 钳位；
- 测试夹具是 4 图 / `batch=2`，于是容量被钳到 **3**，而这几条老断言写的是 `== 8 / 16 / 4`。

取证方式：**字节级回退**本轮 4 处改动 → 重跑这两个测试文件 → 与修复版比对失败集 → 还原并校验文件哈希。结果：**两个状态下的失败集完全相同**，文件哈希还原成功。所以这是"既有测试断言"与"既有容量钳位"之间的矛盾，**本轮有意不动**。

> 这条也是 `inject_check.py` 需要 `--only` 子集过滤的原因：其中一条既有用例（`test_ooo_perf_fixes.py::test_origin_segment_reads_through_the_raw_lru`）在修复后的代码上就是红的，它会把整个注入检查堵在第一个用例上；过滤之后被阻塞的用例会显式报 `SKIP`，**永远不会被算作 OK**。

### 21.5 三条配套契约测试被发现是「空转」的（本节最重要）

给 F1–F4 配的契约测试里有三条**在注入缺陷时依然全绿**——也就是说它们当时保护不了任何东西。发现方式是把每条测试都跑一遍注入验证（`inject_check.py`），而不是只看它当前是绿的。

| 缺陷 | 为什么当时测不到 | 修法 |
|---|---|---|
| ① 断言位置离不变式太远 | F4 那条测试原本断言「到达 `_finalize_label` 的数组不是 LRU 条目」。但 blur / weather / occlusion **各自都新分配结果**，缓存的身份在半路就丢了 → 删掉守卫也照样绿 | 改成断言 `_degrade_frame_ex` 的**返回值**本身不是 LRU 缓冲区 |
| ② 一个不变式有两道独立防线时，只删一道不可观测 | F4 的安全性 = 「`capped is im` 守卫」**且**「每个算子只读输入」。所以单独把 `_apply_occlusion` 改成 `out = img` 没有任何后果（守卫给的是私有拷贝） | ①补一条**算子纯度**测试（传入帧必须逐字节不变，与调用方无关）；②注入用例**成对**（守卫 + 就地写一起删），此时缓存真的被污染 |
| ③ 只验公式、没进调用点 | `test_ratio_pad_matches_the_old_canvas_and_block_copy_on_every_shape` 自己重建两侧画布，**从不经过 `_build_ratio_sample`** → 把 `bottom/right` 传成居中偏移它照样绿 | 补**调用点**测试：在 `_finish_branch` 处取走真正的画布，断言形状 `== (new_h, new_w, 3)` 且逐像素等于参考画布，并断言夹具真的覆盖到了"余数为奇数"的情形 |

为了让 ② 可表达，`inject_check.py` 增加了**一个用例多处编辑**的能力（`"edits": [...]`）——单个锚点表达不了"必须同时删两处才可观测"。

**最终结果：6/6 注入用例全部 `test_bites=True` 且字节还原成功。**

### 21.6 本轮新增/改写的测试

| 用例 | 文件 | 锁住的不变量 |
|---|---|---|
| `test_weather_noise_clamps_at_both_ends_instead_of_wrapping_or_absing` | `test_ooo_core.py` | 噪声尾用确定性 ramp 覆盖 −300…+300，**两端都必须钳位**（防 `convertScaleAbs`） |
| `test_weather_kernels_keep_the_shape_contract_at_every_channel_count` | `test_ooo_core.py` | 噪声/雨/雾在 `(H,W,3)`/`(H,W,1)`/`(H,W)` 下形状与 dtype 守恒 |
| `test_degradation_ops_never_write_through_to_their_input`（新） | `test_ooo_core.py` | **算子只读**：七个算子调用前后入参逐字节相同，且重复调用结果一致 |
| `test_ratio_pad_call_site_puts_the_remainder_on_the_far_side`（新） | `test_ooo_branches.py` | **调用点**用余数填 far 侧：画布形状 == `(new_h,new_w,3)` 且逐像素等于参考画布 |
| `test_degradation_branches_never_hand_the_lru_buffer_downstream`（重写） | `test_ooo_branches.py` | `_degrade_frame_ex` **交出的**帧永远是调用方拥有的（不是 LRU 缓冲区） |
| `test_the_raw_cache_survives_repeated_reads_byte_for_byte` | `test_ooo_branches.py` | 缓存内容必须始终等于磁盘上的那份解码结果（与快照比对不同，抗 LRU 轮换） |

### 21.7 行为变更

**无。** F1–F4 全部逐像素等价（§21.3 的 A/B/C 三段证据），产物、日志、配置语义、段布局、池长、RNG 消费顺序均未改变；唯一可见的变化是速度。

### 21.8 文档同步

- `六模块性能与JPEG解码分析.md`：TL;DR 追加「落地状态」、新增 §5.0 状态表（逐项 + 证据 + 工具）、§5.6 优先级矩阵后加落地注；
- `ultralytics_ooo/README.md`：新增「退化侧旋钮：一个换算关系 + 两个『默认别动』的键」一节（F8：`blur_axis_aligned=True` 5.9×、`degrade_resample="linear"` 4.05×、`degrade_max_side` 的换算关系与三个前提；F7 的换算关系写清楚了，但**默认值没动**）；
- 本节（新增第二十一节）。

### 21.9 未处理

| 项 | 为什么不处理 |
|---|---|
| F5 缩放帧缓存 | 内存 + 驱逐策略 + 别名三处风险；本机可用内存 0.5–1.4 GB，连 `workers=2` 都装不下，无法在本机验收 |
| F6 降采样解码 | 与 SAHI 切片正面冲突（切片必须原分辨率）+ 非逐像素等价；只在"切片关闭"的配置上可用 |
| F7 `degrade_max_side` 默认 `2*imgsz` → ≈`1.2*imgsz` | 这是**增强强度**参数而非缺陷：改了会让历史实验与新实验不可比。已按"需要质量 A/B 才能动"处理，只在文档给出换算关系 |
| 那 6 条既有红测试 | 根因是既有容量钳位（`_legacy_ims_cap`）与老断言冲突，与本轮无关，见 §21.4。若要修，应单独作为"测试夹具与容量钳位对齐"一轮来做 |

> **本表 F5 一行已被 2026-09-18 的重新评估覆盖**：当时"内存不足"的前提失效（机器 16 GB / 空闲 8.2 GB），
> 重新测量后结论改为 **做**（收益为数据侧 epoch 的 6.4%–15.3%）。
> 见 `_perf_review/ooo7/F5-重新评估.md` 与 `六模块性能与JPEG解码分析.md` §5.3.1。**其余三行结论未变。**

> 本节按**工作项**组织；同一轮按**问题**重排的对照见第二十二节。

---

## 二十二、第十三轮的问题与解决方式一览（含验收工具自身的问题）

> 第二十一节按**工作项**组织（改了什么 → 怎么验收 → 配了哪些测试 → 文档同步）。本节把**同一轮**按**问题**重排：每条给「现象 / 根因 / 解决方式 / 证据落点」。
>
> **验收工具自己踩到的坑也一并留档**，理由与 §17.7 相同：自检器的缺陷如果不记录，下一个用它的人会直接拿它的输出当结论。事实上本轮最贵的一次返工就是工具造成的（V2「空转验收」）。

### 22.1 一览

| # | 问题 | 性质 | 解决方式 | 落点 |
|---|---|---|---|---|
| P1 | 噪声核每帧多一次全帧 `astype(np.int16)` | 白做的工作 | `cv2.add(..., dtype=cv2.CV_16S)` 直接做类型转换 | §21.2、§22.2 |
| P2 | 噪声尾 `np.clip` + `astype(uint8)` 走两趟 | 白做的工作 | 一趟饱和转换 `cv2.add(x, 0, dtype=cv2.CV_8U)` | §21.2、§22.2 |
| P3 | 比例填充先 `np.full` 整块画布再块拷贝 | 白做的工作（峰值内存 + 两次写） | `cv2.copyMakeBorder` 一次成型 | §21.2、§22.2 |
| P4 | 雨滴叠加多算一个输出数组 | 白做的工作 | 3 通道帧 `dst=overlay` 原地回填 | §21.2、§22.2 |
| P5 | 退化分支拿到的是 LRU 缓冲区的**拷贝** | 交接拷贝（数据侧 L2 占 38.5%） | `copy=False` 读取 + `capped is im` 守卫 | §21.2、§22.2 |
| T1 | 噪声尾若"简化"成 `cv2.convertScaleAbs`，负值取**绝对值**而非钳位 | 看着等价、实则改像素 | 注释写明理由 + 契约测试钉住**两端**钳位 | §22.2 |
| T2 | `copyMakeBorder` 四参顺序与"居中偏移"混用 | 看着等价、实则错位 1 px | 注释写明 far 侧取**余数** + 调用点测试 | §22.2 |
| V1 | 探针重建改前代码时按缩进硬匹配锚点，`dedent` 后匹配不上 | 工具（直接报错） | 锚点改缩进无关正则，并断言唯一命中 | §22.3 |
| V2 | B 段把 `_closing` 读在循环**外面** → 两个 epoch 都被当成收尾窗口 | 工具（**空转验收**） | 逐 epoch 记录 + `kernels_ran` 断言 | §22.3 |
| V3 | B 段只传 `*_ratio` 没传 `*_keep` → 段为空、内核 0 次调用 | 工具（**空转验收**） | 补 `*_keep=True`；由 `kernels_ran` 兜住 | §22.3 |
| V4 | C 段 cap 判据判错对象 → 守卫那条分支根本没被覆盖 | 工具 | 先取 `_degrade_max_side()` 再逐条判 | §22.3 |
| V5 | D 段的取帧条件取不到帧，且含一条死分支 | 工具 | 从 `_frames()` 取真彩帧再 resize | §22.3 |
| A1 | `regress_check.run()` 的返回值与调用处约定不一致 | 工具（直接报错） | 统一返回 `(dict, stdout)` | §22.4 |
| A2 | pytest 控制台摘要被管道吞掉 → 解析出"零结果"被当成全绿 | 工具（**假绿**） | 改 `--junitxml` + XML 解析 | §22.4 |
| I1 | 注入检查被一条既有红用例堵在**第一个**用例上 | 工具 | `--only` 子集过滤；被阻塞的用例报 `SKIP`，**永不算 OK** | §22.4 |
| I2 | 同一条用例、同一条命令，第一次红、随后复跑绿 | 工具（抖动） | 复跑确认；不拿"跑一次"当结论 | §22.4 |
| I3 | 一个不变式有两道防线，单锚点注入不可观测 | 工具（**假阳性**） | 一个用例支持多处编辑（`edits`） | §22.4、§21.5 |
| C1 | F4 契约测试的断言离不变式太远（算子已换过数组） | 测试（**空转**） | 断言移到 `_degrade_frame_ex` 的**返回值** | §21.5 |
| C2 | 两道防线只删一道 → 无任何可观测后果 | 测试（**空转**） | 补**算子纯度**测试 + 组合注入 | §21.5 |
| C3 | 比例填充测试只验公式、从不进调用点 | 测试（**空转**） | 补调用点测试，取走真画布 | §21.5 |
| D1 | 全量测试 200 → 207，且新出现 6 条红 | 文档 + 归因 | §7 表只改最后一行；6 条红做**字节级回退**取证 | §21.4、§22.6 |

### 22.2 产品代码侧：5 处白做的工作 + 2 个「看着能简化、其实改像素」的陷阱

P1–P5 的逐行改动见 §21.2，这里只补**问题视角的根因**（为什么这些工作量是白做的）：

| # | 根因一句话 |
|---|---|
| P1 | `dtype=cv2.CV_16S` 已经要求 OpenCV 自己做输入转换，先 `img.astype(np.int16)` 再喂进去是把同一件事做了两遍，还多出一帧 int16 临时（内存翻倍） |
| P2 | `np.clip` 一趟 + `astype` 一趟 = 两次全帧遍历；`cv2.add(x, 0, dtype=CV_8U)` 在转换里就饱和，一趟到底 |
| P3 | `np.full` 先把整块画布铺满背景色，再把原图按偏移写进去 = 两次写 + 一个画布大小的峰值；`copyMakeBorder` 本来就是干这个的 |
| P4 | `addWeighted` 不指定 `dst=` 会新分配一个输出；3 通道帧可以直接写回 overlay 本身 |
| P5 | 退化分支**只读**传入帧（各自新分配结果），所以让它们直接读 LRU 条目本身是安全的——拷贝纯属防御性的白做 |

两个陷阱（T1 / T2）是**这轮改动的固有风险**：两处"看起来更简洁"的写法都会静默改像素，形状与 dtype 全都对。

| # | 陷阱 | 为什么危险 | 怎么防 |
|---|---|---|---|
| T1 | 噪声尾"简化"成 `cv2.convertScaleAbs` | 名字里有 abs 也有 scale，看起来正是"取整到 0–255"，但它对负值取**绝对值**：−30 会变成 30 而不是 0。只有跨过两端的测试才抓得住 | 代码注释 + `test_weather_noise_clamps_at_both_ends_instead_of_wrapping_or_absing`（确定性 ramp 覆盖 −300…+300） |
| T2 | `copyMakeBorder` 的 far 侧传"居中偏移" | 它要的是**余数** `new - size - offset`；差值偶数时两者相等，奇数时整体错位 1 px 且画布小 1 px——多数用例静默通过 | 代码注释 + `test_ratio_pad_call_site_puts_the_remainder_on_the_far_side`（强制覆盖奇数余数） |

### 22.3 验收探针 `_perf_review/ooo6/verify6.py` 自身的 5 个缺陷

探针的作用是"重建改前代码、逐像素对拍"，它的输出是本轮唯一的等价性证据——所以它自己出错就是**证据链出错**。这一节记录它被修掉的 5 处：

| # | 现象 | 根因 | 解决方式 |
|---|---|---|---|
| V1 | 重建 `_degrade_frame_ex` 时直接抛 `AssertionError: F4 guard anchor moved` | `getsource` 取到的方法带类体缩进，`textwrap.dedent` 只削公共前缀，而锚点写死了 4 个空格 | 锚点改 `[ \t]*` 的缩进无关正则；F2 的块替换同样改成"捕获缩进 → 按捕获到的缩进回填改前实现" |
| V2 | B 段"整池对拍"差异恒为 **0** ——但那是因为两个 epoch 都没跑增强 | `_closing` 被读在循环**外面**，拿到的是最后一个 epoch 的残留值，于是 epoch 0 与收尾 epoch 被记成同一状态 | `_collect()` 内部**逐 epoch** 记录 `_closing`；再加 `kernels_ran` 断言（增强 epoch 里三个内核至少被调用过一次），并把它算进 `ok` |
| V3 | 即便修好 V2，内核调用计数仍是 0 ——segment 是空的 | 构造配置时只传了 `*_ratio` 键，没传 `*_keep` 布尔开关；段长由 ratio 定，但**分支是否开启由 keep 定** | 配置补齐 `ratio_pad_keep / blur_keep / compose_keep / weather_keep / occlusion_keep = True`。修好后槽位 50/82/50/32、epoch 0 内核 16/8/8 次 |
| V4 | C 段报告"cap 会触发"，与数据集事实（1280 == `2*imgsz`，cap 不可能触发）矛盾 | cap 判据写成了对**错对象**判形状（在字典推导里判了另一个量），恒为"触发" | 先取 `cap = ds._degrade_max_side()`，再判 `any(max(v.shape[:2]) > cap for v in ds._raw_cache.values())` |
| V5 | D 段的计时对象取不到帧（另有 `if False` 死分支） | 取帧条件写成了与真实帧形状不相等的元组比较 | 从 `_frames()` 里取第一张真彩帧，再 `cv2.resize` 到分支实际工作分辨率（`2*imgsz` → 1280×960） |

**V2 + V3 是本轮最贵的教训**：探针打印的是"差异 0"，看起来是最强的结论，实际含义是"没有参与比较"。所以最后判 `ok` 的条件里不仅要求差异为 0，还要求**内核确实被调用过**——见 §22.7 第 1 条。

### 22.4 取证与注入检查侧：`regress_check.py` 2 处 + `inject_check.py` 3 处

| # | 现象 | 根因 | 解决方式 |
|---|---|---|---|
| A1 | 脚本一跑就 `AttributeError: 'tuple' object has no attribute 'items'` | `run()` 返回的是 tuple，调用处按 dict 用 | 约定统一为 `(结果字典, 原始输出)` |
| A2 | 修好 A1 后"零结果"被当成全绿：`summary` 为 `null`、失败集为空 | pytest 的控制台摘要在被捕获的管道里不保证出现（本机既有现象） | 改 `--junitxml` 落盘 + 解析 XML 统计 `testcase`；结果为空时把 stdout/stderr 一起写进报告，**不静默通过** |
| I1 | 注入检查一个用例都没跑就中断 | 首条既有用例 `test_ooo_perf_fixes.py::test_origin_segment_reads_through_the_raw_lru` 在**修复后**的代码上就是红的（同 §21.4 的容量钳位），基线失败直接 `assert` 退出 | 加 `--only` 子集过滤；基线失败的用例显式报 `SKIP` 并**不计入通过数**（默认仍跑全量，不削弱任何东西） |
| I2 | 同一条用例、同一条命令，第一次被判为基线失败（触发 SKIP）、复跑通过 | 未完全定位；本机可用内存长期偏紧，同类抖动在本项目已多次出现（跨会话 ±40%、同会话 ±13%） | 复跑确认后再下结论；`--only` 的 SKIP 语义保证一次抖动不会污染整份报告 |
| I3 | `occlusion-draws-through` 单独注入时 `test_bites=False` | 该不变式有两道独立防线（`capped is im` 守卫 **且** 每个算子自行分配输出），只删一道时另一道仍成立，**观测不到任何后果** | `inject_check.py` 支持一个用例多处编辑（`"edits": [...]`）；该不变式配套"守卫 + 就地写"的组合用例 `guard-removed-plus-occlusion-draws-through-poisons-the-cache` |

> I3 一度让我误判为"测试写得不对"，实际是**注入粒度不对**。判据：注入报 `test_bites=False` 时，先问"这个缺陷单独存在时可观测吗"，再问测试。

### 22.5 契约测试侧：三条空转（C1–C3）

三条测试在**注入缺陷时仍然全绿**，即当时保护不了任何东西；发现方式是逐条跑注入验证，而不是看它当前是绿的。逐条的现象 / 根因 / 修法见 **§21.5**，此处不重复。改完后本轮 6 条注入用例 **6/6 咬住**且字节还原成功。

### 22.6 文档侧：一处口径过期

`pytest tests/ -k ooo` 从第十二轮的 **200** 变成本轮的 **207**（新增 5 条、改写 1 条），并首次出现 **6 条红**。

- 测试数按本文档既有纪律处理：**只改第七节基线表的最后一行 + `项目说明.md` 的「验证状态」**，正文不散落写死数字；
- 6 条红**不是修一修就完事**：先做归因（`_legacy_ims_cap` 容量钳位与老断言冲突），再做**字节级回退取证**证明与本轮无关（§21.4），然后**有意不修**——修测试夹具属于另一轮的工作，混进本轮会让"哪些改动是性能修复"这件事失真。

### 22.7 本轮沉淀的 6 条判断准则

1. **「没有差异」必须与「没有参与比较」可区分。** 对拍/等价性验收里，差异为 0 只是必要条件；还要断言**被测路径确实被执行过**（内核调用计数、`calls > 0`、`kernels_ran`）。本轮 V2 + V3 就是反例。
2. **注入报"没咬住"时，先怀疑观察点，再怀疑被测代码。** 三步排查：断言位置离不变式多远 → 这个不变式有几道防线 → 这个测试到底走没走真实调用点。
3. **两道防线的不变式要用组合注入。** 单点注入"没反应"不等于"那道防线没用"，而可能是另一道还在兜着。
4. **自动化检查里的"基线失败"必须是 SKIP，不能是中断，更不能是 OK。** 否则一条与本次无关的既有红用例就能让整份检查失去意义（I1），或者反过来被当成通过（A2）。
5. **工具的输出本身也是待验证对象。** 能落盘的结构化结果（junit XML）优于控制台文本；解析不到结果时要报错，不要当成空集（A2）。
6. **抖动环境下不拿一次运行下结论。** 本机同会话 ±13%、跨会话 ±40% 的方差是已知事实；一个红点先复跑，再谈根因（I2）。

---

## 二十三、第十四轮：F5「已缩放帧缓存」落地

### 23.1 本轮从哪里来

用户指令：**「开始修复吧」**。承接 §21.1 表下的后续更新：F5 原先判"不做"的**首要依据（内存不足）已被推翻**
（机器升到 16 GB / 空闲 8.2 GB），经 `_perf_review/ooo7/F5-重新评估.md` 重新测量后结论改为**做**，
并在 `Instruction/六模块性能与JPEG解码分析.md` §5.3.1 给出了设计。本节记录**实施与验收**，
问题视角的复盘见第二十四节。

一句话概括这一轮要拿的东西：**同一张源图在同一 epoch 里被同一个降采样算了十几遍，而那个降采样是纯函数。**

| 事实 | 数值 | 来源 |
|---|---|---|
| 池读取同一源图的次数 | 每个 epoch 每图约 **9.6 次**（ds1080 实测 384 次 cap 咨询 / 40 图） | §23.7 |
| 其中重复同一 `(图, cap, kernel)` 的比例 | **85%–89%** | §23.7 |
| 一次重复的计算成本 | **0.36 ms（1280 侧）～4.51 ms（4000 侧）** | §23.8 |
| F4 之后还剩下的"减工作量"杠杆 | **只剩这一个** | §5.3.1 ⑥ |

### 23.2 改动清单（5 处，3 个文件）

| 文件 | 位置 | 改动 |
|---|---|---|
| `pool/constants.py` | `_ONLINE_DEFAULTS` | 新增 `degrade_frame_cache_size`（`0`=auto `2×_legacy_ims_cap`；`>0` 帧数；`<0` 关闭）与 `degrade_frame_cache_mb`（**独立**字节预算，默认 512 MiB/worker）。两个都是**项目专用键**，`install()` 的 `check_online_defaults_are_project_only` 保证不与上游撞车 |
| `pool/dataset.py` | `__init__` | 解析两个键 → `_cap_cache_size / _cap_cache_budget / _cap_cache / _cap_cache_bytes / _cap_hits / _cap_misses` |
| `pool/dataset.py` | 新方法 `_cap_frame_cached` | `_cap_long_side` + FIFO 缓存的包装：键 `(img_index, cap, interp)`；只存"cap 真的触发"的帧；命中返回 `frame.copy()`（`copy=False` 供 compose 用）；每次插入前执行帧数与字节预算双重驱逐 |
| `pool/dataset.py` | 新方法 `cap_cache_stats` | `(hits, misses, resident_MiB, capacity)`，**进程本地**、不进共享内存 |
| `pool/dataset.py` | `_degrade_frame_ex` / `_build_compose_sample` | 两个调用点从 `_cap_long_side(...)` 换成 `self._cap_frame_cached(...)`；**读源图那一行不动** |
| `tests/test_ooo_frame_cache.py` | 新增 | 12 个测试函数 / 13 个用例（见 §23.5） |

compose 传 `copy=False` 与退化分支传默认 `copy=True` 的区别不是随意的：compose 把每个源
`memcpy` 进一张**新分配**的画布（`big[...] = im`）后就不再碰它，所以不需要给自己一份；
退化分支则会把帧交给 `_finalize_label`，而后者在 `r == 1` 时可能把 `label["img"]` **原对象透传**给
Mosaic / affine —— 那两处是**原地写**。

### 23.3 三条不变量（删掉任何一条都是静默错误）

| # | 不变量 | 违反后果（为什么静默） | 防线 |
|---|---|---|---|
| **I1** | 交出去的帧**永远不是**缓存条目 | `_finalize_label` 在 `r == 1` 时把 `label["img"]` 直接透传，Mosaic / affine **原地写**它 → 缓存被改坏。**与 F4 的关键区别：F4 的受害者是当次读进来的 LRU 缓冲区，F5 的受害者是一个会喂后续几十个槽的条目**，污染会**粘住整段缓存**，且没有任何报错或 traceback | 命中返回 `frame.copy()`；compose 的 `copy=False` 由"只 memcpy 进新画布"背书 |
| **I2** | 只有 **cap 真的触发**的帧才进缓存 | cap 未触发时 `_cap_long_side` 返回的是**入参本身**（即 raw-LRU 缓冲区）。缓存它等于把 raw 帧再持有一份、把 I1 的风险引进来，而**可省的工作量为零** | `_cap_frame_cached` 里 `if capped is im: return`（不计命中/未命中）；`_degrade_frame_ex` 里 F4 的 `capped is im → capped = im.copy()` 守卫保持不变、仍是权威 |
| **I3** | 缓存必须严格在**源图 READ 之后** | 若先查缓存，命中时就不再读源图 → **"哪些读算解码"变了** → `_touch_buffer_for_decode` 不再以同样的节奏喂 mosaic 缓冲池 → mosaic 窗口移动 → "全关开关 = 逐字节等于上游"的招牌契约破裂 | **两个调用点都保留无条件的 `_load_image_cached_ex`**，缓存只替换"READ 之后的那个降采样步骤"。这是 F5 与 F6 的分水岭：F6 改的是读本身，所以它不满足契约（§5.4） |

I3 值得单独说一句，因为它是这一轮**最容易被下一次改动破坏**的地方：
把 `self._cap_frame_cached(...)` 提到 `self._load_image_cached_ex(...)` 前面，"看起来"只是少读一次图、
更省，但那正是把 F5 从"纯工作量削减"变成"改变解码模式"的那一步。注入用例
`f5-cache-consulted-before-the-source-read` 专门钉住它（§23.6）。

### 23.4 设计与建议的对应（§5.3.1 ⑦）

| 项 | 建议 | 实装 | 一致 |
|---|---|---|---|
| 缓存对象 | `_cap_long_side(raw, cap, kernel)` 的结果 | 同 | ✅ |
| 键 | `(img_index, cap, kernel)` | `(img_index, cap, interp)` —— 实测确认**两个 cap 值共存**（退化 `2×imgsz`、compose `imgsz`），且 `degrade_resample: area` 下 kernel 也会分叉 | ✅ |
| 驱逐 | **FIFO-by-first-fill**，命中不重排 | `OrderedDict.popitem(last=False)`；命中路径**没有** `move_to_end` | ✅ |
| 容量 | `W ≈ 2 × _legacy_ims_cap` | `degrade_frame_cache_size=0` → `2 × _legacy_ims_cap(ni, batch)` | ✅ |
| 字节预算 | 独立键 | `degrade_frame_cache_mb`（默认 512 MiB/worker），每次插入前 enforce，且**永不驱逐正在插入的那一帧**（`>= 1` 语义与 raw LRU 一致） | ✅ |
| 别名守卫 | `if capped is im: capped = im.copy()` | F4 已有，本轮不动 | ✅ |
| 计数可观测 | — | `cap_cache_stats()`；**有意不进共享内存**：它是纯工作量削减器，不需要跨 worker 汇总，而热路径上再加一个锁不值 | ➕ |

### 23.5 契约测试（`tests/test_ooo_frame_cache.py`，12 个函数 / 13 个用例）

| 用例 | 钉住的不变量 |
|---|---|
| `test_a_frame_handed_to_a_caller_is_never_the_cache_entry` | I1 |
| `test_writing_into_a_handed_frame_leaves_the_cache_byte_for_byte_intact` | I1（写穿交出的帧后，**连读 3 次**缓存内容逐字节不变） |
| `test_a_cap_that_did_not_fire_is_never_cached` | I2 |
| `test_the_cache_never_holds_a_raw_lru_frame` | I2（未触发时不得转存 raw-LRU 帧） |
| `test_the_cache_does_not_change_the_decode_pattern_or_the_mosaic_buffer` | I3 |
| `test_eviction_is_fifo_by_first_fill_not_lru` | 驱逐策略 |
| `test_the_key_separates_cap_and_kernel` | 键含 `cap` 与 kernel |
| `test_a_negative_size_disables_the_cache_but_not_the_cap` | A/B 开关（`<0` 只关缓存、**不**关 cap） |
| `test_auto_capacity_tracks_batch_not_dataset_size` | auto 容量随 `batch` 变、与数据集规模无关 |
| `test_the_byte_budget_bounds_residency_and_never_empties_the_cache` | 字节预算 |
| `test_enabling_the_cache_changes_no_pool_sample` | 开/关逐字节等价 |
| `test_transparency_through_the_auto_cap[16-True]` / `[64-False]` | auto 容量透明（参数化 2 例） |

### 23.6 注入检查：9/9 咬住（`_perf_review/inject_check.py`）

新增的 9 条 F5 用例，每条都要求**指定测试变红**，跑完再用字节比对确认源码被完整还原：

| 注入用例 | 必须变红的测试 | 结果 |
|---|---|---|
| `f5-handout-copy-removed-cache-entry-reaches-the-pipeline` | `..._is_never_the_cache_entry` | OK |
| `f5-poisoning-a-handed-frame-reaches-the-cache` | `..._byte_for_byte_intact` | OK |
| `f5-non-firing-passthrough-gets-cached` | `..._did_not_fire_is_never_cached` + `..._never_holds_a_raw_lru_frame` | OK |
| `f5-hit-refreshes-the-entry-lru-not-fifo` | `..._fifo_by_first_fill_not_lru` | OK |
| `f5-key-loses-the-cap` | `..._key_separates_cap_and_kernel` | OK |
| `f5-byte-budget-ignored` | `..._byte_budget_bounds_residency...` | OK |
| `f5-auto-capacity-ignores-the-mosaic-window` | `..._auto_capacity_tracks_batch...` | OK |
| `f5-negative-size-no-longer-disables-the-cache` | `..._negative_size_disables_the_cache...` | OK |
| `f5-cache-consulted-before-the-source-read` | `..._does_not_change_the_decode_pattern...` | OK |

### 23.7 真实数据逐像素等价（唯一能证明"只减工作量"的验收）

方法（`_perf_review/ooo7/f5_equiv.py`）：同一数据集、同一进程，先跑**整池摘要**（缓存 auto），
`reset_runtime_state` 后把 `_cap_cache_size` 置 0 再跑一遍，两遍都**先重置 RNG 再逐槽摘要**，
使缓存成为唯一差异。摘要覆盖 `img` / `ori_shape` / `resized_shape` / `bboxes` / `cls` 的 SHA-256。

⚠️ **本轮的探针自身是第一版就是错的，见 §24.2（E1）**：第一版遍历 `ds.get_image_and_label(i)`，
而真实 loader 走的是 `ds[i]`（`__getitem__`），后者还要为 Mosaic 拉 3 个伙伴图，**同一 epoch 的
cap 咨询次数差 2.8 倍**。下表已是修正后（`ds[i]`）的口径。

| 配置 | 源图 | 槽位 | 缓存容量 | cap 咨询 | 命中/未命中 | 命中率 | 驻留 | **开=关** |
|---|---|---|---|---|---|---|---|---|
| `ds4k` @640 | 12 | 38 | 22 | 163 | 145 / 18 | 0.890 | 42.2 MB | ✅ |
| `ds1080` @640 | 40 | 125 | 78 | 384 | 327 / 57 | 0.852 | 110.7 MB | ✅ |
| `ds1080` @320（生产口径） | 40 | 125 | 78 | 384 | 327 / 57 | 0.852 | 27.7 MB | ✅ |
| `ds720` @320（生产口径） | 60 | 188 | 118 | 623 | 537 / 86 | 0.862 | 40.9 MB | ✅ |
| `ds1080big` @640 | 400 | 1250 | 126 | 4784 | 3737 / 1047 | 0.781 | 332.2 MB | ✅ |

**`ds1080big` 是唯一"容量真的不够、驱逐真的在发生"的一组**（不同键 ≥574 ≫ 容量 126，
1047 次未命中里有 ~470 次是"被驱逐后重新装入"），因此它才是驱逐路径的活体证据；
小数据集上容量大于工作集、一次驱逐都不发生。它的实测命中率 **0.781** 与 §5.3.1 ④
那条 FIFO 曲线在 W=128 处的预测 **0.772** 吻合，驻留 332.2 MB vs 预测 337.5 MB。

三个可交叉验证的点：① 命中率与 §5.3.1 的模型曲线（0.83–0.89）吻合；
② 未命中数即"不同键数"（ds1080 = 57、ds720 = 86），与 `F5-重新评估.md` 的 `cap_distinct_keys` 完全相等；
③ 缓存容量在 `ni ≤ batch×8` 的数据集上被 `_legacy_ims_cap` 钳位（40 图 → 78、60 图 → 118），
`ni` 很大时落在 `2×(batch×8−1)`（batch=8 → 126）。

### 23.8 净收益（同进程交替计时，唯一可信的口径）

命中的收益**不是** `_cap_long_side` 的全部成本：命中仍必须交出一个**调用方拥有的**数组（I1），
所以净收益 = `resample − copy`。两者**交替 40 轮取最小**（顺序每轮轮换）。

| 配置 | 源帧尺寸 | resample | 命中（含拷贝） | **净省/次** | 比值 | 每图净省 |
|---|---|---|---|---|---|---|
| `ds4k` @640 | 4000×3000 | 5.370 ms | 0.862 ms | **4.509 ms** | 6.2× | 54.5 ms |
| `ds1080` @640 | 1920×1080 | 3.543 ms | 0.656 ms | **2.887 ms** | 5.4× | 23.6 ms |
| `ds1080` @320 | 1920×1080 | 0.833 ms | 0.033 ms | **0.799 ms** | 24.9× | 6.5 ms |
| `ds720` @320 | 1280×720 | 0.394 ms | 0.034 ms | **0.360 ms** | 11.6× | 3.2 ms |
| `ds1080big` @640 | 1920×1080 | 3.626 ms | 0.692 ms | **2.934 ms** | 5.2× | 27.4 ms |

（`ds1080big` 的"每图净省"是 3737 × 2.934 ms ÷ 400 图；它是**全表最接近生产条件的一组**：
400 张 1080p、容量真的不够、驱逐真的在发生。）

**与 §5.3.1 模型口径的对账**：模型用"单次 cap 的**毛**耗时（且来自非交替计时）"当每次命中的收益，
所以系统性偏高——`ds1080` @640 模型 24.7 ms/图 vs 实测 **23.6**（−4%）、`ds1080big` @640 模型 34.6 vs
实测 **27.4**（−21%）、`ds720` @320 模型 5.1 vs 实测 **3.2**（−37%，因为小帧上"交接拷贝"占比大：
0.394 里有 0.034 是拷贝）。
**低的那一侧才是真的**：非交替的算子表在本机最高可高估 10 倍（§1.2），且毛耗时里含了一份命中必须付的拷贝。

### 23.9 端到端复核与门禁

先给结论，再说口径。

**结论**：F5 在 loader 侧**确定性变便宜**（同进程配对 `ds1080`@640 −10.4%，两次独立配对 12/15 与 15/15 同向），
p3 上端到端吞吐 **+5.6%**，p4 上 `mAP50-95` **四次逐位相同**、训练段每 epoch **−1.2% / −7.2%**。
**p4 的 `wall_s` 在本机不可用** —— 它的 setup 相自身漂移 3.9 s，比被测效应（0.44 s）大 9 倍。

#### 23.9.1 P3 重叠率（`p3_overlap.py --cap-cache off|auto`，`ds720`@320/b4/w0，6+6 轮）

| 相 | f5=off 中位 | f5=auto 中位 | 差 |
|---|---|---|---|
| C（仅 loader） | 81.13 ms | 70.26 ms | **−10.9 ms / −13.4%** |
| A（仅 step） | 114.77 ms | 110.13 ms | −4.0 ms（对照，应 ≈0） |
| B（loader+step） | 270.19 ms | 255.70 ms | **−14.5 ms / −5.4%** |
| util = A/B | 0.425 | 0.431 | +1.4%（相对） |
| e2e 吞吐 | 14.81 samples/s | 15.64 samples/s | **+5.6%** |

命中率 0.910–0.935，`cap=62`（batch=4 → `2 × _legacy_ims_cap = 2 × 31`，与 §23.4 的容量规则一致）。

三个必须一起读的读法：

- **`B > A + C`（270 vs 196）**：loader 与 step 不只是"没重叠"，比**串行还差** —— 这正是 P3 早就指出的
  CPU 超订特征（w0 下主进程 torch 4 线程与 loader 抢同 4 个物理核）。所以 C 省的 10.9 ms 在 B 上
  **放大成 14.5 ms**（>1:1），不是噪声。
- **util 几乎不动**：B 与 A 同比例下降。拿 util 判断"F5 有没有用"会得到"没用"的错误结论 ——
  util 是**相对**指标，只有墙钟/吞吐是绝对的。
- **跨进程的 C 相区间重叠**（off 58.65–85.16 / auto 62.03–85.02）→ 上表的中位数只用来定**量级**，
  比值结论一律来自下面的同进程配对。

#### 23.9.2 同进程配对（`f5_loader_ab.py`，本项目纪律第 1 条要求的口径）

同一个 dataset 对象内运行时切换缓存；块内**配对**（`OFF_i − ON_i`），且**配对内顺序也轮换**
（否则进程自身的漂移会把效应淹掉 —— 未轮换时 `ds720`@320 的配对中位数是 6.9 ms 但 p=0.115）。

| 配置 | 未配对 OFF→ON | 配对中位（`OFF_i − ON_i`） | 胜/总 | 符号检验 p |
|---|---|---|---|---|
| `ds720` @320 | 83.41 → 73.57 ms（−11.8%） | **6.9 ms** | 14/20 | 0.115 |
| `ds1080` @640 | 259.70 → 232.58 ms（−10.4%） | **15.7 ms** | 12/15 | **0.035** |
| `ds1080` @640（轮换前的独立一轮） | — | 15.4 ms | 15/15 | <0.001 |

`ds1080`@640 在**两种配对设计下方向与量级一致**（12/15、15/15，合并 27/30）；`ds720`@320 单独不显著
（帧小，单次净省仅 0.36 ms，信噪比低），但方向与 `ds1080` 一致，且与 §23.8 的逐次净省表对得上。

#### 23.9.3 P4 端到端（`p4_e2e.py --cap-cache off|auto --timeline`，CPU/320/b4/2 epoch，各 2 轮）

| 指标 | f5=off | f5=auto | 差 |
|---|---|---|---|
| epoch 0 训练段 | 19.12 / 18.53（中位 18.83） | 18.46 / 18.76（中位 18.61） | −0.22 s / −1.2% |
| epoch 1 训练段 | 2.97 / 3.11（中位 3.04） | 2.73 / 2.91（中位 2.82） | −0.22 s / **−7.2%** |
| 两 epoch 训练段合计 | 21.87 | 21.43 | **−0.44 s / −2.0%** |
| `wall_s` | 34.50 / 38.41 | 34.23 / 35.47 | **不可用，见下** |
| `mAP50-95` | 0.331 / 0.331 | 0.331 / 0.331 | **逐位相同** |

- **`mAP50-95` 四次全部 0.331**（`mAP50` 全部 0.641；训练后 best.pt 复验同为 0.331）→ §23.7 的逐像素等价
  在端到端兑现成"指标一动不动"。
- **epoch 1 是 2/2 干净分离**（两个 auto 都低于两个 off）；epoch 0 的 −0.22 s 落在噪声内。
  epoch 1 恰好是 `close_aug_epoch` 生效（`close_mosaic=1`）的那个 epoch，样本少，所以**比值**大而
  **绝对量同样 0.22 s**。
- **`wall_s` 不能用**：四次运行的 setup 相（`on_pretrain_routine_start`）是 10.41 / 7.14 / 6.54 / 7.09 s
  —— **3.9 s 的漂移**，比全部训练段收益（0.44 s）大 9 倍。上一轮**不带 timeline** 的 p4 曾给出
  off 36.05/36.40 vs auto 30.60/30.34（看着省 5.8 s）—— **那 5.8 s 全部来自 setup 相，是假信号**。
  这是纪律第 1 条的又一次现场证据：端到端墙钟必须切到**可控子相**，否则不要引用。
- 小库本身会放大相对误差：`_mini_val_set` 只有 8 张训练图，两次 epoch 训练段合计仅 ~22 s，
  0.44 s 的效应夹在进程内漂移里。**F5 的绝对收益请读 §23.8 的"每图净省"**；本节只负责回答"不回归"。

#### 23.9.4 门禁

- 全量 `-k ooo`：**220 passed / 6 failed**，6 条与第十三轮**完全相同**（无回归）；
  其中新增 F5 契约用例 13 条、注入用例 9 条（9/9 咬住）。
- `doc_check.py`：三份文档 `format check: PASS`。

### 23.10 行为变更

**无。** 默认即启用缓存，但**输出逐像素不变**（§23.7 全部配置 ✅），因此：

- 训练语义、样本池布局、mosaic 窗口、随机序列**全都不变**；
- 新键 `degrade_frame_cache_size` / `degrade_frame_cache_mb` 各有默认值，**老配置文件不需要改**；
- 回退路径有三条：`degrade_frame_cache_size=-1`（只关缓存）、`=N`（手工定容）、`degrade_frame_cache_mb=0`（取消字节上限）。

### 23.11 文档同步

- `Instruction/六模块性能与JPEG解码分析.md`：§0 TL;DR 与 §5.0 / §5.6 把 F5 改为"已实施"，
  并修正 §0 里"18.4× / 整图 ~40%"这两个**与 F4 收益重叠**的旧数字；新增 §5.3.2 记录落地实现与实测净收益。
- `_perf_review/ooo7/F5-重新评估.md`：新增 §9 指向落地记录。
- `项目说明.md`「验证状态」与本文 §7 基线表**只改最后一行**。

---

## 二十四、第十四轮的问题与解决方式一览（含验收工具与记忆自身的问题）

> 第二十三节按**工作项**组织（改了什么 → 验收 → 测试 → 文档）；本节把**同一轮**按**问题**重排，
> 每条给「现象 / 根因 / 解决方式 / 落点」。工具与记忆自身踩到的坑一并留档，理由同 §17.7 与 §22。

### 24.1 一览

| # | 问题 | 性质 | 解决方式 | 落点 |
|---|---|---|---|---|
| P1 | 同一源图在同一 epoch 里被同一个降采样重算约 9 次 | 白做的工作 | FIFO 缓存 `_cap_long_side` 的产物（键含 `cap` 与 kernel） | §23.2 |
| T1 | 命中若直接交出缓存条目 | 看着更省、实则**静默污染**（Mosaic/affine 原地写，且污染粘住整段缓存） | 命中 `frame.copy()`；compose 的 `copy=False` 由"只 memcpy 进新画布"背书 | §23.3 I1 |
| T2 | 把缓存查到**源图 READ 之前**"更省" | 看着更省、实则改变**解码模式** → 移动 mosaic 窗口 → 破坏上游等价契约 | 两个调用点都保留无条件读；专门加注入用例钉住 | §23.3 I3 |
| T3 | 缓存"cap 未触发"的帧（那其实是入参本身） | 看着无害、实则白持有 raw 帧且零收益 | `capped is im` 早退，不计命中/未命中 | §23.3 I2 |
| T4 | 驱逐用 LRU（命中重排） | 看着"更聪明"、实则改变驻留集 | `popitem(last=False)`，命中路径没有 `move_to_end` | §23.4 |
| V1 | 等价性探针走 `get_image_and_label` 而非 `ds[i]` | 工具（**口径过窄 → 收益被低估 4×**） | 改遍历 `ds[i]`：同一 epoch 的 cap 咨询 120 → 384 | §24.2 E1 |
| V2 | 一次 0.525 的命中率被误读为"容量不足、FIFO 抖动" | 工具（**读数缺少分母**，差一步就去"修"一个没坏的东西） | 见到命中率先问"分母是多少"：0.525 与 0.852 是同一个系统的同一件事 | §24.2 E1 |
| V3 | 长任务在 2 分钟处**静默死亡**，日志里只有数据集横幅 | 工具（假死 + 取证缺失） | 探针加逐阶段进度输出；长任务显式给超时预算 | §24.2 E2 |
| V4 | 探针 `_ROOT = parents[2]`（实为 `parents[1]`） | 工具（直接报错） | 修正 | §24.2 E3 |
| V5 | `inject_check.py` 全部用例被判 `SKIP` | 工具（**假失败**） | pytest 清理系统临时目录触发 safe-delete 钩子 → 改用全新的 `tempfile` 目录作 `--basetemp` | §24.2 E4 |
| D1 | `MEMORY.md` 超出注入上限被截断 | 记忆（**关键事实被静默丢掉**） | 重写为精简版：只留"跨会话仍成立且会改变结论"的事实，过程性内容指向专文 | §24.3 |

### 24.2 验收工具自身：4 处（E1–E4）

**E1（最贵的一处）：等价性探针的口径过窄，且差点导致一次"修好没坏的东西"。**

第一版 `pool_digest` 遍历 `ds.get_image_and_label(i)`。它是**中间产物**，不是 loader 交给训练器的东西：
真实路径是 `ds[i]`（`__getitem__`），而 `__getitem__` 还要为 Mosaic 再拉 3 个伙伴图，每一次都同样经过
`_degrade_frame_ex` / `_build_compose_sample`。后果有两个，一个轻一个重：

- 轻的：**收益被低估 4 倍**。ds1080 @640 上第一版只数到 120 次 cap 咨询，投影 176 ms/epoch；
  真实口径是 384 次，投影 **944 ms/epoch**。
- 重的：**命中率 0.525 看起来像"容量不够、FIFO 在抖"**。当时的设计规则说 auto 容量能拿到无界命中率的 88%，
  实测却差得很远 —— 差一步就会去"修"那个 auto 公式。真正的原因是分母：
  `(120−57)/120 = 0.525`，`(384−57)/384 = 0.852`，**同一个系统、同一批 57 个键**，
  只是第一版的分母小了 2.8 倍。
- 结论：**命中率单独没有意义，必须与"咨询次数"和"不同键数"一起报。** 探针现在同时输出三者。

同一批数字里还藏着一个反向的验证：两个探针（模型的 `f5_reeval.py` 与落地的 `f5_equiv.py`）在
**不同遍历口径**下算出的"不同键数"完全相等（ds1080 = 57、ds720 = 86）。这说明键的定义没跑偏。

**E2：长任务在默认超时处静默死亡。** 后台跑 ds1080huge 的整池两遍摘要时，任务在 **2 分 01 秒**处
被判失败，而落盘日志停在第 7 行（数据集横幅），既无 traceback 也无 `rc=`。
原因不是 OOM 而是**执行器的默认超时**。两处改善：① 探针在每个阶段前后打一行进度（区分"卡住"与"被杀"）；
② 长任务显式给足超时预算。这条的价值不在于修好了什么，而在于**没有把"被杀"当成"不适用"**。

**E3：`_ROOT = _HERE.parents[2]`。** 探针在 `_perf_review/ooo7/` 下，仓库根是 `parents[1]`。
取错一级 → `import ultralytics` 失败（`ModuleNotFoundError`）。属于"直接报错"这一类，代价很小。

**E4：`inject_check.py` 的"假失败"。** 修好 E3 后它开始正常工作，随后 9 条 F5 用例**全部**报 SKIP：
pytest 每次运行结束会清理系统临时目录，本机的 safe-delete 钩子在那一步失败 → 非零退出码 →
基线被判失败 → 所有用例被 SKIP。**这恰好是 §22.7 第 4 条要求的语义起了作用**（"基线失败必须是 SKIP，
不能是中断、更不能是 OK"）：它没有把问题伪装成通过，但如果直接把 SKIP 当结论就会得到
"9 条用例都没咬住"的错误结论。修法是给它一个全新的 `tempfile.TemporaryDirectory` 作 `--basetemp`。

### 24.3 记忆侧：`MEMORY.md` 被截断（D1）

本轮开始时 `MEMORY.md` 已达 24.8 KB / 249 行，**超过注入上限并被截断** —— 也就是说，越靠后的内容
根本不会进入上下文。这和 §24.2 的 E2 是同一类问题：**证据/上下文静默缺失，而使用者以为它在**。

处理方式：按"这条事实是否跨会话仍成立、且会改变结论"重写。留下的只有会改变判断的东西
（环境事实、AMP 在本卡是负优化、数据侧是墙钟瓶颈、缓存别名陷阱的两道防线、测量纪律、
测试基线、上游耦合），删掉一次性过程与已指向专文的细节，全文压到 **15.9 KB / 98 行**。

### 24.4 本轮沉淀的判断准则（续 §22.7）

7. **比率型指标必须与它的分母一起报。** 命中率、利用率、加速比都一样：0.525 和 0.852 可以是同一个系统。
   本轮 E1 的两个后果（低估收益、误判容量）都来自只看了比率。
8. **等价性验收要站在"最终交付物"那一侧。** 对拍中间产物（`get_image_and_label`）比对拍
   训练器真正收到的东西（`ds[i]`）弱：后者才包含 Mosaic / affine 的**原地写**，而那正是本次改动
   唯一的破坏路径（不放大的话，"开=关"的结论会漂亮但没有说服力）。
9. **"更省"的直觉里，有几条是禁区。** 本轮两条（T1 交出缓存条目、T2 把缓存查到读取之前）都写着"更省"，
   两条都会静默破坏契约；判据是问一句"这一步之后，**别人会原地写它吗**"/"这一步之前，**哪些读算解码**"。

## 二十五、第十五轮：影子 FIFO（`slice_decode_shadow_size`）——把「喂入」与「解码」解耦

### 25.1 本轮从哪里来

§10 的容量扫描给了两个结论，方向相反：抬 `slice_raw_cache_size` 最多省 **−66% 解码 / ≈ −26% 墙钟**
（是 F5 带界真值 12.8% 的 **≈2.0 倍**）；**但它同时是"改训练数据"**——解码次数少了，
`_touch_buffer_for_decode` 喂给 mosaic 池的事件就少了，`Mosaic.get_indexes()` 抽到的伙伴图随之改变
（§10.2 实测：同配置两次运行噪声底 ±1.2%，而四档之间 `buffer_len` 差 24–64%）。

于是"抬容量"在契约上不可接受：**同 seed 的既有基线会失效**，只能按"新基线"重新验 mAP，
而没法用"等价"来验收。本轮做的是另一条路：**解码次数下降，而 mosaic 窗口一动不动。**

### 25.2 机制：一个结构同时承担了两件事

症结是**同一个 `_raw_cache` 既决定"要不要读盘"（速度），又通过 `decoded` 决定"要不要喂池"（数据）**。
拆开即可：

- 新增一条**只存 `int` 键 + 字节数**的 FIFO（影子），它按**出厂配置**（出厂 `slice_raw_cache_size=16`，
  经同样的 `max(4, ...)` 与 `_legacy_ims_cap` 钳位）复现基线的命中/未命中判定，并把**这个判定**交给喂入；
  真实像素缓存仍按自己的容量决定要不要解码。
- 两个结构看到**同样的键、同样的顺序**（影子由 `_load_image_cached_ex` 的**两个返回点**驱动，
  与真实缓存同一次调用），所以影子的判定序列 = 出厂基线的解码序列 ⇒ `_touch_buffer_for_decode`
  收到的 `decoded` 序列逐元素不变 ⇒ **样本逐字节不变**。
- 影子**刻意不用 LRU**（命中不重排）：重排会改变驻留集 → 改变解码模式 → 移动窗口，
  正是原始缓存自己注释里已经记录过的坑。
- 真实 hit/miss 仍单独记在 `_raw_hits` / `_raw_misses` 上 ⇒ 探针总能区分
  "这次真的读盘了吗"与"基线本来会读盘吗"——**这两个问题从此不再是同一个问题**。

### 25.3 三态参数 `slice_decode_shadow_size`

| 值 | 语义 | 用途 |
|---|---|---|
| `0`（出厂默认） | **auto**：仅当真实 `slice_raw_cache_size` 超过出厂基线时才启用 | 生产（出厂容量下影子不启用，热路径不进入，行为逐位不变） |
| `>0` | 钉死影子帧数；填 `_legacy_ims_cap` 可改为复现**上游自身**的驻留集 | 想对齐上游时用（保真更高，但**会移动数据**，需重开基线） |
| `<0` | 关：喂入重新跟随真实缓存 = 改动前的行为 | A/B 控制臂 |

另：`slice_raw_cache_size <= 0` 时**强制关闭**影子——那一档的语义是"每读即解码"，
正是"开关全关 = 逐字节等于上游"这条招牌契约依赖的 legacy 路径。

### 25.4 验收（1）：三臂同进程探针

`_perf_review/ooo7/raw_shadow_equiv.py`。三臂：**A 基线**（raw=16, shadow=0）/ **B 影子**（raw=63, shadow=16）/
**C 还原**（raw=63, shadow=0）。判定条件有两条，缺一不可：A 与 B 的喂入序列与池摘要必须**完全相同**；
A 与 C 必须**不同**——否则"相同"只是没测到东西（空转）。

| 数据集 | A 解码 | B 解码 | C 解码 | B 相对 A | A vs B 喂入 | A vs C 喂入 |
|---|---|---|---|---|---|---|
| ds720 @320 | 436 | 60 | 62 | **−86.2%** | 逐元素相同 | 第 17 槽发散 |
| ds1080 @640（94 图） | 217 | 38 | 38 | **−82.5%** | 逐元素相同 | 发散 |
| ds1080big @640（400 图） | 3 711 | 1 899 | 2 408 | **−48.8%** | 逐元素相同 | 第 19 槽发散 |
| ds1080huge @640（2000 图） | 18 352 | 9 911 | 12 220 | **−46.0%** | 逐元素相同 | 第 19 槽发散 |

B 的解码数**可以低于 C**：C 的喂入已经变了 → mosaic 抽到别的伙伴 → 连访问序列都不同。
这正是"改容量 = 改数据"的反面证明。

### 25.5 验收（2）：契约测试 12 条（`tests/test_ooo_raw_shadow.py`）

三态语义四条（auto 的精确边界 / 负数关 / `>0` 钉死 / `raw<=0` 强制关）、**头条**
（容量放大后喂入逐元素相同 + digest 相同 + 解码下降）、**非空转**（同容量无影子时喂入必发散）、
影子不读像素、`decoded` 与真实 hit/miss 解耦、FIFO 而非 LRU、字节预算有界但不清空、影子大小等于缓存时解码模式不变。

⚠️ 夹具必须让容量**真的**超过出厂 16：改用 `PROD_LIKE`（mosaic=1.0）+ `n=32 / batch=8`
（`_legacy_ims_cap=31 > 出厂 16`，mosaic 缓冲 31 深 ⇒ 产生复用）。首版夹具太小
（`n=20 / batch=3`，且 `ALL_ON` 没有 mosaic 伙伴）时 16 与 19 命中完全相同、两臂都是 140 次解码，
**两条核心断言是空转的**——已修（见 §26.3）。

### 25.6 验收（3）：注入检查 7/7 咬住（`_perf_review/inject_check.py`）

新增 7 条影子用例（现共 30 条），全部 `bites=True` 且逐字节还原。覆盖：喂入改由真实解码决定 /
命中时按 LRU 重排 / 忽略字节预算 / 出厂默认下也强行启用 / raw 关闭时不再强制关影子 /
负数不再关 / 影子直接短路源图读取。

### 25.7 验收（4）：门禁

`pytest tests/ -k ooo`：**226 通过 / 6 失败**，6 条与上一轮**完全相同**（§21.4 的容量钳位既有红），
**本轮未新增任何红**。

### 25.8 验收（5）：P4 端到端 —— mAP 逐位不变（本轮最终判据）

`p4_e2e.py`（CPU / 320 / b4 / 2 epoch / `amp=false`，与 F5 那轮同款口径），数据集换成
`_perf_review/ds720`（**60 张训练图**）—— 出厂迷你集只有 8 张图，`_legacy_ims_cap` 会被钳到 7，
**根本触发不了影子**（这是 §26.3 那个夹具问题的同型，先用 `ni=60 / batch=4 → cap=31 > 出厂 16` 解决）。

| 臂 | 配置 | epoch1 `mAP50-95` | epoch2 `mAP50-95` | epoch1 `mAP50` | epoch2 `mAP50` | `whole mAP50-95` |
|---|---|---|---|---|---|---|
| A 出厂 | raw=16 / shadow=0 | 0.00014 | **0.20638** | 0.00136 | **0.27832** | **0.14627** |
| B 影子 | raw=31 / shadow=0（auto → 实启） | 0.00014 | **0.20638** | 0.00136 | **0.27832** | **0.14627** |
| C 控制 | raw=31 / shadow=-1（关） | 0.00001 | **0.28833** | 0.00015 | **0.40504** | **0.18814** |

- **A 与 B 在每一个 epoch、每一列（含双口径的 `whole_*` 列）全部逐位相同** ——
  "喂入逐元素相同 ⇒ 样本逐字节相同 ⇒ 指标一动不动"在端到端兑现；
- **C 每一列都不同**（`mAP50-95` 0.206 → 0.288，相对 +40%）⇒ 同一容量**关掉影子就真的改了训练数据**，
  这条检查不是空转。启动日志两侧也互相印证：只有 B 出现
  `raw-image decode shadow ON (16 frames, 256 MiB budget)`。
- ⚠️ **三臂的 `epoch_time_s` 一律不引用**：它们与前一条探针**并发**跑在本机，属于跨进程且带争抢的读数
  （纪律第 1 条）。这一节的判据是 mAP，不是墙钟。
- 复现：

  ```powershell
  $PY = "D:\miniconda3\envs\langchain\python.exe"
  $env:PYTHONPATH = "C:/Users/Administrator/Desktop/ultralytics-main"
  & $PY _perf_review/ooo7/p4_e2e.py --device cpu --epochs 2 --imgsz 320 --batch 4 --workers 0 `
      --amp false --data _perf_review/ds720/data.yaml --raw-cache 16 --shadow 0
  & $PY _perf_review/ooo7/p4_e2e.py ... --raw-cache 31 --shadow 0      # auto → 实启
  & $PY _perf_review/ooo7/p4_e2e.py ... --raw-cache 31 --shadow -1     # 控制臂
  ```

  原始输出：`ooo7/p4_shadow_e2e.log`；逐 epoch 指标汇总：`ooo7/p4_shadow_arms.json`
  （由 `ooo7/p4_arms_dump.py` 从三份 `ooo7/runs/e2e_cpu_320_4_false_f5auto_rc*_sh*/results.csv` 抽取）。

### 25.9 行为变更

- **出厂配置下无行为变更**（auto 在默认容量下不启用）。
- 一旦显式把 `slice_raw_cache_size` 抬到出厂默认之上，**解码次数下降而池喂入序列不变**。
  与"上一版（无影子）抬容量"相比这是**不同的**行为：这一版**不改训练数据**。

### 25.10 未处理

- **`_perf_review/` 探针索引**：53 个 `.py` 探针、`ooo6/` 与 `ooo7/` 职责有重叠（`f5_reeval` / `p3_overlap` /
  `p4_e2e` / `f5_loader_ab` / `f5_equiv` / `decode_ab`），值得按职责合并一轮并写一份索引。

---

## 二十六、第十五轮的问题与解决方式一览

### 26.1 一览

| # | 类别 | 问题 | 解决方式 |
|---|---|---|---|
| T1 | 验收工具 | 三臂探针首跑 `decodes` 全为 0 | 改 tap 模块级 `imread`（§26.2） |
| T2 | 契约测试 | 夹具太小 ⇒ 容量差异不存在 ⇒ 两条核心断言空转 | 换 `PROD_LIKE` 夹具 + `n=32/batch=8`（§26.3） |
| D1 | 记忆 | `MEMORY.md` 再达 14.3 KB、注入时被截断 | 按"是否改变结论"重写，并把 §2/§4 下沉到 `MEMORY-TOPICS.md` §10/§11（§26.4） |
| D2 | 文档 | `F5-重新评估.md` 过长（§9/§10 属"落地与验收"） | 拆出 `F5-落地与验收.md` |
| C1 | 散件 | 3 个 pytest basetemp 树（3 816 个文件）+ 10 个一次性脚本 | 一次性清理，0 失败 |
| C2 | 散件（收尾） | 首轮清理后仍剩 22 个草稿（`_*.py`/`_*.txt`/`_*.log`）、2 份重复门禁日志、3 份前标准命名的 shadow 日志；`pb_summary.txt` 因 `_pb.py` 被删而成孤儿 | 一并删除，0 失败；被当日记忆引用的 `f5_ctrl_pair.log`/`f5_rawsweep_capacity.log` 保留；两份 headline stdout 日志在 §11.5/§25.8 补上引用后转为被引证据；`_p4_rows.py`→`p4_arms_dump.py`（`p4_shadow_arms.json` 被引用） |
| T3 | 验收（P4） | 三臂与另一条探针并发，墙钟被争抢污染 | 只引用 mAP 判据，墙钟一律不引用（§25.8） |
| T4 | 夹具（P4） | 出厂迷你集 8 张图 → `cap=7`，**触发不了影子** | 换 `ds720`（60 图 / batch 4 → cap 31）（§25.8） |

### 26.2 验收工具自身：三臂 `decodes=0`（T1）

探针原先读 `ds._raw_misses`，而它**在 epoch 末已被 `_publish_raw_cache_stats()` 折进共享内存并清零**，
于是三臂都读到 0。危险之处在于：**"解码真的减少了"与"根本没测到"在数值上无法区分**——
这是 §4 第 9 条那个老毛病换了个位置。

解决：tap 模块级 `imread`（`_dsmod.imread = counting_imread`）数**真实解码次数**，重跑得到
A=436 / B=60 / C=62。教训已写进 `MEMORY.md` §4 第 15 条：
**本项目里"能读到的计数器"默认是经过共享/清零处理的，先确认它还是活的。**

### 26.3 契约测试空转（T2）

12 条测试首跑 **2 失败**：`test_the_shadow_keeps_the_mosaic_feed_when_the_cache_grows` 与
`test_without_the_shadow_the_same_growth_really_does_move_the_feed`。表面看是断言写错了，
实际是**夹具让判别性条件根本不成立**：`n=20 / batch=3` 时 `_legacy_ims_cap = 19`，
16 与 19 两个容量在 `ALL_ON`（没有 mosaic 伙伴）的访问模式下命中完全相同，两臂都是 140 次解码。

解决：夹具换成 `PROD_LIKE`（`mosaic=1.0`、各分支 `ratio=0.5`）+ `n=32 / batch=8`（`cap=31`），
使"容量真超过出厂 16"且 mosaic 缓冲（31 深）比出厂缓存（16 深）更深 ⇒ 产生复用。重跑 **12 passed**。

### 26.4 记忆侧：`MEMORY.md` 再次被截断（D1）

本轮开工时 `MEMORY.md` 14.3 KB，**注入时被截断**——§5/§6 之后的内容根本不会进上下文。
按"这条事实是否跨会话仍成立、且会改变结论"重写，并把**数字**与**完整方法论**下沉：

- `MEMORY.md` → **10.1 KB**（§1–§6，只留判别式、坑、纪律）；
- `MEMORY-TOPICS.md` 新增 **§10 性能事实与数字**（自 §2 下沉）与 **§11 测量方法论完整版**（自 §4 下沉，
  含证据与历史教训，末尾附工具箱清单）。

### 26.5 本轮沉淀的判断准则（续 §24.4）

10. **一个结构同时承担"速度"与"数据"两件事时，先把它拆开，再谈优化。**
    影子 FIFO 不是缓存优化，是把两个语义分开——拆开之后，"更快"不再需要"更不一样"来换。
11. **"检查通过"可能只是因为它检查不到东西。** 对比类验收必须有一臂**故意与基线不同**；
    否则"两者相同"没有信息量。影子探针的 C 臂就是为此存在的。
12. **夹具要能触发判别性条件**（`assert hits > 0`）。夹具太小 → 两条路径退化成同一条 → 断言永真。

---

## 27. 训练日志诊断信息解读

ultralytics_ooo 在训练时会打印若干自定义诊断日志（非原版 Ultralytics 输出），用于自检缓存、增强抽签、验证口径等状态。以下逐条说明。

### 27.1 图像缓存配置

train: self.ims whole-image cache: 7 frames x 1.17 MiB = 8 MiB/worker [ims_cache_frames=auto=min(upstream, budget), imgsz=640, channels=3]

- 每个 DataLoader worker 在内存缓存 **7 张解码后的整图**（self.ims 是原版 Ultralytics 的图像缓存）。
- 每张 640×640×3 ≈ 1.17 MiB，7 张 ≈ 8 MiB/worker。
- ims_cache_frames=auto：缓存帧数自动取 min(上游上限, 预算)。
- 调 ims_cache_frames / ims_cache_mb 可在"重复解码次数"与"内存占用"间取舍。

- **这一行每进程只打印一次**（§28.2 的 `_log_once`），此后完全相同的文本降到 DEBUG；cap / imgsz /
  channels / source / 数据集大小 任一不同就是另一条文本，照常打印。
- 行尾原本还挂着一句 `; tune ims_cache_frames / ims_cache_mb to trade re-decodes for memory`，
  第十六轮已把它移出运行期输出——不随运行变化的常量建议在"多构造/多臂"场景下就是纯噪声。它现在
  写在 `pool/constants.py` 的 `_ONLINE_DEFAULTS` 里这两个旋钮自己的注释上。

### 27.2 原始图 LRU 缓存被截断

train: raw-image LRU capped to 7 frames (requested slice_raw_cache_size=16) to match upstream's whole-image cache bound min(ni, batch*8, 1000)-1 = 7; a larger read cache would freeze the mosaic mix pool and break upstream equivalence.

- 配置了 slice_raw_cache_size=16，实际只生效 **7 张**。
- 原因：原版整图缓存上限为 min(ni, batch*8, 1000)-1 = 7（ni=数据集大小，本例 8 张图 ⇒ 8-1=7）。
- **为什么必须对齐上游上限？** 若 OOO 的原始图 LRU 比上游整图缓存大，Mosaic 的混合池（mosaic mix pool）会被"冻住"——原始图一直留在缓存不淘汰，Mosaic 每次拼的 4 张图不再随机变化，破坏与上游行为的等价性。

验证集同理（上限可能不同，如 2），但验证无 Mosaic，统一走同一套截断逻辑保持一致。

- **同一进程内文本完全相同的这一行只打印一次**：一轮里验证集会被构造多次（整图 loader、切片
  loader……），此前每次构造都打一遍。第十六轮的验收里这一行被调用 5 次、其中 `capped to 2 frames`
  那一种文本重复了 3 次，去重后只剩 1 次（§28.2）。cap 或 `slice_raw_cache_size` 不同则文本不同，
  照常打印。

### 27.3 各增强分支抽签结果

train: augment masks @ epoch 0 (all ratios = 0.5): slice 4/8 | ratio 4/8 | blur 4/8 | compose 1/2 | weather 4/8 | occlusion 4/8 | target tiles: 4 units of 12 (block 1/3, pass 1)

每个 epoch 开始时对全部原图掷骰子，决定每张图走哪些增强：

| 分支 | 结果 | 含义 |
|---|---|---|
| slice | 4/8 (0.5) | 8 张图中 4 张被选中做在线切片（每张切 4 片） |
| ratio | 4/8 (0.5) | 4 张图做宽高比填充增强 |
| blur | 4/8 (0.5) | 4 张图做运动模糊（短+长各一份） |
| compose | 1/2 (0.5) | 2 个合成单元中 1 个被选中做 2×2 合成 |
| weather | 4/8 (0.5) | 4 张图做雨/雾/噪声退化 |
| occlusion | 4/8 (0.5) | 4 张图做矩形/条带遮挡 |

各分支比例由配置的 *_ratio（本例均 0.5）决定，round(N × ratio) 为实际选中数。

- 六个分支 ratio **相同**时（出厂配置就是），ratio 提到行首只印一次 `(all ratios = 0.5)`；**不一致**
  时退回逐分支内联 `(ratio x)`——那时它才是分支特有信息。两种写法信息量完全一致，见 §28.3。

target tiles: 4 units of 12 (block 1/3, pass 1)：分组采样器将样本分为 **4 个 unit**，每 unit 含 12 个虚拟样本；当前为第 1 个 block（共 3 个）、第 1 轮遍历。

### 27.4 切片验证口径警告

val_slice: this validation runs on sub-tiles, so the 'Speed: ... per image' line divides by the TILE count and val/*_loss divides by the tile-batch count -- both are per sub-tile, not per original image, and they are NOT comparable with whole_metrics/val/*_loss. The mAP figures are unaffected: _finalize_sliced_orig maps every prediction back to ORIGINAL-image pixels and scores one entry per original image.

开启切片验证（SAHI）时：
- 验证在**子图切片**上跑，非原图。
- Speed: ... per image（速度）与 val/*_loss（损失）按**切片数**计算，非原图图数。
- **不要与整图验证（whole_metrics）的 loss/速度直接比较**——分母不同。
- **mAP 不受影响**：_finalize_sliced_orig 将每个切片的预测框映射回**原图坐标**，按原图打分，故 mAP 为原图级别，可直接比较。

### 27.5 LRU 缓存命中率统计

train: raw-image LRU (slice_raw_cache_size=7, budget 256 MiB, grouped (configured) sampler): 88.3% hit on 163 reads last epoch, 42 MiB resident

- 上一 epoch 共读 **163 次**原始图，**88.3% 命中缓存**（无需重新 JPEG 解码）。
- 缓存驻留 **42 MiB**（远低于 256 MiB 预算，因仅 7 帧）。
- grouped (configured) sampler：当前使用分组采样器（GroupedImageSampler），同一张原图的多个虚拟样本连续进出，是命中率高的主因。

### 27.6 一句话总结

这些日志均为 OOO 包的**自检输出**：
- §27.1–§27.2：缓存被截断到上游上限，保证 Mosaic 随机性不被破坏；
- §27.3：本轮各增强分支的抽签结果；
- §27.4：切片验证的 loss/速度口径与整图不同，mAP 不受影响；
- §27.5：LRU 命中率（88.3% 说明分组采样确实省了解码）。
- §28：第十六轮对上面这些提示本身做的三项修复（非 ASCII / 重复报告 / 冗余 ratio）。

---

## 28. 第十六轮：训练日志三项修复（P0 非 ASCII / P1 重复报告去重 / P2 ratio 合并）

本轮**不改任何数据、槽位、掩码或开关语义**，只改打印出来的字。三项修复各自的对象、判据与证据：

### 28.1 P0 Windows 下日志里的非 ASCII 字符会被静默删除

**根因（一手证据）**：上游 `ultralytics/utils/__init__.py` 的日志格式化器最后一步是
`emojis(formatted)`，而 `emojis()` 在 Windows 上是 `s.encode().decode("ascii", "ignore")`——
**删除每一个非 ASCII 码位**。删除发生在 **format 阶段**，即我们的消息已经生成、任何我们能装的
handler 之后：

| 观测点 | 结果 |
|---|---|
| `record.getMessage()`（`tests/test_ooo_branches.py::_capture_messages` 用的就是这个） | **完好** |
| `PrefixFormatter.format()`（真正落盘的那份） | **已被删除** |

`_perf_review/ooo7/log_ascii_probe.py` 把同一字符串同时挂在两个观测点上，实测：

```
source           '[resume_extend] 自动修补 checkpoint 元数据: 已完成 4 轮 -> 续训至 20 轮'
getMessage()     '[resume_extend] 自动修补 checkpoint 元数据: 已完成 4 轮 -> 续训至 20 轮'
formatter output '[resume_extend]  checkpoint :  4  ->  20 '      <- payload 整段消失
上游自己的一行    'ping: 0.0±0.0 ms, read: 721.7±170.6 MB/s'
        ->        'ping: 0.00.0 ms, read: 721.7170.6 MB/s'        <- 两个数字被粘成一个数
```

三种损坏形态，源头都是"一个码位被删"：**数字粘连**（`±` 落在两个数字中间）、**悬空分隔符**
（`mAP/精度` → `mAP/`）、**整句消失**（resume 那条中文）。

**本轮动作**（只改本包、不动上游）：
- `pool/resume.py` 的 `[resume_extend]` 日志整条中译英（并在代码里写明"故意用英文，别翻回去"）；
- `pool/valslice.py` 的 `val_slice:` 日志删掉"精度"二字（现为 `The mAP figures`）。

**防复发**：新增 `tests/test_ooo_log_ascii.py`。它**不抓日志**（抓不到，见上表），而是 AST 扫描源码里
所有**非 docstring、非 `raise`** 的字符串字面量，出现任何非 ASCII 即失败。范围刻意比"只看 `LOGGER.*`
调用内的字面量"更宽：`_describe_ims_cap()` 这种**在辅助函数里拼出来**的消息，窄规则根本看不见——正是
本项目已踩过一次的"观测点离不变式太远"。测试自带两重非空转保证：
`test_the_scanner_flags_a_planted_character` 对内联 / 辅助函数 / 混入三种植入各断言恰好命中 1 条，
`test_the_scan_really_looked_at_the_package` 断言扫描器确实看到了包（≥40 个 `LOGGER.*` 调用、≥120 条
字面量；实测 52 / 156）。

**两条 `raise ValueError` 的中文保留**：异常经 Python 的 traceback 直达 stderr，不经过 ultralytics 的
Formatter，因此不受 `emojis()` 影响（探针的最后一个 case 就是验这个）。测试据此把 `raise` 显式豁免，
并断言"豁免确实被用到过"，免得豁免悄悄吞掉真命中。

**上游那行没动**：`Fast image access (ping: 0.0±0.0 ms, read: ...)` 的 `±` 属上游 `utils.py` 自己的文案，
按"不动上游"的约定保留；本机 720p 实测它写成 `read: 721.7170.6 MB/s`。反过来这也说明该缺陷不是假设：
`tests/test_ooo_branches.py` 的捕获输出里就有一行 `Slow image access detected (ping: 0.0±0.0 ms,
read: 48.4±4.8 MB/s)` 被写成 `read: 48.44.8 MB/s`。

### 28.2 P1 配置类报告去重 + 把"建议"从运行期输出里挪走

两条报告描述的是**配置**而不是**事件**，而一次训练会把同一份数据集构造多次（验证集拿到整图 loader，
开 `val_slice` 再拿切片 loader；一个进程里跑多臂时更多）。它们此前逐次无条件打印：

| 报告 | 修复前的实测（`_perf_review/ooo7/p4_shadow_e2e.log`，2 epoch × 3 臂，16 次数据集构造） |
|---|---|
| `raw-image LRU capped to ...` | **9 行，只有 2 种不同文本**（6 行 requested=31、3 行 requested=16），其余逐字节相同 |
| `self.ims whole-image cache: ...` | **3 行，逐字节相同** |

修复：`pool/dataset.py` 新增 `_log_once()` / `reset_log_once()`，`_LOGGED_ONCE` 按**整条消息文本**去重，
重复的那次降到 DEBUG（不是丢弃）。**键是全文，所以任何差异都漏不掉**：cap、imgsz、channels、source、
数据集大小任一变化 → 字符串不同 → 照打。仅影响日志，不碰数据 / 槽位 / 开关语义。

同时把 `_describe_ims_cap()` 末尾那句永不变化的建议
（`; tune ims_cache_frames / ims_cache_mb to trade re-decodes for memory`）从运行期输出移进
`_ONLINE_DEFAULTS` 里这两个旋钮自己的注释——**状态留在日志，建议留在配置**。

**真实训练验收**（`_perf_review/ooo7/r16_log_e2e.py`：2 epoch / 8 图 / imgsz=320 / CPU / workers=0 /
`val_slice_dual_metric=True`，40.9 s）：

```
train: self.ims whole-image cache: 7 frames x 0.29 MiB = 2 MiB/worker [ims_cache_frames=auto=min(upstream, budget), imgsz=320, channels=3]
train: raw-image LRU capped to 7 frames (requested slice_raw_cache_size=16) to match upstream's whole-image cache bound min(ni, batch*8, 1000)-1 = 7; ...
val:   raw-image LRU capped to 2 frames (requested slice_raw_cache_size=16) to match upstream's whole-image cache bound min(ni, batch*8, 1000)-1 = 2; ...
R16_DEDUP calls=5 emitted=3 suppressed=2 distinct_texts=3
```

即：5 次数据集构造 → 5 次 `_log_once` 调用 → 3 种不同文本照打、**2 行被去重**（`val` 那种文本重复了
3 次）。`_perf_review/ooo7/r16_log_check.py` 对**落盘字节**断言 20 项全过，含"每种不同文本恰好 1 行"
与"旧文案必须消失"的反向对照。本轮必须跑真实训练而不是只看单测：单测读 `getMessage()`，对 P0 结构性
失明；P1/P2 的格式也只有过一遍真实 formatter 才算数。

### 28.3 P2 `augment masks` 行不再逐分支重复 ratio

六个分支同 ratio（出厂配置就是这样）时，`(ratio 0.5)` 被印了 6 遍，而它是**常量**：

```
改前: augment masks @ epoch 0: slice 4/8 (ratio 0.5) | ratio 4/8 (ratio 0.5) | blur 4/8 (ratio 0.5) | compose 1/2 (ratio 0.5) | weather 4/8 (ratio 0.5) | occlusion 4/8 (ratio 0.5) | target tiles: 4 units of 12 (block 1/3, pass 1)
改后: augment masks @ epoch 0 (all ratios = 0.5): slice 4/8 | ratio 4/8 | blur 4/8 | compose 1/2 | weather 4/8 | occlusion 4/8 | target tiles: 4 units of 12 (block 1/3, pass 1)
```

ratio 不一致时**保持逐分支内联**（那时它才是分支特有信息）：

```
augment masks @ epoch 0: slice all/4 | ratio 2/4 (ratio 0.5) | blur 2/4 (ratio 0.5) | weather all/4 (ratio 1) | ...
```

收尾 epoch 的标记仍跟在行首括号之后：
`train: augment masks @ epoch 1 (all ratios = 0.5) [close_aug_epoch: every slot emits the original]: slice 4/8 | ...`。
两种写法信息量完全一致，只是把常量从 6 次降到 1 次。**纯展示改动**：不涉及任何槽位、掩码或抽签。

同步的断言：`tests/test_ooo_branches.py` 的 `test_mask_summary_reports_the_actual_per_branch_counts`
（混合 ratio → 断言**不出现** `(all ratios`、且逐分支内联仍在）与
`test_mask_summary_labels_ratio_1_as_all_augmented`（全 1.0 → 断言行首 `(all ratios = 1)`、且
`(ratio ` 完全不出现）。两条合起来把新格式的两个分支都锁住。

### 28.4 本轮沉淀的判断准则（续 §26.5）

13. **"测试看不见"与"测试通过"是两件事。** 抓日志的单测读 `getMessage()`，而删除发生在 formatter：
    观测点位于缺陷之前 → 这个缺陷**在单测里永远不可能变红**。要么把观测点挪到真正的出口（落盘字节），
    要么把不变式放到源码上（AST 扫描）。
14. **豁免要能自证被用到。** `raise` 豁免若一次都没命中，它与"扫描器坏了"无法区分 → 额外断言
    `raise_only >= 1`。这与 §24.4 的 `assert hits > 0` 是同一条纪律。
15. **运行期输出里不放常量文案。** 一行中不随运行变化的部分（建议、用法、说明），在"多构造 / 多臂"
    场景下就是纯噪声；**状态进日志，建议进配置注释**。
16. **重定向的日志文件里有 ANSI 色码**（本轮顺带量到：一个 117 行的日志里有 53 处 `ESC`）。来源是上游
    的 `colorstr()`（对 `self.prefix` 与上游自己的消息无条件加色，无开关）。终端里正常显示，用脚本
    grep 这个文件时先剥色（`\x1b\[[0-9;]*m`）。
