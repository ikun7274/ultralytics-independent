> 所有功能更改（参数、逻辑、数据结构）都会记录在本文件，便于追溯每次改动的原因、范围与验证结果。

## 2026-09-14：代码清理与健壮性修复（8 项核验：7 项属实并修复，1 项误报）

依据代码复查清单，逐项先核验"问题是否属实/合理"，再修复；其中 1 项经核验为误报，不改代码。

### 核验结论

| 问题                            | 结论                                          | 说明                                                                                                                                                                                         |
| ------------------------------- | --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 注释引用失效行号                | ✅ 属实并修复                                 | 注释引用 `see _save in augment.py: line 941`，实际保存函数为 `OnlineSlice._save_tile`，行号已漂移，且 `_save` 符号不存在                                                                     |
| 保存分支表含死条目 + 配额双机制 | ✅ 属实并修复                                 | `_SAVE_BRANCHES` 含 `"tile"` 死条目（5 处 `_save_annotated` 调用无 tile）；`dataset.slice_save_max_tile` 只写不读（tile 配额走 `OnlineSlice` 实例 `save_max`，构造时注入）——同一旋钮两套机制 |
| 默认值漂移守卫覆盖不全          | ✅ 属实并修复                                 | 漂移守卫只正向遍历已列键；`prefetch_factor` / `slice_raw_cache_size` 默认值 `2` 在 default.yaml / base.py / build.py 多处字面量重复且未纳入 `_ONLINE_DEFAULTS`                               |
| 死赋值 / 缺类型标注             | ⚠️ 部分属实                                   | ① `oc_color = "auto"` 死赋值（三分支全覆盖，str 与最终 tuple 类型不一致）✅修复；② `_ratio_pad_params` 缺返回类型标注 ✅修复；③ **stripe 宽度为误报，见下**                                  |
| 文本文件写入未指定编码          | ✅ 属实并修复                                 | val.py 写 `paper_data.txt`、pytools 11 处文本 `open` 未指定 `encoding`，中文 Windows 下默认 cp936                                                                                            |
| 验证产物查找全树扫描            | ✅ 属实并修复                                 | val.py 每次运行 `rglob` 全树扫描 + 全部候选 `stat()` 后排序；`n_l`/`n_g` 解包未用；`model_info` 返回值部分丢弃                                                                               |
| compose 下限语义 / 插值参数化   | ✅ 属实（部分已随合成限幅修复），剩余项已修复 | compose `<0` 语义、`_cap_long_side` 参数化插值此前已完成；本次补上仍开放的 `degrade_resample` 配置键                                                                                         |
| 保存分支 zip 静默截断           | ✅ 属实并修复                                 | `_save_annotated` / `_save_tile` 两处 `zip(boxes, cls)` 无长度校验（`zip` 静默截断），segments 版已有告警对照                                                                                |

### stripe 宽度误报结论（重要，不改代码）

- **报告声称**：stripe 分支实际宽度 `≈2×thick+1 ≈ 4%·min(h,w)`，与 docstring 的 "~2% of the short side" 不符，建议把 docstring 改成 "~2% 半宽（总宽约 4%）"。
- **核验结果（误报）**：`thick = max(2, int(0.02 * min(h, w)))`（≈ 2%·短边），画框 `x0 = cx - thick//2`、`x1 = cx + thick//2 + 1`，实际宽度 = `2×(thick//2)+1 = thick+1 ≈ 2%·短边`，与 docstring **一致**。报告把 `x1` 的 `+1` 误算成了多一个 `thick`（得到 2×thick≈4%）。
- **处理**：**不改 docstring**（本来正确）；若按报告改反而会把正确文档改错。

### 修复明细

- **失效注释**（`augment.py`）：注释 `see _save in augment.py: line 941` → `see OnlineSlice._save_tile`（引用符号名而非行号）。
- **死条目与双机制**（`base.py` + `augment.py`）：`_SAVE_BRANCHES` 删除 `"tile"`；`v8_transforms` 删除 `dataset.slice_save_max_tile = ...` 只写不读赋值，注释改为"tile 故意不列（配额在 OnlineSlice 实例上）"。
- **默认值漂移守卫扩展**（`base.py` + `build.py` + `tests/`）：
  - `_ONLINE_DEFAULTS` 纳入 **20 个此前未保护的键**（值取自 default.yaml，由既有 `test_online_defaults_match_default_cfg` 守护零漂移）：切片几何/过滤 `slice_prob`、`slice_overlap_ratio`、`slice_min_tile_area_ratio`、`slice_min_box_retain_ratio`、`slice_min_center_retain_ratio`、`slice_center_constraint`、`slice_center_bias`、`slice_bias_margin`、`slice_bias_jitter`、`slice_full_box_only`、`slice_background_ratio`；保存 `slice_save_exist_ok`、`slice_save_max_tile/blur/ratio/compose/weather/occlusion`；缓存 `slice_raw_cache_size`、`prefetch_factor`。
  - `base.py:490/542`、`build.py:387` 的 `getattr(..., 2)` 字面量兜底 → `_online_default(...)`（字面量 2 仅剩 default.yaml 一处真源）。
  - **新增反向漂移守卫** `test_online_defaults_cover_every_online_cfg_key`：`DEFAULT_CFG` 中所有 `slice_*` / `*_keep` / `*_ratio` 键必须已在 `_ONLINE_DEFAULTS`（显式排除上游 `mask_ratio` 与验证侧 `val_slice_ratio`/`val_slice_overlap_ratio`）。
- **死赋值 / 类型标注**（`online_degrade.py`）：删除死赋值 `oc_color = "auto"`（三个分支全覆盖）；`_ratio_pad_params` 补返回标注 `-> tuple[int, int, int, int] | None`。**stripe 宽度未改（误报）**。
- **编码显式化**（`val.py` + `pytools/`×3）：`val.py` 写 `paper_data.txt`、`sahi_equal_division_slice_dataset_auto_improved.py` 7 处、`compose_slice_dataset_auto_improved.py` 2 处、`change_image_resolution_slice_dataset_auto_improved.py` 2 处文本 `open` 全部显式 `encoding="utf-8"`（PIL `Image.open` 二进制路径不受影响，未动）。
- **查找单遍化**（`val.py`）：`sorted(rglob(...), key=st_mtime, reverse=True)[0]` → `max(rglob(...), key=st_mtime, default=None)` 单遍；`n_l, n_p, n_g, flops` → `_, n_p, _, flops`。
- **插值可配置**（`base.py` + `augment.py` + `default.yaml`）：新增配置键 **`degrade_resample`**（`"area"`=抗锯齿但慢 ~21.5ms / `"linear"`=快 3 倍但略软 ~7.8ms，几何一致，默认 `area` 保持原行为）；`_degrade_frame` 按配置选择插值核；`v8_transforms` 镜像到 dataset；三处齐动（default.yaml + `_ONLINE_DEFAULTS` + mirror），由漂移守卫守护。
- **保存前长度校验**（`base.py` + `augment.py`）：`_save_annotated` 与 `OnlineSlice._save_tile` 在 `zip(boxes, cls)` 前加显式长度校验 + `LOGGER.warning`（与 occlusion segments 版告警风格一致；不用 `strict=` 以兼容上游 py3.8 声明）。

### 验证

- `py_compile`：全部改动文件通过。
- `pytest tests/test_online_augment.py tests/test_lint_config.py tests/test_pytools_safety.py -q` → **225 passed**（含新增反向守卫测试；默认值键补齐后零漂移）。

---

### 给 `self.ims` 整图缓存加字节预算（并暴露为配置） `self.ims` 的上限只随 `batch` 增长，与 `imgsz` 相乘后可达 GB 级

- **原因**：`_ims_cap` 直接复刻上游 `min(ni, batch*8, 1000) - 1`。它只跟 `batch` 走，而该缓存一帧的大小是 `imgsz*imgsz*channels`（`load_image` 存的是**缩放到 imgsz 之后**的帧），两个维度相乘 → `batch=64` 时 511 帧：`imgsz=640` 是 599 MiB，`imgsz=1280` 是 2.3 GiB，`imgsz=1920` 是 **5.3 GiB / worker**（`workers=8` 就是 42 GiB）。在线分支还有独立的原图 LRU（4 帧原始分辨率，4000×3000 时 144 MB）并存，而内存建议文档只提了 `prefetch_factor`，没覆盖这一项。
- **修改**（只动 `ultralytics/data/`、`cfg/`、`tests/`，不碰任何增广算子）：
  - **新增两个配置键**（三处齐动：`default.yaml` + `CFG_INT_KEYS` + `_ONLINE_DEFAULTS`，值由既有 `test_online_defaults_match_default_cfg` 守护）：
    - `ims_cache_frames`（默认 `0`）：`0`=**自动**、`>0`=显式帧数（可以超出上游，用内存换回重解码）、`<0`=**上游公式原样**（逃生舱）。
    - `ims_cache_mb`（默认 `1024`，MiB）：**自动档**的每 worker 字节预算；`<= 0` 表示"无预算"→ 回退上游公式。显式帧数档不读它。
  - **解析器**（`base.py` `_resolve_ims_cap`）：返回 `(cap, 预算档)`——cap 直接进缓存；预算档标识"自动/显式/上游"，供测试与日志。两条维度都是新键，**超参哈希默认不含它们**（默认值 0/1024 全等）。
  - **装配**：`v8_transforms` 把两键传给 dataset（`_set_ims_cap`），同时提供**旧路径兼容**（构造后才设置时无需任何 `__init__` 改动）。
  - **健康检查**（`check_dataset` 前）：`_ims_cache_frames<0` 时 `LOGGER.info` 说明"回退上游公式"；预算档=自动且 `ims_cache_mb<=0` 时 `LOGGER.warning`（"无预算，回退上游公式"）。
  - **注解**：`_ims_cap` 换 `functools.lru_cache`；对自动档 `≤0` 的有效帧数补 docstring（不可为 0/负——为 0 会把全图缓存禁用，比上游更差）。
- **验证**：基准行为（配置默认）帧数不变（`batch=2, imgsz=320` 等）；显式档（`ims_cache_frames=3`）生效；自动档解析（`_resolve_ims_cap` 单测 ×3）；`ims_cache_mb=0` 回退上游公式；`ims_cache_frames=-1` 回退上游公式；健康检查两种日志；回归测试全过（`pytest tests/test_online_augment.py -q` → 20 passed）。

---

### lint/format 配置落地（防漂移、可自查）

### 背景

仓库没有任何 lint/format 配置：轻微问题累积到 232 条可自动修复项；PR 上 CI 会替你改写代码；无法判断"本次 diff 是否引入了新 lint 问题"。

### 新增三件套

- **`pyproject.toml`**（仓库根）：Ruff 配置——`target-version = "py38"`、`line-length = 120`、select 为本项目常用集合（E/F/W/I/UP/B/C4/SIM/PERF/RUF）、ignore `RUF001/002/003`（本项目大量中文注释与字符串）、`extend-exclude` 覆盖 `*.md` 与 `tests/` 外的构建产物。
- **`pytools/lint.py`**：自查入口——运行本仓库既有 test 集合（`test_online_augment.py` + `test_pytools_safety.py` + `test_lint_config.py`）作为**口径指纹**，支持 `--strict` 门禁（任何一项失败即非零退出），是提交前自查的单一入口。
- **`tests/test_lint_config.py`**（14 项）：守护 lint 配置本身——配置可解析、select 含预期规则、ignore 正确、exclude 覆盖 md、目标版本 py38、line-length 120，且**把 `pytools/lint.py` 里声明的测试集合与磁盘上的测试文件做双向一致性校验**（防 lint.py 声明的口径与实际测试脱节）。

### 验证

`pytest tests/test_lint_config.py -q` → 14 passed；`python pytools/lint.py` → 全绿（lint.py 本身作为标准输出无冲突）。

---

### 段边界缓存失效防护（版本戳替代"首次调用即固化"）

### 背景

`_segment_bases()` 每次 `__getitem__`/`__len__` 都重建（6 个 getattr + 列表），遂加缓存。但缓存键最初是**分支开关状态**——前提是"分支开关在 `v8_transforms` 装配后训练期不变"。**该前提不成立**：开关是在 `__init__` **之后**由 `v8_transforms` 挂上的，任何发生在挂上之前的 `len()` 都会把旧边界永久固化（静默丢样本）。

### 修复（base.py）

- 段长**单一真源** `_segment_lengths()`（原 `_compute_segment_bases` 更名）；
- 缓存键改为**版本戳** = `tuple(_segment_lengths())`：开关改变 → 段长变化 → 缓存键变化 → 自动失效重建；
- 键中天然携带全部区段长度，无需再校验"total == len"（版本戳不同即重建）。

### 验证

- 构造后**手动翻开关**（slice_keep_origin / ratio / blur / compose / weather / occlusion）：`len()` 立即反映新区段布局，无陈旧缓存；
- 正常路径回归：`test_segment_bases_full_pool` 全过；`py_compile` 通过。

---

### 读参统一兜底（`_hyp_get`，新增配置键"三处齐动"由代码强制）

### 背景

每个在线参数默认值三处 fallback（`default.yaml` / `augment.py` / `base.py`），当前值一致但结构上是漂移隐患；从旧 `args.yaml` / 旧 checkpoint 的 `train_args` 恢复配置时，缺键会在数据增强构造阶段抛 `AttributeError`，训练起不来且不提示缺哪个键。

### 修复

- **`_hyp_get(hyp, key)`**（`augment.py` 模块级）：解析顺序 = `hyp` 属性 → 显式默认值 → `default.yaml`（默认值的**唯一真源**）；键在 `hyp` 和 `default.yaml` 里都没有时抛 `ValueError` 并指名该键；
- `v8_transforms` 全部在线参数读取改走 `_hyp_get`（删掉各参数的隐式 fallback 链），并加 `_ONLINE_DEFAULTS` 注释：**"新增配置键必须三处齐动（default.yaml + \_ONLINE_DEFAULTS + 实际传递点）"**——本函数是第二处与第三处的强制汇合点；
- `base.py` 保留 `_RATIO_DEFAULTS` 兜底（仅 dataset 直读场景）并加"必须与 default.yaml 同步"注释。

### 验证

- 缺键场景：构造 dataset 抛 `ValueError` 并指明键名（不再静默 `AttributeError`）；
- 回归 `pytest tests/test_online_augment.py -q` → 20 passed。

---

### 切缝确定性（4 片共享网格，`slice_center_bias` 收敛）

### 背景

`slice_all_tiles=True` + `slice_center_bias=True` 时，4 个切片来自 **4 张不同的切缝网格**（每次调用都重新取随机偏置）——切缝非确定，且并集不再保证覆盖全图（可能整体漏目标）。

### 修复（augment.py `OnlineSlice._grid`）

- 抖动 RNG 改为**确定性派生**：`seed = crc32(f"{epoch}:{img_index}")` + 逐图自增序号；
- 同图同 epoch 的 4 片**必然共享同一张网格**；跨 epoch 才变化（防同图每 epoch 同缝过拟合）；
- 确定性来自 **(`epoch`, 原图索引)**，与全局 `random` 流解耦——多 worker / `persistent_workers` 下各进程重建完全一致（与掩码重建同款确定性 RNG 策略）。

### 验证

- 单测：同图同 epoch 4 片网格一致（bias 值相同）；跨 epoch 变化；`jitter=0` 固定网格；
- ⚠️ **覆盖空洞边界实测**：`jitter ≳ 0.21`（`overlap=0.2` 时）重新出现空洞（0.22 时 20/200 图、0.25 时 53/200）——抖动幅度已接近重叠余量；默认 0.05 安全。**覆盖承诺仅对 jitter < overlap 成立**，已写入项目说明。

---

### fraction 取整归零崩溃修复（小数据集 + 小 fraction 无法启动训练）

### 现象

`train.py` 设 `fraction=0.001` 配 8 张小验证集，训练启动即崩：

```text
WARNING train: Image speed checks: No files to check
Traceback ... get_labels: cache_path = Path(label_files[0]).parent.with_suffix(".cache")
IndexError: list index out of range
```

### 根因

`fraction` 裁剪在 `assert im_files` **之后**执行，且是 `round(N×fraction)` 取整——8×0.001=0.008 → 0 → 静默返回空列表。因此"Image speed checks"能看到空文件集，但 `get_labels` 里 `label_files[0]` 直接越界。**没有走原生 "No images found" 报错路径**，极难排查。

### 修复（两处）

1. `train.py`：`fraction=0.001` → `fraction=1.0`，注释"验证用小比例，但 <1 可能取整为 0"；
2. `base.py::get_img_files`：裁剪后为空时 `LOGGER.warning`（"fraction=... round(...)=0, fallback to 1 image"）+ 兜底保留 `orig[:1]`（保证训练可启动）。

### 验证

- 原崩溃场景重建：`fraction=0.001` + 8 张小集 → 训练正常启动（日志 `im_files:1 / label_files:1 / labels:1`）；
- 验证产生的 `_mini_val_set/labels/train.cache` 已清理；
- 全量回归：`pytest tests/test_online_augment.py tests/test_pytools_safety.py tests/test_lint_config.py -q` → **225 passed**。

---

## 2026-09-13：审查问题全量修复（性能专项 + 全面复审，32 项）

依据两次代码复查，完成 **32 项**问题的核验与修复。以下按主题分组记录（问题 → 修复）。

### 数据安全（pytools 空参数会删目录，最严重）

**问题**：`pytools/*.py` 的 `--input_dir/--output_dir` 非 required 且默认 `"."`；`shutil.rmtree(output_dir)` 无存在性/安全性检查——**空参数运行时 `-o .` 会递归删除当前工作目录**（还连带删掉 `-i .` 的输入数据）。

**修复**：

- `pytools/_safe_io.py` 新增四个护栏：
  - `assert_deletable(path)`：删除前校验——路径存在且不是磁盘根 / 家目录 / 当前工作目录 / 仓库根（任一命中抛 `RuntimeError` 拒绝删除）；
  - `safe_rmtree(path)`：`assert_deletable` 通过后按序 `os.rmdir`（自底向上）→ 失败则 `shutil.rmtree`（带 `onerror` 忽略缺失），并返回被删文件数；
  - `require_nonempty(path)`：`--input_dir` 为空目录或不存在时抛 `FileNotFoundError`；
  - `validate_io_dirs(input_dir, output_dir)`：input 必须是存在的目录且非空；output 若存在必须是目录。
- 全部 4 个离线脚本改造：`--input_dir/--output_dir` 改 **required**；`--overwrite` 默认 `False`；删除目录一律走 `safe_rmtree`；input 先 `validate_io_dirs` 再处理。

### 原图 LRU 全量 shuffle 下命中率趋近 0（每图 ~13 次冗余解码）

**问题**：在线增强按原图解码，但全量 shuffle 把同一原图的子样本打散到约 6.5N 个样本之外，远超 worker 内原图 LRU 容量（默认 16 帧）→ 命中率 ≈0，每张原图被重复解码 13 次（4000×3000 单次 ~203ms）。

**修复**（新增 `slice_grouped_sampler`，默认 True）：按 7 区段真实布局把全池索引组织成「4 张源图一单元、每图一块」，单元间随机、单元内块轮转、每 epoch 重新置换。实测（mosaic=0）：

| 场景         | 命中率 | 解码 / 图 |
| ------------ | ------ | --------- |
| 分组采样     | 89.7%  | 1.0       |
| 全局 shuffle | 8.0%   | 8.95      |

**代价（已写入 default.yaml 注释）**：mosaic 的近期样本池收窄为当前单元（≤4 张原图）——每个 mosaic 仍是 4 张不同原图，但同单元组合会重复；需完全恢复上游随机性时设 `slice_grouped_sampler=False`。

### 合成分支仍在原始分辨率拼接（5.6× 耗时、8.7× 峰值内存）

**问题**：`_compose_at` 在 ORIGINAL 分辨率上拼接 4 张 4000×3000 → 8000×6000（≈144 MB/张），之后才缩放。

**修复**（`compose_max_side`）：**逐张源图先限幅、再分配画布**（不再"拼到全分辨率再整体缩小"）；bbox/segment/keypoint 均为归一化坐标，只缩放像素不影响标签。实测 8000×6000（144 MB）→ 2560 上限（18 MB）：**7.8× 提速、20× 内存下降**；几何（拼接线、相对布局、裁剪余量）完全不变。语义说明：合成图保留的是"多目标上下文"而非细节，降采样零语义损失。

### 两个最贵的像素算子仍有数倍优化余量（噪声 4.8×、模糊 ~35×）

**问题**：`_apply_weather` 噪声用 `np.random.normal` 分块循环 + 拷贝；`_apply_motion_blur` 每样本建 `size×size` 方格 PSF 走 `filter2D`（核边长 ~11px 处 filter2D 性能断崖）。

**修复**（`online_degrade.py`）：

- 噪声：`np.random.normal` 分块循环 → `cv2.randn` 单缓冲。**关键陷阱**：`cv2.randn` 按通道填充，`(h,w,3)` 矩阵会把 σ 缩小 √3 倍（实测 σ=15 → 8.665），需把缓冲以 `(h, w×c)` 单通道视图交给它。提速 **4.8×**（实测 1500×2000、n=100）。
- 模糊：PSF 从 `size×size` 方格改为**裁剪零填充核**（裁掉零边 + 回推 anchor，数学上与原实现逐位相同），避开 filter2D 断崖，**1.35×**；另加 `blur_axis_aligned` 轴对齐路径（`cv2.blur` 行程和，**1.82×**，见下）。

### 气象退化的两处静态错误（噪声/雾、伪影 + 归一化污染）

- **噪声/雾归一化污染**：`noise`/`haze` 用 `(img.astype(np.float32) / 255.0)` 归一化后直接覆写原 `img` → 后续 `cv2.bitwise_and`/`bitwise_or` 的位运算被污染（浮点 → uint8 截断），无噪声/雾退化时 `res == img` 断言失败。修复：浮点运算**只在局部作用域内**进行，结束后以 `np.clip(..., 0, 255).astype(np.uint8)` 结果整体覆写 `img`。
- **雨线伪影**：`_apply_weather` 的雨线 `alpha` 仅 0.1，在 `img` 上是 255×0.1=25 灰阶增量，肉眼不可见且极端偏色；修复：alpha 提至 0.2~0.6 随机。

### 双口径验证序列化成本翻倍（且 dev 模式默认开启 ×5）

**问题**：`save_model` 在每轮验证时把 `train_metrics` 与模型权重写进 ckpt；双口径（切片+整图）下 `train_metrics` 长度翻倍 → 序列化成本翻倍（约 0.6s→1.1s），且 dev 模式默认双口径全开。

**修复**：默认关闭 `val_slice_dual_metric`；关键参数只存首个片段；文档写明双口径训练建议。

### 整图 GT 每轮重复计算

**问题**：`_finalize_sliced_orig` 每轮验证用 `_gt_orig_pixels` 把 GT bbox 从归一化还原到原图像素（约 500 次 `np.quantile`），重复计算且每次随机重采样。

**修复**：`SliceValDataset` 构建时一次性把所有整图 GT 还原到原图像素（`_gt_orig_cache`），复用 + 加锁。

### Mosaic buffer 用 list + pop(0)，每次采样冗余拷贝整表

**问题**：`Mosaic.get_indexes` 里 `random.choices` 每次拷贝整个 buffer（容量 64 时一次 ~10μs，每 batch 一次，仅 2% 开销）；`buffer.append + pop(0)` 是 O(n) 头删。

**修复**：`deque(maxlen=64)`（原生 `append` 自动淘汰最旧，O(1) 头删）；`random.choices` 改为 `[buffer[i] for i in randint(...)]`（按索引取值，零拷贝）。注意：改 deque 后 `max_buffer_length` 语义变化（从"缓冲满即淘汰"到"软上限 + 淘汰最旧"），`len` 恒为容量 64 不再波动。

### 原图 LRU 命中即全分辨率拷贝

**问题**：`_load_image_cached` 命中时 `arr.copy()` 全分辨率拷贝（4000×3000 一次 ~36 MB，命中率低时 ~600 MB/s 拷贝量）；且 LRU 默认容量 16 < compose 访问跨度 32 → 命中率受限。

**修复**：命中时先 `np.ascontiguousarray(im)`（零拷贝或浅拷贝），仅当确实需要写时才 `arr.copy()`；容量默认 16→32（compose 跨度 32 全覆盖）。验证：命中率 0.66→0.97（mosaic=0、128 图、16 worker、8 epoch、stride 4096）。

### 验证侧兜底通道是死代码

**问题**：`SliceValDataset.__getitem__` 缓存失败后 `_load_image_cached` 重试一次；该函数返回 None 时用**同一路径**再 `cv2.imread`——与缓存通道失败同源，必然再失败，随后 `im[y0:y1,x0:x1]` 以 None 切片崩 TypeError；且 `_build` 内 `import bisect` 未使用。

**修复**：缓存失败改走**独立 imread 通道**重试，再失败则带路径 `FileNotFoundError` 明确报错；删除 `_build` 未用 import。

### 验证子集每轮抖动

**问题**：`val_slice_ratio < 1` 用未设种子的 `random.sample` → 每轮验证子集不同，指标抖动、调参反馈失真。

**修复**：用确定性 RNG（`epoch` 派生）采样，跨 worker 一致、跨 epoch 变化。

### 每样本一次跨进程加锁读

**问题**：`_sync_epoch_masks` 每样本一次 `mp.Value` 加锁 int 读。

**修复**：缓存到 `self._mask_stamp`：`epoch` 不变时直接返回（零开销）；`set_epoch` 调用一次 `reset_counters`。**留待观察**：跨进程掩码语义仍靠 epoch 通道，未验证 `persistent_workers=True` 安全时再开。**后续已确认安全并写入文档（见 09-12 跨进程掩码条目）**。

### 每验证 pass 完整重建数据集

**问题**：`validate()` 每个 pass 重建数据集（每 epoch 2~3 次），且 dual 模式固定重建 2 次。

**修复**：整合为 `_run_val()`——参数变化时才重建（整图↔切片切换时），同口径多次验证复用同 loader。

### 极端长宽比先撑大再缩小

**问题**：`_ratio_at` 在原始分辨率 `_ratio_pad_params`（如 2000×1000 → 16:9 目标 = 撑大 2000×1125 → 再缩到 640）像素膨胀 ~4.5×。

**修复**：比例调整**只在目标分辨率上做边框**（比例调整后输出直接是 640 等目标尺寸，不再先撑大再缩小），像素膨胀消除。

### 用扩展索引访问 `self.batch`

**问题**：`get_image_and_label` 里 `self.batch[index]` 访问 batch 下标（`self.batch` 是 batch 索引列表），扩展索引会越界——靠"rect 关闭在线增强"隐性兜底。

**修复**：在线分支不访问 `self.batch`（该字段只对上游 rect 模式有意义）；文档标注 `self.batch` 只在非扩展池路径使用。

### `_occlusion_at` 的 segments 用 zip 静默截断

**问题**：`zip(keep, ...)` 静默截断（keep 与 segments 长度不一致时不报错）。

**修复**：改显式 `zip(strict=True)`？——上游 py3.8 无 strict 参数，用显式长度断言 + `RuntimeError` 替代。

### 双口径验证数据泄漏（严重）

**问题**：`trainer.py::validate()` 双口径第二遍（整图验证）直接复用 `self.test_loader`——该 loader 在 `_setup_train` 时构建，**在训练侧数据增强开启的前提下**构建（在线增强的 `get_image_and_label` 对验证集也生效，但验证侧 transforms 不含增强）——实际是无害的；但**构造时传入的 `train=True`**（`build_yolo_dataset` 的 `mode='val'` 但 `train=...`）导致验证集也走训练侧 transforms 装配，在线增强对验证图生效 → **验证指标被增强污染**。

**修复**：`DetectionValidator.get_dataloader` 用 `mode='val'` 构建验证 loader 时强制 `train=False`；`SliceValDataset` 包装时显式 `transforms=None`（继承原生验证 transforms）。

### 其余修复（明细）

| 问题                                                           | 修复                                                     |
| -------------------------------------------------------------- | -------------------------------------------------------- |
| `val_slice_ratio < 1` 时每轮验证子集不同、指标抖动             | 确定性 RNG（epoch 派生）采样                             |
| `SliceValDataset` 标签 0 类目标（`-1` 语义）与 bbox 重映射冲突 | 标签过滤与 bbox 重映射顺序调整                           |
| `train.py` 导入 `fix_checkpoint_for_extension` 但不使用        | 移除                                                     |
| `pyproject.toml` 无 ruff 配置                                  | 新增（见 09-14 lint 条目）                               |
| 默认 `val_slice_enable=True` 改变默认验证口径                  | 保持 `True`（语义：训练侧切片，验证侧应匹配）；文档标注  |
| `detect.py` 导入重复清屏                                       | 删除多余导入                                             |
| 默认验证行为改动太大                                           | 维持 `val_slice_enable=True` 默认，提供 `False` 一键回退 |

### 验证

- `py_compile`：全部改动文件通过；
- 单测：`test_online_augment.py` 20 项、`test_pytools_safety.py` 5 项全过；
- 端到端：`_mini_val_set` 8 原图 + 全增强 1 epoch（CPU）跑通，`Online augment: 82 training samples from 8 images (4 slices + 1 origin + 1 ratio + 2 blur + N/4 compose per image)`；
- 双口径验证：两遍均出指标表、两套权重并排保存。

---

## 2026-09-13：修复验证（工单标签清理 / degrade_frame 口径）

### 工单标签清理

`train.py` 注释中的工单编号（非公开信息）已清理。

### `degrade_frame` 口径

`_degrade_frame` 返回的 scale 与实际缩放一致（回归断言通过）；与 `_cap_long_side` 的语义核对无误。

---

## 2026-09-13：detect.py 导入重复清屏 + 验证侧数据泄漏修复

### detect.py 导入重复清屏

`detect.py` 顶部重复导入导致终端输出被清屏两次，删除多余导入。

### 验证侧数据泄漏修复（重要）

**问题**：验证侧 loader 在 `_setup_train` 时构建，误传 `train=True` → 训练侧 transforms 装配到验证数据集 → 在线增强对验证图生效，验证指标被增强污染。

**修复**：`DetectionValidator.get_dataloader` 的 `mode='val'` 路径强制 `train=False`；`SliceValDataset` 包装时显式 `transforms=None`。

**验证**：`_mini_val_set` 双口径验证两遍指标一致（无增强污染）；回归 20 passed。

---

## 2026-09-13：补充修复（ims 字节预算 / fraction 兜底 / 段边界版本戳 / 读参兜底 / mosaic 保存 / 续训修补独立 / 在线属性白名单）

### ims 整图缓存字节预算

见 2026-09-14 同名条目（`ims_cache_frames` / `ims_cache_mb`）。

### fraction 取整归零兜底

见 2026-09-14 同名条目（`get_img_files` 裁剪为空时警告 + 兜底保留 1 张）。

### 段边界版本戳

见 2026-09-14 同名条目（`_segment_lengths` 版本戳）。

### 读参统一兜底

见 2026-09-14 同名条目（`_hyp_get`）。

### mosaic 保存

`Mosaic._save_mosaic` 保存块与 `slice_save_*` 同款基建（unicode-safe 写盘 / 去重 / 限额 / 画框开关 `mosaic_save_annotated`）。

### 续训修补独立成方法

`resume_training` 内的修补块抽为独立方法，逻辑复用 `pytools/fix_checkpoint_for_extension.py` 的修补语义。

### 在线属性白名单（`_online_attrs`）

在线相关属性（`_slice_mask` 等）集中于模块白名单，防止与其他模块属性冲突。

### 验证

全量回归 `pytest tests/test_online_augment.py tests/test_pytools_safety.py -q` → 25 passed；`py_compile` 全过。

---

## 2026-09-13：拖影方向轴对齐（`blur_axis_aligned`，默认 True）—— 零像素风险的 1.82× 提速

### 决策依据（用户确认）

实际退化方向**本来就固定**——相机安装固定、运动方向在像面上恒定地映射到水平或垂直（车载 / 航拍沿航迹方向）。这类域里把拖影约束为轴对齐**不是妥协，而是更贴合域的建模**，故直接启用，未做 A/B；同时保留 `blur_axis_aligned: False` 作为任意角路径的回退与对照。

### 为什么不损失保真

轴对齐时现有 PSF **恰好就是一个均匀箱式**——运动模糊核画的 AA 线段落在单行/单列上，归一化后每格恰为 `1/n`。因此 `cv2.blur` 与 `filter2D` 的输出**完全相同**：242 组 (length, axis)（L 从 5 到 35、步长 0.25，两个轴向，3 通道 1280×960，含失焦开关）实测 **`max|diff| = 0`**。

### 改动

- `online_degrade.py`：抽出 `_psf_size(length)` 供稠密 PSF 与箱式**共用**（两者不会漂移）；`_apply_motion_blur` 增加 `axis_aligned` 参数，走 `cv2.blur(img, (n, 1) / (1, n))`（n 恒为奇数，cv2 默认居中锚点与 PSF 中心一致）；
- `base.py::_build_blur_sample`：`blur_axis_aligned` 为真时角度取 `{0°, 90°}`（二选一），否则维持 `U[0, 180)`。**刻意仍只消费 `random` 流的一格**（`random.random()` 与 `random.uniform(0.0, 180.0)` 同为一次抽样），开关翻转不会让下游所有随机决策整体错位——这是 A/B 可解释的前提。

### 实测增量（相对当前代码的真实增量）

| 档位                              | 裁剪核 filter2D | 箱式 `cv2.blur` | 倍数      |
| --------------------------------- | --------------- | --------------- | --------- |
| short（len 5~12，无失焦）         | 7.0 ms          | 5.2 ms          | 1.33×     |
| long（len 20~35，含 defocus σ=1） | 20.5 ms         | 10.5 ms         | **1.96×** |
| **两档合计（生产配置）**          | **28.1 ms/图**  | **15.4 ms/图**  | **1.82×** |

省 **12.7 ms/图 ≈ 108 s CPU/epoch（8520 图）≈ 1.8 min CPU，8 worker 下约 13 s 墙钟**。箱式代价**与核长无关**，`blur_*_len_max` 以后调大时收益只会更高。

### 验证

38 项参数化用例：`_psf_size` 不变式与"PSF 是均匀箱式"6 项、逐位等价 28 项、机制守卫 1 项（断言真的走 `cv2.blur` 且**不得**调用 `filter2D`）、采样层 3 项（只产生 0/90、关闭后恢复任意角、开关翻转不改变 `random` 流消费）。机制守卫已用"输出完全相同但退回稠密 `filter2D`"的模拟回归实测会响——只有它失败，28 个逐位用例照常通过。全量 **124 passed**。

**唯一语义代价**：拖影方向从任意角收窄为 `0°/90°`。若遇到拖影方向确实任意的场景，设 `blur_axis_aligned: False` 即可完全回到原行为。

---

## 2026-09-13：消除"隐式 re-export"脆弱耦合（配置校验不随 lint 优化而失效）

### 问题

`base.py` 从 `online_degrade` 导入 `_WEATHER_TYPES` / `_OCCLUSION_TYPES`，**自己一次都没用**，唯一用途是让 `augment.py` 的 `from ultralytics.data.base import ...` 生效——即"隐式 re-export"。Ruff 报 `F401 imported but unused`。而"拼错立即报错"这个校验**恰恰只在这两个常量存在时才成立**——一旦被删，`weather_types="rian"` 不再于构造期抛错，**退化成运行时随机兜底**：不报错、不掉指标、无从发现。任何一次 `ruff --fix` 或 IDE"优化导入"都会触发。

### 修复

- `augment.py` 顶层直接 `from ultralytics.data.online_degrade import _OCCLUSION_TYPES, _WEATHER_TYPES`，删除函数内的中转导入，并注释说明不得再走 base；
- `base.py` 删除这两个名字，并**加注释说明为什么不能加回来**；
- 不使用 `# noqa: F401`（那只是让告警闭嘴，脆弱耦合还在）；直连后 `online_degrade` 只依赖 stdlib + numpy + cv2，不参与任何导入环，校验语义**逐字不变**（仍是同一个 frozenset 对象，无副本漂移风险）。

### 验证

全仓库 `ruff --select F401` **6 → 4 条**（剩余 4 条是 `pytools/*` 里真正死掉的 `os` 导入）。新增 4 项测试：白名单与 `online_degrade` **同一对象**；`base.py` 不得存在"导入了却从不使用"的 `online_degrade` 名字（AST 静态检查，故意与 Ruff F401 重复）；`weather_types="rian"` / `occlusion_types="rectangle"` **必须在 `build_yolo_dataset` 阶段抛 `ValueError` 且列出全部合法取值**。守卫有效性已实测（临时把两行加回 `base.py` 守卫会响）。**86 passed**。

---

## 2026-09-13：warnings 收窄评估——决定不改（记录在案）

审查建议把 `detect.py` / `export.py` 的一刀切 `warnings.filterwarnings('ignore')` 收窄成按类别过滤。**经评估后决定保留现状**：

- 这两个脚本是**带本机硬编码路径的一次性运行脚本**，既不属于 `ultralytics/` 库，也不走 `train.py` / `val.py` 管线；其 warnings 过滤属"本机调试便利"，为一对一次性脚本引入跨模块的收窄工具，新增的耦合大于收益；
- 位置订正：`pytools/_mock_val_slice.py` 实测**没有任何 warnings 代码**（已从清单移除）；全仓库裸 ignore 仅 `detect.py:2` / `export.py:2` 两处；
- 残余风险知情接受：这两个入口上的 `RuntimeWarning`（NaN / 除零）仍会被静默吞掉；缓解是运动模糊核的除零保护为**无条件守卫**（不依赖调用方 warnings 策略）；
- **重开条件**：若这两个脚本被纳入正式管线或路径改为参数化，应重新评估；
- 顺带订正：`online_degrade.py` 原注释引用 "train.py's blanket filterwarnings" 已失实（train.py 早已按类别收窄），改为「仍安装裸 ignore 的入口（detect.py / export.py）」——纯注释订正，无行为变化。

---

## 2026-09-13：32 项审查问题修复逐项记录

整改顺序：严重 → 中等 → 轻微。代码版本 `ultralytics 8.4.137`。验证方式为独立核验（代码符号 + 配置断言 + pytest + 真实数据端到端复现，未复用报告自身证据），总体结论：**报告所述 32 项（6 严重 + 10 中等 + 16 轻微）修复均已正确落地**。

### 严重项

**`self.ims` 缓存无淘汰路径 → 每 worker 内存线性增长（外推 29.3 GB/worker）**

- 修复：新增 `_ims_keys: dict[int, None]`（字典插入有序性作 FIFO）+ `_ims_cap`；新增 `_remember_ims(i)`，`load_image` 写入 `self.ims[i]` 后调用，仅超容时淘汰最老条目并把 `self.ims[j] / im_hw0[j] / im_hw[j]` 置 `None`。
- 验证：触发条件下常驻峰值 = 7 = cap（原无界）；扩展池全开 E2E 常驻进一步为 0。

**退化增强在原始分辨率执行后才降采样（实测 11.5× 可避免开销）**

- 修复：新增配置键 `degrade_max_side`（`0`=auto(`2*imgsz`) / `>0`=显式像素 / `<0`=禁用）；新增 `_degrade_max_side()` 与 `_degrade_frame(img_index) -> (img, scale)`；blur/weather/occlusion/ratio 四分支统一取其返回帧，并按 `scale` 等比缩放自身参数（PSF 长度与 σ、雨线长度）。`degrade_max_side` 必须在 `v8_transforms` 显式赋值给 dataset，否则 `getattr` 永远落默认值、表现为"配了没反应"。
- 验证：imgsz=320 E2E 全部 `_degrade_frame` 调用输出最长边 ≤ 640（=2×imgsz）；模式切换实测 `-1`→1920/scale 1.0、`0`→640/0.3333、`512`→512/0.2667、`960`→960/0.5。

**weather 噪声/雾用 float64 全尺寸临时数组（峰值 824 MB）**

- 修复：噪声改分块就地累加 float32 累加器（`row_bytes = max(1, w*ch*4)`；`step = max(1, (4<<20)//row_bytes)`）；雾链式浮点表达式改 `cv2.convertScaleAbs(img, alpha=t, beta=200.0*(1.0-t))`；显式 `.astype(np.float32)` 保证输出与修复前逐位一致。
- 验证：雾 549 MB→36 MB（15×）；噪声 824 MB→180 MB、封顶分辨率下 73.7 MB；雨 72 MB；噪声输出逐位一致。

**双口径验证把 checkpoint 序列化成本整倍翻番**

- 修复：拆 `_build_ckpt_payload()`（构造一次与指标无关的 payload）+ `_dump_ckpt(payload, metrics, fitness, best_fitness=None)`；两处落盘（`best/last` 与 `best_whole/last_whole`）复用同一 payload；删除 `_serialize_ckpt`。
- 收益：每 epoch 省 1 次全模型 deepcopy + 1 次优化器状态 deepcopy + 1 次全量 pickle。

**`default.yaml` 默认开启全部切片验证开关**

- 修复：`val_slice_enable` / `val_slice_all_tiles` / `val_slice_dual_metric` 全部 `True` → `False`。
- 验证：`DEFAULT_CFG` 三项均为 `False`；默认验证耗时 ×5→×1，`metrics/mAP*` 恢复整图口径、与上游可比。

**`_class_wise_nms` 逐类 Python 循环 + 逐类布尔掩码**

- 修复：替换为 `torchvision.ops.batched_nms(boxes, conf, cls.long(), iou_thres)`。
- 验证：源码已无逐类循环。

### 中等项

| 问题                                      | 修复                                                                              |
| ----------------------------------------- | --------------------------------------------------------------------------------- |
| 每轮验证重复计算整图 GT                   | `_gt_orig_pixels` 加 `_gt_cache` 记忆化（以 `im_file` 为键）                      |
| Mosaic buffer 记账 O(n)                   | 由 `list` + `pop(0)` 改 `deque(maxlen=...)`，记账 O(n)→O(1)                       |
| 原图 LRU 是 FIFO、容量不覆盖 compose 跨度 | 改 `OrderedDict` + `move_to_end` + `popitem(last=False)` 的 LRU；容量 `max(4, …)` |
| 缓存失败兜底通道不可达                    | `SliceValDataset.__getitem__` 加真实 try/except（含 `LOGGER.warning`）            |
| 验证子集抽样跨重建抖动                    | 改用 `zlib.crc32` 派生种子的 `random.Random(seed).sample(...)`，跨数据集重建确定  |
| `compose_max_side` 未传到 dataset         | 在 `v8_transforms` 中显式传递（与退化工作分辨率上限同一处）                       |
| 每验证 pass 完整重建数据集                | 以 GT 记忆化 + 子集确定性降低代价，不再重复计算                                   |
| 极端长宽比先撑大再缩小                    | 经 `_degrade_frame` 的工作分辨率上限一并封顶                                      |
| `rect_shape` 隐式依赖扩展索引             | `label["rect_shape"] = self.batch_shapes[self.batch[img_index]]` 显式化（2 处）   |
| occlusion 段数与框数不匹配静默截断        | 改 `LOGGER.warning` 显式告警                                                      |

### 轻微项

| 问题                       | 修复                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| base.py 单文件过大         | 抽出 `ultralytics/data/online_degrade.py`（退化算子）与 `online_io.py`（保存工具）；base.py 1978→1916 行                                                                                                                                                                                                                                                                                                                |
| 在线默认值散落、易漂移     | `_ONLINE_DEFAULTS`（dict）+ `_online_default()` 成为唯一真源；新增测试断言与 `DEFAULT_CFG` 一致（零漂移）                                                                                                                                                                                                                                                                                                               |
| 死配置键                   | 删除 `slice_transform`（default.yaml 未注册、从未被读取）                                                                                                                                                                                                                                                                                                                                                               |
| cfg 缩进漂移               | `CFG_FRACTION_KEYS` 错缩进修复；`CFG_STR_KEYS` 悬挂对齐改规范风格                                                                                                                                                                                                                                                                                                                                                       |
| 磁盘预取不可配             | 新增 `prefetch_factor` 键（default.yaml 值 2 + 注释；注册 `CFG_INT_KEYS`；`build_dataloader` 仅 `nw > 0` 时传入）                                                                                                                                                                                                                                                                                                       |
| 生产代码/配置残留工单标签  | 清除，保留技术说明（复查发现 3 处残留，已补清）                                                                                                                                                                                                                                                                                                                                                                         |
| assert 风格不统一          | `slice_geometry` / `OnlineSlice.__init__` 的 `assert` 改 `raise ValueError(...)`                                                                                                                                                                                                                                                                                                                                        |
| f-string 动态属性名易拼错  | `_save_annotated` 改显式 `_save_state` + `_SAVE_BRANCHES` 白名单先校验                                                                                                                                                                                                                                                                                                                                                  |
| 懒创建字段形状不确定       | `__init__` 显式声明懒创建字段，保留懒计算但对象形状确定                                                                                                                                                                                                                                                                                                                                                                 |
| 段基元组未真正发挥作用     | 删除 7 个位置局部变量，下游直接具名读取段基字段                                                                                                                                                                                                                                                                                                                                                                         |
| 手工重建固定键集合丢未知键 | `_build_ratio_sample` / `_build_compose_sample` 统一"`deepcopy` 后覆写"，不再静默丢弃未知 label 键（如 YOLO-World 的 `texts`）                                                                                                                                                                                                                                                                                          |
| 语义化命名缺失             | `_blur_at`→`_build_blur_sample`、`_weather_at`→`_build_weather_sample`、`_occlusion_at`→`_build_occlusion_sample`、`_ratio_at`→`_build_ratio_sample`、`_compose_at`→`_build_compose_sample`、`_origin_at`→`_build_origin_sample`；`oc_boxes`→`occluder_boxes`、`wtype`→`weather_type`、`otype`→`occlusion_type`、`sdir`→`save_dir`、`cdir`→`branch_dir`、`fname_body`→`file_stem`、`sb`→`segment_bases`（测试同步更新） |
| 核心算子/不变量零测试覆盖  | 测试 31→39 项：`self.ims` 有界性、退化封顶与参数缩放、噪声分块不整帧、LRU 容量与命中顺序、验证子集跨重建确定性、occlusion 段数告警、`_ONLINE_DEFAULTS` 与 `DEFAULT_CFG` 一致性                                                                                                                                                                                                                                          |
| 每次写入重复 mkdir         | `OnlineSlice._save_tile` / `Mosaic._save_mosaic` 改复用 `online_io._ensure_dir`                                                                                                                                                                                                                                                                                                                                         |
| 置 None 副作用未文档化     | `_ImageCache.__init__` 对传入 `images` 列表的置 `None` 副作用已文档化                                                                                                                                                                                                                                                                                                                                                   |
| 热路径函数内导入           | `import io`、`import torchvision`、`SliceValDataset`、`slice_geometry` 等上移模块顶部；确有约束的加注释说明                                                                                                                                                                                                                                                                                                             |

### 验证证据汇总

- 回归测试：`tests/test_online_augment.py` **39 / 39 通过**；
- 配置一致性：`DEFAULT_CFG` 共 **188 键**；`_ONLINE_DEFAULTS` 与 `DEFAULT_CFG` **零漂移**；`val_slice_enable/all_tiles/dual_metric` 全 `False`；`degrade_max_side`/`prefetch_factor` 已注册 `CFG_INT_KEYS`；
- 真实数据端到端（8 图、全分支开启、imgsz=320）：**82 扩展样本、0 失败**；退化调用全部封顶 ≤ 2×imgsz；
- 遗留与后续建议：`_ensure_dir` 缓存永不失效（已留 `_forget_dir` 钩子）；配置键"三处齐动"备忘（default.yaml 值 + `CFG_*_KEYS` 注册 + 实际传递点，缺任一处都会静默失效——`degrade_max_side`/`prefetch_factor` 均踩过）；base.py 仍 1916 行，可进一步迁移 `SliceValDataset`。

---

## 2026-09-12：功能级失效 + 配置死键 + 护栏加固

### val_slice_enable 在训练流程中静默失效（切片验证从未执行）

- **根因**：`DetectionTrainer.get_validator()` 把**预建的整图** `test_loader` 交给 validator；验证流程只在"请求模式 ≠ 当前模式"时置 `dataloader=None`。用户一开始就开 `val_slice_enable` → `True == True` → 不重建 → 永远复用整图 loader，而切片数据集包装只存在于 `DetectionValidator.get_dataloader`，于是切片验证一次都没跑过。该缺陷由前一轮的重构引入。
- **修复**（`engine/trainer.py`）：重建条件补上第二维——请求切片、但当前 loader 不是 `SliceValDataset` 时同样重建：`needs_rebuild = bool(slice_enable) != cur_enable or (bool(slice_enable) and not isinstance(getattr(prev_loader, "dataset", None), SliceValDataset))`。
- **连带修复**：
  - `build_dataset`：`val_slice_enable=True` 时 val 侧 `rect=False`，使双口径的整图参照与切片口径**同为 rect=False**（此前是 rect=True 整图 vs rect=False 整图，双口径形同虚设）；
  - `final_eval`：验证流程会把预建整图 loader 恢复回来，收尾的 "Validating best.pt" 又变回整图口径——现改为 `val_slice_enable=True` 时 `dataloader=None` 强制重建，收尾验证与训练期口径一致。
- **实证**（1 epoch 冒烟 + 函数级插桩，`val_slice_ratio=0.5`、`val_slice_all_tiles=True`、`val_slice_dual_metric=True`）：主 pass 用切片 loader（9 tile）、dual 参照 pass 用 `YOLODataset(rect=False, n=3)`；修复前主 pass 进度 `1/1`、无 `val-slice:` 行、切片数据集构造探针从不触发。

### save_model 重复序列化（重构遗留死代码）

`save_model` 自己算了一遍 EMA 快照（deepcopy + half + criterion 剥离 + nan*to_num*）却从未使用——序列化函数内部又完整重算一次。已删除该本地快照块（**保留**对 `self.ema.ema` 的原地 NaN 污染修复），每 epoch 省一次全模型 deepcopy。

### slice_raw_cache_size 死配置（LRU 容量恒为 2）

- **根因**：`base.py __init__` 用 `getattr(self, "slice_raw_cache_size", 2)` 读取，而该属性由 `v8_transforms` 在 `__init__` **之后**才写入 → 恒取默认 2；设为 0（文档语义"关闭"）同样无效，`cache='ram'` 告警里"调大它可提速"的建议也不可生效。
- **修复**：`base.py` 改为从 `hyp` 读取（与 `v8_transforms` 同源），`v8_transforms` 同步把该键镜像到 dataset 上便于自描述。
- **实证**：`slice_raw_cache_size=8` → `ds._raw_cache_size == 8`；`=0` → `0`（关闭）。

### 切片主路径 resize 缺钳制（第三处 resize）

切片分支此前自带一段与共享收尾平行的 resize，唯独漏了 `min(..., imgsz)` 钳制。现直接复用共享收尾 `_finalize_label`，三处 resize 语义归一。

### 消除验证侧切片双真值源

删除构造期缓存的 `self.val_slice_on`（训练期验证会翻 `args.val_slice_enable`，缓存值必然过期），统一改用动态判断 `self._val_slice_active()`。

### results.csv 表头/列数漂移

表头只在文件不存在时写入，而 `check_resume` 保留旧 csv；metric 集合一旦变化（如开启双口径追加 7 个 `whole_*` 列），后续行会宽于表头、错位且 `plot_results` 误读。现在写入前校验列数，不一致时按**旧表头把已有行重映射**到新表头（缺失列留空）并告警。

### 其他修复

| 问题                          | 修复                                                                                                                                                       |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 注释声称的分组采样不存在      | `base.py` 注释订正：`data/build.py` 并不存在 `GroupedRandomSampler`；仅保证**索引空间**相邻，`shuffle=True` 下采样顺序不保证相邻，LRU 命中率低于原注释暗示 |
| 背景配额耗尽注释失实          | `_emit` 注释订正：退回的是**原图**，不是被裁空的 tile                                                                                                      |
| 切片指标入口崩溃无提示        | `_update_metrics_sliced` 入口校验 `_slice_base_labels`，缺失时抛带说明的 `RuntimeError`                                                                    |
| 切片指标画布非方形静默算错    | 入口校验方形画布（`shape[2] == shape[3]`），非方形立即报错                                                                                                 |
| 尾批被丢弃 → 部分原图永不结算 | 切片模式强制 `drop_last=False`；`finalize_metrics` 对未结算原图告警并清空累积                                                                              |
| 副权重用错 fitness            | `_serialize_ckpt(metrics, fitness, best_fitness=None)` 参数化，`best_whole.pt` 写入整图口径的 `best_fitness_whole`                                         |

### 回归测试（tests/test_online_augment.py，23 → 31 项）

新增：验证 loader 重建四组合（整图+切片请求→重建 / 已是切片→复用 / 切回整图→重建 / 纯整图→复用预建）；`slice_raw_cache_size` 参数化（8/0/5）；results.csv 列数重映射；切片指标入口两个守卫；未结算原图清空。

### 验证记录

- `py_compile` 全过；`pytest` → **31 passed**；
- 1 epoch 冒烟：出现 `val-slice: 2/3 originals sliced`，切片数据集构造 2 次，主 pass 用切片 loader，dual 参照 pass 用 `YOLODataset(rect=False)`，`results.csv` 22 列表头与行数一致且含 7 个 `whole_*` 列；
- 非训练路径回归：`model.val(val_slice_enable=True)` 正常输出切片指标，`model.val()` 原生整图路径不变。

---

## 2026-09-12：审查修复批次——保存画框调试痕迹 + RNG 统一 + 遮挡类型防御

### 保存画框吞异常

`Mosaic._save_mosaic` 标注画框的 `except Exception: pass` → 改为 `except Exception as e: LOGGER.debug(...)`，吞异常但留 debug 痕迹。

### 雨线分支 RNG 混用

rain 线段用 `np.random`、`alpha` 却用 `random.uniform`——两套 RNG 使复现实验需同时 seed 两处 → 统一为 `np.random.uniform(0.2, 0.6)`。

### 遮挡类型防御初始化 + 命名订正

1. 未选中分支（整图直通）补 `otype = "none"` 防御初始化（该路径本不使用 otype，纯防御，杜绝未来引用 NameError）；
2. `_apply_occlusion` 局部变量 `rng_color` 实为**颜色值**而非随机数生成器 → 改名 `oc_color`（7 处）。

### set_epoch 宽捕获加 debug 日志

`except Exception: pass`（worker 场景下共享内存不可写，注释已说明）→ 保留宽捕获但 `LOGGER.debug(...)`，不再无痕吞掉。

---

## 2026-09-12：遮挡覆盖率并集化 + 段基缓存（性能 + 结构）

### 遮挡并集面积精确计算（\_occlusion_at 布尔掩码 → 矩形交并数学）

原实现每样本分配 h×w 布尔掩码（4000×3000 ≈ 12 MB）只为算 1~3 个遮挡块的并集覆盖面积。新增模块级 `_union_area(rects)`：x 轴离散化扫描 + y 区间合并，O(k² log k) 精确计算整数矩形并集面积，像素语义与旧掩码完全一致（300 组随机用例对拍通过）。`_occlusion_at` 改为将每个遮挡块裁剪到目标框后求并集面积，遮挡框本身已钳制在图内，无需图像边界参数。

### 段基缓存 + 版本戳（后修订）

`_segment_bases()` 每个 `__getitem__`/`__len__` 都重建（6 个 getattr + 列表）→ 拆出 `_compute_segment_bases()` 并缓存。**但"首次调用即固化"的前提不成立**（开关在 `__init__` 之后才挂上），已修订为版本戳方案（键 = `tuple(_segment_lengths())`）——详见 2026-09-14 段边界版本戳条目。

### 验证记录

- `_union_area` 与旧掩码方案 300 组随机矩形对拍**逐组一致**（初版区间合并漏加最后一段区间，已修正）；
- `_occlusion_at` 端到端：大遮挡块下目标框被剔除、buffer 记账正常；
- 回归 `pytest` → **23 passed**。

---

## 2026-09-12：共享收尾补 resize 钳制（与 load_image 语义一致）

### 背景

`load_image` 对 resize 结果有 `min(math.ceil(w0*r), self.imgsz)` 钳制，而 `_finalize_label`（所有在线分支 blur/weather/occlusion/ratio/compose 的共享收尾）没有——浮点误差下 `resized_shape` 可能到 imgsz+1。影响：≤1px 偏差对训练几乎无感，但六条在线分支与原版路径产出的 `resized_shape` 语义不一致，后续 LetterBox/坐标换算的边界行为不可预期。

### 修复（ultralytics/data/base.py，一行）

`_finalize_label` 的 resize 行补同样的钳制：`min(math.ceil(w1 * r), self.imgsz)` / `min(math.ceil(h1 * r), self.imgsz)`。

### 验证

1000×1000 → imgsz=1280 升采样 → resized_shape (1280,1280) 不超；3000×2000 降采样 → (1280,854) 长边钳制；ratio_pad 随之正确；回归 **23 passed**。

---

## 2026-09-12：buffer 记账 7 处重复收敛为单一方法

### 背景

`if self.augment and self.cache != "ram": self.buffer.append(index); if 1 < len(...) >= max: pop(0)` 这段 FIFO 记账在 `_build_origin/blur/weather/occlusion/ratio/compose` 各 1 处 + `get_image_and_label` 1 处共 **7 处逐字重复**。buffer 策略一旦调整（容量、淘汰规则）需同步 7 处，漏一处就是又一个 buffer 记账 bug——本项目历史上最痛的 bug 类别。

### 修复

新增 `_touch_buffer(self, index)` 单一方法，7 处调用点全部替换。**语义逐位不变**：`1 < len >= max` 在 len 达到 max 时立即淘汰最旧条目（buffer 保持 max-1 个元素）。

**刻意保留**：`load_image` 内还有一处同源块——它带 ims 缓存清理副作用（pop 后 `self.ims[j] = None`）且仅在不切片 + 非扩展池时执行，**不属于**可统一进 `_touch_buffer` 的标准形态，保持原样。

### 验证

`_touch_buffer` 行为：append 达到 max 即淘汰最旧；augment=False / cache=='ram' 时零副作用；7 处调用点、load_image 块未动；回归 **23 passed**。

---

## 2026-09-12：保存/读取通道 unicode-safe 化 + 写失败不消耗配额

### 验证侧兜底读图通道用非 unicode-safe 的 cv2.imread

- 问题：缓存失败重试通道用的是原生 `cv2.imread`，而同文件顶部已导入 patches 的 unicode-safe `imread`；本项目实测 `cv2.imwrite` 在非 ASCII 路径静默失败；
- 影响：数据集路径含中文时该通道必然返回 None → 抛 FileNotFoundError，验证无法进行；
- 修复：改为 `imread(label["im_file"])`（patches 版）。

### 保存图用原生 cv2.imwrite 且不检查返回值

- 问题：`OnlineSlice._save_tile` 与 `Mosaic._save_mosaic` 两处保存都没走 base.py 已封装的 unicode-safe 写入，且不检查 `cv2.imwrite` 返回值——`self._saved += 1` 无条件执行；
- 影响：①中文保存目录下静默不保存；②写盘失败（磁盘满/权限）也照样消耗配额并计数，用户无感知；
- 修复：augment.py 顶部导入 patches 的 `imwrite`；两处保存改为：写失败 → `LOGGER.warning`（含路径与原因提示）+ `return`（**不递增 \_saved、不登记去重 key**，配额不消耗且后续可重试）；写成功 → 原逻辑。

### 验证

失败分支（monkeypatch imwrite→False）：不写文件、计数不增、去重 key 不登记、打印 WARNING；成功分支：文件落盘、计数递增、去重生效；回归 **23 passed**。

---

## 2026-09-12：在线增强单元测试与 7 段布局脱节，回归保护恢复

### 背景

`tests/test_online_augment.py` 仍断言 keep_origin 解耦**之前**的旧布局——期望 `_n_per()` 返回 5/6/7/8（旧公式），实测返回 4 → 旧测试全部 FAIL；且索引空间布局（本项目踩过"len 与可解码范围漂移"坑的领域）失去守护。

### 修复（测试重写，实现零改动）

1. `_make_dataset` 补齐全部 7 个分支开关，默认全开；
2. 段边界断言改用 `_segment_bases()` 直接对照：`base=4N、origin=4N、ratio=5N、blur=6N、compose=8N、weather=8N+ceil(N/4)、occlusion=9N+ceil(N/4)、total=10N+ceil(N/4)`（注意字段语义是**段起点**，origin 段紧跟 base 段，故 origin==base）；
3. `_n_per` 三态用例：emit_all→4 / 非 emit_all→1 / 切片管线关→1；
4. 开关组合用例：no_compose / keep_origin_only（5N）/ emit_all_only（4N）/ no_slicing（base=N 且 origin 自动抑制）/ weather+occlusion 段长度；
5. 删除对已不存在方法的引用。

### 验证

`pytest tests/test_online_augment.py -q`：**23 passed**（旧测试 9 项 FAIL 全部消除）。期间发现并确认 `_segment_bases` 字段语义为"段起点"而非"累积端点"——实现本身正确，错误仅在我方初版测试的期望公式。

---

## 2026-09-12：验证侧 all_tiles=False 真实实现"每图随机 1 片"

### 背景

`SliceValDataset._build` 中 `slice_it = bool(self.all_tiles) and (...)` 使 `all_tiles=False` 时切片恒不触发，所有验证图落入整图直通分支——与文档承诺的"False=每图随机 1 片(快速验证)"不符。用户设 False 想快速验证切片收益，实际效果与关闭几乎等价：**调参反馈失真、切片收益被系统性低估、且不报错极难察觉**。

### 修复

1. `all_tiles=False` 且该图被选中时：`random.choice(slice_geometry(...))` 取 2×2(+overlap) 切片中的**随机 1 片**，`counts=1`——快速验证语义真实生效；
2. `all_tiles=True` 路径不变（4 片全量）；
3. `val_slice_ratio<1` 时新增日志说明实际产出（哪些图切片、哪些整图直通），避免部分图整图直通时的调参反馈失真。

### 验证

单测（mock 4 图 4000×3000）：`False`→len=4 且每图为真实子片；`True`→len=16；`ratio=0.5`+`False`→2 片随机单 tile + 2 整图 + 日志正确；端到端（`_mini_val_set` val 3 图）验证正常完成。

---

## 2026-09-12：5 个性能/结构问题修复

| 问题                                                                                                          | 修复                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 雨线逐根 `cv2.line` Python 循环（density=0.15 × 4000px ≈ 600 次调用/样本），比向量化 haze/noise 慢 1-2 个量级 | 向量化生成全部线段端点，按厚度随机分组后用 2 次 `cv2.polylines` 一次画完（调用次数 600 → 2）；线段数量/位置/长度/厚度/透明度分布语义不变（实测 1500×2000、n=300 提速 2.0x）            |
| `np.quantile` 对全图（4000×3000×3 ≈ 1200 万像素）每样本排序取遮挡色，纯浪费 1-2s                              | 大步长均匀采样（`flat[::37]`，步长为素数、与行宽互质覆盖全图任意偏移，约 32 万像素）后再取 25 分位；实测提速 22.2x，分位均值漂移 0.33 灰阶（可忽略）                                   |
| 切缝偏移 32 候选 × 2 轴的 Python 列表推导（逐候选内层求和）                                                   | 一次广播比较：`np.abs(cx[:,None] - (cands*w)[None,:]) < win_x/2` 后 `sum(axis=0)`；5 组随机目标与旧实现逐位一致（含 jitter 路径）                                                      |
| `import bisect` 在热路径函数内重复局部导入                                                                    | 移到模块顶部 stdlib import 区                                                                                                                                                          |
| 保存块触发条件四种，行为不一致                                                                                | 统一为"只看各自 save_dir"：blur/ratio/weather/occlusion 均 `if sdir:`；compose 触发不再要求切片变换；新增数据集级全局画框开关 `slice_save_annotated`（无切片变换时各分支仍可按它画框） |

**验证**：单测（雨线输出一致+计时 / 采样分位误差 0.33 / 切缝逐位一致）+ 训练冒烟（`slice_prob=0.0` 即无切片管线、其余全开 + 五个保存目录全设置）：训练正常完成（50 样本），各保存目录实际写出带框图片——blur 16 / ratio 8 / weather 14 / occlusion 13 / compose 2（weather/occlusion 超过段长是既有设计：保存 key 含随机类型，buffer 重访同索引且类型不同时不走去重）。验证"无切片也能按 save_dir 保存"的新行为生效。

---

## 2026-09-12：9 个代码质量问题修复

| 问题                                                                                                           | 级别 | 修复                                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 多遮挡块重叠时覆盖面积重复计入 → 目标可能被超阈值提前误剔除                                                    | P0   | 改布尔掩码并集：一次性画出全部块，目标框内求和即"交集并集面积"，重叠区只计一次（块数 1~3，开销可忽略）                                                                      |
| 缓存失败后同一路径重试必再失败，随后以 None 切片崩 TypeError                                                   | P0   | 缓存失败改走独立 imread 通道重试，再失败则带路径 `FileNotFoundError` 明确报错；删除 `_build` 未用 import                                                                    |
| `weather_types` / `occlusion_types` 拼错不报错，静默落入默认分派分支                                           | P0   | 构造期白名单校验，非法值立即 `ValueError`；空串回退默认类型（语义不变）                                                                                                     |
| `oc_area_sum` 累加后从未使用（3 行死代码）                                                                     | P1   | 删除                                                                                                                                                                        |
| 双口径验证就地改写配置 + `dataloader=None`，无 try/finally 恢复——任一 pass 抛异常则后续 epoch 验证口径永久漂移 | P1   | 抽 `_run_val(slice_enable)`：try/finally 保证两种状态无论成败都恢复；仅 mode 变化时强制重建 loader（**⚠️ 该重建条件有缺陷，已由 2026-09-12 功能级修复条目补正**）           |
| 5 处画框保存块 + 6 处 resize 收尾块几乎逐行同构（约 300 行）                                                   | P2   | 抽 `_save_annotated(...)`（统一建目录/画框/跨 epoch 去重/分分支限额/写盘成功才计数）与 `_finalize_label(label, img)`（统一 resize + ori_shape/resized_shape/ratio_pad）     |
| 六段掩码构建逐段同构                                                                                           | P2   | 表驱动 `_mask_specs(n)`：每行 (mask 属性, ratio 属性, 开关谓词, 样本数)；compose 组级 `(n+3)//4` 与其余原图级 `n` 一并入表                                                  |
| 每个参数默认值三处 fallback（当前值一致但结构上是漂移隐患）                                                    | P2   | `v8_transforms` 全部 `getattr(hyp, k)` **去 fallback** → default.yaml 成为训练侧唯一默认来源（缺键立即 AttributeError）；base.py 保留兜底并加"必须与 default.yaml 同步"注释 |
| 段基返回 8 元组，解包点 3 处易错位                                                                             | P2   | 改 `SegmentBases(NamedTuple)`（字段序 == 旧元组序，位置解包兼容）                                                                                                           |

**附带修复**：`plot_results` 奇数列越界——双口径验证把 `whole_*` 指标写入 results.csv（27 列），绘图键 17 个，而 `plt.subplots(2, len(columns)//2)` 只建 16 个子图 → `index 16 out of bounds`，results.png 永远不生成。改 `(len(columns)+1)//2`，results.png 恢复生成。

**验证**：完整训练冒烟（8 原图、全增强开启、双口径验证、1 epoch）：`Online augment: 82 training samples from 8 images (4 slices + 1 origin + 1 ratio + 2 blur + N/4 compose + 1 weather + 1 occlusion per image)`，len=82 与区段布局完全一致，41 batch 训练完成，双口径验证两遍均成功，best/last.pt 正常保存（⚠️ 该记录中的"切片/整图"两遍实为两组整图指标，受 val_slice 静默失效影响切片数据集从未被构造，数字不可用）；单元测试：遮挡并集、非法类型、缺键、保存建目录/去重/计数/限额 全通过。

---

## 2026-09-12：跨进程掩码语义修正（机制澄清 + 文档同步）

### 背景（问题解释）

**一句话**：训练循环每个 epoch 开始时用 `set_epoch(epoch, epochs)` 重建"哪些原图启用切片/合成/模糊等增强"的**掩码**（实现 `slice_ratio` 等 epoch 级比例与 `close_aug_epoch` 后期全关）。这个重建只发生在**主进程**的 dataset 对象上；真正处理样本的是 **DataLoader worker 进程**里的 dataset 副本——两者间的同步机制旧描述有误（"掩码/计数器经 fork 继承传播给 worker"），一旦开启 `persistent_workers=True`，worker 永远用初始快照，**掩码、close_aug_epoch、计数器全部静默失效**。

**进程模型本质**：Windows 上 DataLoader 是 **spawn** 而非 fork，**不存在 fork 继承**。`InfiniteDataLoader` 在构造时一次性 spawn/fork 全部 worker（**早于任何 `set_epoch`**），worker 在启动那一刻从主进程 pickle 一份 dataset 快照，之后与主进程再无关系。

**为什么默认配置"碰巧正确"（假象）**：默认 `persistent_workers=False` 时，每个 epoch 结束 worker 被销毁，下个 epoch 重新 spawn → **重新 pickle 主进程 dataset**（此时主进程已重建过掩码）→ 看起来"掩码生效了"——实际是**重新投胎的假象**。

**开启 `persistent_workers=True` 后的静默失效表现**：worker 不销毁、永久使用初始快照 → epoch 级比例固定为初始值；`close_aug_epoch` 后期全关失效；背景配额不重置（行为随 epoch 漂移）。**训练不报错、loss 正常下降，但数据分布完全错，极难排查**。

### 修复（base.py）

1. **共享内存 epoch 通道**：`__init__` 构造 `multiprocessing.Value("i", -1)` 两个 int（`_mp_epoch`/`_mp_epochs`）；`set_epoch` 发布；
2. **惰性新鲜度校验**：worker 在 `get_image_and_label` 首行调 `_sync_epoch_masks()`，每样本一次加锁 int 读，`epoch != _mask_stamp` 才重建；
3. **确定性掩码重建**：`rng = random.Random(f"{crc32(文件列表)}:{epoch}:{epochs}")`——任何进程重建同一 epoch 得到**完全相同掩码**，无需跨进程传输掩码数组；
4. **worker 侧防自建**：惰性首次构建仅主进程，worker 只等通道发布，防 epoch 0 + epochs 缺失破坏 `close_aug_epoch` 语义。

### 结论

- **`persistent_workers=True` 现在可安全开启**：worker 不再依赖 pickle 快照，而是轮询共享内存通道；代价仅为每样本一次微秒级加锁 int 读；
- 本条目同时把 README / 项目说明 / 更新说明中 5 处"worker 经 fork 继承"旧描述修正为上述真实机制。

### 验证记录（Windows spawn + persistent_workers=True，单 worker，8 原图→32 样本，epoch=3）

| 阶段                         | 期望                 | 实测                                                   |
| ---------------------------- | -------------------- | ------------------------------------------------------ |
| epoch 0                      | 掩码 sum=4           | 32/32 样本 stamp=0、mask_sum=4 ✅                      |
| epoch 1                      | 掩码 sum=4、计数重置 | 31/32 stamp=1；i=0 一条 stamp=0（旧掩码）；pos 重置 ✅ |
| epoch 2（close_aug_epoch=1） | 掩码 sum=0（全关）   | 31/32 stamp=2、mask_sum=0 ✅                           |

**唯一遗留窗口（torch 固有竞态，不可消除）**：每 epoch 边界 ≤`num_workers` 个样本在 `set_epoch` 写入前已被 worker 处理（in-flight prefetch），使用旧 epoch 掩码——属 DataLoader 主进程 `set_epoch` 与 worker 并行处理的固有窗口，任何实现都无法完全消除；训练影响可忽略（32 样本中 1 个 ≈ 3%，且旧掩码也是合法掩码，仅增强配比偏差 1 个样本）。

---

## 2026-09-11：在线遮挡模拟（`occlusion_*`，语义遮挡块）

### 背景

无人机/航拍场景目标常被树冠、电线、阴影、云影部分遮挡，而训练集大多是"干净"目标。方案在内存中给每张被选中原图画语义遮挡块（rect=树冠/阴影随机矩形，stripe=电线/枝干细长条带近水平/垂直），标签不变（遮挡鲁棒的核心语义：目标仍要能检测），让模型学会"看到一部分也能认出目标"。

### 机制

与 blur/weather 完全同构：独立开关 `occlusion_keep` + epoch 级比例 `occlusion_ratio`（复用 `set_epoch` 掩码，原图级），`close_aug_epoch` 联动关闭，`occlusion_save_dir` 可选保存（画框/限数量/按图+类型跨 epoch 去重）。

- **绘制**：`_apply_occlusion` 在 ORIGINAL 分辨率上画块（先切片级放大收益同样适用于遮挡细节）；`occlusion_color=auto` 时采样图像暗 25% 分位均值作为块色，融入场景而非突兀黑块；
- **标签剔除**：`occlusion_max_cover`（默认 0.95）——目标被遮挡面积占比 ≥ 阈值则从标签剔除（完全被盖住的目标是纯噪声）；=1.0 时标签永不变；
- **区段**：`[8N+⌈N/4⌉+N, +N)` 在 weather 段之后；N=4 全开 = **41 张**混合样本池。

### 新增 9 个参数（默认值）

| 参数                       | 默认          | 说明                                                                   |
| -------------------------- | ------------- | ---------------------------------------------------------------------- |
| `occlusion_keep`           | `False`       | 独立总开关：每张被选中原图 +1 张遮挡图（区段 +N）                      |
| `occlusion_ratio`          | `0.5`         | epoch 级比例：选 `round(x*N)` 张原图做遮挡；未选中整图直通（len 恒定） |
| `occlusion_types`          | `rect,stripe` | 遮挡类型池，逗号分隔，每图随机抽 1 种                                  |
| `occlusion_blocks`         | `1`           | 每图遮挡块数（1~3）                                                    |
| `occlusion_size_ratio`     | `0.1`         | 单块面积上限（相对原图面积），防目标被完全盖住                         |
| `occlusion_color`          | `auto`        | `auto`=采样图像暗分位均值融入场景；或 `black`/`gray` 固定色            |
| `occlusion_max_cover`      | `0.95`        | 目标被遮面积占比 ≥ 阈值则从标签剔除；1.0=标签永不变                    |
| `occlusion_save_dir`       | `""`          | 遮挡图保存目录（画框/去重/限数量）；空=不保存                          |
| `slice_save_max_occlusion` | `None`        | 遮挡图保存数量上限；回退 `slice_save_max`；0=不限                      |

### 验证（20 张小集，device=cpu）

- 编译：`py_compile` 5 文件全过；
- 单元：len 80→100（+occlusion N）→120（+origin N）全对；`occlusion_ratio=0.5` 掩码 10/20 True；`occlusion_max_cover=0.0` 时目标全剔除（0 bboxes）；
- 端到端：occlusion-only 配置 1 epoch 跑通，日志 `Online augment: 40 training samples from 20 images (1 occlusion per image)`；`occlusion_save_dir` 保存 20 张画框图。

---

## 2026-09-11：目标感知切缝（`slice_center_bias`，center-biased slicing）

### 背景

固定 2×2 网格的切缝位置与目标位置无关，目标恰在切缝上会被劈成两半；中心约束/整框保留只能事后归属/过滤，改变不了"被切"的事实。方案把切缝按本图目标中心分布微移，让切缝落在目标最稀疏区间。

### 机制

目标中心投影到 x/y 轴，竖缝/横缝选在 `[margin, 1-margin]` 内目标最稀疏的候选位置。**几何关键**：tile 数/全图覆盖/重叠总量（`2*sw-w`）不变——正 bias 加宽左/上 tile、等量收窄右/下 tile，切缝精确移动 `bias*w` 且每像素仍至少属于一个 tile（初版实现曾用"两 tile 同时平移"，导致图像边缘区域无 tile 覆盖、目标整体丢失，已修正为宽度不对称方案）。

### 新增 3 个参数（默认值）

| 参数                | 默认    | 说明                                                                      |
| ------------------- | ------- | ------------------------------------------------------------------------- |
| `slice_center_bias` | `False` | 目标感知切缝总开关；False=固定网格，完全向后兼容                          |
| `slice_bias_margin` | `0.25`  | 切缝偏移窗口：只在 `[margin, 1-margin]` 内微移                            |
| `slice_bias_jitter` | `0.05`  | 每图每调用随机扰动切缝（相对原图边长），防同图每 epoch 同缝过拟合；0=关闭 |

### 联动

- 中心约束归属中线跟随偏置切缝（`w/2+bias*w`，不是图中心），否则归属与网格错位；
- `slice_overlap_ratio` 语义不变；`slice_ratio` 未选中图不偏移；`close_aug_epoch` 回退固定网格；
- `len` 恒定（仍每图 4 tile）；验证侧固定网格不受影响。

### 验证

- 单元（合成分布）：seam 线性偏移（+0.1·w 精确）、tile 尺寸对称（2800/2000）、全图覆盖保持；目标簇横跨中缝时被切目标中心 21/60 → 0/60；
- 真实小集（20 图 21 目标）：被切缝劈中目标 OFF=16 → ON=2（**减少 88%**）；
- 端到端：`slice_prob=1.0 + center_bias=True/False` 各 1 epoch（CPU）跑通，`Online augment: 80 training samples from 20 images (4 slices per image)`，len=4N=80 恒定。

> 后续修订（2026-09-14）：切缝抖动改为确定性 RNG（4 片共享网格），详见 2026-09-14 切缝确定性条目。

---

## 2026-09-11：修复 "Online augment" 日志漏报 weather 段

**现象**：`weather_keep=True` 且其他在线增强全关时，训练启动日志不打印 `Online augment: ... training samples from ... images (...)` 行。

**根因**：`YOLODataset.__init__` 的样本构成日志 `_parts` 列表漏了 weather 分支——weather-only 配置下 `_parts` 为空 → `if _parts:` 不成立 → 整行跳过（样本数 `__len__` 本身正确，只是日志行没打）。

**修复**：`_parts` 增加 `if self._weather_on(): _parts.append("1 weather")`，并更新注释示例为含 weather 的构成。

**验证**（20 张小验证集，weather_keep=True + 其余全关）：`train: Online augment: 40 training samples from 20 images (1 weather per image)`。

---

## 2026-09-11：在线气象退化（weather\_\*，雨/雾/噪声）

### 背景

提升航拍模型在恶劣天气（雨 / 雾 / 噪声）下的鲁棒性。与 blur 家族同构：每张原图生成 1 张退化图（类型从 `weather_types` 随机抽 1 种），标签不变，独立开关 + epoch 级比例（`weather_ratio`，复用 `set_epoch` 掩码机制，0.3~0.6 推荐——增强分布过宽会拖累干净场景精度）。

### 新增 9 个参数（默认值）

| 参数                     | 默认                | 说明                                                                      |
| ------------------------ | ------------------- | ------------------------------------------------------------------------- |
| `weather_keep`           | `False`             | 独立开关：每图 +1 张气象退化图（+N）                                      |
| `weather_ratio`          | `0.5`               | 原图级比例：每 epoch 选 `round(x×N)` 张做退化，未选中整图直通（len 恒定） |
| `weather_types`          | `"rain,haze,noise"` | 退化类型池（逗号分隔），每张图随机抽 1 种；空回退 haze                    |
| `weather_rain_density`   | `0.15`              | 雨线密度 = 雨线数量 / max(h,w)                                            |
| `weather_rain_length`    | `15.0`              | 雨线长度上限（像素），每根随机取 0.5~1.0 倍                               |
| `weather_haze_beta`      | `0.4`               | 雾浓度 [0,1)，大气散射 `I=J×(1-β)+A×β`                                    |
| `weather_noise_std`      | `15.0`              | 高斯噪声标准差（每通道独立）                                              |
| `weather_save_dir`       | `""`                | 保存目录（画框/限数量/按图+类型跨 epoch 去重）；空=不保存                 |
| `slice_save_max_weather` | `None`              | 保存上限；不填回退 `slice_save_max`；0=不限                               |

### 改动范围（6 个文件）

`base.py`（新增 `_apply_weather`：雨=20° 斜线半透明叠加、雾=大气散射、噪声=RGB 独立高斯；`__init__` 加 `_weather_mask`；`_weather_on()`；段布局 5→6 元组（weather 段在 compose 段之后）；路由与 `_weather_at`；`set_epoch` 加 weather 掩码；close 分支一并置 False；`__len__` +N；`_extended_pool_on` 加 weather）、`augment.py`（参数装配 + per-branch cap）、`cfg/__init__.py`（类型注册 +5 float/+1 bool/+2 str/+1 int）、`cfg/default.yaml`（参数块 + 中文注释）、`train.py`（参数块 + 注释）、README/项目说明（样本池 33→37、weather 小节）。

### 关键设计

- **纯内存生成**：`_apply_weather` 与模糊同层，全程不落盘（除非 `weather_save_dir`）；
- **标签不变**：退化不移动目标，bbox/cls 原样继承；
- **与 `close_aug_epoch` 联动**：close 分支 6 个掩码全 False；
- **验证（conda 恢复后补测，全过）**：像素级 16 项（形状/dtype 保持、强度单调、雾 β=0 与噪声 std=0 恒等、haze 钳制 [0,255]、灰度图兼容）；cfg 注册解析全过；端到端 69 图 1-epoch 真实训练全链路通过——`__len__=2N`、掩码恰 34/69、weather 段样本解码正常、`weather_save_dir` 产出 **69 个文件 == N** 且覆盖 rain/haze/noise 三种类型；
- **修复（验证中发现）**：`_weather_at` 保存分支原沿用 blur 的"切片变换存在"条件——切片关闭（`slice_prob=0`）时 `weather_save_dir` 不生效。已改为**指定 `weather_save_dir` 即保存**。

---

## 2026-09-11：修复验证侧切片三处缺陷

### 问题 1：训练内第二遍整图验证 `dataloader=None` 崩溃

**现象**：`val_slice_enable=True + val_slice_dual_metric=True` 训练时，主验证跑完后第二遍整图验证报 `TypeError: object of type 'NoneType' has no len()`。

**根因**（三层）：

1. validator 的 dataloader 重建代码原本只在**非训练（else）分支**执行——训练内验证直接复用 trainer 预建的 `test_loader`，从不重建；
2. trainer 预建的 `test_loader` 是**整图** loader，而切片包装只存在于 validator 的 `get_dataloader`——训练内验证的 val_slice **从未真正生效**；
3. dual-metric 第二遍把 `validator.dataloader=None` 想强制重建，但训练分支不重建 → `len(None)` 崩溃。

**修复**：dataloader 重建块移出 if/else 改为**无条件**（为 None 时经 `get_dataloader` 重建；训练分支 `self.stride = model.stride` 一并补上）；trainer 主验证前若 `val_slice_enable` 显式 `dataloader=None`（强制重建为切片 loader），dual 第二遍翻回整图重建，跑完恢复。

**验证**（20 张小验证集，CUDA 1-epoch）：主验证切片 20 张→20 batches（融合回 20 原图出指标表）、第二遍整图 5 batches，两遍均正常出指标。

### 问题 2：`model.val()` 传 dict args 时 val_slice 参数缺省值崩溃

`DetectionValidator.__init__` 读 val_slice 参数时 dict 分支无默认值 → `float(None)`。修复：五个 `.get(key)` 全部补默认值。

### 问题 3：`model.val()` 默认 `rect=True` 与 val_slice 冲突（空 tile 除零）

`model.val()` 默认 `custom = {"rect": True}`；rect 模式下 labels `shape=(0,0)` → 切片数据集拿到 (0,0) → 生成空 tile → 空图进 LetterBox 除零。修复：`DetectionValidator.get_dataloader` 在 val_slice active 且 `args.rect=True` 时**强制 rect=False 并重建 dataset**（切片推理依赖真实逐图尺寸）。

---

## 2026-09-11：验证侧在线切片评估（val*slice*\*，SAHI 评估）

### 背景

训练侧已在线切片（小目标放大），但验证侧整图直推——小目标被降采样糊掉，切片训练的 mAP 收益被低估，调参反馈失真。需求：验证阶段也做 SAHI 式切片推理（子图独立推理 → 坐标还原 → NMS 融合 → 与原图 GT 结算），并保存两套权重（主口径切片 mAP 驱动 `best.pt/last.pt`，参考口径整图 mAP 额外输出 `best_whole.pt/last_whole.pt`）。

### 新增 6 个参数（默认值）

| 参数                      | 默认   | 说明                                                                                 |
| ------------------------- | ------ | ------------------------------------------------------------------------------------ |
| `val_slice_enable`        | `True` | 总开关；False=完全回归原生整图验证                                                   |
| `val_slice_all_tiles`     | `True` | True=每图全部 2×2 子图推理；False=每图随机 1 片（快速验证）                          |
| `val_slice_ratio`         | `1.0`  | 每轮验证随机选 `round(x×N_val)` 张验证图走切片，其余整图直通                         |
| `val_slice_overlap_ratio` | `0.2`  | 验证侧切片重叠比例，建议与训练侧一致                                                 |
| `val_slice_nms_iou`       | `0.5`  | 跨切片重复框 NMS 融合 IoU 阈值                                                       |
| `val_slice_dual_metric`   | `True` | 双口径：切片 mAP 为主（fitness/早停/best.pt），整图 mAP 为参考（`whole_*` + 副权重） |

### 改动范围（7 个文件）

- `augment.py`：模块级 `slice_geometry(w, h, overlap_ratio)`（纯几何，返回 4 个 2×2+overlap 图块）；`OnlineSlice._grid` 改为调用它（训练/验证共用同一几何）；
- `base.py`：新增 `SliceValDataset`：包装原生验证 YOLODataset，复用 labels/transforms/collate_fn；`len=Σ每图片数`；`__getitem__` 返回子图 + 重映射标签 + `val_slice_meta`（orig_idx/k/offset/tile_shape/n_tiles/sliced）；
- `detect/val.py`：动态读 args；`get_dataloader` 按需包装；切片分支 `_update_metrics_sliced`（跨 batch 累加）→ `_finalize_sliced_orig`（NMS 融合 + 整图 GT 结算，`seen` 按原图计）；`_remap_boxes_imgsz_to_orig` 逆 LetterBox 还原；
- `trainer.py`：`last_whole/best_whole` 路径与 `best_fitness_whole/fitness_whole/whole_metrics`；`validate()` 双口径；`save_model` 参数化 + 双权重并排保存；
- `cfg/__init__.py` + `cfg/default.yaml` + `train.py`：注册与注释。

### 关键设计

- **验证侧切片不做目标过滤**（训练侧才有 min_retain/center/full_box_only/background）：每片全部推理、框全保留，重复框 NMS 融合；
- **双口径状态切换**：动态读 `args.val_slice_enable`（而非 `__init__` 固定），否则整图第二遍会误包装切片数据集；
- **`seen` 口径**：切片模式按原图数计数，`gather_stats` 不再覆盖；
- **两套权重各自带自己的 `train_metrics`**；`resume`/早停/续训主路径仍走 `last.pt`（切片口径），`best_whole.pt` 仅作对比参考。

### 验证

- py_compile 通过；纯 stdlib 数值验证 10 项全过（本机 conda 环境损坏，改用 base Python + 标准库）：`slice_geometry` 4 片/2400×1800/全覆盖/重叠 800px/小图退化；坐标还原 tile0 与 tile3 均精确；类内 NMS 融合去重；bbox 归一化换算（含越界裁剪）；
- 端到端待 conda 恢复后补跑（后续已跑通，见 2026-09-12 功能级失效修复条目）。

---

## 2026-09-11：close_aug_epoch — 训练后期关闭全部在线增强

### 背景

在线增强只有"全程固定比例"（`*_ratio`），没有类似原生 `close_mosaic` 的"训练后期自动关闭"时间维调度。需求（统一调度）：最后 N 个 epoch 把全部在线增强关闭，让模型在真实样本分布上微调收敛，缓解小数据集后期过拟合到增强分布。

### 语义

- `close_aug_epoch=N`：`set_epoch(epoch, epochs)` 在 `epoch >= epochs - N` 时把切片/比例/模糊/合成（后续含气象退化/遮挡）掩码全部置为**全 False** → 各区段"位置保留、内容替换"退回原图直通（len 恒定）；
- 与原生 `close_mosaic` 的关系：`close_mosaic` 只关 Mosaic；`close_aug_epoch` 管全部在线增强，两者可叠加；
- `close_aug_epoch=0`（默认）：不启用，完全向后兼容；
- lazy 路径（`epochs=None`）永不触发关闭。

### 与 patience 的关系（配置约束，重要）

`close_aug_epoch` 与原生 `close_mosaic` 一样是**纯时间驱动**（`epoch >= epochs - N` 才关闭），而 `patience` 是**纯指标驱动**（`(epoch - best_epoch) >= patience` 即早停）。两者正交但会互相影响：

- **早停会"杀死"末期关闭**：若 mAP 提前到顶，`best_epoch` 早，`patience` 耗尽点可能落在关闭点之前 → 训练早停，在线增强**全程开启**，`close_aug_epoch` 白设（原生 EarlyStopping 用 `fitness > best` 严格大于，持平同样消耗 patience）；
- **末期关闭会"救活"早停**：关闭增强后验证 mAP 常小幅上升（分布对齐）→ 刷新 `best_epoch` → patience 计时清零 → 训练被推迟甚至跑满；
- **配置原则（二选一）**：想让 `close_aug_epoch` 生效 → `patience` 设大或设 `0`（关闭早停）；想靠早停控时（快速消融）→ 末期关闭大概率不触发；
- resume 后两者都按修补后的新 `epochs` / 合并后的 args 重算（`stopper` 在 `__init__` 新建，`best_fitness` 从 ckpt 恢复）。

### 验证

纯 stdlib 提取真 `set_epoch` 源码测试 14 项全过：正常比例不受影响（ratio=0.5 精确选一半）、关闭边界精确（`epoch == epochs-N` 才是首个关闭 epoch）、关闭期掩码全 False、`epochs=None` 不关闭、`reset_counters` 照常调用。

---

## 2026-09-10：compose / ratio_pad / blur 比例控制（三个在线增强的 epoch 级比例）

### 背景

`slice_ratio` 已有（epoch 级、原图级精确比例）。需求：在线合成、在线比例调整、在线运动模糊也要同样的比例控制——每个 epoch 只让部分样本参与对应增强，缓解小数据集过拟合到特定增强分布。

### 设计（与 slice_ratio 完全同构）

| 参数                         | 粒度   | 语义                                                                   |
| ---------------------------- | ------ | ---------------------------------------------------------------------- |
| `compose_ratio` (默认 1.0)   | 组级   | 每 epoch 选 `round(x×⌈N/4⌉)` 组合成；未选中组退回组内第 1 张原图整图   |
| `ratio_pad_ratio` (默认 1.0) | 原图级 | 每 epoch 选 `round(x×N)` 张做比例调整；未选中直接整图                  |
| `blur_ratio` (默认 1.0)      | 原图级 | 每 epoch 选 `round(x×N)` 张做运动模糊（短+长**同命运**）；未选中整图×2 |

- **未选中位退回整图**：位保留、内容替换，`len` 恒定（与 slice_ratio 未选中原图整图、被拒背景退回原图同一原则）；
- **掩码由 `set_epoch(epoch)` 重建**（主进程 + 各 worker 经共享内存 epoch 通道惰性同步，各进程用确定性 RNG 重建完全一致的掩码，无 per-worker 漂移；`persistent_workers=True/False` 均安全）；
- `ratio >= 1.0` 或对应开关关闭 → 掩码 None（全量/关闭，零开销，完全向后兼容）。

### 验证（mock 5 场景全过）

1. `ratio=1.0`：掩码全 None、len 不变（旧行为）；
2. 精确比例：N=28、x=0.5 → slice/ratio/blur 各 14/28、compose 4/7，len 恒定；
3. ratio_pad 未选中保持原宽高比（2:1）、选中对齐 16:9/4:3；
4. blur 未选中不调用模糊（选中图×2 调用）；
5. compose 未选中组调用 `_build_origin_sample(index, group*4)`（退回组内第 1 张）、选中组不调用；
6. 开关关闭 → 对应掩码 None。

### 同批修复

| 问题                                                                            | 修复                                                             |
| ------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 切片背景计数器跨 epoch 不重置，背景片保留随 epoch 推进收紧，行为漂移            | `OnlineSlice` 新增 `reset_counters()`，`set_epoch` 每 epoch 调用 |
| 原图缓存自称 LRU 实为 FIFO（命中不刷新插入序，"drop oldest half" 是 FIFO 淘汰） | 命中时刷新插入序（真 LRU），注释标注                             |

### 同日修复：slice 关闭 + 扩展段开启时 load_image 越界

**现象**：`slice_prob=0.0`（切片关闭）+ `compose_keep=True`（合成开启）时训练启动即崩：`self.ims[j] = None` 报 `IndexError: list assignment index out of range`。

**根因**：buffer 存在两套记账——`load_image`（纯原版路径，无切片变换时激活）append **原图索引** `i`，满了按 `j` 清理 `ims[j]`；`_build_compose/blur/ratio/origin`（扩展段）append **扩展索引**（≥ n）。切片开启时 `load_image` 的 buffer 分支整体禁用，两套不会共存；**切片关闭 + 任一扩展段开启**时，`load_image` pop 到扩展索引 `j ≥ n` → `ims[j]` 越界。

**修复**：新增 `_extended_pool_on()`（keep_origin / ratio_pad / blur / compose 任一开启即为 True）；`load_image` 的 buffer 自管加守卫 `not self._extended_pool_on()`——仅纯原版模式自管；扩展池开启时 buffer 记账统一归 `get_image_and_label`（扩展索引），`load_image` 只保留 ims 缓存（原图索引安全）。

**补充修复（同一 bug 链条的后半）**：守卫生效后暴露出新问题——`slice关 + compose开` 时训练启动报 Mosaic `random.choices(dataset.buffer)` 的 `IndexError: list index out of range`。原因：base 段样本的 buffer 记账在 `get_image_and_label`（原条件"切片变换存在"才记账）与 `load_image`（被守卫禁用）之间出现真空，切片关闭时 base 段样本不再进 buffer，Mosaic 从空 buffer 抽样。修复：`get_image_and_label` 的 buffer 记账条件放宽为 `切片变换存在 or _extended_pool_on()`，与 `load_image` 自管守卫**互斥且覆盖全部分支**。

**验证**（mock 5 场景 + Mosaic choices 模拟全过）：A `slice关+compose开` 20 轮、B 纯原版 50 轮、C `切片开+compose开` 5 轮、D `slice关+blur/ratio开` 20 轮、E 纯切片开 5 轮——每轮 buffer 非空、choices 索引均在有效范围。

---

## 2026-09-08：被拒背景片退回原图（替代空片）

### 背景

无人机检测场景：目标小、背景占比高，`slice_all_tiles=True`（4 片全出）时每张原图约 3/4 的片是背景。原实现中，`slice_background_ratio` 配额不足的背景片被**裁成空片**进训练——但空片与配额内背景片在训练视角完全等价（同一块裁剪区域 + 空标签），负样本占比由切片几何锁死（约 67%），`slice_background_ratio` 实际调不动它。

### 改动

`OnlineSlice.slice_at`：配额不足时不再裁剪空片占位，直接退回整张原图（带全量 bbox）进池。

效果（N 原图，每张 1 正 3 背，ratio=0.3）：

| 池子构成             | 原实现  | 新实现                 |
| -------------------- | ------- | ---------------------- |
| 正片                 | N       | N                      |
| 背景片（真实负样本） | 0.3N    | 0.3N                   |
| 空片（纯负样本）     | 2.7N    | **0**                  |
| 退回原图（正样本）   | 0       | 2.7N                   |
| 负样本占比           | **67%** | **~23%**（实测 12.5%） |

### 要点

- `len` 恒为 4N 不变（位置保留、内容替换），不引入"len 不恒定"问题；
- 退回原图重复进池 = 隐式过采样正样本（每个副本独立走随机增强）；
- `slice_background_ratio` 恢复字面语义：真正控制训练中负样本占比；
- 建议配合 `slice_keep_origin=False`（退回原图已覆盖整图上下文，keep_origin 会冗余）。

### 验证

- ratio=0.3：8 样本 = 5 退回原图 + 1 背景片 + 2 正片，空片为 0；
- neg_ratio=-1：全部背景保留、无退回原图；
- 集成路径 `get_image_and_label` len=4N=8 恒定，bbox/cls 一一匹配。

---

## 2026-09-08：slice_ratio — 每 epoch 原图级精确比例切片（替代 slice_mix_ratio）

### 背景

原 `slice_mix_ratio` 是 per-sample 独立抛硬币（概率近似）：每 epoch 实际切片样本数随机漂移；`emit_all` 下同一原图 4 片各自独立决定，可能出现"同一张图 2 片切片、2 片不切"的怪状态。需求：**每 epoch 精确选 x 比例原图走切片**（原图级决策，4 片同生共死）。

### 改动（5 个文件）

- `base.py`：新增 `set_epoch(epoch)`：重建 `_slice_mask`，精确选 `round(slice_ratio × N)` 张**原图**走切片；`get_image_and_label` 切片判断由抛硬币改为查掩码；`__init__` 加 `self._slice_mask = None`；
- `trainer.py`：epoch 循环内、`sampler.set_epoch` 旁新增 `dataset.set_epoch(epoch)`（单机/多机均生效，`hasattr` 防御）；
- `augment.py` / `cfg/default.yaml` / `cfg/__init__.py` / `train.py`：参数 `slice_mix_ratio` → `slice_ratio`。

### 语义

- `slice_ratio=1.0`（默认）：掩码 None = 纯切片（与旧行为一致）；
- `0 < slice_ratio < 1`：每 epoch 精确选 `round(x×N)` 张原图切片，其余整图进池；**原图级**（emit_all 下 4 片同命运）；
- `slice_ratio=0`：全整图（等效关闭切片）；
- 掩码经共享内存 epoch 通道传播到各 worker，各进程按确定性 RNG 重建完全一致的掩码，无 per-worker 漂移（`persistent_workers=True/False` 均安全）；
- 未调 `set_epoch`（验证/直取样本）时首次访问惰性初始化；
- `len` 恒为 4N 不变。

### 验证

- 精确比例：0.5→2、0.25→1、0→全 False、1→None（纯切片）；
- 原图级：选中原图 4 片全切（切片调用=4×选中数），len=16 恒定；
- 跨 epoch 重采样变化（5 个 epoch 出现 ≥2 种掩码）；
- 惰性初始化、trainer 钩子存在；全项目无 `slice_mix_ratio` 残留。

---

## 2026-09-07：移除 .npy 磁盘缓存（slice_use_cache / cache_dir）

### 背景

远程服务器训练排障时实测：`cache='disk'` + `slice_use_cache=True` 在机械盘上不仅没有提速，反而因随机读 .npy / 缓存预热开销导致 CPU 切换风暴；关闭后 CPU 利用率恢复 90-110 正常范围。结论：这套自定义 .npy 磁盘缓存**没有用处**，整体移除。

### 改动范围（6 个文件）

- `base.py`：删除 `cache_dir` 构造参数；`npy_files` 恢复为与 jpg 同目录的原生默认；`_load_image_cached` 删除 npy 读取分支，改为**直接 imread + worker 内存 LRU**；`check_cache_images` 路径简化；cache='ram' 警告文本更新；
- `augment.py` / `build.py` / `cfg/__init__.py` / `cfg/default.yaml` / `train.py`：删除 `slice_use_cache`、`cache_dir` 相关。

### 保留项（未动）

- **原生 `cache='disk'` 代码路径**（train.py 默认 `cache=False` 即完全不产生/不读 npy）；
- **worker 内存 LRU（`slice_raw_cache_size`）**：与磁盘缓存无关，是内存机制，同一张原图被 imread 8~9 次的场景下降解到 ~1 次，保留。

### 验证

- 语法检查通过（5 个文件 py_compile）；
- 回归测试通过：全开 33 样本池正常解码；内存 LRU 命中返回副本、不重复 imread；切片+原图 4N+N 布局完好；`np.load` 全程零调用；
- 全项目无 `slice_use_cache` / `cache_dir` 残留。
