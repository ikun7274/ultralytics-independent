# 更新说明

> 本次更新内容：依据 `代码质量审查报告.md` 的结论，**按优先级顺序完成全量修复**，并补齐对应的回归测试与工程化门禁（第一 ~ 八节）；随后在审查过程中立项的**样本池 ratio 定长切分（B+）**也按推荐顺序实施完毕（**第九节**）。
> 更新日期：2026-09-15　｜　目标版本：干净 Ultralytics **8.4.126**（未修改上游任何源码、未改上游 `default.yaml`）
> 验证环境：conda `D:\miniconda3\envs\langchain`（Python 3.12.13 / torch 2.5.1 / opencv 5.0.0.93）

---

## 〇、一句话总结

**测试基线从 16 项提升到 179 项全通过**（演变见第七节的测试基线表）；第一轮修复 3 个严重缺陷（其中 1 个是修复过程中新增集成测试才发现的）、9 个中等缺陷、14 个轻微缺陷，并新增 lint 配置、CI 门禁与 A/B 对照模板；第二轮把样本池从"按最大值预分配 + 原图回退"改成"按 ratio 定长切分"，池缩减 **5.32×**（N=40 实测 `410 → 77`），并修掉一个让**每轮抽样与 `close_aug_epoch` 从未生效**的静默缺陷（S4）；第五轮新增 `slice_target_tiles`（按目标挑片 + 跨 epoch 不重复调度）。

> ⚠️ **两轮合计有 10 处会改变训练行为的修复**：第一轮 5 处见第五节；第二轮 5 处见第九节 §9.8。如果你的历史实验结论依赖旧行为，请先读这两节再决定是否重跑。

---

## 一、严重缺陷修复

### S1 双口径验证自 epoch 2 起静默退化为整图单口径

**问题**：`pool/dual.py::_run_whole` 为跑"整图第二遍"把 `v.dataloader` 换成整图 loader，`finally` 只还原了 `val_slice_enable`，未还原 loader。而 validator 实例全训练只创建一次，上游 `validator.py:235` 又是 `self.dataloader = self.dataloader or ...` 的真值短路，于是**从第 2 个 epoch 起，主（切片）口径实际跑在整图 loader 上**，`update_metrics` 因批次里没有 `val_slice_meta` 而回落到原生整图路径。`best.pt` / 早停 / 续训所依据的"切片主口径"因此失效，全程零告警。

**修复**（`pool/dual.py`）
- `_run_whole` 在进入时保存 `orig_loader`，在 `finally` 中**还原**；
- 整图 loader 只构建一次并缓存到 `self._whole_val_loader`（顺带修掉 M6：此前每轮都重建 `build_dataset`）；
- 整图那一遍失败时也能正确还原（`finally` 覆盖异常路径）。

**回归测试**：`tests/test_ooo_dual.py` 复刻上游 `validator.py:235` 的短路契约，连跑 3 个 epoch 断言主口径恒为 `SLICED` / `sliced-metrics`。
在**未修复**的代码上该测试失败并给出 `['SLICED', 'WHOLE', 'WHOLE']` —— 证明它确实能拦住这个 bug。

### S2 六个保存分支默认全开，同步 JPEG 编码+落盘压在取样路径上

**问题**：`train.py` 出厂即把 `slice_save_annotated=True`、`compose_save=True` 与 6 个 `*_save_dir` 全部打开，且 `slice_save_max=0`（不限量）。保存是**同步**的 `cv2.imencode` + 磁盘写，内联在每条分支的取样函数里；切片分支保存用的是**原图分辨率** tile，不受 `degrade_max_side` 限幅约束。实测单次带标注写盘：1280×960 = **23.6 ms**、4000×3000 = **182.6 ms**。

**修复**（`train.py`）
- 6 个 `*_save_dir` 全部注释化（保留为可直接启用的示例）、`compose_save=False`；
- `slice_save_max=100` 作为默认上限；
- 附注释说明"需要抽查时只开一条分支并限量"的原因与实测代价。

> 包本身默认是关闭的（`_ONLINE_DEFAULTS` 里各 `*_save_dir` 为空），此次只收敛了示例脚本的默认值。

### S3 三个"每 epoch 增强比例"开关从未到达 dataset，配置被静默忽略 ★修复阶段新发现

**问题**：`_rebuild_epoch_masks` 用 `getattr(self, ratio_attr, _online_default(ratio_attr))` 读取比例。`v8_transforms` 镜像了 `slice_ratio` / `weather_ratio` / `occlusion_ratio`，却**漏掉了 `ratio_pad_ratio` / `blur_ratio` / `compose_ratio`** → 属性不存在 → 静默取到兜底值 `1.0` → 掩码恒为 `None` → **这三条分支对每张原图、每个 epoch 全部生效**。

实测（4 图，六个比例全配 `0.1`）：

| 分支 | 属性是否存在 | 实际增强比例 |
|---|---|---|
| slice / weather / occlusion | ✅ | 0%（按配置，`round(0.1×4)=0`） |
| **ratio / blur / compose** | ❌ | **100%（配置被忽略）** |

`train.py` 配的正是 `0.1`，注释也写着"每 epoch 随机选 round(x*N) 张原图"，实际是 100% 全量 —— 三条分支与另外三条形成 10 倍失衡，且因池长度恒定（未选中位回退原图）而**不报任何错**。

**修复**
- `pool/augment_setup.py`：与 `weather_ratio` / `occlusion_ratio` 同样显式镜像三个比例到 dataset；
- `pool/dataset.py`：修正 `_mask_specs` docstring 里"training always copies them from default.yaml"这句从未成立的断言。

**回归测试**：`tests/test_ooo_branches.py::test_every_mask_ratio_knob_actually_reaches_the_dataset` 遍历 `_mask_specs()` 的每一行，要求比例属性存在、且 `ratio=0.5` 时确实产出掩码。**表驱动的开关必须配一个遍历该表的测试**，否则漏一行只能靠人工发现。

---

## 二、中等问题修复

| 编号 | 修复内容 | 文件 |
|---|---|---|
| **M1** | 补回上游 seed 表达式的 `+ seed` 项，并把 `seed` 传给 `GroupedImageSampler`。此前分组路径的 generator 种子恒为 `6148914691236517204`，`args.seed` 对 worker 的 numpy/random 随机流**完全失效** | `pool/sampler.py` |
| **M2** | 补回被丢掉的 npu/xpu `pin_memory_device` 选择；新增 loader 属性漂移测试，把 nw / prefetch_factor / pin_memory / drop_last / generator seed / worker_init_fn 全部与上游公式对齐断言 | `pool/sampler.py`、`tests/test_ooo_sampler.py` |
| **M3** | `best_whole.pt` 的写盘判定从浮点 `==` 改为显式 `_whole_improved` 标志。此前本轮 whole fitness 与历史最优**打平**时也会用当轮 `last.pt` 覆盖掉真正的历史最优 | `pool/dual.py` |
| **M4** | `_ONLINE_DEFAULTS` 从 174 条精简到 **77 条（仅项目自有键）**，删掉 96 条复述上游 `default.yaml` 的键（实测这些行对 `DEFAULT_CFG` 是死代码、对约 120 处 `getattr` 兜底却是活代码，上游升版即静默漂移）。同时在 `install()` 中新增自检 | `pool/constants.py`、`installer.py` |
| **M5** | `_sync_epoch_masks` 的两处静默 `except` 改为**每进程一次**的告警。此前掩码同步失败会让所有 `*_ratio` 与 `close_aug_epoch` 无声失效 | `pool/dataset.py` |
| **M6** | 整图 val loader 构建一次并复用（并入 S1 修复） | `pool/dual.py` |
| **M7** | `hyp` / `fraction` 改为按 `BaseDataset.__init__` 的真实签名解析，位置传参不再静默回退到默认值 | `pool/dataset.py` |
| **M8** | `OnlineSlice` 补齐 `bias_margin` / `bias_jitter` 校验；`slice_geometry` 夹紧偏移，**保证两个方向的 tile 宽/高 ≥ 1**。此前 `slice_bias_margin=0` + `slice_overlap_ratio=0` 会产生 0 面积切片，在 DataLoader 子进程内炸出 `cv2.resize (-215:Assertion failed) !ssize.empty()` | `pool/augment_setup.py`、`core/geometry.py` |
| **M9** | 保存目录校验前移到**构造期**（`v8_transforms` 组装时，主进程内），不再等到首次保存才在 worker 里抛错 | `pool/augment_setup.py` |

---

## 三、轻微问题修复

| 编号 | 修复内容 |
|---|---|
| **L1** | 删除 10 处真实死导入（`augment_setup.py` 的 `math`/`time`/`torch`/`Instances`；`constants.py` 的 `numpy`；`dataset.py` 的 `bisect`/`time`/`torch`/`Dataset`；`valslice.py` 的函数内 `Path`） |
| **L2** | `_save_cap` 双实现收敛为单实现（删除 `core/saver.py` 中无人调用的那份，保留 `pool/dataset.py` 中读包默认表的那份），并消除模块级对导入的遮蔽（F811）；首参由 `self` 改名 `dataset` |
| **L3** | `core/__init__.py` 的 `__all__` 补齐 `_RATIO_PAD_COLORS` |
| **L4** | 修正 3 处文档与实现矛盾：`installer.py`/`__init__.py` 引用不存在的子模块名；`_hyp_get` docstring 自称会 `raise ValueError`（实际回落默认表）；运动模糊"~1.8x faster"改为实测 **5.3–5.5x**（1280×960：9.5 vs 52.4 ms） |
| **L5** | `_geometry` 支持 `only=k`，mode A 与 `slice_at` 只计算被使用的那个 tile。此前 mode A 白算 3/4；`slice_all_tiles` 下 4 次 `slice_at` 各算 4 个 tile（16 个顶 4 个用） |
| **L6** | val 侧 `_load_image_cached(copy=False)`：切片只读该数组，省掉每个 tile 一次全分辨率 memcpy（4000×3000 实测 13.6 ms，`all_tiles` 下每图约 54 ms）；对不支持该参数的 base 自动降级 |
| **L7** | `degrade_max_side` / `compose_max_side` 取负值关闭工作分辨率上限时告警一次（关闭后 weather-noise 峰值 18.4 MB → 180 MB，dense-PSF 模糊 52 → 451 ms） |
| **L9** | `_patch_plot_results` 改用 `TemporaryDirectory`，不再每次 `plot_results` 泄漏一个 `ooo_plot_*` 临时目录 |
| **L10** | `_remap_boxes` 去掉未使用的 `self`，按 `staticmethod` 挂载（与 `_class_wise_nms` 一致） |
| **L11** | 抽出 `pool/fraction.py`，`patch_fraction_guard` 归位，恢复"一个 patch 一个模块"约定 |
| **L12** | 抽出三条共享 helper（`_begin_branch_label` / `_save_branch` / `_finish_branch`），`blur` / `weather` / `occlusion` / `ratio` / `compose` 五条分支复用，消除每条约 8 行的重复骨架 |
| **L14** | `train.py` 的 `project` 改为相对路径，仓库可搬迁 |
| **L13** | ⏸ **未改**：`_geometry` 内 bbox 格式转换的 4 段重复。属纯可读性，改动收益低于回归风险，留作后续 |
| — | 顺带移除 `prefetch_factor` 配置键（读入 `dataset.prefetch_factor` 但**从未被任何 loader 使用**，上游硬编码 4），详见第五节 |

---

## 四、工程化（原清单第 12 项）

| 项 | 内容 |
|---|---|
| **Lint 配置** | 新增 `ultralytics_ooo/ruff.toml`：只作用于本包（ruff 就近发现配置），规则针对本包**真实出现过**的缺陷类别选择 —— `F401`（死导入）、`F811`（重定义/遮蔽）、`B009`（`getattr` 常量属性访问，即 `_hyp_get` 存在的理由）、`BLE`（盲 `except`，正是 M5 的病根）、`B`、`E/W`、`I`、`UP`；`E501` 与 `UP031/032` 显式忽略并注明原因 |
| **CI 门禁** | 新增 `.github/workflows/ooo-quality.yml`：`ruff check ultralytics_ooo` + `pytest tests/ -k ooo`（Python 3.10 / 3.12 矩阵），仅在本包与 `tests/test_ooo_*.py` 变更时触发 |
| **不改上游文件** | 完全未触碰 `pyproject.toml`（属上游 Ultralytics），lint 配置放在包内，符合"零侵入"契约 |
| **未安装 ruff** | 为**不改动用户 conda 环境**，未在 `D:\miniconda3\envs\langchain` 中安装 ruff。等价规则已用自制 AST 检查器复核：`F401` / `F811` / 浮点相等比较**全部归零**。首次 CI 运行如报 `I`(import 顺序) / `UP` 类提示，可用 `ruff check --fix ultralytics_ooo` 自动修复后移除 workflow 里对应的 `continue-on-error` |

---

## 五、⚠️ 行为变更（需要你确认的部分）

以下 5 处修复会让**相同配置产生不同的训练结果**。历史实验若依赖旧行为，建议重跑或至少重新评估结论。

| # | 变更 | 影响 |
|---|---|---|
| 1 | **S3：`ratio_pad_ratio` / `blur_ratio` / `compose_ratio` 现在真正生效** | `train.py` 配的 `0.1` 从"实际 100%"变为真正的 10%。这三条分支的样本占比大幅下降，"未选中回退原图"的比例大幅上升 —— **训练数据的有效构成变化最大的一处**。若此前按旧行为调过参，需重新评估 |
| 2 | **S1：双口径自 epoch 2 起真正走切片口径** | `best.pt` 的选择依据从"整图 fitness"变为"切片 fitness"。**修复后的 `best.pt` 会与历史实验里的不同**，`results.csv` 的主指标列也不再与 `whole_metrics/*` 相同 |
| 3 | **M1：`seed=` 现在真正影响分组采样路径的随机流** | 同一 `seed` 下，增强随机流与修复前**不同**；好处是换 seed 终于能复现出不同结果。与关闭 `slice_grouped_sampler` 的同配置运行也不再可比 |
| 4 | **M8：`slice_bias_margin` 必须 > 0** | 出货默认 `0.25` 不受影响；仅当显式设为 `0` 时现在会在构造期报错（此前会生成退化切片并在训练中途崩溃）。`slice_overlap_ratio=0` 仍合法 |
| 5 | **`prefetch_factor` 不再是合法参数** | 此前传入被静默忽略（读入一个从未被使用的属性）；现在传入会报"未知参数"。同时 `OnlineSlice` 在 `slice_save_exist_ok=False` 且目录已存在时改为**构造期**报错（默认 `True`，不受影响） |

---

## 五·补 行为变更处置建议（推荐方案）

### 总览：建议怎么处理

| # | 变更 | 推荐方案 | 需要重跑历史实验？ |
|---|---|---|---|
| 1 | S3 三个比例开关生效 | **接受，并重新标定三个比例**；若要做修复前后 A/B，把三个比例显式设为 `1.0` 即可**逐位复现旧行为** | 若旧结论依赖"这三条分支全量增强"→ 是 |
| 2 | S1 双口径自 epoch 2 起生效 | **接受**；按"线上推理是整图还是切片"决定部署产物与验证口径 | 是（旧的 `best.pt` 口径本身就是混合的，不可信） |
| 3 | M1 `seed=` 生效 | **接受**，当作实验纪律改善；**不要试图逐位复现旧运行** | 否（但旧运行的"可复现"是假象） |
| 4 | M8 `slice_bias_margin` 必须 > 0 | **默认配置无需动作**；若曾设为 `0`，改 `0.05` | 否 |
| 5 | `prefetch_factor` 移除 / 保存目录校验前移 | **接受**；已实测确认**旧 checkpoint 续训不受影响** | 否 |

**本轮据此落地的配套改动**（推荐方案中需要动代码/配置的部分）：

| 项 | 落地内容 |
|---|---|
| #1 可观测性 | `pool/dataset.py` 新增每 epoch 一行"各分支实际选中数"日志（`_log_mask_summary`），并在 `ratio > 0` 但取整为 0 时告警（`_warn_ratio_selects_nothing`）。**这是修复前最缺的一行**——当时配置写 0.1、实际跑 100%，日志里没有任何东西戳破它 |
| #1 语义说明 | `train.py` 顶部补充 `*_keep`（占不占池位）与 `*_ratio`（强度）的分工、取整陷阱、以及"日志怎么看" |
| #1/#3 A/B 模板 | 新增 `tools/ooo_ab_ratios.py`：默认**干跑**（不训练，直接打印两个 arm 的实际选中数），`--train` 时额外跑两次训练并汇总 `results.csv` 对比 |
| #2 场景配置 | `train.py` 的 `val_slice_*` 段补充三种场景的收敛建议（调参 / 整图部署 / 切片部署）与"口径对齐才能与历史对比" |
| #5 不做 | 未加回 `prefetch_factor` 旋钮（避免再加一处与上游的偏差），仅在文档说明如何按需加回 |

---

### #1 S3（影响最大）—— 接受修复，但要理解 `*_ratio` 的真实语义

**推荐**：接受修复，并把这三个比例当作"新引入的真实旋钮"重新标定。`train.py` 现在的 `0.1` 就是文档原本承诺的语义。

**关键点：`*_ratio` 控制的是"这条分支的强度"，不是"这条分支占多少池位"。**
池长度是恒定的（区段长度只由 `*_keep` 决定）：比例之外的位置不会消失，而是**内容被替换成"原图直通"**。所以：

- 想让池子**变小**、这条分支**彻底不参与** → 关 `*_keep`（池减少 N / 2N / ceil(N/4)）；
- 想**降低这条增强的强度**（仍保留它的池位） → 降 `*_ratio`。

**小数据集上的取整陷阱（实测，`tools/ooo_ab_ratios.py` 可直接复现）**：选中数 = `round(ratio × N)`。

出货的迷你冒烟集 `_mini_val_set` 有 **8 张**训练图，compose 是**组级**的（组数 = `ceil(8/4)` = 2），所以三个键在 `0.1` 下的实际行为**并不一致**：

| 分支 | count | `ratio=0.1` 时选中 | 结果 |
|---|---|---|---|
| `ratio_pad_ratio` | 8（原图级） | `round(0.8)` = 1 | 1 张真正做比例填充，其余 7 张退化为原图直通 |
| `blur_ratio` | 8（原图级） | `round(0.8)` = 1 | 1 张做模糊（短+长同命运），其余 7 张退化 |
| `compose_ratio` | **2（组级）** | `round(0.2)` = **0** | **完全不合成** —— 日志打 `<-- NONE` 并告警 |

一般规律（以 N=4 为例）：

| ratio | N=4 时的增强位数 | 含义 |
|---|---|---|
| `1.0` | 4 / 4 | 全量增强（= 修复前行为） |
| `0.5` | 2 / 4 | 一半 |
| `0.25` | 1 / 4 | 一条 |
| `0.1` | **0 / 4** | **该分支不产生任何增强样本**（全部退化为原图直通，**池位仍保留**） |

**注意 compose 更容易归零**：它的 count 是 `ceil(N/4)`，只有原图数的四分之一，所以 `compose_ratio` 需要 `>= 0.5/ceil(N/4)` 才能选到至少一组（N=8 时需 `>= 0.25`）。想在小数据集上看到 compose 生效，用 `1.0`；真实数据集（如 8520 图 → 2130 组）上 `0.1` → 213 组，正常。

两个 arm 的**池长度完全相同**（实测迷你集两种配置都是 82 样本，layout `[32, 8, 8, 16, 2, 8, 8]`）——比例只把增强位换成原图直通位。

**具体做法**
```python
# (A) 复现修复前的行为，用于做修复前后 A/B 对照（与旧代码逐位一致）
ratio_pad_ratio=1.0, blur_ratio=1.0, compose_ratio=1.0,
weather_ratio=1.0, occlusion_ratio=1.0, slice_ratio=1.0,

# (B) 按文档意图（推荐）
ratio_pad_ratio=0.1, blur_ratio=0.1, compose_ratio=0.1,   # 需满足 ratio x count >= 0.5
```

**可观测性（本次新增）**：每条分支每 epoch 的**实际选中数**现在会打印在主进程日志里，一行就能看出配置是否真的生效：

```
augment masks @ epoch 0: slice all/8 (ratio 1) | ratio 1/8 (ratio 0.1) | blur 1/8 (ratio 0.1) \
                       | compose 0/2 (ratio 0.1)  <-- NONE | weather all/8 (ratio 1) | occlusion all/8 (ratio 1)
```
`all/N` = 全量增强（ratio ≥ 1），`sel/N` = 只有 sel 个位真正增强，`<-- NONE` = 该分支本次一个都没选到。**这正是修复前缺失的那一行** —— 当时无论配置写多少，日志都只会显示 `all/N`。同时，`ratio > 0` 但取整为 0 时会额外发一次告警。

---

### #2 S1 —— 接受，并按"部署口径"选择产物

**推荐**：接受修复。修复前 epoch 2 之后 `best.pt` 与 `best_whole.pt` 其实是**同一个（整图）口径**选出来的，两者区别是假的；修复后 `best.pt` = 切片口径、`best_whole.pt` = 整图口径，才真正有意义。

**历史对比的正确做法**：旧运行的 `best.pt` 口径本身是**混合**的（epoch 1 按切片选、epoch 2 起按整图选），不可直接用来对比。要比就**口径对齐比**：
> 新运行的 `best_whole.pt` ↔ 旧运行的 `best.pt`（两者都是整图口径）。

**按场景收敛验证口径**（双口径每轮多跑一整遍验证，是报告里 P2 的耗时来源）：

| 场景 | 建议配置 | 说明 |
|---|---|---|
| 调参 / 摸清两个口径的差距 | `val_slice_enable=True, val_slice_dual_metric=True` | 两个都看，代价是验证跑两遍 |
| 线上是**整图**推理 | `val_slice_enable=False` | 回到纯整图，验证最省时，`best.pt` 即部署产物 |
| 线上是**切片（SAHI）**推理 | `val_slice_enable=True, val_slice_dual_metric=False` | 只跑切片主口径，省掉第二遍整图验证 |

---

### #3 M1 —— 接受，当作实验纪律的改善

**推荐**：接受。旧行为看着"可复现"，实则是**对 seed 不敏感**——同 seed 恒定、换 seed 也一样，等于你拿不到独立的随机重复。修复后 `seed` 才真正有效。

**建议的实验纪律**
- 每个实验**记录所用 `seed`**；
- A/B 对照**固定同一 seed**（现在跨 arm 同 seed 才真正可比，修复前这点是无效的）；
- 要估计种子方差就跑 3 个不同 seed，不要指望靠"换 seed 得到不同结果"以外的办法；
- 配合默认的 `deterministic=True`，同 seed 运行现在才**逐位可比**。

**不要**为了复现某次旧运行去把 loader generator 种子钉死（那是恢复到"seed 无效"的旧状态），不值得。

---

### #4 M8 —— 默认配置无需动作

出货默认 `slice_bias_margin=0.25`（合法），`train.py` 也没改，所以**不设该参数就没有任何影响**。

若某处配置写了 `slice_bias_margin=0`：
```python
slice_bias_margin=0.05,   # 替代 0：仍然允许切缝大范围搜索，但不会贴到图像边缘
```
理由：`0` 允许切缝落在图像边界，即使几何已被夹紧到 ≥1 px，那一侧也会退化成 **1 px 的"废切片"**——不再崩溃，但等于白丢一个样本。

推荐组合（开启目标感知切缝时）：
```python
slice_center_bias=True, slice_bias_margin=0.25, slice_bias_jitter=0.05,
slice_overlap_ratio=0.2,        # 与 center_bias 同开时建议 >= 0.1，给切缝留余地
```

---

### #5 `prefetch_factor` / 保存目录校验 —— 接受，续训安全（已实测）

**`prefetch_factor` 的移除是否影响旧 checkpoint 续训？→ 不影响。** 实测：

- 新增训练时传 `model.train(prefetch_factor=2)` → 报清晰错误
  `SyntaxError: 'prefetch_factor' is not a valid YOLO argument.`
- 但**旧 checkpoint 的 `train_args` 里带着 `prefetch_factor` 续训正常**：`trainer.check_resume` 走的是
  `self.args = get_cfg(ckpt_args)`，ckpt 的 train_args 是**基准（base）而不是 overrides**，而
  `check_dict_alignment` 只校验 overrides 中不存在的键、从不校验 base 的多余键。实测 `get_cfg(ckpt_args)` 正常通过，`prefetch_factor` 只是变成一个无人读取的惰性属性。

所以这是安全清理：**已有 checkpoint 可以照常 `resume`，只是别再在新训练里传这个参数。**

若确实需要调 `prefetch_factor`：目前上游硬编码 4，包内没有入口。需要的话可以按需加回一个"真正生效"的版本（读 `dataset` 上的值并保证与上游默认一致），当前不做是为了避免再加一处与上游的偏差。

**保存目录校验前移**：`slice_save_exist_ok` 默认 `True`，**无影响**。仅当显式设 `False` 且目录已存在时，现在在**数据集构建期（主进程）** 报错，而不是训练数分钟后在 DataLoader 子进程里抛错——这是期望中的改进。显式设 `False` 时请先清空或更换目录。

---

## 六、文件清单

### 新增

| 文件 | 说明 |
|---|---|
| `ultralytics_ooo/pool/fraction.py` | fraction 兜底 patch 独立成模块（L11） |
| `ultralytics_ooo/ruff.toml` | 本包专属 lint 配置 |
| `tools/ooo_ab_ratios.py` | S3 行为变更的 A/B 对照模板：干跑打印两 arm 的实际选中数，`--train` 汇总 `results.csv` 对比 |
| `tools/ooo_pool_measure.py` | 池膨胀量化工具：逐段统计"真增强位 vs 原图回退位"，并给出按 ratio 定长切分的对照布局 |
| `.github/workflows/ooo-quality.yml` | lint + 测试的 CI 门禁 |
| `tests/test_ooo_dual.py` | 双口径跨 epoch 不退化为整图口径；`best_whole.pt` 判定（9 项） |
| `tests/test_ooo_branches.py` | 临时数据集端到端：7 区段总长、各分支标签结构、掩码回退、遮挡剔除、`close_aug_epoch`、表驱动比例开关、每 epoch 选中数日志（16 项） |
| `tests/test_ooo_sampler.py` | 分组采样种子对齐上游、loader 属性漂移（7 项） |
| `tests/test_ooo_guards.py` | 切片几何不退化、构造期校验、`only=k` 等价性、位置传参解析、配置表不撞上游（77 项参数化） |
| `代码质量审查报告.md` | 本轮审查报告（含附录 C 修复状态） |
| `样本池膨胀分析与方案.md` | 后续议题：「未选中位退回原图直通」的池膨胀可行性分析（含实测量化与推荐方案 B+） |
| `更新说明.md` | 本文件 |

### 修改

| 文件 | 改动摘要 |
|---|---|
| `train.py` | 关闭 6 个保存目录与 `compose_save`、`slice_save_max=100`、`project` 改相对路径；补充 `*_keep`/`*_ratio` 语义与取整陷阱说明、验证口径的三场景收敛建议 |
| `ultralytics_ooo/installer.py` | 配置表自检（在注册前取上游快照）；修正文档中不存在的子模块名 |
| `ultralytics_ooo/__init__.py` | 修正子模块清单（`resume`/`valslice` 实际在 `pool.` 下） |
| `ultralytics_ooo/pool/dual.py` | S1（loader 还原 + 缓存）、M3（显式改进标志）、L9（临时目录） |
| `ultralytics_ooo/pool/dataset.py` | M5（告警）、M7（签名解析）、L1（删死导入）、L2（`_save_cap` 去重）、L5（`only=k`）、L7（限幅告警）、L11（移出 fraction）、L12（三条共享 helper）、移除哑开关 `prefetch_factor`；**新增每 epoch 各分支实际选中数日志 + `ratio` 取整为 0 的告警** |
| `ultralytics_ooo/pool/augment_setup.py` | S3（镜像三个比例键）、M8（参数校验）、M9（构造期校验）、L1（删死导入）、L4（`_hyp_get` docstring） |
| `ultralytics_ooo/pool/sampler.py` | M1（种子）、M2（`pin_memory_device`）、漂移说明 |
| `ultralytics_ooo/pool/valslice.py` | L6（`copy=False` + `_read_orig`）、L10（`staticmethod`）、L1（删局部导入） |
| `ultralytics_ooo/pool/constants.py` | M4（表精简至 77 条项目自有键）、新增 `check_online_defaults_are_project_only` |
| `ultralytics_ooo/core/geometry.py` | M8（夹紧偏移，保证 tile 不退化） |
| `ultralytics_ooo/core/saver.py` | L2（删除重复的 `_save_cap`） |
| `ultralytics_ooo/core/__init__.py` | L3（补齐 `__all__`）、移除 `_save_cap` 再导出 |
| `ultralytics_ooo/README.md`、`项目说明.md` | 目录结构、测试说明、验证状态、兼容性说明同步 |

### 未改动
- `ultralytics/` 全部上游源码（含 `cfg/default.yaml`）—— 零侵入契约不变
- `pyproject.toml`（属上游）：lint 配置放在包内

---

## 七、验证方式与结果

```bash
export PYTHONPATH="<repo root>"
python -m pytest tests/ -k ooo -q
```

| 阶段 | 测试数 | 结果 |
|---|---|---|
| 修复前基线 | 16 | 全通过（严重缺陷在上述 16 项全绿的状态下静默存在） |
| 第一轮（质量修复） | **134** | **全通过**（约 2.9 s，全部离线：不下载数据与权重） |
| 第二轮（样本池 B+ + §7.1，见第九节） | **146** | **全通过**（约 2.8 s，全部离线） |
| 第三轮（`img_origin` 统一覆盖 + lint/文档收敛） | **153** | **全通过**（约 4.1 s，全部离线） |
| 第四轮（审查发现修复：lint 归零 + 门禁扩容 / LRU 实测纠偏 / 死配置告警 / 切片 memcpy 优化，见第十节） | **157** | **全通过**（约 3.3 s，全部离线） |
| 第五轮（新功能 `slice_target_tiles`：按目标挑片 + 跨 epoch 不重复调度，见第十一节） | **171** | **全通过**（约 2.0 s，全部离线） |
| 第六轮（修复验证集构建崩溃 `batch_size=0` + `img_origin` 默认值回归，见第十二节） | **179** | **全通过**（约 4.5 s，全部离线） |

> 后续任何一轮新增/精简测试后，**请只更新本表最后一行与 `项目说明.md` 的「验证状态」**，不要在正文散落写死测试数——本轮之前正文里有 146/134 两个互相矛盾的数字，正是散落写死的后果；而第三轮留下的 153 也在第四轮新增 4 条用例后立刻过时，再次印证了这条纪律。

补充验证：
- **反向验证**：把 S1 的旧 `_run_whole` 在运行时换回去，`test_primary_pass_stays_sliced_across_epochs` 失败并输出 `['SLICED','WHOLE','WHOLE']` —— 确认该回归测试真实有效；
- **AST 复核**：自制检查器复跑，`F401`（排除 `__init__` 有意再导出与 `__future__`）、`F811`、浮点相等比较**全部归零**；
- **配置一致性**：`check_online_defaults_are_project_only` 对 `cfg/default.yaml` 返回空列表；
- **实测数据**：S2 写盘耗时、L5/L6 收益、L7 内存与耗时差异均为本机实测（见审查报告第六节）。

---

## 八、未处理事项与后续建议

0. **【已实施】样本池"原图回退"膨胀**（第二轮，见第九节）：实测 `ratio=0.1` 时**池中 91.0% 的位是"原图直通"回退位**（base 段未被选中的图各占 4 个**逐字节相同**的位）。按 `样本池膨胀分析与方案.md` 的方案 B+ 实施了 **ratio 定长切分**，并按该文 §7.1 收窄了 `keep_origin`：池缩减 **5.32×**（N=40 实测 `410 → 77`）、回退占比 → 0%、`ratio >= 1` 时布局**逐位不变**（已用快照工具比对）。工具：`tools/ooo_pool_measure.py`、`tools/ooo_layout_snapshot.py`。
1. **L13 未改**：`_geometry` 内 bbox 格式转换的 4 段重复（纯可读性）。
2. **真实端到端训练未重跑**：本次修复以离线测试与运行时复现为准。**强烈建议按第五节 + 五·补的建议确认行为变更后，用 `train.py` 跑一次 1–2 epoch 冒烟**，重点核对：`ratio/blur/compose` 三条分支的样本构成、`best.pt` 与 `best_whole.pt` 的指标关系、`results.csv` 中 `whole_metrics/*` 与主指标列的差异。
   - 冒烟前可先跑 **`python tools/ooo_ab_ratios.py`**（干跑，不训练，秒级）看清两种配置下每条分支的**实际选中数**；它已经显示出在出货的 8 图迷你集上 `compose_ratio=0.1` 会选到 **0/2 组**（`round(0.2)=0`），即 composition 分支在该数据集上完全不生效。
3. **首次 CI 运行需人工确认**：`ruff` 未在本地安装，`I`（import 顺序）/`UP` 类提示可能需要在 CI 上跑一次 `ruff check --fix ultralytics_ooo` 后固化。
4. **建议补的真实回归**：目前 S1 的验证是"复刻上游契约的最小复现"（逐行对应 `trainer.py:408/871` + `validator.py:235`）。若条件允许，建议再加一次真实小数据集的 `val_slice_dual_metric` 多 epoch 训练断言。
5. **本轮已落地的两项可观测性**（原"可选跟进"）：每 epoch 分支选中数日志 + `ratio` 取整为 0 的告警。二者都有测试锁定（`tests/test_ooo_branches.py`），因此**以后新增一条增强分支却忘了镜像它的 ratio，会在测试里立刻暴露**，而不是像 S3 那样潜伏到下一轮审查。
6. **仍然存在的结构性风险**（非缺陷，但值得记录）：
   - `pool/sampler.py` 仍复制了上游 `build_dataloader` 的 loader 尾部 —— 无法直接复用上游实现，因为 `InfiniteDataLoader` 会在构造时把 `batch_sampler` 包进 `_RepeatSampler` 并**当场创建 iterator**，事后替换 sampler 不会生效。现已用属性对齐测试兜住漂移，但上游若新增 loader 参数仍需同步。
   - patch 冲突无检测：`install()` 会替换 7 处上游对象，第三方库若 patch 同一方法会互相覆盖且无提示。`_ooo_*_patched` 只防自身重复安装。

---

## 九、第二轮：样本池 ratio 定长切分（B+）与 S4

> 起因：`*_ratio` 未选中的位"保留池位、内容退回原图"导致原图在池子里重复堆积。可行性分析与方案见 `样本池膨胀分析与方案.md`，本节记录**实施结果**。
> 实施日期：2026-09-15　｜　改动范围：`pool/dataset.py`、`pool/sampler.py`、`installer.py`、`train.py`（注释）、`tools/*`、测试与文档；**仍未改上游任何源码**。

### 9.1 S4 每 epoch 抽样从未生效（本题外发现，严重）★

为了验证"每轮真的换了选中的图"而加日志时发现：`installer._ooo_set_epoch` 读的是 `trainer.train_dataloader`，而**上游 `BaseTrainer` 从来没有这个属性**（它叫 `train_loader`，见 `trainer.py:286`）。后果：

- `on_train_epoch_start` 回调静默空转 → **`set_epoch` 从未被 trainer 调用**；
- 每 epoch 的 `*_ratio` 抽样、以及 `close_aug_epoch` **全程冻结在第 0 轮**（只有"没有 trainer 时"的懒初始化路径会把 epoch 0 的选择集建一次）；
- 没有任何异常或告警，`validate`/`resume` 等其它 patch 都正常，所以从现象上完全看不出来。

**修复**：改读 `train_loader`（保留 `train_dataloader` 兜底，两者都找不到时打印 WARNING 而不是静默返回），并把回调注册从"函数身份去重"改为"标记去重"，避免重复 `install()`（例如 `sys.modules` 被清理后重新导入）注册出两个回调。

**验证**（真实训练，2 epoch）：修复前用 trace 包装 `set_epoch` —— 一次都没被调用；修复后每轮都被调用，并且 `augment masks @ epoch 0/1` 开始出现在训练日志里。回归测试：
- `test_trainer_dataset_attribute_name_matches_upstream`（上游属性名仍在，且没有改名成 `train_dataloader`）；
- `test_epoch_callback_publishes_set_epoch_through_the_train_loader`（真实调用已注册的回调列表，断言 `set_epoch(epoch, epochs)` 恰好被调一次）；
- `test_epoch_callback_tolerates_an_unsupported_trainer_without_crashing`。

### 9.2 方案 B+：段长由 ratio 决定

| 段 | 改造前 | 改造后 |
|---|---|---|
| base | `4N` | `4·K_slice + (N − K_slice)`：选中图 4 片，未选中图**恰好 1** 个原图位 |
| origin | `N` | `K_slice`（§9.4 收窄后） |
| ratio / weather / occlusion | `N` | `K_x` |
| blur | `2N` | `2·K_blur` |
| compose | `ceil(N/4)` | `K_compose` |

`K_x = round(x_ratio × count_x)` **只由配置决定**，与"选中了哪几张图"无关 → **池长全程恒定**（mosaic buffer 索引、`nb`/`nw`、sampler units 都按训练开始时的池长预计算，不能中途变化）。

三条不变量（都有测试）：

1. **`ratio >= 1` 时布局与旧形状逐位一致**（`K == count` → 段宽退化）——由快照工具比对确认 `82` 索引逐位相同，并由 `test_ratio_1_layout_is_bit_identical_to_the_legacy_shape` 锁死（对照的是**旧的映射公式**，不是新实现，避免自证）；
2. **`ratio = 0` 等价于关闭切片**：base 退化为 `N`，每图恰好 1 个原图位（旧实现会在这里产生 `4N` 个重复原图）；
3. **池长与 `set_epoch` 无关**：`test_pool_length_is_constant_across_epochs` 连续 6 轮断言段长与池长不变，同时断言选择集确实在变（否则 ratio 是哑的）。

其它落地细节：

- 布尔掩码 `_*_mask` → **有序选择集** `_sel_*`，抽样条件、顺序与旧实现保持一致（仍只在 `0<x<1` 时 `rng.sample`），所以**"哪几张图被增强"不变，变的只是未选中的图不再占位**；
- `_extended_pool_on()` 改用 `total != N` 判断，不再维护第二份开关清单；
- 分组采样收益的判据从"总样本数 ≥ 4N"改为"存在某张图的读取次数 ≥ 原始 LRU 容量"——否则 ratio 定长后平均复用率下降会让分组被**静默关掉**（实测仍为 82–84% 命中）；
- `close_aug_epoch` 不改段长：选择集变恒等序列、构建器直接输出原图，代价是末几轮多出 `ΣK_*` 个重复原图（ratio=0.1、N=8520 时约占新池 30%，语义上无害）。

### 9.3 实测效果

| 场景 | 改造前 | B+ | +§9.4 收窄 | 缩减 |
|---|---|---|---|---|
| N=40，六 ratio 全 0.1 | 410 | 113 | **77** | **5.32×** |
| N=8520，六 ratio 全 0.1 | 87 330 | 24 069 | **16 401** | **5.32×** |
| N=8（出货迷你集），全分支 ratio 1.0 | 82 | 82 | 82 | 1.0×（兼容基线） |
| N=8，全分支 ratio 0.1 | 82 | 17 | **17** | 4.8× |

"原图回退"占比：**91.0% → 0%**（N=40 实测 `python tools/ooo_pool_measure.py --n 40 --ratio 0.1`）。

### 9.4 §7.1 `keep_origin` 收窄（独立改动）

`keep_origin` 原本为**每张图**补一张整图，未选中的图在 base 段已是整图 → 每图出现 2 次逐字节相同的原图。收窄为"只为**被切片选中**的图补整图"，段宽 `N → K_slice`。`ratio >= 1` 时 `K_slice = N` → 布局不变，兼容性不受影响。

### 9.5 §7.2 `GroupedImageSampler` 补 `set_epoch`

上游 `trainer.py:456` 在 DDP 下调用 `train_loader.sampler.set_epoch(epoch)`；分组采样补丁只在 `rank == -1` 生效，所以当前不会触发。补上该方法（记录 epoch 并折进顺序种子）以免将来 DDP + 分组采样在 epoch 循环里 `AttributeError`。

### 9.6 日志与工具

- **构造期日志改成按真实段长打印**：`Online augment: 17 training samples from 8 images (segment slots: base 11, origin 1, ratio 1, ...)`。旧文案是"每条每图 N 张"的静态描述，在 ratio < 1 时会与实际布局矛盾。
- `tools/ooo_pool_measure.py`：打印各段真实槽位、每图占位直方图、与旧布局的倍率。
- `tools/ooo_layout_snapshot.py`（新增）：存/比对"索引 → 样本"的逐位快照，布局改动前先存基线，`--compare` 有差异时退出码 1。
- `tools/ooo_ab_ratios.py`：更新为 ratio 定长语义（两条 arm 的池长现在不同）。

### 9.7 真实训练验证（`_mini_val_set`：8 训练图 / 3 验证图 / 320px / 2 epoch / CPU）

| arm | ratio | workers | close_aug_epoch | 池长（段长） | 关键日志 |
|---|---|---|---|---|---|
| legacy | 1.0 | 0 | 0 | 82 (`32,8,8,16,2,8,8`) | `slice all/8` …；LRU 82.8% |
| intended | 0.1 | 0 | 1 | 17 (`11,1,1,2,0,1,1`) | `slice 1/8 (ratio 0.1)` … `compose 0/2 <-- NONE`；末轮 `[close_aug_epoch: …]` |
| mw2 | 0.5 | 2 | 1 | 45 (`20,4,4,8,1,4,4`) | 同上；LRU 83.3% |

三个 arm 均训练完成并产出 `results.csv`；`augment masks` 与 `raw-image LRU … % hit` 每轮正常打印，Windows spawn 多 worker 下选择集跨进程同步正常。

### 9.8 ⚠️ 本轮行为变更

1. **`ratio < 1` 时池子变小**（这是本轮的**目的**）。例：出货迷你集全分支 `ratio=0.1` 从 82 → 17 样本。若历史实验结论依赖"每类增强样本数量 = N × 分支数"，需要重跑。
2. **`keep_origin=True` 且 `slice_ratio < 1` 时**，未被切片选中的图不再有第二张整图（少了一份重复样本）。
3. **`compose_ratio` 现在真的生效**（第一轮 S3 的延续）：`round(0.5 × ceil(N/4))` 在小数据集上常为 0，日志会告警。
4. **`set_epoch` 现在真的每轮被调用**（S4 修复）：每轮的选中集合会变、`close_aug_epoch` 会生效。**如果此前用 `close_aug_epoch>0` 训练过，那些运行的收尾期其实一直是开增强的**，结论不可比。
5. 复现旧布局的开关：把六个 `*_ratio` 全设为 **1.0**（段宽退化回旧形状；`keep_origin` 也回到 `N`）。

---

## 十、第四轮：审查发现的 P0–P2 修复

> 起因：一次**从零独立审查**（不依赖此前结论）的结论，全文见 `全面重新审查报告.md`。本节记录这些发现的**处置**。
> 全部结论都有可复跑的命令支撑；本节末尾 §10.12 列出命令与实测值。

### 10.1 P0-1 lint 归零：20 → 0

`ruff check ultralytics_ooo` 从 **20 errors** 变为 **All checks passed**：

| 规则 | 数量 | 处置 |
|---|---|---|
| BLE001 盲 `except` | 8 | 逐个判断后**加 `# noqa: BLE001` 并写明为什么必须盲捕**（共享内存已关闭、worker 内不可注册回调等），不是无脑放过 |
| I001 import 未排序 | 5 | `ruff check --fix` 机械修复 |
| UP037 引号注解 | 2 | `--fix` |
| B010 `setattr` 常量 | 2 | 改为普通属性赋值 |
| F841 未用变量 | 2 | 删除（其中 `f = self.im_files[img_index]` 确认确实无用） |
| E731 lambda 赋值 | 1 | 改为 `def` |

### 10.2 门禁扩容：本包自己的测试原先**完全未被 lint 覆盖**（28 → 0）

这是本轮顺带发现的一个覆盖漏洞。`tests/` 与 `ultralytics_ooo/` 的 lint 配置来源完全不同：

- `ultralytics_ooo/ruff.toml` 显式选了 `E,W,F,B,BLE,I,UP`（并逐条写了为什么）；
- 而 `tests/` 只能继承仓库根的 `pyproject.toml`，那份**属于上游、且没有 `[tool.ruff.lint] select`** —— 于是实际生效的是 **ruff 自己的默认规则集**：既不是本包的规则，也不随版本稳定。

处置：
1. 把 8 个 `tests/test_ooo_*.py` 的 **28 条**清零（16 条 `--fix` 机械修复，12 条手工）；
2. CI 增加一步 `ruff check --select E,W,F,B,BLE,I,UP tests/test_ooo_*.py`，**显式钉住规则集**，避免隐式默认值漂移；
3. 移除原先那个 `continue-on-error` 的 advisory 步骤——它的注释写着「如果首轮跑出 import-order/UP 的 nit，就 `--fix` 掉并把 continue-on-error 去掉」，而这件事现在真的做完了。

清零过程中**有两条不是「为了过 lint」，而是顺手把测试写强了**（原先是「变量取了不用」）：

- `_cap_long_side` 的 docstring 写着「不需要缩放时返回 **同一个对象**」，测试却只断言了 `scale == 1.0` → 补 `assert same is img`；
- 返回值 `scale` 是调用方用来缩放框的**长边比例**，原先没断言 → 补 `assert abs(scale - 640/4000) < 1e-9`；
- `resume` 的单测里 `called` 记录器取了不用 → 补 `assert called["orig"] is True`，锁住「补丁后的 `resume_training` 仍委托原始实现」。

三条新断言都做了**注入验证**（改坏 → 必须失败 → 字节级还原 → 复绿）：3/3 全部按预期失败。

### 10.3 P0-2 测试数统一为 157

数字链条：`134`（第一轮）→ `146`（第二轮）→ `153`（第三轮）→ **`157`**（本轮）。

之所以必须再改一次：上一轮刚把散落的 146/134 统一成 153，本轮又新增 4 条回归测试（见 §10.5–10.8），153 立刻过时。**这正是「不要在正文散落写死测试数」这条纪律的又一次实证**——第七节的测试基线表与 `项目说明.md` 的「验证状态」是唯一真源。

当前分布（`pytest tests/ -k ooo --collect-only -q`）：guards 96 / branches 27 / dual 9 / sampler 7 / augment 6 / valslice 5 / resume 4 / core 3 = **157**。

### 10.4 P0-3 `img_origin` 文档同步

`slice_keep_origin` 已在上一轮被 `img_origin` 取代（旧键只作为 deprecated 别名映射为 `img_origin=True`），但 5 个文档仍在用旧键，且 `origin` 段宽已在方案 B+ 下从 `K_slice` 变为 **N**。已全量同步：`项目说明.md`、`ultralytics_ooo/README.md`、`样本池膨胀分析与方案.md`、`背景切片配额与原图回退机制说明.md`、`代码质量审查报告.md`。

### 10.5 P1-1 ⚠️ 实测推翻了审查报告自己的建议（重要）

审查报告原本建议「把 `slice_raw_cache_size` 从有效默认 4 提到 8–16，以降低大图重复解码」。**实测结果是反的**，因此本轮没有采纳：

| 配置 | 顺序 | 分组 | 命中率 |
|---|---|---|---|
| cache=4（默认） | 分组采样顺序 | ON | **80.0%** = 强制失效下限 `1 − N/reads` |
| cache=8 | 分组采样顺序 | ON | 80.8%（同量级，纯噪声） |
| cache=64 | 分组采样顺序 | **OFF** | 60.0% |
| cache=4 | 随机打乱顺序 | ON | 60.0% |

机制：`_grouped_units` 只在「**某张图的槽位数 ≥ LRU 容量**」时才启用分组（用**最大值**而非平均值判断，因为 ratio 定长下一步未选中的图恒占 1 槽，平均值会被严重稀释）。把 cache 调过最忙图片的扇出，分组就被**静默关掉**——而打乱顺序会把同一张图的多个槽位与别人的解码交错开，LRU 再也留不住那张图，于是**解码更慢**。这是个没人会猜到的反转。

处置：**不调大**，改为在超容量时**告警一次**（说明「更大会更慢」并给出 `slice_raw_cache_size=N` 的实际值），并补回归测试 `test_default_lru_keeps_grouping_and_an_oversized_lru_warns`（同时锁住默认值下分组**必须真的产出**，避免告警写反）。

端到端坐实（`train.py` 实跑日志）：
```
train: raw-image LRU (slice_raw_cache_size=4, grouped sampler): 81.7% hit on 131 reads last epoch
```
81.7% 与该 8 图池的强制失效下限吻合。

### 10.6 P2-1 `mosaic_save_*` 死配置改为告警

上游 `Mosaic.__init__` 签名是 `(dataset, imgsz, p, n)`，没有 `save_*`；`_compat` 用 `inspect.signature` **静默剔除**这 4 个键，所以 `mosaic_save_dir/max/annotated/exist_ok` **永不生效且无任何提示**。改为：只要用户显式设了其中之一，就**一次性告警**并指向真正有效的 `slice_save_dir`。新增 `test_mosaic_save_knobs_warn_that_they_are_inert`（含「默认值必须保持安静」与「只告警一次」两个方向）。

> 之所以保留这 4 个键而不是直接删除：删掉会让历史 `args.yaml` 在续训时静默失效，告警是更安全的过渡。

### 10.7 P2-2 切片热路径的 memcpy 优化（按身份判定）

`_load_image_cached(copy=True)` 在切片路径下每次多一次**全分辨率**拷贝（4000×3000 ≈ **20.7 ms**），而切片读取约 80% 命中 LRU，所以这是最贵的一笔。

关键约束：`OnlineSlice` 有**三条路径会把入参原样返回**——① `p` 抛硬币失败、② 退化尺寸守卫、③ `_emit` 的 Plan A 背景配额回退。此时返回的就是 LRU 里的那个数组，而下游会**原地写入**它，因此不能简单改成 `copy=False`。

处置：用**对象身份判定**（`im is shared`）精确识别这三条路径，只在这里才拷贝；其余路径直接复用 LRU 数组。这样无需给三个调用点各加标志，且三条路径任一新增加入都会被同一判据覆盖。

配套：
- 契约测试 `test_slice_branch_never_hands_the_lru_buffer_downstream`（强制走直通路径，断言交给下游的数组**从不**是 LRU 的条目）；
- **反向验证**：把 guard 临时禁用后，**16/16** 个样本直接别名了 LRU 缓冲 —— 证明这条测试真的能抓到回归，而不是永真。

### 10.8 P2-3 / P2-4 两处稳健性加固

- **P2-3 `valslice` 的 `shape` 依赖**：切片 tile 的边界来自 `label["shape"]`，但上游 `get_image_and_label` 会 `label.pop("shape")`。缺失时原代码退化为 `(0,0)` → 图块边界 `(0,0,0,0)` → `im[0:0,0:0]` 空数组，直到 DataLoader worker 里才以 `cv2.resize` 断言炸掉，**报错点离病因很远**。改为**显式整图退化 + 一次性告警**（否则「切片验证」会静默地什么都不切），附 `test_missing_shape_is_an_explicit_passthrough_not_an_empty_crop`。
- **P2-4 内存敏感用例**：`test_cap_long_side_self_check` 原先用 `(rng.random(...) * 255).astype(np.uint8)`，其 float64 中间量对 3000×4000×3 是 **288 MiB**（图像本身只有 36 MiB），在低内存机器上直接 `MemoryError`，把「全绿」结论变成看机器负载的运气。改为 `rng.integers(0, 255, size=..., dtype=np.uint8)`（**峰值降 8×**，覆盖等价）+ `pytest.skip` 环境门控。

### 10.9 ❗ 新发现（**未处理**）：门禁与其配置根本没进版本控制

`git status` 显示以下路径**未被跟踪**：

```
?? .github/                      ← 整个 CI 工作流
?? tools/                        ← 4 个诊断工具（ooo_pool_measure / ooo_slice_knobs / ...）
?? ultralytics_ooo/ruff.toml     ← lint 配置本身
?? ultralytics_ooo/pool/fraction.py
?? tests/test_ooo_{branches,dual,guards,sampler}.py   ← 承载 139/157 个用例
?? 代码质量审查报告.md / 更新说明.md / 样本池膨胀分析与方案.md / 背景切片配额与原图回退机制说明.md / 全面重新审查报告.md
```

含义很直接：**文档反复引用的 CI 门禁、它的规则配置、以及用来复现所有实测数字的诊断工具，在版本控制里都不存在**；一旦 clone，本包只剩 **18/157** 个测试用例、零门禁、零工具。§10.2 刚把门禁扩容到测试，但如果 `.github/` 本身不进仓，这次扩容同样不会被任何人执行到。

**建议动作**（不在本轮自动执行，因为涉及提交状态，需要你确认）：把上述路径 `git add` 并提交，或按需要写入 `.gitignore` 并调整文档表述。

### 10.10 本轮行为变更

对**配置正确的运行**：**无**——没有改动任何默认值、段长公式或数值语义。用户可见的变化只有两类：

1. **新增两条一次性告警**：LRU 容量超过最忙图片扇出时（§10.5）、设置了永不生效的 `mosaic_save_*` 时（§10.6）。两者原本都是静默的，因此把「安静地做错」改成「出声地提示」不算行为回归，但会让既有日志多出一行。
2. **两条测试断言变强**（`scale` 的长边比例、`cap<=0` 返回同一对象）——若有人依赖「`cap<=0` 会拷贝一份」这个**未被 docstring 承诺**的行为，现在会被测试拦住。这属于把隐含契约显式化。

**性能**：切片路径每次读取最多省一次全分辨率拷贝（4000×3000 ≈ 20.7 ms），实测 16/16 样本不再别名 LRU 缓冲；其余保持不变。

### 10.11 未处理

- §10.9 的未跟踪文件（等你的提交决策）；
- 审查报告 P3 项（`_geometry` bbox 重复等可读性问题，文档自认未改）；
- 审查报告提到但本轮未动的：OpenCV 全局 RNG 污染的进一步收敛（现状已有 `cv2.setRNGSeed` 处理，未再扩大改动面）。

### 10.12 验证方式与结果

```bash
# 1) 包本体 lint（CI 门禁步骤 1）
ruff check ultralytics_ooo
#    -> All checks passed!            （修复前：20 errors）

# 2) 本包测试 lint（CI 新增步骤，显式钉住规则集）
ruff check --select E,W,F,B,BLE,I,UP tests/test_ooo_*.py
#    -> All checks passed!            （修复前：28 errors）

# 3) 全量测试（CI 门禁步骤 2）
python -m pytest tests/ -k ooo -q
#    -> 157 passed, 339 deselected

# 4) 端到端训练（切片热路径 + 双口径 + valslice 都被本轮改动触及，必须实跑）
python train.py
#    -> 2 epochs completed；池 45 样本 [16,8,4,8,1,4,4]；
#       产出 best.pt / best_whole.pt / last.pt；
#       results.csv 含 7 个 whole_metrics/* 列；
#       "raw-image LRU (slice_raw_cache_size=4, grouped sampler): 81.7% hit on 131 reads"
```

补充验证：
- **注入验证**（§10.2、§10.7）：3/3 新断言按预期失败；禁用身份 guard 后 16/16 样本别名 LRU，证明契约测试非永真；
- **常量等价性**：`ALL_ON` 由 `dict(k=v)` 改写为字面量后，重新 `exec` 校验 15 个键与取值逐一相同，`SEG`/`TOTAL` 未变；
- **字节级还原**：所有注入实验都断言还原后哈希与原文一致（发现并修正了脚本自身的换行翻译问题——用 `read_text`/`write_text` 会把整个文件从 LF 改写成 CRLF，已改用 `newline=""` 并验证 `degrade.py` 与 HEAD 字节一致）。

---

## 十一、第五轮（新功能）：`slice_target_tiles` —— 按目标挑片 + 跨 epoch 不重复调度

### 11.1 需求（用户原话）

> 能否实现：当 `slice_all_tiles=False` 时，每个 epoch 挑"未被上个 epoch 选中，切含目标的那一片"，当所有含有目标的切片选中后，重新开始选取。

背景：`slice_all_tiles=False` 时 base 段是「1 张选中图占 1 个槽」，而槽里放哪一片由 `OnlineSlice.__call__` 的
`k = random.randrange(4)` **盲抽**决定——实测只有 ~1/4 概率抽到含目标那片，其余 3/4 落到空片。
上一轮（`背景切片配额与原图回退机制说明.md`）把它列为"要改代码才能拿到的第四个解法"，本轮落地。

### 11.2 实现

新增旋钮 **`slice_target_tiles`**（`pool/constants.py`，默认 `False`＝与旧行为完全一致）。

- **调度单位是"片"不是"图"**：先扫一遍**标注缓存**（只读 `labels[i]["bboxes"]` 与 `["shape"]`，**不解码任何图片**），
  用 `OnlineSlice.target_tiles()` 求出每张图中"经 `_geometry` 的盒过滤后仍保留 ≥1 个目标"的片号，
  把所有 `(图, 片)` 排成一个队列并按 `(mask_seed, pass)` 打乱。
- **每轮取队列的下一段**（段长 = `K_slice` = `round(slice_ratio × N)`）→ 连着两轮不会出现同一个单位。
- **队列走完才回绕，且回绕时重新洗牌**（`_target_tile_pass(pass_idx)`）→ 即"全部选完后重新开始选取"。
- **零新增状态**：段/趟编号是 `epoch` 的纯函数（`n_blocks = ceil(queue / K)`、`pass = epoch // n_blocks`、
  `block = epoch % n_blocks`），所以主进程与每个 DataLoader worker 各自重算的结果必然一致，**不需要跨进程传任何东西**；
  全程不碰全局 `random`，因此**不会扰动其它分支的抽样**（有测试锁死）。
- **池长不变**：`_segment_lengths()` 一个字都没改（base 仍是 `n_per × K_slice`），改的只是"这个槽装哪一片"。
- **兜底**：调度映射基于**居中网格**（为了免解码）；真正发射时 `slice_at(prefer_target=True)` 会按**实际网格**复查，
  若被指的片空了就换成第一片仍有目标的片。队列为空时告警一次并退回盲抽（而不是发布一个填不满的调度）。

关键实现点（**易踩的坑**）：`_sel_indices("slice")` 同时被 base 段映射和 `grouped_sample_units` 当作
"第 i 个槽属于哪张图"使用，所以开启调度后**必须同步改写 `_sel_slice`** 为队列段的图列，
否则分组采样会与实际内容错位。

### 11.3 涉及文件

| 文件 | 改动 |
|---|---|
| `ultralytics_ooo/pool/augment_setup.py` | 新增 `OnlineSlice.target_tiles()`；`slice_at()` 新增 `prefer_target` 参数（含按实际网格换片的兜底）；`v8_transforms` 镜像新键；新增 `_warn_if_target_tiles_are_inert()`（`all_tiles=True` 或切片关闭时告警一次） |
| `ultralytics_ooo/pool/dataset.py` | 新增 `_target_tile_schedule_on()` / `_target_tile_queue()` / `_target_tile_pass()` / `_apply_target_tile_schedule()` / `_warn_target_tiles_unusable()`；`_rebuild_epoch_masks` 末尾调用调度；`_has_partial_ratio` 纳入该旋钮（ratio≥1 也要每轮前移）；`_base_slot` 返回调度片号；base 分支改走 `slice_at`；`_log_mask_summary` 打印段/趟 |
| `ultralytics_ooo/pool/constants.py` | 新增 `slice_target_tiles: False` |
| `train.py` | 切片旋钮注释块加 ⑤ 层说明 + 实参 |
| `tools/ooo_slice_knobs.py` | 新增 `--target-tiles`；报告新增"含目标片数（队列长度）"与"本轮调度了几个"，并给出无重复所需的 `slice_ratio` 上限 |
| `tests/test_ooo_target_tiles.py` | **新增 12 项** |
| `tests/test_ooo_augment.py` | **新增 2 项**（inert 告警 / `prefer_target` 换片） |
| 文档 | `ultralytics_ooo/README.md`（旋钮表 4→5 + 要点）、`背景切片配额与原图回退机制说明.md`（第四个解法由"待评估"改为"已实现"）、`项目说明.md` / `更新说明.md`（测试数与本节） |

### 11.4 实测

**（a）400 图稀疏合成集**（96×96，每图 1 个 24×24 象限框 → 队列 400 单位；`ratio=0.25`，`img_origin=False`，`bg=-1`）：

| | base 槽 | 正片 | 空片 | 整图 | 池长 |
|---|---|---|---|---|---|
| `slice_target_tiles=False`（默认，盲抽） | 100 | **24** | 76 | 0 | 100 |
| `slice_target_tiles=True` | 100 | **100** | 0 | 0 | 100 |

调度本身：`epoch 0..3` 分别是 `block 1..4/4, pass 1`，每轮 100 个**互不重复**的单位，4 轮并集 = 全部 400；
`epoch 4` 进入 `block 1/4, pass 2`（重新洗牌后重新开始）。池长/段长在 `ratio ∈ {0.25, 0.5, 1.0}` 下与关闭时**完全相同**。

官方诊断工具输出（`python tools/ooo_slice_knobs.py --data … --one-tile --ratio 0.25 --bg -1 [--target-tiles]`）：

```
盲抽        slots=100  positive= 35 (35.0%)  empty= 65  whole=0   target-bearing tiles=400 (schedule OFF)
按目标挑片  slots=100  positive=100 (100.0%) empty=  0  whole=0   target-bearing tiles=400 (100 scheduled this epoch)
```

**（b）端到端真实训练**（`_mini_val_set`，320px，2 epoch，CPU，**`workers=2`**，`img_origin=True`）：
队列 28 单位；日志逐轮 `target tiles: 4 units of 28 (block 1/7 → 2/7 → 3/7, pass 1)`；
训练正常完成并产出 `best.pt` / `last.pt`。
**跨进程可复现**：同一脚本独立跑两次，打印的 `_tile_units` **逐字相同**（`diff` 为空）。

### 11.5 行为变更

- **默认 `False` 时，与第四轮行为逐字节一致**（唯一新增的是 `constants.py` 里一个默认 `False` 的键）。
- 开启后：该分支不再消耗那次 `random.randrange(4)`，因此**与"盲抽"运行的全局随机流不同**（自身仍完全可确定、可复现）。
  这是任何"确定性挑片"方案的必然代价。
- 开启后 base 段几乎不含空片 → **背景误检（FP）可能变多**；要负样本请调小 `slice_ratio`、开 `img_origin`，
  或让 blur/weather/occlusion 供负样本。

### 11.6 未处理

- `K_slice > 队列长度`（如 N=8 但只有 2 张图有框）时会在**同一轮内重复单位**，已告警一次并提示所需 `slice_ratio`；
  更精细的"跨轮预算调度"未做（当前按块切分，实现最简且可复现）。
- 调度队列用**居中网格**估算（为免解码）。`slice_center_bias=True` 且目标正好贴着切缝时，会走"按实际网格换片"的兜底，
  该轮的实际片号可能与调度记录不同（不影响"含目标"这一性质，也未额外告警）。

---

## 十二、验证集构建崩溃修复（`batch_size=0`）+ `img_origin` 默认值回归

### 12.1 现象

`python train.py` 在 `_build_train_pipeline` 构建 `test_loader` 时抛：

```
File "ultralytics/engine/trainer.py", line 296, in _build_train_pipeline
    self.test_loader = self.get_dataloader(
File "ultralytics/models/yolo/detect/train.py", line 97, in get_dataloader
    return build_dataloader(
...
ValueError: batch_size should be a positive integer value, but got batch_size=0
```

### 12.2 根因：val 数据集（`augment=False`）的 `len()` 是 0

`installer.py` 把 `build.YOLODataset` 换成 `InstalledYOLODataset`（= `OnlinePoolDataset` + `YOLODataset`），
**训练与验证共用同一个类**。而 `OnlinePoolDataset.__len__` 返回的是**混合样本池布局**的总长：

```python
return self._segment_bases().total   # base + origin + ratio + blur + compose + weather + occlusion
```

问题在于 **val 构建时 `v8_transforms` 根本不会运行**：上游 `YOLODataset.build_transforms` 在
`augment=False` 时直接返回 val 变换，于是 `slice_transform` / `img_origin` / `*_keep` 这些键
**一个都不会被镜像到对象上**，七个段全部为 0：

```
=== VAL (img_origin=False, 3 张图) ===
  augment    : False
  len(ds)    : 0
  segments   : base 0, origin 0, ratio 0, blur 0, compose 0, weather 0, occlusion 0
```

而 0 并不是"val 集为空"这么无害 —— `build_dataloader` 的第一行就是

```python
dataset_len = len(dataset)
batch = min(batch, dataset_len)      # ultralytics/data/build.py:348-349
```

**0 长的 val 集把自己的 batch 改写成 0**，torch 在 `BatchSampler` 里报 `batch_size=0`，
而栈里完全不提样本池、`img_origin` 或 `slice_prob` —— 排查方向被彻底带偏。
（detect 任务传的是 `batch_size * 2`，所以 `batch=4` 时 val 拿 8，再被 `min` 压成 0。）

### 12.3 为什么以前能跑

实测四种历史配置：

| 运行 | `val` | `img_origin` | 结果 |
|---|---|---|---|
| `runs/detect/runs/exp` | true | **true** | 正常 |
| `runs/detect/runs/exp-2` | true | **true** | 正常 |
| `ooo_final/*`、`ooo_smoke/*` | true | 键尚不存在（`slice_keep_origin` 时代） | 正常 |
| `runs/detect/runs/exp-3`（本次） | true | **false** | **崩** |

即 **val 一直是在"蹭" `img_origin`**：`_img_origin_on()` 用
`getattr(self, "img_origin", _online_default("img_origin"))` 读取，键没被镜像到对象上时落到默认值。
默认 `True` 时，val 靠 origin 段**歪打正着**地得到 `len == N`（而且恰好是正确的整图集）；
默认一旦变 `False`，同一次构建立刻变成 0。

### 12.4 修复（两处）

**（a）`pool/dataset.py` — val 构建完全交还上游（结构性修复）**

`__len__` 与 `get_image_and_label` 各加一道 `if not self.augment:` 前置守卫，分别委派
`super().__len__()`（= `len(self.labels)`）与 `super().get_image_and_label(index)`：

- `augment=False` 时 `index` 就是普通图片下标，**不是池内槽位**；继续走池分发会把它当成空布局的 base 槽，
  越界读空的 `_sel_*` 数组；
- `BaseDataset.get_image_and_label` 正是 val 需要的实现（含 rect 的 `batch_shapes` 查表），因此只委派、不重写；
- 训练路径（`augment=True`）**一行未动**。

**（b）`pool/constants.py` — 恢复 `img_origin` 默认 `True`**

`git diff` 显示该文件相对 HEAD 只有两处实质改动：新增 `slice_target_tiles`，以及把 `"img_origin"` 从
`True` 改成了 `False`（**没有任何注释说明这个默认值变更是有意的**）。这个翻转同时打破两件事：

1. **零侵入契约**（本项目第一类设计目标）：全开关默认关闭时池应当就是原图集；`img_origin=False`
   让七个段全为 0 → **训练集 `len` 也是 0** → 同一个 `batch=0` 崩溃；
2. `tests/test_ooo_branches.py::test_no_switches_means_no_expansion_at_all` —— 这条**已提交的**测试
   在改动后的工作区里是**红的**（它断言的正是 `origin == N` 的零侵入契约）。

现已恢复 `True` 并补上说明性注释；该测试转绿。

### 12.5 回归测试

新增 `tests/test_ooo_val_dataset.py`（8 项，含两组 `parametrize`）：

| 用例 | 锁定的不变量 |
|---|---|
| `test_val_build_reports_one_slot_per_image`（`img_origin` ∈ {False, True}） | `len(val) == N`，两种默认值下都成立 |
| `test_val_never_allocates_slicing_tiles`（同上） | val 无 `slice_transform`，base 段结构上为 0 |
| `test_val_items_are_whole_frames_with_their_ground_truth` | val 下标解析出**整图**（`ori_shape == 原图尺寸`），且框与标签文件逐值一致（切片 tile 会被重新归一化，可判别） |
| `test_the_val_dataloader_can_actually_be_built` | 真正建一次 `build_dataloader` 并取一个 batch —— 直接复现"`min(batch, len)` 变 0"这条崩溃路径 |
| `test_the_guard_does_not_leak_into_the_training_build` | `augment=True` 仍读池布局，且 `_tile_units` 仍按调度给出 |
| `test_a_train_build_with_img_origin_still_adds_the_whole_frame_segment` | `img_origin` 语义未变（训练 `+N`） |

### 12.6 验证

- 全量 `pytest tests/ -k ooo -q` → **179 项 0 失败**（含上述 8 项）；
- `ruff check ultralytics_ooo` 与测试文件 lint → 全部 `All checks passed`；
- **端到端真实训练**（`_mini_val_set`，即 `train.py` 原配置 `img_origin=False`、320px、2 epoch、CPU）：
  正常完成 2 epoch，产出 `best.pt` / `last.pt` / `best_whole.pt`，切片口径与整图口径均出指标；
  val 集 3 张 1920×1080 整图逐张可读、各 1 个框。

### 12.7 行为变更提醒

`img_origin` 默认值从 `False` 回到 `True`。**仅在用户未显式配置该键时才有差别**：不配置时，
每张原图会额外以整图形态进入池（宽度 `N`）—— 这也正是文档与已提交测试一直描述的默认语义。
`train.py` 显式写了 `img_origin=False`，因此示例脚本行为不变。

