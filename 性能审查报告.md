# 性能审查报告（从零重做）

> 审查对象：`C:\Users\Administrator\Desktop\ultralytics-main`（`ultralytics_ooo` 在线增强池，4834 行 / 16 个文件）
> 审查日期：2026-09-15
> 审查方式：**全部结论来自实测**（真实 `build_yolo_dataset` + 真实 `build_dataloader` + 真实 `cProfile`），不引用此前的任何结论。所有数字均可复现，脚本见 §8。

---

## 0. 一句话结论

**这条数据管线是纯 CPU 密集型的。瓶颈自上而下依次是：① 样本池规模（`slice_all_tiles=True` 时池子放大到 `5N`）；② 每样本 4 次的 Mosaic 连带读取；③ 单次 JPEG 解码与 Mosaic 画布/仿射；④ 三个退化分支（噪声/长拖影）的像素级算子。**

其中**三处确定性缺陷/失配**让本该生效的优化静默失效，合计占可省成本的 60%–70%：

| 缺陷 | 实测代价 | 性质 |
|---|---|---|
| **A. 原始图 LRU 容量（默认 4）与 Mosaic 复用窗口（63）严重失配** | 默认配置 +**85%**（20.4 → 37.7 items/s） | 调参失配 |
| **B. 分组采样器在真实训练路径下被静默关闭**（门控遗漏） | 同缓存下 +**29%**（20.4 → 26.4 items/s） | **功能缺陷** |
| **C. "把容量调小"的守卫用错统计量**，在真实规模数据集上给出反向建议 | 它把 B 的收益挡在门外；修好后 **+117%**（→ 44.3 items/s） | 判据错误 |

**三项全部修好后的实测上限：20.4 → 44.3 items/s（2.17×），49.1 → 22.57 ms/样本。** 且三项都**不改变** 7 段布局、`len()` 恒定性与 worker 可复现性——它们只改变**访问顺序与缓存命中**，不改变**选中的图**。

> 与上一版报告的差异：上一版把"`ims` 缓存在切片开启时失效"和"分组采样器被关闭"列为两处缺陷。本轮从零重测后，确认这两条成立，并**新增了一条量级更大的失配（A）与一条判据错误（C）**——`raw LRU` 的容量扫描显示它是单项收益最大的旋钮，而此前只被当作静态事实记录。`ims` 失效（原 P0-2）本轮保留，但它与 A 是同一类问题的两个实例：**缓存没有对着真实的复用窗口来定尺寸。**

### 0.1 修复状态（本报告已落地）

> **P0-1 / P0-2 / P0-3 三项已于第七轮全部修复并验证，详见 §9。** 一句话：`slice_raw_cache_size` 默认 4 → **16**，新增字节预算 `slice_raw_cache_mb`（256）；`grouped_sample_units()` 的分支门控改读 `_mask_specs` 同一张表（compose 不再漏）；oversized 守卫**删除**（判据不可外推）。回归测试 **+4**（全量 **183 项通过**），并新增一条**复刻 trainer 真实构建顺序**的集成测试。**P0-4 及以下（P1/P2/P3）尚未实施。**

---

## 1. 审查方法与边界

### 1.1 实测对象

- 数据集：合成"类照片"数据（平滑渐变 + 软形状 + 轻噪点，JPEG q=88），保证压缩率与现实接近。
  - `ds240`：240 训练图 @1280×720，均值 215.7 KB/张（62 MB）——本轮主力
  - `ds80`：80 训练图 @640×360，均值 55.7 KB/张（5.5 MB）
  - 每图 2–6 个框，3 类
- 环境：Python 3.12.13 / torch 2.5.1（intra-op 4 线程）/ OpenCV 5.0.0 / numpy 2.0.1 / 8 逻辑核 / `cv2.getNumThreads()=8`
- 配置矩阵：11 组（A 惰性 … L 无 mosaic），见 §8.2
- **测量层次（本报告的核心方法论）**：
  1. **原语级微基准**——解码 / resize / warpAffine / 各退化算子，确定单价；
  2. **逐样本直读 dataset**（绕过 sampler）——分阶段耗时、解码计数、LRU 命中；
  3. **真实 `build_dataloader`**——唯一能看到"实际生效的 sampler"与端到端吞吐的层次。**结论 A/B/C 都靠这一层才能测出来。**

### 1.2 一个必须写在前面的环境事实

本机 **物理内存 7.9 GB，可用长期只有 0.5–1.4 GB**（用户侧 douyin / WorkBuddy×5 / Edge / VSCode 占 6.5 GB）。这直接导致：

- 多次 `numpy._core._ArrayMemoryError: Unable to allocate 1.17 MiB`、`cv2.error: Insufficient memory (2.7 MB)`、`OSError [WinError 1455] 页面文件太小` 无法加载 torch DLL，甚至若干次 **段错误 / 进程被 OOM 杀死**。
- 这些**不是被测代码的缺陷**，而是环境容量不足。但它们本身是一条有价值的审查发现：**这套管线在 8 GB 级机器上跑一张 240 图 720p 的数据集就会持续撞上提交限制**，部署时必须按 §3.2 的每 worker 内存预算预留。

### 1.3 本次测量的口径声明（重要）

**所有吞吐数字均为 `workers=0`（单进程、`batch=8`、imgsz=320、240 图）。** 原因见 §1.2：`workers≥2` 在本机必然 OOM（`torch` DLL 加载失败 / mosaic 画布分配失败 / 进程被 SIGTERM）。口径选择的理由：单进程排除了进程启动与 IPC 噪声，**更适合比较配置间的相对差异**，而本报告的结论全部是相对比较。
**绝对 items/s 随机器变化；"解码主导"与"缓存失配"这两条结构性结论与机器无关。**

---

## 2. 关键路径的阶段分解

### 2.1 路径拓扑（每 1 个训练样本实际发生了什么）

```
DataLoader worker
 └─ dataset[i]  (BaseDataset.__getitem__)
     ├─ get_image_and_label(i)                       ← ① 主样本
     │    ├─ _sync_epoch_masks()     每 16 样本一次锁读
     │    ├─ _ensure_epoch_sel()     1 次 int 比较
     │    ├─ _segment_bases()        4.6 µs（缓存命中）
     │    └─ 按段分派 → base / origin / ratio / blur / compose / weather / occlusion
     │         ├─ _load_image_cached() → imread()  ← 唯一的重活（1280×720 实测 7.7–9.6 ms）
     │         │      └─ raw LRU 命中时退化为一次全分辨率 memcpy（2.64 MiB）
     │         ├─ (分支算子)  ← 见 §4.2
     │         ├─ _finalize_label() → cv2.resize → imgsz
     │         └─ _touch_buffer()   ← 把本槽位推进 Mosaic 复用窗口
     └─ transforms(...)  Compose
          ├─ Mosaic.__call__  (p=hyp.mosaic，默认 1.0)
          │    └─ get_params → random.choices(list(dataset.buffer), k=3)
          │         → get_image_and_label(j) × 3   ← ② 三个连带样本！
          │         (每个都是完整的 ① 流程，只是不过 transforms)
          ├─ RandomPerspective (warpAffine)
          ├─ MixUp / CutMix (p=0 默认短路，不额外读)
          ├─ CopyPaste (p=0 默认短路)
          ├─ Albumentations / RandomHSV / RandomFlip×2
          └─ Format (cvtColor + transpose + ascontiguousarray)
```

**② 是最重要的结构事实**：上游 `ultralytics/data/augment.py:497-498`

```python
if self.buffer_enabled:  # select images from buffer
    return random.choices(list(self.dataset.buffer), k=self.n - 1)
```

实测确认 **Mosaic 乘数 = 4.0**（全组一致）。即 `hyp.mosaic=1.0`（默认，前 `epochs-10` 轮）时，每个训练样本要走 4 遍数据集侧流程；`close_mosaic` 的末 10 轮降到 1。

**而"从 buffer 里取"这一点决定了优化的方向**：伴随图并非从全库均匀抽取，而是从**最近访问窗口**里抽。窗口大小 `max_buffer_length = min(ni, batch_size*8, 1000)`（`ultralytics/data/base.py:131`）——本数据集为 `min(240, 64, 1000) = 64`，即**窗口 63 个槽位**。这既意味着伴随图大量重复（缓存可以接住），也意味着**缓存必须对着这个窗口来定尺寸**——这正是缺陷 A 的由来。

### 2.2 单样本分解（`ds240` / imgsz=640 / 1280×720 源图）

| 阶段 | 惰性配置 A | 切片配置 C |
|---|---|---|
| **总耗时 / 样本** | **29.92 ms** | **55.62 ms** |
| 数据集侧 | 10.75 ms（36%） | 35.64 ms（64%） |
| ├ JPEG 解码 `imread` | 0.93 次 × 9.59 ms ≈ **8.9 ms** | 2.90 次 × 9.59 ≈ **27.8 ms** |
| ├ resize → imgsz (`_finalize_label`) | ≈ 1.0 ms | ≈ 2.9 ms |
| └ label deepcopy / 段戳 / buffer | ≈ 0.1 ms | ≈ 0.5 ms |
| 变换侧 | 19.18 ms（64%） | 19.98 ms（36%） |
| ├ Mosaic 画布分配（1280×1280×3 = 4.69 MiB） | **3.08 ms** | 3.08 ms |
| ├ `RandomPerspective` warpAffine | **9.63 ms** | 9.63 ms |
| ├ `Format`（cvtColor+transpose+ascontiguous） | ≈ 5.4 ms | ≈ 5.4 ms |
| └ HSV / Flip / collate 等 | ≈ 1.1 ms | ≈ 1.9 ms |
| **Mosaic 读数乘数** | 4.0 | 4.0 |

C 比 A 每样本贵 86%，而池子只大 1.5 倍——**多出来的 25 ms 几乎全是额外 2 次 JPEG 解码**。

### 2.3 池规模乘数（真正的量级来源）

| 配置 | 段布局 [base, origin, ratio, blur, compose, weather, occlusion] | 池 | 池/图 | 每 epoch 耗时（ds80@320） |
|---|---|---|---|---|
| A 惰性 | `[0, 80, 0, 0, 0, 0, 0]` | 80 | 1.00 | 0.85 s |
| C 单片刻度 0.5 | `[40, 80, 0, 0, 0, 0, 0]` | 120 | 1.50 | 2.07 s |
| L 全片 r=1.0，无 mosaic | `[320, 80, 0, 0, 0, 0, 0]` | 400 | 5.00 | 4.52 s |
| **B 全片 r=1.0，有 mosaic** | `[320, 80, 0, 0, 0, 0, 0]` | 400 | 5.00 | **6.78 s** |
| H 全分支 r=0.5，无 mosaic | `[160, 80, 40, 80, 10, 40, 40]` | 450 | 5.62 | 9.15 s |

**同一份 80 张图，B 的一个 epoch 是 A 的 8.0 倍。** 拆开看：池子 ×5.0（结构成本），每样本 ×1.6（解码与变换成本）。

> 关键点：`slice_ratio` 的**默认值就是 1.0**，`slice_all_tiles` 一旦打开，池子立刻变成 `4N + N (origin) = 5N`。用户"只是打开切片"就会拿到 5 倍 epoch。

### 2.4 真实 DataLoader 吞吐（本轮新增，缺陷 A/B/C 的证据层）

`ds240` / imgsz=320 / batch=8 / 16 个 batch（128 样本）/ workers=0 / 真实 `build_dataloader`：

| cap | set_epoch 先行 | 实际 sampler | items/s | ms/样本 | LRU 命中 | RSS MB |
|---|---|---|---|---|---|---|
| **4（默认）** | ✗（**trainer 真实顺序**） | RandomSampler ⚠️ | **20.4** | **49.10** | 23.5% | 417 |
| 4 | ✓ | GroupedImageSampler | 26.4 | 37.84 | 29.0% | 418 |
| 8 | ✗ | RandomSampler | 27.2 | 36.71 | 46.1% | 428 |
| 16 | ✗ | RandomSampler | 29.9 | 33.44 | 67.2% | 450 |
| 16 | ✓ | **被守卫降级为 RandomSampler** | 34.4 | 29.07 | 62.9% | 448 |
| 16 | ✓ + 强制分组 | GroupedImageSampler | **41.0** | **24.40** | 63.8% | 449 |
| 32 | ✗ | RandomSampler | 37.7 | 26.55 | 78.2% | 491 |
| **32** | ✓ + 强制分组 | GroupedImageSampler | **44.3** | **22.57** | 76.6% | 491 |

**读法**：默认那一行（cap=4 + 分组失效）是现状，44.3 那一行是修好 A+B+C 后的上限。**2.17×。**
（注：命中率是"命中/(命中+未命中)"的比值，而分组会改变"同一时刻在用的不同图集合"，所以命中率与吞吐不是严格单调——例如 cap=16 分组后命中 63.8% 略低于不分组 67.2%，但吞吐 41.0 > 29.9。**以吞吐为准。**）

---

## 3. 资源占用

### 3.1 CPU —— 唯一的瓶颈，且已存在超订

| 指标 | A@320 | B@320 | C@320 | H@320（无 mosaic） |
|---|---|---|---|---|
| 墙钟 / 样本 | 10.65 ms | 16.96 ms | 17.29 ms | 20.07 ms |
| **CPU 时间 / 样本** | 37.11 ms | 59.61 ms | 60.42 ms | 32.11 ms |
| **CPU/墙钟 比** | **3.49** | **3.52** | **3.50** | **1.60** |

单 worker 的 CPU 时间就是墙钟的 1.6–3.5 倍：**OpenCV 自带线程池在每个 DataLoader worker 内部就已经并行展开**。默认 `cv2.getNumThreads()=8`，于是 `workers=8` 时理论并发线程 = 8×8 = 64，跑在 8 个核上——典型的 CPU 超订。

另一组独立口径（`ds240`@640 逐样本直读）给出 **CPU/墙钟 = 0.88–0.98**，说明在该层次上管线**没有等 IO**、是纯 CPU 密集——两条口径不矛盾：前者含 OpenCV 内部多线程展开，后者是进程总 CPU 除以墙钟。

Profiler 佐证（`ds240` / C 配置 / 37 个样本 / 2.10 s）：

```
ncalls  tottime  cumtime  function
   137    1.049    1.049  {imdecode}        ← 50% 的总时间
    37    0.149    0.149  {warpAffine}
   148    0.129    0.129  {resize}
    74    0.114    0.114  {cvtColor}
   199    0.099    0.099  {numpy.ascontiguousarray}
   137    0.075    0.076  {numpy.fromfile}
    37    0.068    0.069  numpy.full          ← Mosaic 画布
```

`imdecode` 独占 50%；纯 Python 侧（`_geometry`、`_segment_bases`、`_sel_indices`、`deepcopy`）加起来不到 2%。

### 3.2 内存 —— 每 worker 预算与瞬时尖峰

**常驻（每 worker，imgsz=640）**

| 项 | 实测值 | 说明 |
|---|---|---|
| `self.ims` 整图缓存 | 63 帧 × 1.17 MiB = **74 MiB** | 启动日志自报；**切片开启时完全不写入（见 §4.4）** |
| `_raw_cache` 原图 LRU | cap × 2.64 MiB（1280×720×3） | cap=4 → **10.5 MiB**；cap=32 → **84.4 MiB** |
| Mosaic buffer | 63 个**索引** | 仅整数，可忽略 |
| **合计** | cap=4 ≈ **85 MiB/worker**；cap=32 ≈ **159 MiB/worker** | `workers=8` → 0.68 / 1.27 GiB |

**实测的精确线性关系（本轮新增，对定预算很关键）**：`ds240`@320 下 cap 4→32 时 RSS 由 417 → 491 MB（**+74 MB**），而缓存本身由 10.5 → 84.4 MiB 也正好 **+74 MiB**。
> **调大缓存的代价是精确可预测的：`cap × 源图字节数 × workers`，与 imgsz 无关。**
> 1280×720 源图：cap=32 → 84 MiB/worker（可接受）；**4000×3000 源图（36 MB/帧）：cap=32 → 1.1 GB/worker（会 OOM）**。
> 而当前 `slice_raw_cache_size` **只有"条目数"、没有字节预算**（对比 `ims` 早有 `ims_cache_mb`）——这是缺陷 A 的修复必须配套补上的东西。

**瞬时（可远超常驻）**

| 分配点 | 720p 实测 | 代码自述的 4000×3000 |
|---|---|---|
| Mosaic 画布 `(2·imgsz)²×3` | 4.69 MiB | 4.69 MiB（与源图无关） |
| `_apply_weather(noise)` 的 float32 缓冲 | **10.55 MiB** | **824 MB** |
| `_load_image_cached(copy=True)` 命中拷贝 | 2.64 MiB | ~36 MiB |
| ratio-pad 画布（`np.full`） | 1920×1440×3 = **3.5 ms / 7.9 MiB** | 可达原图数倍（8000×4500 = 108 MB，代码注释已自述） |
| `degrade_max_side<0` | 全部退化算子按原分辨率 | 代码自述 ~10× 内存 / ~5× 时间 |

### 3.3 IO —— 不是瓶颈（默认）

- 每样本解码读取量：**56.9–206.5 KB**（`jpeg_kb_per_item` 实测；与解码次数严格成正比，≈215 KB/次）。240 图 720p 全量也才 62 MB，几乎全在 OS page cache（上游 "Fast image access" 探针实测 75–1100 MB/s）。
- **默认零写盘**（所有配置 `write_bytes = 0`）。唯一的写路径是 `*_save_dir`。
- 结论：IO 只在两种情形成为问题——① 数据集远大于内存（此时 `imread` 会真的落盘）；② 打开 `slice_save_dir` 等保存开关（见 §5-P2-4）。

---

## 4. 瓶颈定位与成因

### 4.1 【一级】池规模：`slice_all_tiles=True` × `slice_ratio=1.0` = 5N

- **现象**：同一数据集 epoch 耗时 8.0×。
- **成因**：`_segment_lengths()` 里 `base = n_per * K_slice`，`n_per = 4`（all_tiles），`K_slice = round(slice_ratio × N) = N`（默认 1.0）；再加 `img_origin=True` 的 `N`。
- **性质**：这是**设计使然**（4 片全发 + 整图覆盖），不是 bug；但 `slice_ratio` 默认 1.0 会让"只想试试切片"的用户被动支付 5 倍成本。

### 4.2 【一级】退化分支的像素级算子（默认参数下已很贵，改一个开关可再贵 5–11 倍）

单线程、1280×720 实测：

| 算子 | ms | 备注 |
|---|---|---|
| `blur` 短档 len=8，**axis_aligned=True（默认）** | 4.80 | `cv2.blur`，O(1) 行和 |
| `blur` 短档 len=8，angle 任意 | 6.04 | `filter2D` |
| `blur` 长档 len=27，**axis_aligned=True + defocus**（默认） | 12.31 | `cv2.blur` + `GaussianBlur` |
| `blur` 长档 len=27，angle 任意 + defocus | **59.53** | 稠密 PSF，`filter2D` O(k²) |
| `blur` 长档 len=27，angle 任意、无 defocus | 53.49 | 可见代价来自 ~27 抽头 PSF |
| `weather` rain | 9.14 | |
| `weather` haze | 2.62 | |
| **`weather` noise** | **32.44** | **单样本最贵的算子**；`weather_types` 默认含 `noise` |
| `occlusion` rect | 5.63 | |
| `cap_long_side` → 1280（对 720p 素材是 **no-op**） | **0.00** | |
| `cap_long_side` → 960（0.75×） | 9.80 | 所以"降采样省钱"只在算子比 resize 更贵时成立 |

**成因与一个被误读的旋钮**：`degrade_max_side` 的 auto = `2 × imgsz`。对 imgsz=640 即 1280——**恰好等于 720p 素材的长边，于是 cap 完全不触发**（实测 0.00 ms）。也就是说：**在"720p 数据 + imgsz 640"这个最常见组合下，三个退化分支全都在原始分辨率上跑**，而结果马上又被 `_finalize_label` 缩回 640。`2×` 的"过采样"理由在这里付出的是 4× 像素成本，收益却看不见。

### 4.3 【一级·缺陷 A】原始图 LRU 容量（默认 4）与 Mosaic 复用窗口（63）失配 ★本轮新增

**现象**：默认配置下 LRU 命中率仅 **23.5%**，每样本仍要解码 **2.33 次**（imgsz=640 逐样本直读口径为 2.33；真实 loader 口径 23.5% 命中）。

**成因**：Mosaic 窗口 = 63 个**槽位**（`min(ni, batch*8, 1000) - 1`）。`all_tiles` 下每张图占 5 个槽位（4 片 base + 1 整图 origin），因此**窗口内实际只有 ~13 张不同图**。而 `_raw_cache` 默认容量是 **4** —— 只覆盖窗口内 13 张图里的 4 张。

**容量扫描（B 配置，逐样本直读，imgsz=640，240 图）**：

| `slice_raw_cache_size` | 缓存 MiB | ms/样本 | 解码次/样本 | 命中率 | RSS |
|---|---|---|---|---|---|
| 0（关闭） | 0 | 67.77 | 4.00 | — | 405 |
| **4（默认）** | 10.5 | **49.90** | **2.33** | 44.5% | 416 |
| 16 | 42.2 | 32.07 | 1.38 | 68.9% | 448 |
| **32** | 84.4 | **31.08** | **0.93** | **82.1%** | 490 |
| 64 | 168.8 | 37.80 | 0.93 | 82.1% | 532 |

**收益在 16–32 之间饱和**（命中率 82% 到顶），64 处反而变慢（RSS 532 MB，开始内存抖动）。
**真实 loader 口径独立复核（§2.4）**：cap 4→32 为 20.4 → 37.7 items/s = **+85%**，ms/样本 −46%。两个层次、两种口径，结论一致。

> 修复方向：默认容量应由 Mosaic 窗口推导（≈ `max_buffer_length / fan_out`，本数据集 ≈13 → 取 16），并与新增的 `slice_raw_cache_mb` 预算取 `min`，保留 `slice_raw_cache_size` 作为显式覆盖。

### 4.4 【一级·缺陷 B】分组采样器在真实训练路径下被静默关闭

这是上一版报告已发现、本轮用**真实 loader 层次**再次确认并补齐因果链的问题。

**证据（按训练器真实顺序：先建 loader，`set_epoch` 发生在 epoch 开始之后）**：

| 配置 | 实际拿到的 sampler | 池 |
|---|---|---|
| `slice_prob=1, all_tiles=True, slice_ratio=1.0`（**默认切片配置**） | **RandomSampler** ⚠️ | 1200 |
| `slice_prob=1, all_tiles=False, slice_ratio=1.0` | RandomSampler | 480 |
| `slice_prob=1, all_tiles=True, slice_ratio=0.5` | GroupedImageSampler ✓ | 720 |
| `slice_prob=1, all_tiles=True, slice_ratio=1.0, compose_keep=True` | GroupedImageSampler ✓ | 1260 |
| `slice_prob=1, all_tiles=True, slice_ratio=1.0, weather_keep=True(r=0.5)` | GroupedImageSampler ✓ | 1320 |

伴随一条**误导性警告**（原文）：

```
WARNING train: grouped_sample_units produced 1260 indices but the pool holds 1200: the pool layout
and the grouping rules have drifted apart. Falling back to the plain shuffle; fix the grouping rules
```

**根因（两条叠加）**：

1. `dataset.py::grouped_sample_units()` 构造 compose 槽位时**没有按 compose 是否开启过滤**：
   ```python
   compose_slot = {g: compose_base + j for j, g in enumerate(self._sel_indices("compose", (n + 3) // 4))}
   ```
   它对 `img_origin` / `ratio_pad_keep` / `blur_keep` / `weather_on` / `occlusion_on` **五个分支都做了显式门控**（`dataset.py:1163-1178`），**唯独漏了 compose**。compose 关闭时该段宽度为 0，但 `compose_ratio` 的**默认值就是 1.0**，于是 `_sel_compose` 处于 `None` 哨兵态 → `_sel_indices` 返回 `range(60)` → 凭空造出 60 个 ≥ 池总长的槽位（1200…1259）→ `flat != list(range(total))` → 走 `return None` 分支。
2. **`None` 哨兵被重载了两种语义。** `_sel_indices` 的注释（`dataset.py:546-554`）写明：`None` = "尚未重建，或 ratio ≥ 1"，返回 `range(count)`。也就是说 **`None` 同时表示"ratio ≥ 1（该用全部槽位）"与"分支关闭（该段宽度为 0）"**。而 `grouped_sample_units()` 由 `build_dataloader()`（`trainer.py:407`）在**第一次 `set_epoch`（`trainer.py:449` 的 `on_train_epoch_start` 回调，由 `installer.py:75-93` 安装）之前**调用；唯一能校正哨兵的惰性重建 `_ensure_epoch_sel()` 在 `_has_partial_ratio()` 为假时直接返回——**当所有启用分支的 ratio 都 ≥1（含默认 1.0）时它恰好为假**。

**最小复现（`diag2.py`，严格复刻 trainer 构建顺序）**：

```
[trainer-order]  mask_stamp = -1  _has_partial_ratio() = False
[trainer-order]  len(_sel_indices('compose', 60)) BEFORE set_epoch = 60  (compose segment = 0)
WARNING train: grouped_sample_units produced 1260 indices but the pool holds 1200: ...
[trainer-order]  grouped_sample_units -> None (DISABLED)

[after set_epoch] len(_sel_indices('compose', 60)) = 0  compose segment = 0
[after set_epoch] grouped_sample_units -> ok          ← 唯一差别是 set_epoch 先执行
```

于是：**只要"切片开启 + `slice_ratio=1.0`"或任何"所有启用分支 ratio≥1、同时某个关闭分支 ratio≥1"的组合，分组采样永不生效。**

**为什么测试没抓到**：所有测试与 `tools/` 都在调用 `grouped_sample_units()` 前先调 `ds.set_epoch(...)`（`grep set_epoch` 可见 20+ 处），而 `test_ooo_sampler.py` 用的是带 `groupable` 标志的**桩对象**，直接替换了 `grouped_sample_units` 本身——**"loader 构建早于 set_epoch"这个真实集成顺序，恰好落在两者的盲区里。**

**代价（本轮实测，同缓存容量下对比）**：

| 组合 | 分组 | items/s | ms/样本 | 命中率 |
|---|---|---|---|---|
| cap=4，分组生效 | ✅ | 26.4 | 37.84 | 29.0% |
| cap=4，分组失效（**现状**） | ❌ | **20.4** | 49.10 | 23.5% |

**修好 B 本身值 +29%。**

### 4.5 【一级·判据错误 C】"把容量调小"的守卫用错了统计量 ★本轮新增

`dataset.py:1186-1201` 有一条守卫：

```python
fan_out = max((len(b) for b in per_image), default=0)
if fan_out < self._raw_cache_size:      # 默认 4，all_tiles 下 fan_out = 5
    ... 告警"Lower it to <= {fan_out}（the default 4 is usually best）"
    return None                          # 关闭分组
```

它的依据是注释里的实测：**"slice_raw_cache_size=16 vs 4，24 图池，命中率 80.0% → 60.0%"**。

**但这是在 24 图池上得到的结论，不能外推到真实规模。** 24 图池下 `max_buffer_length = min(24, 64, 1000) = 24`，**Mosaic 窗口（23）比数据集还小**，整库都装得进缓存，容量当然无关紧要。而真实规模（240 图）下，窗口 63 槽位 ≈ 13 张不同图，容量至关重要——实测正好相反：

| 组合 | 分组 | items/s | ms/样本 | 命中率 |
|---|---|---|---|---|
| cap=4，分组生效 | ✅ | 26.4 | 37.84 | 29.0% |
| cap=16，**被守卫关闭分组** | ❌ | **34.4** | 29.07 | 62.9% |
| cap=16，强制分组 | ✅ | **41.0** | 24.40 | 63.8% |
| cap=32，**被守卫关闭分组** | ❌ | 37.7 | 26.55 | 78.2% |
| cap=32，强制分组 | ✅ | **44.3** | 22.57 | 76.6% |

**守卫的建议（"调小到 4"）选到了更差的那个区域：34.4 > 26.4。** 判据应该是 **"Mosaic 窗口内可达的不同图数"（≈ `max_buffer_length / fan_out`）**，而不是"单图扇出"。当数据集远大于窗口时，这个守卫不应触发（或应反向建议"调大"）。

### 4.6 【二级·缺陷】开启切片后，整图解码缓存 `ims` 完全失效

前一版已发现，本轮确认。

**证据**：`ims_resident` 实测 **A=63（缓存打满）**，而 **B/C/H/L 全部 = 0**；`decodes_per_item` 由 **0.925 → 2.9~3.7**；`jpeg_kb_per_item` 由 199.5 KB → **615–626 KB**。

**根因**：`load_image()` 只在 `slice_transform is None` 时才写入 `self.ims`：

```python
if self.augment and self.cache != "ram":
    if getattr(self, "slice_transform", None) is None:
        self.ims[i], self.im_hw0[i], self.im_hw[i] = im, (h0, w0), im.shape[:2]
        self._remember_ims(i)
    # With slicing: do NOT cache in self.ims here.
```

而 `_build_origin_sample()`（覆盖每张原图 1 槽的 origin 段）走的正是 `load_image()`。结果是：**启动日志报出"74 MiB/worker"的 `self.ims` 预算，在切片开启时一分钱都没花，origin 段每次访问都重新解码一张原图。**这是纯粹的浪费，而不是取舍。

> **A 与它的关系**：两者是同一类问题的两个实例——**缓存没有对着真实复用窗口定尺寸**。区别在于 `ims` 是"缓存压根没启用"（结构性），`_raw_cache` 是"启用了但容量差一个数量级"（参数性）。修 A 的收益更大、风险更低，建议先做。

### 4.7 【三级】每样本固定开销（实测，占比小，但可去掉）

| 项 | 实测 | ×4 次/样本 | 占比（720p） |
|---|---|---|---|
| `_segment_bases()` 版本戳 | 4.6 µs | 18 µs | 0.06% |
| `_mask_specs()` | 1.8 µs | — | — |
| `deepcopy(label)`（4 框） | 11.7 µs | 47 µs | 0.16% |
| `OnlineSlice._geometry`（4 框，only=k） | 60 µs | 0.24 ms | 0.8% |
| `OnlineSlice._geometry`（4 框，all 4 片） | 160 µs | 0.64 ms | 2.1% |
| `target_tiles()`（全 4 片） | 176 µs | — | 仅调度版每 epoch 首次 |
| `np.ascontiguousarray` | ~0.5 ms/次 | 5.4 次 → **2.7 ms** | **9%** |
| Mosaic 画布 alloc | 3.08 ms | 1 次 | 10% |
| warpAffine | 9.63 ms | 1 次 | 32% |

**结论**：Python 侧簿记（段戳/deepcopy/几何）完全可以忽略；真正的固定开销是 `ascontiguousarray` 的多次全数组拷贝与 Mosaic/仿射。
注意 `_geometry` 随框数线性增长（代码注释：200 框时 0.58 ms with `center_bias`），在**密集标注**场景 ×4 可达 2.3 ms/样本（8–14%），届时值得缓存。

### 4.8 【三级·潜在】`count_slice` 从未被传 `False`

`get_image_and_label(index, count_slice=True)` 的文档说"Mosaic/CutMix/MixUp 的辅助样本传 `False`，以免污染 `neg_ratio` 配额或重复保存"。实际上全仓库只有 `dataset.py` 三处出现 `count_slice`（定义 + 2 处使用），上游 `augment.py:365` 调用时**只传了一个参数**，默认即 `True`。

后果（**潜在，不是热路径成本**）：

- Mosaic 的 3 个连带样本会**计入 `_pos_count`/`_bg_count`**，使 `slice_background_ratio` 的"每 epoch 配额"实际按 ~4 倍速度消耗。
- 若设置了 `slice_save_dir`，连带样本也会触发保存（虽有 `_saved_keys` 去重）。
- 默认参数下（`slice_background_ratio=-1` → `_allow_background()` 恒真；`slice_save_dir=None`）**不可见**，所以这是一个"配置一改就咬人"的潜伏缺陷。

---

## 5. 优化项与优先级排序

评分口径：**影响** = 实测/可推算的对 epoch 墙钟降幅；**成本** = 改动面 / 回归风险。带 ★ 的是本轮从零重测新增的项。

| 优先级 | 优化项 | 影响（实测） | 成本 | 依据 |
|---|---|---|---|---|
| **P0-1 ★** | **重定 `_raw_cache` 容量**：默认由 Mosaic 窗口推导（≈ `max_buffer_length / fan_out` → 16），并**新增 `slice_raw_cache_mb` 字节预算**（与 `ims_cache_mb` 对称）以防大图 OOM | **+85%**（20.4 → 37.7 items/s）；逐样本口径 49.9 → 31.1 ms（**−38%**）；解码 2.33 → 0.93 次/样本 | **低**（约 15 行 + 回归测试） | §4.3 |
| **P0-2** | **修好分组采样器门控**：`grouped_sample_units()` 的 compose 块按开启状态过滤；或让 `_sel_indices` 对关闭分支返回 `[]`（恢复其文档承诺的语义） | **+29%**（20.4 → 26.4 items/s）；消除每次训练的误导性警告 | **低**（1–3 行 + 1 个回归测试） | §4.4 |
| **P0-3 ★** | **修正 oversized 守卫的判据**：改用"窗口内可达图数"（`max_buffer_length / fan_out`），数据集远大于窗口时不触发/反向建议 | 解锁 P0-1 的完整收益（**P0-1+P0-2+P0-3 合计 20.4 → 44.3 items/s = +117%**） | 低（判据 1 行 + 注释补全实测条件） | §4.5 |
| **P0-4** | **让 origin 段走 `_load_image_cached` + `_finalize_label`**（或允许 `load_image` 在切片开启时也填 `ims`） | origin 段（池内 N 槽）**不再每次重解码**；解码次数 −1.0/样本左右 | 低（一处分派改动） | §4.6 |
| **P1-1** | **降 `weather_types` 默认去掉 `noise`，并把 noise 改成不分配 float32 整图**（`cv2.randn` 到 int16 + `cv2.add`，或分块） | 单样本最贵算子 **32.4 ms → 目标 <10 ms**；瞬时内存 −10.5 MiB@720p（−824 MB@4000×3000） | 中（核心算子改写 + 逐位对齐测试） | §4.2 |
| **P1-2** | **把 `degrade_max_side` 的 auto 从 `2×imgsz` 降到 `1×~1.5×imgsz`**，或在 `longest ≤ cap` 时明确日志提示"cap 未生效" | 720p/imgsz640 下让退化算子直接省 4× 像素成本；1080p 素材省 ~44% | 低（改常量 + 日志） | §4.2 |
| **P1-3** | **`slice_all_tiles=True` 时把 `slice_ratio` 默认降到 0.25~0.5**，并在旋钮旁写出 `池 ≈ (n_per×ratio + 1)×N` | epoch 成本按比例下降（r 1.0→0.25：池 5N→2N，**−60%**） | 低（默认值 + 文档；**会改变既有实验语义，需用户确认**） | §4.1 |
| **P1-4** | **对 `blur_axis_aligned=False` 加一次性告警 / 或把任意角长档改成两次可分离 1-D 卷积** | 关掉轴对齐时**避免 5–11× 退化分支暴涨**（53–60 ms → 目标 ~12 ms） | 中（算法改动） | §4.2 |
| **P2-1** | **去掉冗余全数组拷贝**：`_emit` 的 `ascontiguousarray` 可直接把切片视图交给 `cv2.resize`；`_load_image_cached` 的 `hit.copy()` 改为"调用方声明会写时才拷" | 省 ~2.7 ms/样本（**~9%**） | 中（`_emit`/`cv2.resize` 契约需验证） | §4.7 |
| **P2-2** | **每 worker 固定 OpenCV 线程数**：`cv2.setNumThreads(max(1, cpu_count // num_workers))`，worker 内 `torch.set_num_threads(1)` | 消除 `8 worker × 8 cv2 线程 = 64 线程 / 8 核` 的超订，提高实际吞吐并降低 CPU 抖动 | 低（安装期一行 + 文档） | §3.1 |
| **P2-3** | **修 `count_slice`**：包装 `BaseMixTransform.get_params` 传 `count_slice=False`，或在 `get_image_and_label` 内用"是否处于 mix 阶段"判定 | 修正 `slice_background_ratio` 配额与保存去重的语义（对默认配置无性能影响，但消除潜伏陷阱） | 中（需摸清 mix 相位） | §4.8 |
| **P2-4** | **给保存加上有限默认上限**（`slice_save_max` 默认 0=无限） | 避免首轮对 5N 池逐张 `imencode`+写盘（IO/CPU 双爆） | 低 | §3.3 |
| **P2-5 ★** | **启动时按内存预算打印 worker 建议**（`min(核数, 可用内存 / 单 worker RSS)`），并在 `workers≥2` 时提示 `cv2` 线程会按 worker 数摊分 | 避免"8 worker 打爆 8 GB 机器"这类部署事故 | 低 | §3.2 |
| **P3-1** | `_geometry` 结果按 (图, epoch) 缓存（仅当框数多时收益显著） | 密集标注下省 2.3 ms/样本（8–14%）；4 框场景无收益 | 中 | §4.7 |
| **P3-2** | `ims_cache_frames`/`ims_cache_mb` 在 P0-4 之后重新标定 | 内存与重复解码的再平衡 | 低 | §3.2 |

### 5.1 建议的执行顺序

1. **先做 P0-1 + P0-2 + P0-3（一组，一起做）。** 三者互相解锁：只调容量会被守卫拦下（P0-3），只修分组会被容量拖住（P0-1）。**这是全表投入产出比最高的一组：约 20 行改动换 2.17× 吞吐**，且不改变布局、池长、增强语义。
   - 回归测试必须用**真实构建顺序**：`build_yolo_dataset` 之后**不调 `set_epoch`**，直接断言 `sum(len(blk) for unit in units for blk in unit) == len(ds)`，并断言无 `drifted apart` 告警。
   - 容量默认值改动必须**保留显式覆盖路径**（`slice_raw_cache_size` 语义不变、默认关闭时零行为变更可回退）。
2. **再做 P0-4**（origin 段 / `ims`），它和 P0-1~3 是同一类问题的另一实例。
3. **复核顺序敏感的既有结论**：P0-1~3 修好后池布局不变，但**采样顺序与缓存命中会变**，任何依赖顺序的对比实验（`tools/ooo_pool_measure.py` / `ooo_layout_snapshot.py` / `ooo_ab_ratios.py`）都要重跑，并确认**各分支抽样集未变**（性能项只应改变访问顺序，不应改变选中的图）。
4. **P1-1 / P1-2 / P1-3 需要用户决策**：它们改变默认语义（增强强度、池规模），不是纯优化。
5. P2 系列作为工程卫生项批量处理。

---

## 6. 结论

1. **瓶颈定性**：这是**CPU 密集型**管线。单样本 CPU 时间是墙钟的 1.6–3.5 倍（OpenCV 内部线程池），`imdecode` 一项占 50%，`warpAffine` + Mosaic 画布 + `cvtColor` 再占 ~35%。IO 与磁盘完全不是瓶颈（默认零写盘、读全部命中 page cache）；内存是"常驻中等（85–159 MiB/worker）但瞬时尖峰很高（退化算子可达 824 MB/样本）"。
2. **量级来源是结构而非常数**：`slice_all_tiles=True` 把池子放大到 `(4·ratio + 1)×N`，默认 ratio=1.0 即 **5N**；再乘以 Mosaic 的 **4× 读取**，同一 epoch 的解码量可以到惰性配置的 **16 倍**。实测同一数据集 epoch 耗时 **8.0×**。
3. **三处失配/缺陷让既有优化白费**：① `_raw_cache` 容量 4 与 Mosaic 窗口 63 差一个数量级（**+85%** 可拿）；② 分组采样器在真实训练路径下静默退回 `RandomSampler` 并打出误导性警告（**+29%**）；③ "调小容量"的守卫判据用错统计量，把①的收益挡在门外（合计 **+117% → 44.3 items/s，2.17×**）。
4. **哪些不是瓶颈（避免误优化）**：`_segment_bases()` 版本戳（0.06%）、`deepcopy(label)`（0.16%）、`_geometry`（4 框场景 0.8%）、Python 侧簿记整体 <2%、IO（读取 <210 KB/样本、写 0）、`build_yolo_dataset`（30–40 ms）、`set_epoch`（0.03–0.70 ms/epoch）——都不必动。
5. **最大单点算子**：`_apply_weather(noise)` 32.4 ms、任意角长档拖影 53–60 ms（默认轴对齐下为 12.3 ms），且 `degrade_max_side=2×imgsz` 在 720p+imgsz640 下**完全不触发**——"退化分支"是单样本层面最该先削的一块（P1）。
6. **一条方法论教训**：缺陷 ②③ 之所以长期未被发现，是因为**测试都从"`set_epoch` 已调用"这个状态出发**，而真实 trainer 是先建 loader 再发 epoch；同时守卫的实测条件（24 图池）与真实规模（240 图）差一个数量级。**性能结论必须标注它的数据集规模与调用顺序，否则会得到方向相反的结论。**

---

## 7. 已知的审查限制

1. **本机内存不足**导致多个配置的端到端组合无法完成（`MemoryError` / 页面文件不足 / 段错误 / 进程被 OOM 杀死）。相关结论由**实测微基准 + 段布局算术**外推给出，已在表中标注为"预期"。
2. **`workers>1` 的端到端吞吐未测**（进程内存不足）。§2.4 的所有吞吐数字均为 `workers=0`。CPU 超订结论基于"单 worker 已用 1.6–3.5 核"的实测 + `cv2.getNumThreads()=8` 的静态事实。**配置间的相对差异可比，绝对 items/s 不可比。**
3. 大型帧（4000×3000）无法在本机分配，故以 1920×1080 与 960×540 两个点验证**线性像素标度**，4000×3000 的数字引用被测代码自身的注释而**未独立复现**。
4. "分组采样器在真实训练路径下失效"由**走真实 `build_dataloader`** 的 `bench_loader.py` + 复刻 trainer 构建顺序的 `diag2.py` 双重确认；**完整 1 个 epoch 的 trainer 端到端运行在训练途中被 OOM 杀掉，未能跑完**——结论依据的是 **loader 构建期（训练开始前）** 的日志与 sampler 类型，不依赖训练是否跑完。
5. LRU 容量扫描在**单进程顺序访问**口径下完成（`sweep_lru.py`），与真实 loader 口径（`bench_loader.py`）独立复核，两者结论一致；但 `cap=8` 与 `cap=128` 两个点因内存压力未能取到。

## 8. 复现方式

### 8.1 脚本

| 脚本 | 用途 | 位置 |
|---|---|---|
| `ooo_perf_bench.py` | 原语级微基准：解码 / resize / cvtColor / Mosaic 画布 / warpAffine / 各退化算子 / cap / label 操作 / 切片几何 | `tools/` |
| `ooo_perf_run.py` | 单配置端到端：池布局、每 worker 缓存预算、Mosaic 乘数、数据集侧/变换侧耗时、解码次数与字节数、LRU 命中率、RSS、**实际生效的 sampler** | `tools/` |
| `ooo_perf_gen_data.py` | 生成类照片合成数据集 | `tools/` |
| `perf_probe.py` | 逐样本直读 dataset 的分阶段插桩（解码计数 / 相位计时 / 缓存预算） | `%TEMP%\ooo_perf\` |
| **`bench_loader.py` ★** | **真实 `build_dataloader` 吞吐基准**，支持 `--set-epoch-first`（复刻 trainer 顺序）与 `--force-grouping`（绕过守卫） | `%TEMP%\ooo_perf\` |
| **`sweep_lru.py` ★** | `slice_raw_cache_size` 容量—命中率曲线（证据 §4.3） | `%TEMP%\ooo_perf\` |
| **`diag2.py` ★** | 分组采样失效的最小复现（含 trainer 构建顺序与哨兵态，证据 §4.4） | `%TEMP%\ooo_perf\` |
| `summarize.py` | 汇总渲染对比表 | `%TEMP%\ooo_perf\` |

```bash
python tools/ooo_perf_gen_data.py <root> 240 40 --size 1280x720
python tools/ooo_perf_bench.py <root> --imgsz 640
python tools/ooo_perf_run.py  <root> --key B_slice_all --imgsz 640

# 本轮新增的缺陷证据（必须走真实 loader 层次）
python %TEMP%\ooo_perf\bench_loader.py --root <root> --cfg B --cap 4  --workers 0            # 现状：20.4 items/s
python %TEMP%\ooo_perf\bench_loader.py --root <root> --cfg B --cap 32 --workers 0            # +85%
python %TEMP%\ooo_perf\bench_loader.py --root <root> --cfg B --cap 32 --workers 0 --set-epoch-first --force-grouping   # 上限 44.3
python %TEMP%\ooo_perf\sweep_lru.py                                                          # 容量曲线
python %TEMP%\ooo_perf\diag2.py                                                              # 分组失效复现
```

### 8.2 本文引用的配置键

`A_inert`(全关) / `B_slice_all`(slice r=1.0 all_tiles) / `C_slice_one`(r=0.5 单片) /
`D_full`(全分支 r=0.5) / `D_full_r1`(全分支 r=1.0) / `E_full_uncapped`(D + `degrade_max_side=-1`) /
`F_target_one`(r=0.5 + `slice_target_tiles`) / `G_slice_nolru`(`slice_raw_cache_size=0`) /
`H_full_nomosaic`(D + `mosaic=0`) / `I_slice_all_r025` / `J_degrade_only` / `K_degrade_nocap` / `L_slice_only_nomosaic`

### 8.3 数字出处

- 逐样本口径：`%TEMP%\ooo_perf\res_A.json` … `res_I.json`（`perf_probe.py`）
- 容量曲线：`%TEMP%\ooo_perf\log_lru.txt` / `res_lru.json`（`sweep_lru.py`）
- 真实 loader 口径：`%TEMP%\ooo_perf\bench_*.json` / `blog_*.txt`（`bench_loader.py`）
- 分组失效复现：`%TEMP%\ooo_perf\diag2.txt`（`diag2.py`）

---

## 9. 修复记录（第七轮已落地：P0-1 / P0-2 / P0-3）

三项一起做，因为它们互相解锁：**只调容量会被守卫拦下（P0-3），只修分组会被容量拖住（P0-1）**。改动面约 20 行，**不改变 7 段布局、`len()` 恒定性与每 epoch 随机语义**——只改变访问顺序与缓存命中。

### 9.1 P0-2 分组门控（`pool/dataset.py::grouped_sample_units`）

**改法**：删掉手写的 6 处分支门控，改为**从 `_mask_specs(n)` 这张表读出 `on[...]`**（`_segment_lengths` 也用它定段宽，因此两者不可能再漂移），`origin` 仍取其自有开关 `_img_origin_on()`；compose 槽位生成加 `if on["compose"]` 门控，门控外返回 `{}`。

```python
on = {attr: flag for attr, _ratio_attr, flag, _count in self._mask_specs(n)}
on["origin"] = self._img_origin_on()
...
compose_slot = (
    {g: compose_base + j for j, g in enumerate(self._sel_indices("compose", (n + 3) // 4))}
    if on["compose"] else {}
)
```

**效果**：默认配置（ratio 全 1.0、compose 关闭）下 `grouped_sample_units()` 不再返回 `None`，误导性的 `drifted apart` 警告消失。同缓存下 **+29%**（20.4 → 26.4 items/s）。

### 9.2 P0-3 删除 oversized 守卫

**改法**：删除 `if fan_out < self._raw_cache_size: warn + return None`，只保留 `if fan_out < 2: return None`（无图占两槽 → 无重读可省）。同时在原位留下一条 `REMOVED GUARD` 注释，写明它比错了什么、证据为何不可外推、以及为什么是**删除而非反转**。

### 9.3 P0-1 容量与字节预算（`pool/constants.py` + `pool/dataset.py`）

- `_ONLINE_DEFAULTS`：`slice_raw_cache_size` **4 → 16**；新增 `slice_raw_cache_mb: 256`。
- `__init__`：`_raw_cache_size = max(4, v) if v > 0 else 0`（保留 `0=关闭`、地板 4）；新增 `_raw_cache_mb` / `_raw_cache_bytes` / `_raw_cache_budget`。
- `_load_image_cached`：驱逐条件由"仅条目数"改为"**条目数或字节预算**任一越界即驱逐"，`>= 1` 保证至少留下刚解码的帧。

```python
budget = self._raw_cache_budget
while cache and (len(cache) >= size or (budget and self._raw_cache_bytes + im.nbytes > budget)):
    _old_key, old = cache.popitem(last=False)
    self._raw_cache_bytes -= old.nbytes
```

**效果**：240 图池实测 4 → 32 为 20.4 → 37.7 items/s（**+85%**）；配合 P0-2 后到 **44.3 items/s（2.17×）**。

### 9.4 回归测试（`tests/test_ooo_branches.py`，+4）

| 测试 | 锁住什么 |
|---|---|
| `test_grouping_is_kept_whatever_the_cache_size` | cap ∈ {4,16,64} 都**必须**产出 units；断言不再有"扇出/超容量"告警（替代已删除的 `test_default_lru_keeps_grouping_and_an_oversized_lru_warns`） |
| `test_grouping_survives_the_trainers_build_order` | **不调 `set_epoch`**、复刻真实构建顺序，断言 units 是排列且真实 `build_dataloader` 拿到 `GroupedImageSampler` —— 补上"loader 构建早于 set_epoch"这个盲区 |
| `test_the_raw_cache_default_covers_the_reuse_window` | 默认 16 / 256 MiB；显式覆盖与 `0/1` 边界不回归 |
| `test_the_raw_cache_byte_budget_caps_resident_frames` | 3 MiB 预算 / 1.17 MiB 帧 → 常驻正好 2 帧；字节计数与真实占用一致 |

### 9.5 验证与遗留

- `pytest tests/ -k ooo -q` → **183 passed, 339 deselected**（5.4 s，全部离线）。
- `diag2.py` 重跑：真实 trainer 顺序下分组已生效，越界槽与误导性警告消失。
- **本机未装 `ruff`**：改用 `py_compile` + 行长自检脚本代跑；改动文件全部编译通过。`ruff` 默认规则集（`E4/E7/E9/F`）**不含 `E501`**，故既有的超长行不是新增违规。（CI 上仍建议跑一次 `ruff check ultralytics_ooo` 固化。）
- **未实施**：P0-4（origin 段走 `_load_image_cached`/`ims`）及全部 P1/P2/P3。
- **待重跑**：§5.1 第 3 条——P0-1~3 修好后访问顺序变化，`tools/ooo_pool_measure.py` / `ooo_layout_snapshot.py` / `ooo_ab_ratios.py` 等顺序敏感的工具应重跑，并确认**各分支抽样集未变**（性能项只应改变访问顺序，不应改变选中的图）。

