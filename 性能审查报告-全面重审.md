# 性能审查报告（全面重审版）

> 审查对象：`ultralytics-main`（Ultralytics YOLO26 工程扩展：在线切片 / Mosaic / 在线退化 / 混合样本池 / 切片验证）
> 审查方式：**从零开始的实测审查**，不引用、不继承任何此前的报告结论。
> 审查日期：2026-09-16　审查环境：Windows / conda `langchain`（Python 3.12.13, torch 2.5.1+cu124, OpenCV 5.0.0）
> 测量脚本与原始数据：`_perf_review/`（可复现，见文末「复现方法」）

---

## 0. 结论摘要（TL;DR）

**这台机器上真正的瓶颈不是代码，而是两个环境约束，它们决定了所有优化的优先级：**

1. **算力**：`train.py` 用 `device='cpu'`。实测 yolo26n 训练单步（fwd+bwd，imgsz=640）CPU **1853.9 ms/图** vs GPU（CMP 30HX）**49.1 ms/图**，差 **37.7 倍**。在 CPU 上，整条数据管线只占训练步的 **2.9%**——数据侧优化做得再好，收益也被 37 倍的算力差距淹没。
2. **内存**：物理内存 7.9 GB，实测空闲仅 **0.39–0.7 GB**（占用 95%）。`workers=4` 两次实测直接 **MemoryError / cv2 Insufficient memory** 崩溃。这直接把 `workers` 锁死在 **≤2**。

**一旦切到 GPU（或换个内存充裕的机器），矛盾立刻转移到数据侧。** 数据侧的实测结论是：

- 关键路径上 **68.2% 的耗时在数据集侧**（`ds_side_share`），变换链只占 31.8%；
- 单样本成本被 **mosaic ×3.9 放大**结构性决定：开 mosaic 72.09 ms/项 → 关 mosaic 27.05 ms/项（**−62%**）；
- 数据侧第一成本是 **原图分辨率的 JPEG 解码 + 缩放**，而它被一个**明确的设计疏漏**放大：`origin` 段绕过 LRU，每次都全量重解码（D_full 占 15.1%，`C_slice_one` 占 **51.7%**，4K 下 `load_image` 占 **31.8%**）；
- 第二个明确缺陷在**大图切片**：`_emit` 在原图分辨率做一次冗余 ROI 拷贝，紧接着又缩放——实测可直接省掉，**4K 下快 5.9× 且逐像素一致**（已验证 max|diff|=0）。

**最高性价比的三个动作（均为低成本）：**
① 训练切 GPU（37.7×）；② `_build_origin_sample` 改走 LRU（−9%~15%）；③ 去掉 4K 切片的冗余 ROI 拷贝（−14% 于 4K 切片配置）。

---

## 1. 审查范围与方法

### 1.1 环境基线（实测）

| 项目 | 实测值 | 备注 |
|---|---|---|
| 物理内存 | 7.9 GB，空闲 **0.39–0.7 GB**（95% 占用） | 第一硬约束 |
| CPU | 8 逻辑核（1 物理封装） | `os.cpu_count()=8` |
| torch 线程数 | `torch.get_num_threads()=4` | 只用了一半逻辑核 |
| GPU | NVIDIA CMP 30HX，**6.4 GB**，CUDA 12.4 可用 | torch 可见，`train.py` 未使用 |
| OpenCV | 5.0.0 | 未 pin 线程数 → 默认按核数起线程池 |
| 磁盘 C: | 剩余 25 GB | 数据集页缓存空间紧张 |
| Python / torch | 3.12.13 / 2.5.1+cu124 | conda `langchain` |

### 1.2 数据集（自建，可复现）

为了让 IO 与解码数字有意义，用 `tools/ooo_perf_gen_data.py` 生成三档**压缩率贴近真实**的合成检测集（纯噪声图会被压缩到 ~1.2 MB/张，数字完全失真）：

| 数据集 | 数量 | 分辨率 | 平均 JPEG |
|---|---|---|---|
| `ds720` | 60 训 + 8 验 | 1280×720 | 216 KB |
| `ds1080` | 40 + 6 | 1920×1080 | 482 KB |
| `ds4k` | 12 + 3 | 4000×3000 | 2758 KB |

### 1.3 测量方法（四层，逐层收敛）

| 层级 | 手段 | 回答的问题 |
|---|---|---|
| L1 算子级 | `tools/ooo_perf_bench.py`（中位数，双分辨率） | 一个单位的活儿值多少 ms |
| L2 端到端 | `tools/ooo_perf_run.py`，10 配置同口径扫描 | 配置 × 耗时 × 解码次数 × 内存 |
| L3 专属耗时 | `_perf_review/instrument2.py`（自研递归剖析器） | 关键路径各阶段**独占**耗时，95% 归因 |
| L4 定向验证 | `_perf_review/tile_probe.py`、`noisebench.py`、`fix_probe.py`、`scale.py`、`compute.py` | A/B 候选修复 + 算力 roofline |

**为什么需要 L4**：本机同配置**同会话内 ±13%、跨会话 ±40%** 的运行间方差（内存压力主导）。任何 <15% 的端到端 A/B 结论在这台机器上都不可信，必须退回到定向微基准。这一点在后文 §6 有实测证据。

---

## 2. 关键路径各阶段耗时

### 2.1 阶段定义

一次训练迭代 = 1 个训练项（item），其成本由两段构成：

```
[DataLoader 请求 index]
   └─ dataset.get_image_and_label(index)        ← 数据集侧（取样）
        ├─ 分支分发：按 index 落在哪个区段       sahi(tile) / origin / ratio / blur / compose / weather / occlusion
        ├─ 取原图：_load_image_cached (LRU)  或  load_image (裸解码)
        ├─ 分支算子：切片几何+取块 / 运动模糊 / 气象退化 / 遮挡 / 比例填充 / 2×2 合成
        └─ _finalize_label：统一缩放到 imgsz
   └─ transforms(label)                         ← 变换侧
        ├─ Compose → Mosaic.__call__  （内部再发起 ~3 次额外 get_image_and_label ← 放大源）
        ├─ Compose → RandomPerspective (warpAffine)
        └─ 其余（HSV/翻转/Format/collate 等）
```

**mosaic 放大倍数实测稳定在 3.75–3.99**：每 1 个训练项触发约 3.9 次 `get_image_and_label`。这是全表最关键的一个系数。

### 2.2 端到端配置扫描（`ds720`，imgsz=640，batch=8，workers=0，同口径）

| 配置 | 池 | 项 | **ms/项** | 数据集侧 | 变换侧 | mosaic× | 解码/项 | JPEG KB/项 | CPU/墙钟 | LRU 命中 | epoch s |
|---|---|---|---|---|---|---|---|---|---|---|---|
| A_inert（全关） | 60 | 64 | 39.8 | 13.4 | 26.4 | 3.75 | 0.94 | 203 | 2.50 | — | 2.55 |
| B_slice_all | 300 | 304 | 46.8 | 24.0 | 22.8 | 3.95 | 1.52 | 328 | 2.24 | 0.72 | 14.23 |
| C_slice_one | 90 | 96 | **93.2** | 58.1 | 35.2 | 3.75 | **3.28** | 708 | 1.24 | 0.60 | 8.95 |
| D_full（train.py 同款） | 338 | 344 | 72.1 | 49.2 | 22.9 | 3.93 | 1.64 | 355 | 1.72 | 0.70 | 24.80 |
| D_full_r1（比率全 1.0） | 615 | 616 | 62.8 | 43.2 | 19.6 | 3.99 | 1.61 | 348 | 1.98 | 0.70 | 38.68 |
| F_target_one | 90 | 96 | 68.1 | 43.4 | 24.8 | 3.75 | **3.28** | 709 | 1.66 | 0.67 | 6.54 |
| G_slice_nolru（关 LRU） | 300 | 304 | **78.2** | 53.1 | 25.1 | 3.95 | **3.95** | 853 | 1.57 | — | 23.78 |
| H_full_nomosaic | 338 | 344 | **27.1** | 13.8 | 13.3 | 0.98 | 0.82 | 176 | 3.49 | 0.27 | 9.31 |
| J_degrade_only | 300 | 304 | 56.1 | 39.8 | 16.2 | 3.95 | 0.98 | 213 | 2.25 | 0.76 | 17.04 |
| L_slice_only_nomosaic | 300 | 304 | 22.5 | 9.5 | 13.0 | 0.99 | 0.83 | 180 | 3.29 | 0.19 | 6.83 |

**四条直接读出来的结论：**

- **epoch 时长 ≈ 池大小 × ms/项**。池大小由分支开关与 `*_ratio` 线性决定，ms/项 基本恒定 → 控制 epoch 时间的第一杠杆是**池膨胀系数**，不是单样本速度。
- **mosaic 是最大的单一成本**：D_full 72.1 → H_full_nomosaic 27.1（**−62%**）；B_slice_all 46.8 → L_slice_only 22.5（**−52%**）。
- **LRU 关闭代价极高**：G 78.2 vs B 46.8 ms/项（**+67%**），解码/项 1.52 → 3.95。
- **小池子反而更慢**：C/F 池仅 90 项，但 ms/项 高达 93.2/68.1——因为池 90 里 60 个是 `origin` 段，而 `origin` 走的是**无缓存**的 `load_image`（见 §3.3）。这是"池小 = 快"的直觉陷阱。

### 2.3 关键配置的**独占**耗时拆解

用自研递归剖析器（inclusive 减嵌套 inclusive = exclusive；`get_image_and_label` 内含所有构建器，`Mosaic.__call__` 内含它发起的 3 次取样，不这样拆就会重复计数）。

**D_full（与 `train.py` 同款配置），64.2 ms/项，归因率 94.6%：**

| 阶段 | 独占 ms/项 | 占比 | 调用/项 | µs/次 |
|---|---|---|---|---|
| 变换链其余部分（HSV/翻转/Format/collate 等） | 10.80 | 16.8% | 1.97 | 5497 |
| **origin 段 load_image（解码+缩放，无缓存）** | **9.67** | **15.1%** | 0.77 | 12508 |
| 运动模糊核 | 6.94 | 10.8% | 0.64 | 10853 |
| RandomPerspective（warpAffine） | 6.49 | 10.1% | 0.98 | 6610 |
| LRU 读（未命中解码 + 命中全帧拷贝） | 5.85 | 9.1% | 3.27 | 1789 |
| **Mosaic 自身像素工作**（画布+拼接） | 4.55 | 7.1% | 0.98 | 4628 |
| `_finalize_label` 统一缩放 | 4.18 | 6.5% | 3.16 | 1324 |
| 气象退化 | 3.60 | 5.6% | 0.26 | 13932 |
| 比例填充 ratio_pad | 2.86 | 4.5% | 0.23 | 12608 |
| 遮挡 | 2.71 | 4.2% | 0.48 | 5677 |
| 切片 emit / 几何 / 分发 | 1.83 | 2.8% | 1.52 | — |
| 2×2 合成 | 0.43 | 0.7% | 0.04 | 11259 |
| **合计归因** | **60.7** | **94.6%** | | |

> 注意 `Mosaic` 的 **inclusive = 31.34 ms/项**，但 **exclusive 仅 4.55 ms/项**——差值 **26.8 ms/项** 是它发起的 3 次额外取样。**mosaic 的代价不在拼接像素，而在于它把取样次数乘了 3.9 倍。**

**B_slice_all（切片专用），34.45 ms/项：**

| 阶段 | 独占 ms/项 | 占比 |
|---|---|---|
| **origin 段 load_image** | **8.19** | **23.8%** |
| 变换链其余部分 | 7.91 | 23.0% |
| RandomPerspective | 5.32 | 15.4% |
| `_finalize_label` | 3.11 | 9.0% |
| Mosaic 自身 | 3.01 | 8.8% |
| LRU 读 | 1.66 | 4.8% |
| `_emit`（tile 取块） | 1.63 | 4.7% |
| `_geometry` | 0.78 | 2.2% |

**C_slice_one（切片 + 单块），48.48 ms/项：**

| 阶段 | 独占 ms/项 | 占比 |
|---|---|---|
| **origin 段 load_image** | **25.08** | **51.7%** |
| 变换链其余部分 | 7.19 | 14.8% |
| RandomPerspective | 4.70 | 9.7% |
| Mosaic 自身 | 2.96 | 6.1% |
| LRU 读 | 2.67 | 5.5% |

**H_full_nomosaic（关 mosaic 后的对照），21.65 ms/项**：`Compose` 独占升到 **7.67 ms/项 = 35.4%**，`load_image` 降到 1.63（7.5%）——因为取样次数从 3.9 降到 1.0，所有"每次取样都要付"的成本同步缩小。

### 2.4 大图（4000×3000）阶段拆解

| 阶段 | B_slice_all 独占 ms/项 | 占比 | D_full 独占 ms/项 | 占比 |
|---|---|---|---|---|
| `_load_image_cached`（LRU） | 22.67 | 11.7% | **65.87** | **29.2%** |
| `load_image`（origin，裸解码） | **61.84** | **31.8%** | 43.87 | 19.4% |
| `_degrade_frame`（工作分辨率降采样） | — | — | 31.35 | 13.9% |
| `_emit`（原图分辨率 ROI 拷贝） | **51.51** | **26.5%** | 16.20 | 7.2% |
| `_finalize_label` | 16.29 | 8.4% | 8.99 | 4.0% |
| Compose / RandomPerspective / Mosaic | 11.07 / 7.67 / 7.25 | 17.3% | 9.38 / 6.59 / 5.46 | 9.5% |
| 遮挡 / 气象 / 模糊 / 比例 | — | — | 8.03 / 7.88 / 6.60 / 3.60 | 11.6% |
| **ms/项 总计** | **194.5** | | **225.6** | |

大图端的成本结构与 720p 完全不同：**原图分辨率的"解码 / 拷贝 / 缩放"三件事占了 70% 以上**。而且 `E_full_uncapped`（关掉工作分辨率上限）在 4K 上把 ms/项从 276.96 拉到 **360.43（+30%）**、`K_degrade_nocap` 从 307.38 拉到 **415.83（+35%）**——`degrade_max_side` 这个默认优化是真实有效的，不要关。

---

## 3. 瓶颈识别与成因

### 3.1 瓶颈 B1【P0】内存：7.9 GB 装不下这条管线

**现象**：`workers=4` 时 **两次独立实测直接崩溃**：
```
RuntimeError: Caught MemoryError in DataLoader worker process 3.
cv2.error: (-4:Insufficient memory) Failed to allocate 2764800 bytes  # 仅申请 2.7 MB！
```
**成因分解（实测）**：

| 项 | 实测 |
|---|---|
| 进程基线（导入 torch+cv2+池，0 worker） | 406 MB |
| 2 worker 时进程树 | 1098 MB（+692） |
| 4 worker 时进程树 | 1770 MB（+1364） |
| 每 worker 固定基线 | **≈340 MB**（spawn 重新导入 torch/cv2 + 数据集副本） |
| 每 worker 预取张量 | `prefetch_factor(4) × batch(8) × 3 × 640² × 4 B` = **157 MB** |
| 每 worker LRU 潜在占用 | `slice_raw_cache_mb` 默认 **256 MB**（720p 实际用 16×2.6=42 MB；4K 只用 7 帧=240 MB） |
| 每 worker `self.ims` | 59 帧 × 1.17 MiB = **69 MiB** |

即：4 worker 的最坏情形 = 1364（worker 固定）+ 628（预取）+ 240×4（LRU）+ 69×4（ims）≈ **3.2 GB**，而系统只剩 0.7 GB。**崩溃是必然的，不是偶发。**

**旁证**：该 OOM 也让多次端到端 A/B 报废——同一配置相邻两次 epoch 的 ms/项可从 32.96 跳到 59.86。

### 3.2 瓶颈 B2【P0】算力：`device='cpu'` 与 GPU 差 37.7 倍

yolo26n 训练单步（fwd + bwd + step，batch=4，`torch_threads=4`）：

| 设备 | imgsz | step | ms/图 | 吞吐 | 显存 |
|---|---|---|---|---|---|
| cpu | 320 | 1908 ms | 477.0 | 2.1 img/s | — |
| **cpu** | **640** | **7416 ms** | **1853.9** | **0.5 img/s** | — |
| cuda:0 | 320 | 182 ms | 45.5 | 22.0 img/s | 345 MB |
| **cuda:0** | **640** | **197 ms** | **49.1** | **20.4 img/s** | **1168 MB** |

**roofline 判定**：

| 训练设备 | 单项加载 | 单图计算 | 加载占比 | 谁是瓶颈 |
|---|---|---|---|---|
| CPU（当前 `train.py`） | 54.9 ms | **1853.9 ms** | **2.9%** | **模型算力**（数据侧优化无可观测收益） |
| GPU（CMP 30HX） | 54.9 ms | 49.1 ms | **53%** | **数据管线**（GPU 一半时间在等） |

**这解释了为什么"数据侧优化值不值得做"必须先回答"用什么设备训"。** 另外两个顺带的问题：`torch_threads=4` 只用了一半逻辑核；6.4 GB 显存按 1168 MB/batch4 估算可跑到 batch≈16–20。

### 3.3 瓶颈 B3【P1】`origin` 段绕过 LRU（明确的实现疏漏）

`_build_origin_sample` 调 `self.load_image(img_index)`，而 `load_image` 里有一段：
```python
if self.augment and self.cache != "ram":
    if getattr(self, "slice_transform", None) is None:   # ← 切片开启时为 False
        self.ims[i], ... = im, ...                       # 于是什么都不缓存
        self._remember_ims(i)
```
**切片一开，`load_image` 就完全不缓存**（实测 `ims_resident = 0`）。而 `origin` 段（`img_origin=True` 时每张原图各占 1 槽，是包默认值）正是走 `load_image` 的。后果：

- D_full 720p：独占 **9.67 ms/项 = 15.1%**（0.773 次/项 × 12.5 ms）
- **C_slice_one：独占 25.08 ms/项 = 51.7%**
- 4K B_slice_all：独占 **61.84 ms/项 = 31.8%**（0.517 次/项 × **119.7 ms**）
- 4K D_full：独占 43.87 ms/项 = 19.4%（114.7 ms/次）

即：**每一张 origin 图，每个 epoch 都被从头解码 + 缩放，而同一张图的其它分支样本就在 LRU 里躺着。**

### 3.4 瓶颈 B4【P1】大图切片：原图分辨率下的冗余 ROI 拷贝

`OnlineSlice._emit`：
```python
sub = np.ascontiguousarray(img[y0:y1, x0:x1])     # 原图分辨率整块拷贝
```
而 `get_image_and_label` 紧接着做的事就是把这个 tile 缩放回 `imgsz`。4K 实测（tile 2400×1800 → 640×480）：

| 做法 | 耗时 | 正确性 |
|---|---|---|
| 先 `ascontiguousarray` 再 `cv2.resize`（现状） | 10.79 ms | 基准 |
| **直接 `cv2.resize(跨步视图, target)`** | **1.84 ms（5.9×）** | `np.array_equal=True`，max\|diff\|=**0**，且与独立构造的裁剪结果一致 |
| 仅 `ascontiguousarray` | 8.03 ms | — |

720p 下差异只有 0.06 ms（tile 太小），**所以这是大图专属收益**。在 4K `B_slice_all` 里 `online._emit` 独占 51.5 ms/项 = 26.5%（3.483 次/项 × 14.8 ms）。

### 3.5 瓶颈 B5【P1】原图分辨率解码是数据侧第一成本

| 操作 | 1280×720 | 4000×3000 | 倍数 |
|---|---|---|---|
| JPEG 解码 | 7.66 ms | **129.9 ms** | 17× |
| cv2.resize → 640 | 1.35 | 3.24 | 2.4× |
| `cvtColor` BGR→RGB | 2.76 | 40.0 | 14.5× |
| 全帧 `copy()` | 2.52 | 18.2 | 7.2× |
| `cap_long_side` INTER_LINEAR / INTER_AREA | — | 11.9 / 64.5 | **5.4×（当前选的 LINEAR 是对的）** |

解码耗时随像素面积超线性增长（17× vs 面积 13×，含 JPEG 熵解码的特性）。**IO 不是瓶颈**：顺序读实测数百 MB/s，而每项只需读 176–355 KB（720p）/ 1.7–3.2 MB（4K）→ 换算峰值 IO 仅 **3.5–7 MB/s（720p）、34–64 MB/s（4K）**，任何 SSD 都绰绰有余。**瓶颈是解码 CPU 与内存带宽，不是磁盘。**

### 3.6 瓶颈 B6【P1】气象噪声核是单算子最贵项

| 类型 | 1280×720 | 4000×3000（未限幅） |
|---|---|---|
| rain | 8.43 | 89.9 |
| haze | 2.93 | 33.4 |
| **noise** | **29.6–57.9** | **373.0** |

`weather_types` 默认 `"rain,haze,noise"`，即 1/3 的气象样本落在最贵的 noise 上。**这是全项目最贵的单个算子。**

### 3.7 瓶颈 B7【P2】变换链"其余部分"被低估

`Compose` 的独占部分（HSV/翻转/`Format`/collate，即非 mosaic/仿射的一切）在 D_full 占 **10.80 ms/项 = 16.8%**，**高于运动模糊核（10.8%）**；在 `H_full_nomosaic` 里升到 **7.67 ms/项 = 35.4%**。其中 `cvtColor+transpose@640` 单次 3.96 ms。`workers=0` 时这段完全串行在主进程。

### 3.8 瓶颈 B8【P2】mosaic 放大 ×3.9 —— 机会也是它

mosaic 本身像素工作只占 4.55–5.46 ms/项，但它把**所有每次取样都要付的成本**（解码、缩放、`_geometry`、`_emit`、label 深拷贝）都乘了 3.9 倍。所以：

> **任何"降低单次取样成本"的优化，在 mosaic 开启时收益自动 ×3.9。** 这是 P1-1/P1-2 的价值放大器。

### 3.9 实测确认为**非瓶颈**的项（不要在这里花时间）

| 项 | 实测 | 结论 |
|---|---|---|
| `_segment_bases()` 每样本重算 | 4.1–4.9 µs/次 × 3.95/项 = 0.18 ms/项（0.3%） | 可忽略，不需要缓存 |
| `get_image_and_label` 自身分发开销 | 独占 0.66 ms/项（1.0%） | 可忽略 |
| label `deepcopy` | 0.012–0.019 ms/次 | 可忽略 |
| 切片几何 `_geometry` | 0.17 ms（4 块）/ 0.055 ms（1 块） | 可忽略 |
| target-tile 队列构建 | 214 项 < 0.1 ms | 可忽略 |
| 池构建 | 12–35 ms / 次 | 可忽略 |
| `ratio_pad_params` | 0.0025 ms | 可忽略 |
| 磁盘 IO 带宽 | 峰值 3.5–64 MB/s | 远未饱和 |

---

## 4. 优化项与优先级排序

排序依据：**收益（对本项目的实测影响）× 实施成本**。实测支持写在每一项里。

### P0 —— 阻塞级，先做，否则其余优化不可观测

| # | 优化项 | 实测依据 | 影响 | 成本 | 建议 |
|---|---|---|---|---|---|
| **P0-1** | **训练切到 GPU：`device='cpu'` → `device=0`** | CPU 1853.9 vs GPU 49.1 ms/图 = **37.7×**；CUDA 峰值 1168 MB/batch4 | 极高 | 极低（改一个参数） | 若必须用 CPU，请明确接受"数据管线只占 2.9%、优化收益不可观测"这一事实 |
| **P0-2** | **`workers` 限制到 ≤2，并治理预取内存** | 4 worker 时进程树 1770 MB、**两次 MemoryError 崩溃**；每 worker 固定 340 MB + 预取 157 MB | 高 | 低 | `workers=2`（实测 B 32.16→29.96、D 54.92→48.60 ms/项）；`batch` 同时下调；把硬编码的 `prefetch_factor=4` 变成可配置键 |
| **P0-3** | **让 `GroupedImageSampler` 真正生效（含修工具）** | 同会话配对：B **43.85→32.16 ms/项（1.36×）**，LRU 命中 **0.751→0.939**；D 59.14→54.92（1.08×），命中 **0.728→0.950** | 中高 | 低 | 生产路径已默认生效；**但 `tools/ooo_perf_run.py` 在 `install()` 之前导入 `build_dataloader`，绑定的是补丁前的函数 → 该工具永远报 `RandomSampler`，永远测不到自家优化**。把导入移到 `install()` 之后 |

### P1 —— 高收益，成本低

| # | 优化项 | 实测依据 | 影响 | 成本 | 建议 |
|---|---|---|---|---|---|
| **P1-1** | **`_build_origin_sample` 改走 LRU**（`_load_image_cached` + `_finalize_label`） | origin 段独占：D_full **15.1%**、C_slice_one **51.7%**、4K B **31.8%**；端到端 A/B f1 **−14.9%** | 高 | 极低（~6 行） | ⚠️ 未命中时会更慢（多一次全帧拷贝：720p 2.52 ms / 4K 18.2 ms），盈亏平衡命中率 720p≈0.44、4K≈0.22，而实测命中率 0.90–0.95 → 稳赢。**必须与 P1-3 同做**，否则 origin 读会挤出 LRU |
| **P1-2** | **4K 切片去掉冗余 ROI 拷贝**：`_emit` 返回跨步视图，让 `_finalize_label` 的 `cv2.resize` 直接消费 | 4K tile 2400×1800：10.79 → **1.84 ms（5.9×）**，**max\|diff\|=0 逐像素一致**；720p 仅差 0.06 ms | 高（大图） | 低 | 4K `B_slice_all` 中 `_emit` 占 26.5% → 预计省 **~31 ms/项（≈14%）**。注意 `*_save_*` 路径需要连续内存，那里保留拷贝 |
| **P1-3** | **大图自适应 LRU 预算**：`slice_raw_cache_mb=256` 对 34.3 MB/帧只装 7 帧，而 `slice_raw_cache_size=16` | 4K D_full `_load_image_cached` 独占 **65.87 ms/项 = 29.2%**（4.147 次/项） | 高（大图） | 低 | 按"帧字节数 × 目标帧数"反推预算，或对大图显式提高；配合 P0-2 腾出的内存 |
| **P1-4** | **气象 noise 换 int16 核**（`cv2.randn`→CV_16S + `cv2.add(CV_16S)`） | 5 种实现横评：现行 29.59 ms → **int16 25.31 ms（−14%）**，σ 实测 14.98 **完全一致**；numpy Generator 三种写法 47.9/78.5/50.6 ms **全部更慢→否决** | 中 | 低 | 只动一个算子，行为不变。**不要**用 numpy `Generator.normal` 替换（实测慢 1.6–2.7×） |

### P2 —— 中收益或需权衡

| # | 优化项 | 实测依据 | 影响 | 成本 | 建议 |
|---|---|---|---|---|---|
| **P2-1** | **保持 `blur_axis_aligned=True`（默认即 True）** | 任意角稠密 PSF vs 轴对齐：720p 67.25 vs 10.74 ms（**6.3×**）；4K 1195.36 vs 171.66 ms（**7.0×**） | 中（任意角场景） | **0** | 文档化即可：轴对齐 PSF 精确等于均匀箱式，输出与"任意角恰好取 0/90"逐位相同。**不要为了"更通用"把它关掉** |
| **P2-2** | 变换链其余部分：`workers≥1` 让 Format/色彩与主进程计算重叠；或裁掉 hsv 系列 | `Compose` 独占 10.80 ms/项 = 16.8%（高于模糊核） | 中 | 中 | 优先靠 `workers` 重叠，而不是关增强 |
| **P2-3** | **不要在 mosaic 开启时关 LRU/降缓存** | mosaic 乘数 3.75–3.99；LRU 关闭代价 +67%（G vs B） | 高（防错） | 0 | 这是"配置纪律"项：grouped sampler + LRU 是 mosaic 的前提条件 |
| **P2-4** | `_load_image_cached` 命中拷贝：只读分支（ratio/weather/blur）改 `copy=False` | 命中拷贝 720p 2.52 / 4K 18.17 ms；4K D_full 命中 3.59 次/项 → 约 10 ms/项 | 低-中 | 低-中 | `compose` 已这么做；其余分支需先确认不写输入（现有注释已列出哪些路径会回传输入，可直接沿用其判定） |
| **P2-5** | 池膨胀治理：按需下调 `*_ratio` / 关闭不需要的分支 | epoch ≈ 池大小 × ms/项；D_full_r1 池 615 → epoch 38.68 s vs D_full 338 → 24.80 s | 中高 | 极低（配置） | 收益直接线性，且不需要改代码。但它改变训练语义，必须与效果评估一起决定 |

### P3 —— 影响可忽略，不要投入

| # | 优化项 | 实测 |
|---|---|---|
| P3-1 | 缓存 `_segment_bases` / `_segment_lengths` | 0.18 ms/项（**0.3%**） |
| P3-2 | 优化 `get_image_and_label` 分发逻辑 | 独占 0.66 ms/项（**1.0%**） |
| P3-3 | 优化 label `deepcopy` | 0.012–0.019 ms/次 |
| P3-4 | 引入 `.npy` 磁盘缓存 | 代码注释已记录"试过、无收益"；本次 §3.5 亦支持（IO 未饱和） |

---

## 5. 优先级矩阵（影响 × 成本）

```
影响
 高 │  P0-1 切GPU      P1-1 origin走LRU        P1-3 大图LRU预算
    │  (37.7×)         P1-2 去ROI拷贝          P2-5 池膨胀治理
    │                  P0-3 grouped生效
 中 │  P2-1 axis对齐   P1-4 noise int16        P2-2 workers重叠
    │  P2-3 配置纪律   P2-4 copy=False
 低 │  P3-1~P3-4（不要做）
    └──────────────────────────────────────────────────────────
        极低/零            低                    中
                        实施成本
```

**推荐执行顺序**：
1. `P0-1` 切 GPU → 立刻拿到 37.7×；同时它把 `P0-2/P0-3/P1-*` 从"不可观测"变成"主要矛盾"。
2. `P0-2` 内存预算（`workers=2` + `batch` 下调 + `prefetch_factor` 可配置）→ 消除崩溃与测量噪声。
3. `P0-3` 修工具导入顺序 → 恢复"能测出自家优化"的能力（在此之前无法验收 P1-*）。
4. `P1-1 + P1-3` 一起做（origin 走 LRU + 大图 LRU 预算）→ 实测 −9%~15%。
5. `P1-2` 大图去 ROI 拷贝 → 大图 −14%。
6. `P1-4` / `P2-*` 按需。

---

## 6. 测量可信度与已知限制（诚实声明）

**1. 本机运行间方差极大，端到端小改动测不出来。**
同一会话内 baseline 三次重复：49.25 / 55.14 / 63.78 ms/项（**±13%**）；单次极值跨度 32.96–80.08 ms（**±40%**）。根因是内存占用 95%、页缓存反复被驱逐。
**因此**：本文所有 <15% 的结论**都不来自端到端 epoch 差值**，而来自 (a) §2.3 的独占耗时归因（内部自洽，合计 94.6%）、(b) 中位数微基准（L1）、(c) §3.4 的定向探针（含正确性校验）。端到端 A/B 仅用于印证方向。

**2. 自带工具的测量口径缺陷（已实测复现）。**

| 工具 | 缺陷 | 后果 |
|---|---|---|
| `tools/ooo_perf_run.py` | `from ultralytics.data.build import build_dataloader` 在 `install()` **之前**，绑定补丁前函数 | **永远报 `RandomSampler`**，自家 grouped sampler 的收益被系统性埋没 |
| `tools/ooo_perf_bench.py` | 同上（`build_yolo_dataset` 在 `install()` 前导入）；未 pin OpenCV 线程数 | 单进程多池，8 GB 机器上易 OOM 干扰 |
| 两个工具 + 扫描用法 | 单进程内连续构建多个池（注释里写了 "one configuration per process is the intended use"，但 sweep 用法并非如此） | 2 个池之后 OOM 概率显著上升 |

我复测时通过**在 `install()` 之后从模块属性读取 `build_dataloader`**、**一进程一配置**规避了这两点，并在 `_perf_review/` 留下了脚本。

**3. 未覆盖的部分（本次审查的边界）。**
- `val_slice_*`（切片验证）与 `val_slice_dual_metric`（双口径验证）**未做耗时实测**。定性判断：`dual_metric=True` 时每轮跑两遍验证，验证开销直接翻倍；`val_slice_all_tiles=True` 时验证样本数 ×4。**建议单独补测**（`train.py` 当前 `val_slice_enable=True` + `val_slice_dual_metric=True`，这两项很可能被低估）。
- DataLoader worker 内多进程行为只在 `workers ∈ {0,2,4}` 上测过，且受内存限制无法测 8。
- 训练效果（mAP）与吞吐的权衡未纳入——本报告只回答"快慢"，不回答"值不值"。

---

## 7. 复现方法

```powershell
$py = "D:\miniconda3\envs\langchain\python.exe"
Set-Location "C:\Users\Administrator\Desktop\ultralytics-main"
$b = "C:\Users\Administrator\Desktop\ultralytics-main\_perf_review"

# 数据（三档真实压缩率）
& $py tools\ooo_perf_gen_data.py "$b\ds720"  60 8 --size 1280x720
& $py tools\ooo_perf_gen_data.py "$b\ds4k"   12 3 --size 4000x3000

# L1 算子级
& $py tools\ooo_perf_bench.py "$b\ds720" --imgsz 640 --big none --out "$b\bench_ds720.json"
& $py tools\ooo_perf_bench.py "$b\ds4k"  --imgsz 640 --big none --out "$b\bench_ds4k.json"

# L2 端到端（每配置一次；key 见 --list）
& $py tools\ooo_perf_run.py "$b\ds720" --key D_full --imgsz 640 --batch 8 --out "$b\run720_D_full.json"

# L3 独占耗时归因（自研递归剖析器）
& $py _perf_review\instrument2.py "$b\ds720" --key D_full --imgsz 640 --batch 8 --out "$b\excl720_D_full.json"

# L4 定向验证
& $py _perf_review\tile_probe.py  "$b\ds4k"  --imgsz 640 --out "$b\probe4k.json"
& $py _perf_review\noisebench.py
& $py _perf_review\fix_probe.py   "$b\ds720" --key D_full --imgsz 640 --batch 8 --repeats 3
& $py _perf_review\scale.py       "$b\ds720" --key B_slice_all --grouped 1 --workers 2
& $py _perf_review\compute.py     --imgsz 320,640 --batch 4

# 汇总所有 JSON → 控制台表格
& $py _perf_review\agg.py
```

**原始数据文件**：`bench_ds720.json` `bench_ds4k.json`、`run720_*.json`(10)、`run4k_*.json`(6)、`excl720_*.json`(4)、`excl4k_*.json`(2)、`sc_*.json`(8)、`probe720.json` `probe4k.json`、`noisebench.log`、`fix_probe_D_full.json`、`compute.json`，以及汇总表 `agg.txt`。

---

## 附录 A. 修复落地与实测结果（2026-09-16 同日完成）

本报告的 P0/P1 清单已按顺序落地。代码变更的完整说明见 `更新说明.md` §15；此处只记录**修复后的实测结论**，以及一处**对报告本身的纠正**。

### A.1 落地范围

| 项 | 状态 | 说明 |
|---|---|---|
| P0-1 训练切 GPU | ✅ | `device='cpu'` → `device=0` |
| P0-2 `workers≤2` + 预取治理 | ✅ | `workers=2`、`prefetch_factor=2`；新增 `prefetch_factor` 配置键（默认 4 = stock 值） |
| P0-3 修工具口径 + grouped 生效 | ✅ | 工具改为 `install()` 后取 `build_dataloader`；同一命令现报告 `GroupedImageSampler` |
| P1-1 origin 段走 LRU | ✅ | 最大单点收益，见 A.3 |
| P1-2 去 4K 冗余 ROI 拷贝 | ✅ | tile 改为视图；LRU 别名守卫同步升级 |
| P1-3 大图 LRU 预算 | ⚠️ 仅可观测性 | 默认值未改（本机内存不允许），改为打印有效常驻帧数 |
| P1-4 noise 换 int16 核 | ✅ | −14%，σ 实测一致 |
| P2-1 保持 `axis_aligned` | ✅ 无需改动 | 默认已是 `True` |
| P2-2/P2-4/P2-5 | ⏸ 未做 | 见 A.5 |

### A.2 验收前提：修复前后**逐像素一致**

两处修复都是删除工作，唯一风险是"产出变了"。因此重建修复前实现并逐数组比对（`_perf_review/verify_fixes.py`）：

| 检查 | 1280×720 | 4000×3000 |
|---|---|---|
| 切片槽位逐像素差异 | **0 / 240** | **0 / 48** |
| origin 逐像素差异（含 `ori_shape`/`resized_shape`） | **0 / 60** | **0 / 12** |
| 交给下游的数组别名 LRU（身份或视图） | **0 / 300** | **0 / 60** |

即：**输出不变，契约不变**。

### A.3 真实收益（同进程配对，而非跨会话对比）

先把修复在**同一进程内回退**再配对测量，每个变体由一对基线夹住；两次基线的重复性极好（4K 切片例：46.270 / 46.202 ms/项）：

| 场景 | 修复全开 | 全部回退 | **整体** | origin 修复 | emit 修复 |
|---|---|---|---|---|---|
| 720p `D_full`（train.py 同款） | 32.14 ms/项 | 35.09 | **1.09×** | 1.12× | 1.02× |
| 4K `B_slice_all` | 46.24 ms/项 | 135.28 | **2.93×** | 1.51× | 1.52× |
| 4K `D_full` | 93.08 ms/项 | 165.56 | **1.78×** | 1.55× | 1.10× |

与 §3.3 / §3.4 的机理预测吻合：**origin 修复两种尺寸都有效**；**emit 修复只在大图有意义**（720p 的 tile 太小，实测仅 0.06 ms/张）。

### A.4 ⚠️ 对本报告一处数据的纠正

本报告 §4 与本节 A.3 的前身（跨会话对比）曾给出 1.60× / 1.76× / 2.75× / 3.71×。**这些数字被机器状态污染**——内存占用在 0.4~3 GB 之间波动，而这是本机的主导变量（§6）。同场景的真实收益是 A.3 的 **1.09×**。

**教训**：在本机（或任何内存受限机器）上，跨会话的端到端对比**不能**用于验收 <2× 的改动。必须把被比较的两个版本放到**同一进程内交替运行**。这条已经记入 `_perf_review/` 的方法论与项目长期记忆。

### A.5 仍未处理（按优先级）

| 项 | 为什么没做 |
|---|---|
| **P2-5 池膨胀治理**（调 `*_ratio` / 关分支） | 收益最大且线性，但**改变训练语义**，属"值不值"的取舍，需你决定 |
| P2-2 变换链与计算重叠 | 已由 `workers=2` 部分实现；单独裁剪 HSV/Format 未做 |
| P2-4 只读分支 `copy=False` 扩展 | 需逐分支确认不写输入，收益约为 4K 下 10 ms/项 |
| P1-3 调大默认 LRU 预算 | 本机内存不支持，需在内存充裕的机器上重新定标 |
| `val_slice_*` 墙钟拆分 | 只量了**结构倍数**（见 A.6），两遍验证的耗时未单独插桩 |

### A.6 补测：验证侧开销（原报告 §6 列为"未覆盖"）

只读标注与几何、不解码的结构计数（`_perf_review/valslice_cost.py`）：

| 配置 | 每轮样本 | 相对整图 | ×2（双口径） |
|---|---|---|---|
| `val_slice_all_tiles=True, ratio=1.0` | 12（整图 3） | 4.0× | **8.0×** |
| `val_slice_all_tiles=True, ratio=0.5` ← `train.py` 当前 | 9 | 3.0× | **6.0×** |
| `val_slice_all_tiles=False` | 3 | 1.0× | 2.0× |

**`train.py` 当前的验证开销是朴素整图验证的 6 倍。** 对短 epoch / 大验证集场景，这可能是被低估的主要开销之一——建议按"是否需要整图口径参考"决定是否保留 `val_slice_dual_metric`。

