# 项目说明：ultralytics_ooo 即插即用增强包

> 本仓库 = **干净 Ultralytics 8.4.126 原版** + **零侵入增强包 `ultralytics_ooo`**。
> 一行 `install()` 把「在线数据增强 + 混合样本池 + 修补续训 + 切片验证 + 双口径 mAP」装到原版上，**不修改上游任何源码、不改上游 `default.yaml`**。

---

## 一、这个项目是什么

原本这些能力是深度侵入改造进 Ultralytics 源码的（改 `data/base.py`、`data/augment.py`、`engine/trainer.py`、`models/yolo/detect/val.py`、`cfg/default.yaml`）。本项目把它们**提取为独立包**，用「子类化 + 运行时 monkey-patch」挂到干净原版上：

- **不改上游源码**：所有改动发生在运行时对象上，不落盘。
- **不改 `default.yaml`**：新增超参在 `install()` 时运行时注册到 `DEFAULT_CFG`。
- **可整体关闭**：不传任何开关，训练/验证**行为**与上游一致（池恰为 N 张原图、段布局退化为 `[0, N, 0, 0, 0, 0, 0]`，损失/指标/样本内容逐字节相同）。注意这是"行为一致"而不是"内部状态一致"：`self.ims` 是空的（池内样本走 raw LRU，这是既定的性能取舍），`cache='ram'` 因此对池内样本无效并会告警（见 `更新说明.md` 第二十节 §20.2/§20.4）。
- **自包含仓库**：克隆后即得到「原版源码 + 增强包」，可直接跑测试与训练。

## 二、仓库结构

```
ultralytics-main/                 # git 仓库根（= 干净 8.4.126 原版源码树）
├── ultralytics/                  # 干净原版 Ultralytics 源码（未修改）
├── ultralytics_ooo/              # ★ 提取出的即插即用包
│   ├── installer.py              # install() 唯一入口
│   ├── dataset_class.py          # InstalledYOLODataset（顶层，Windows spawn 多 worker 可 pickle）
│   ├── README.md                 # 包内说明（上手/开关表/兼容性）
│   ├── core/                     # 纯函数：退化算子、切片几何、unicode-safe 保存（零依赖）
│   └── pool/
│       ├── dataset.py            # OnlinePoolDataset（混合样本池 7 区段）
│       ├── augment_setup.py      # OnlineSlice + 在线感知 v8_transforms
│       ├── sampler.py            # GroupedImageSampler + build_dataloader 接线
│       ├── resume.py             # resume_extend_epochs 修补续训
│       ├── valslice.py           # SliceValDataset + DetectionValidator patch
│       ├── dual.py               # val_slice_dual_metric 双口径 + best_whole.pt / last_whole.pt + 收尾 strip
│       ├── fraction.py           # fraction 舍入为 0 的兜底
│       └── constants.py          # 在线超参默认表（仅项目自有键）
├── tests/
│   ├── test_ooo_core.py          # core 纯函数自校 + 零依赖边界
│   ├── test_ooo_augment.py       # OnlineSlice / v8_transforms
│   ├── test_ooo_guards.py        # 切片几何不退化 / 构造期校验 / only=k 等价 / 配置表不撞上游
│   ├── test_ooo_sampler.py       # 分组采样种子与 loader 属性对齐上游
│   ├── test_ooo_branches.py      # 临时数据集端到端：7 区段 + ratio 定长切分 + ratio=1 逐位兼容 + 遮挡剔除
│   ├── test_ooo_resume.py        # 修补续训
│   ├── test_ooo_valslice.py      # 切片验证
│   └── test_ooo_dual.py          # 双口径跨 epoch 不退化为整图口径 + 整图 checkpoint 家族与 strip
├── .github/workflows/ooo-quality.yml  # lint + 本包测试的 CI 门禁
├── tools/
│   ├── ooo_slice_knobs.py        # 切片 5 旋钮的实际产出诊断（槽位密度 / 空片 / 整图，含 --grid 扫 bg）
│   ├── ooo_pool_measure.py       # 各段真实槽位 / 每图占位 / 与旧布局倍率
│   ├── ooo_ab_ratios.py          # *_ratio 行为对照模板（干跑打印实际选中数，--train 跑对照）
│   ├── ooo_layout_snapshot.py    # "索引 → 样本"逐位快照与比对（布局改动的回归守卫）
│   └── ooo_compat_check.py       # 换 Ultralytics 版本/环境时的迁移自检（符号 / 签名 / 补丁生效 / 端到端冒烟）
├── weights/                      # 本地权重（gitignored）
├── runs/                         # 训练输出（gitignored）
└── 项目说明.md                    # 本文件
```


## 三、功能清单（均在 8.4.126 上端到端验证）

| 功能 | 效果 | 关键开关 |
|---|---|---|
| **在线切片增强** | 训练时在线 SAHI 切片，标签同步重映射；支持目标感知切缝/背景占比约束 | `slice_prob`, `slice_overlap_ratio`, `slice_center_bias`, `slice_background_ratio` |
| **混合样本池** | 每张原图每 epoch 动态扩展成多个训练样本（7 区段，**段长由 ratio 决定**，见「二·补」） | 各 `*_keep` 开关 + 各 `*_ratio` |
| **每轮选中数日志** | 主进程每 epoch 一行打印各分支实际选中的原图数（`slice 4/8 (ratio 0.5) \| ...`），配置与行为不一致时立刻可见 | 自动 |
| **宽高比填充** | 把图 pad 到目标比例，新增一个池区段 | `ratio_pad_keep`, `ratio_pad_target` |
| **运动模糊** | 轴对齐/旋转运动模糊，逐张限幅 | `blur_keep`, `degrade_max_side` |
| **在线合成** | 每 4 图合成一张，逐张限幅 | `compose_keep`, `compose_max_side` |
| **气象退化** | 雨/雾/噪声 | `weather_keep`, `weather_types` |
| **遮挡退化** | 矩形/条带遮挡 | `occlusion_keep`, `occlusion_types` |
| **修补续训** | resume 已训完的 ckpt 时自动修补元数据，续到更长总轮数 | `resume_extend_epochs` |
| **切片验证** (SAHI eval) | val 大图切子块推理，预测 remap 回原图 + 类别级 NMS 融合 | `val_slice_enable`, `val_slice_overlap_ratio`, `val_slice_nms_iou` |
| **双口径 mAP** | 每轮切片(主)+整图(副)跑两遍；主 fitness→`best.pt`/`last.pt`，副 fitness→`best_whole.pt`/`last_whole.pt` | `val_slice_dual_metric` |
| **分组采样** | 同一原图所有子样本连续进出，raw LRU 命中、避免重复解码 | `slice_grouped_sampler`（默认开） |
| **fraction 兜底** | fraction 舍入为 0 的小数据集自动保留源图，不再 IndexError | 自动生效 |

## 四、三步上手

```python
# 1. 在本仓库环境里，一行安装（改运行时对象，不改源码）
from ultralytics_ooo import install
install()

# 2. 像平常一样训练，多传几个开关；不传则完全等于上游行为
from ultralytics import YOLO
model = YOLO("yolo11n.pt")
model.train(
    data="coco8.yaml",
    epochs=100, imgsz=640, batch=16, workers=8,
    slice_prob=True, img_origin=True,
    blur_keep=True, ratio_pad_keep=True, compose_keep=True,
    weather_keep=True, occlusion_keep=True,
    val_slice_enable=True, val_slice_overlap_ratio=0.2,
    val_slice_dual_metric=True,
)

# 3. 续训延长：一个已跑完的 last.pt 直接续到 200 轮
model.train(resume="path/to/last.pt", resume_extend_epochs=200)
```

> Windows 多 worker 训练脚本需有 `if __name__ == "__main__":` 保护（spawn 要求）。

## 五、工作原理（install() 做的事）

全部运行时 monkey-patch / 协作多继承，零侵入：

1. `build.YOLODataset` → `InstalledYOLODataset(OnlinePoolDataset, YOLODataset)`（MRO 融合）
2. `dataset.v8_transforms` → 在线增强感知版（装配 `OnlineSlice`）
3. `BaseTrainer`：`on_train_epoch_start` 回调发布 `set_epoch`（数据集由 **`trainer.train_loader`** 解析）；`resume_training`/`check_resume` 实现修补续训；`validate`/`save_model` 实现双口径，`final_eval` 把整图 checkpoint 一并 strip
4. `DetectionValidator`：`val_slice_enable` 时用 `SliceValDataset` 包装 val loader 并融合 tile 预测
5. `build_dataloader`：单机训练时改用 `GroupedImageSampler`
6. `get_img_files`：fraction 舍入为 0 时回退保留源图

## 六、验证状态（在干净 8.4.126 上）

- 单元 / 集成测试：`pytest tests/ -k ooo -q` → **200 项通过**（全部离线，不下载数据与权重；分布：guards 96 / branches 34 / target_tiles 12 / dual 15 / val_dataset 8 / augment 8 / perf_fixes 8 / sampler 7 / valslice 5 / resume 4 / core 3）
  - 其中 `val_dataset` 8 项锁定"**val 构建（`augment=False`）必须与上游逐字节一致**"：池布局在 val 侧不成立（`v8_transforms` 不运行 → 各段全 0），若让 `len(val)` 走池布局会得到 0，进而被 `build_dataloader` 的 `batch = min(batch, len(dataset))` 改写成 `batch=0` 而崩溃。详见 `更新说明.md` 第十二节。
- 真实训练（`_mini_val_set`，8 训练图 / 3 验证图，320px，2 epoch，CPU，`img_origin=True` + 全分支 + `val_slice_dual_metric=True`）：

  | arm | ratio | workers | close_aug_epoch | 池长（段长 sahi/origin/ratio/blur/compose/weather/occlusion） | 日志核对 |
  |---|---|---|---|---|---|
  | legacy | 1.0 | 0 | 0 | 82 (`32,8,8,16,2,8,8`) | `slice all/8`… |
  | intended | 0.1 | 0 | 1 | 17 (`11,1,1,2,0,1,1`) | `slice 1/8 (ratio 0.1)`…`compose 0/2 <-- NONE`；最后一轮带 `[close_aug_epoch: …]` |
  | mw2 | 0.5 | 2 | 1 | 45 (`20,4,4,8,1,4,4`) | 同上，末轮 `close_aug_epoch` 生效 |

- 每轮 `augment masks` 与 `raw-image LRU … % hit` 均正常打印；分组采样命中率随池规模变化（240 图池 `slice_raw_cache_size=16` 实测 63–77%，受 Mosaic 窗口上限约束，详见 `性能审查报告.md` §4.3）
- 多 worker：`workers=2` 训练干净（Windows spawn，`_mp_epoch` 跨进程选择集同步）
- 修补续训：1 轮 ckpt + `resume_extend_epochs=3` 自动续训成功
- 双口径：`val_slice_dual_metric=True` 一轮产出 `best.pt` + `last.pt` + `best_whole.pt` + `last_whole.pt`，四个都在 `final_eval` 里被 strip 成 fp16 推理快照
- fraction 兜底：`fraction=0.01` 触发 guard 并保留 4 图
- 高级切片：`center_bias/center_constraint/full_box_only/background_ratio` 训练干净
- 代码质量治理：完成一轮全量审查与修复，详见 `代码质量审查报告.md` 与 `更新说明.md`
- 样本池治理：完成 ratio 定长切分 + `keep_origin` 收窄，详见 `样本池膨胀分析与方案.md`
- 性能治理：完成一轮"从零重做"的性能审查并按**优先级顺序**落地 P0/P1（训练切 GPU、`workers≤2` 与 `prefetch_factor` 可配置、修掉让自带工具永远测不到分组采样器的导入顺序缺陷、`origin` 段改走 LRU、切片 tile 由连续拷贝改为视图、气象 noise 换 int16）
  - 验收口径：修复为**删除工作**，因此先证明**逐像素等价 + LRU 契约不变**，再用**同进程配对**测收益（4K 切片配置 **2.93×**、4K 全池 **1.78×**、720p **1.09×**）
  - 详见 `性能审查报告-全面重审.md`（含附录 A）与 `更新说明.md` 第十五、十六节

## 七、与原深度侵入版的差异（有意为之）

| 项 | 原改造版 | 本包 |
|---|---|---|
| 安装方式 | 直接改源码 | `install()` 运行时 patch |
| 新超参注册 | 写进 `default.yaml` + 类型校验 | 运行时注册到 `DEFAULT_CFG`，不改 yaml |
| `_hyp_get` 缺键行为 | 缺键即 raise（强制三处齐动） | 包内默认表静默兜底 |
| 上游版本 | 8.4.137 | 8.4.126（干净原版） |

这些差异是「零侵入」目标的必要取舍：不为挂载功能而修改上游 `default.yaml`，代价是新键不经过上游 CLI 类型校验。

## 八、边界与限制

- 仅支持 `task=detect` 的切片验证分支；`segment/pose/obb` 的 val 切片未接入。
- 未在其他 8.4.x 小版本逐一枚举验证；与版本相关的构造差异由 `_compat` 运行时按签名过滤。
- `best_whole.pt` / `last_whole.pt` 仅作与整图基线公平对比的参考产物；resume/早停/续训仍走切片主口径 `last.pt`。`last_whole.pt` 与 `last.pt` **内容等价**（权重逐元素相同、键集合相同、716 个 zip 条目载荷完全相同），但**不是逐字节相同**——`strip_optimizer` 用 `torch.save` 重新序列化，其 zip 容器把归档名（文件名）写进每个条目前缀，任何两个 checkpoint 都如此（上游自己的 `best.pt`/`last.pt` 也是从同一份字节写出、strip 后哈希不同）。比较这两个文件请比内容或 `results.csv` 里的指标，不要比哈希。

## 九、关键问题解析
### 1. 样本池布局：`*_keep` 占不占位，`*_ratio` 定段长

| 段 | 长度 | 说明 |
|---|---|---|
| sahi（内部名 `base`） | `n_per*K_slice` | 只有被 `slice_ratio` 选中的图占位，各占 `n_per` 个（`slice_all_tiles=True` 时 `n_per=4`，否则 1）；**未被选中的图在 sahi 段不占任何位** |
| origin | `N`（`img_origin=True`）/ `0`（关闭） | `img_origin`：为**每张原图**恒定补 1 个整图槽（取代旧的 `slice_keep_origin`，后者只覆盖被切片选中的图） |
| ratio / weather / occlusion | `K_x` | 每张**被选中**的原图 1 个位 |
| blur | `2*K_blur` | 每张被选中的原图 2 个位（短+长，同命运） |
| compose | `K_compose` | 每个**被选中的组** 1 个位（组 = 连续 4 张原图） |

`K_x = round(x_ratio × count_x)`，`count_x` = 原图数 N（compose 为 `ceil(N/4)` 组）。

- **降低 ratio 真的会缩小池子**：未被选中的图不占这个区段的位，因此没有"位保留、内容退回原图"造成的重复膨胀。要彻底去掉某条分支 → 关它的 `*_keep`。
- **`ratio >= 1` 时布局逐位退化为旧的满宽形状**，可直接复现旧实验；该不变量由 `test_ooo_branches.py` 锁死。
- **池长全程恒定**：`K_x` 只由配置决定，每 epoch 只换"选中了哪几张图"，不动段宽 —— mosaic buffer 索引、`nb`/`nw`、sampler units 都按开始时的池长预计算。
- **收尾期（`close_aug_epoch`）不动段宽**：只让每条分支直接输出原图，段长/池长全程不变。
  但**选择集仍然每轮真实抽签**：一个 epoch 能看见哪些图 = 各分支选择集的并集，所以若在收尾窗口里把选择集写成固定前缀 `range(K)`（历史行为），N=8 / ratio 0.5 时**只覆盖 4 张图、另 4 张完全不出现且每轮相同**。
  该缺陷已修（第十一轮，见 `更新说明.md` 第十九节）：删掉那段短路，覆盖恢复 8/8 且子集随 epoch 轮换。
  另外这 5 条分支里只有 sahi 与 compose 产出**真原图**，其余 4 条走 `_degrade_frame` 的工作分辨率上限（二次重采样），这是有意的性能取舍，未改。
- 实测（N=40，六 ratio 全 0.1）：池 `410 → 77`（**5.32× 更小**），原图回退占比 `91% → ~0%`。

### 2. 六条分支的 `*_ratio` 抽的是同一批原图吗？

**不是 —— 每条分支各抽各的，六次抽取的结果互不相同。**

机制（`pool/dataset.py:353`、`381`、`404`）：

```python
rng = random.Random(f"{self._mask_seed}:{epoch}:{epochs}")     # 每 epoch 重抽
for attr, ratio_attr, on, count in self._mask_specs(n):
    k   = int(round(x * count))
    sel = sorted(rng.sample(range(count), k))                  # 每分支一次独立 sample
```

- **不是六条独立 RNG，而是共用一个 RNG 顺序抽。** 集合互不相同、随机重叠，但严格说不是统计独立 —— 正因如此，`k == 0` 时也必须调用 `sample()`，否则会平移后面所有分支的名单。
- **`compose` 是组级**，不是图级：`count = ceil(N/4)` 组，一组展开 4 张连续原图，所以它一选中就锁定 4 张。
- **每 epoch 全部重抽**，种子只由 `(mask_seed, epoch, epochs)` 决定，与全局 `random` 流无关 → 主进程与每个 worker 各自重建出**完全相同**的名单，无需跨进程传递。

实测（`ratio = 0.5`；探针 `%TEMP%\ooo_gate\sel_overlap.py`）：

| N | sahi | ratio | blur | compose | weather | occlusion |
|---|---|---|---|---|---|---|
| 8 | `2,3,4,7` | `0,3,6,7` | `1,2,3,4` | 组 `0` → `0,1,2,3` | `0,1,3,5` | `1,2,3,7` |
| 16 | `0,1,5,6,7,11,12,14` | `1,2,3,4,6,7,10,13` | `1,4,6,7,9,10,11,15` | 组 `0,2` → `0,1,2,3,8,9,10,11` | `4,5,7,9,10,11,12,14` | `1,3,5,7,8,10,13,15` |

两两交集只有 1–3 张（N=8）/ 3–5 张（N=16），而各支各抽 4 张 / 8 张；**没有任何两个分支抽到同一批**（identical pairs = NONE）。同一分支跨 epoch 也在换，例如 N=8 的 sahi：`[2,3,4,7] → [4,5,6,7] → [1,4,5,7] → [0,1,3,4] → [0,3,6,7] → [0,1,4,7]`。

**由此推出一个容易误判的结论。** 图级分支的并集覆盖期望是

```
E[池内原图数] = N · (1 − (1 − r)^B)      B = 开启的图级分支数
```

| B | 分支组合 | N=8 实测 | 期望 | N=16 实测 | 期望 |
|---|---|---|---|---|---|
| 1 | slice | 4.00 | 4.00 | 8.00 | 8.00 |
| 2 | +weather | 6.30 | 6.00 | 12.45 | 12.00 |
| 3 | +occlusion | 7.35 | 7.00 | 14.30 | 14.00 |
| 5 | +ratio, blur | 7.90 | 7.75 | 15.25 | 15.50 |

`*_ratio` 全设 0.5 时并集几乎打满 —— 20 个 epoch 的缺席统计：N=8 只有 1 个 epoch 出现 1 张（均值 0.05），N=16 有 6 个 epoch 各 1 张（均值 0.30）。**也就是说，"每支只增强一半"并不等于"一半的图被丢弃"**：每支各自漏掉的那半，会被别的分支捞走。

配套澄清一个易混点：训练日志 `Online augment: 25 training samples from 8 images` 里的 `8` 是**数据集图数**（`self.ni = len(self.labels)`，`ultralytics/data/base.py:120`），**不是**池内去重后的图数。

> 若要让六条分支共享同一批名单（全局一次抽签），需要新增共享门控；可行性与改动点见 `丢弃未选中图片-可行性分析报告.md`。