# ultralytics_ooo 从零独立验证报告

> **修复状态（2026-09-17 后续更新）**：本报告列出的问题已按推荐开始修复，实现细节与验收证据见
> `更新说明.md` 第二十节。已修：**S1**（mosaic buffer 记账语义与上游对齐，8/8 样本摘要恢复与上游一致）、
> **S2**（双口径下 `results.png` 落回 run 目录）、**M1**（`cache='ram'` / `cache='disk'` 旁路告警）、
> **M5**（舍入告警文案）、**L1**（occlusion 标签长度防御）、**L3**（resume 日志措辞）。
> 有意未改：**M4**（`degrade_max_side` 默认值是增强强度参数而非缺陷，改动会让历史实验不可比，留作 A/B 决策）、
> **M2**（`.npy` 旁路，改为文档说明）、**M3**（双口径验证倍数，改为文档标注）、**L2**（见下方更正）、
> **L4/L5**（行为安全，仅补文档）。
> 下方 §2 的问题清单保留**修复前**的原始证据，未回改——它是这些修复的验收基线。

- 验证时间：2026-09-17
- 仓库根：`C:\Users\Administrator\Desktop\ultralytics-main`
- 环境：conda `D:\miniconda3\envs\langchain`（Python 3.12.13 / torch 2.5.1 / OpenCV 5.0.0 / CPU）
- 上游版本：`ultralytics 8.4.126`（仓库内 `ultralytics/`，未被修改）
- 原则：不信任文档自述，一切结论以**本报告复现实验**为准
- 我**没有修改任何 `ultralytics_ooo/` 或 `ultralytics/` 源码**；新增文件只有 `_verify_probe/*.py`、`_verify_out/*`、`runs/exp-4/`

---

## 0. 方法与证据清单

| 手段 | 产物 | 结果 |
|---|---|---|
| 静态通读 4.7k 行（`ultralytics_ooo/**/*.py` + patch 点上游源码） | 数据流/patch 点表（见 §1） | — |
| 自带迁移自检 | `_verify_out/compat_check.txt` | **63/63 PASS** |
| 自带单测 | `_verify_out/pytest_ooo.txt` | **198 passed** |
| P1/P1b 零侵入对照（2 进程 + sys.modules 净化） | `_verify_out/p1_*.json`、`p1b_*.json` | **不等价（见 S1）** |
| P2 全索引分流枚举（4 套配置） | `_verify_out/p2.json`、`p2b.log` | 通过 |
| P3 切片像素级/坐标/close_aug_epoch 边界/舍入 | `_verify_out/p3.json` | 通过（含 1 处文案缺陷） |
| P4 验证侧重映射往返 + results.png 存活 A/B | `_verify_out/p4.json` | 重映射通过；**results.png 丢失** |
| P5 零依赖/边界数据集/cache='ram'/resume 守卫 | `_verify_out/p5.json` | 通过（含 1 项旁路） |
| P6/P7 分支 CPU 归因 + 冷缓存解码次数配对 A/B | `_verify_out/p6.json`、`p7.json` | 通过 |
| P8 重复安装幂等（purge sys.modules 后重装） | `_verify_out/p8.json` | 通过 |
| 真实训练 `python train.py`（2 epoch，配置与 `train.py` 完全一致） | `_verify_out/train_run.log`、`runs/exp-4/` | 通过 |

复现命令：

```powershell
$py = "D:\miniconda3\envs\langchain\python.exe"
& $py tools\ooo_compat_check.py
& $py -m pytest tests/test_ooo_*.py -q
& $py _verify_probe\p1_zero_intrusion.py upstream  _verify_out\p1_upstream.json
& $py _verify_probe\p1_zero_intrusion.py installed _verify_out\p1_installed.json
& $py _verify_probe\p2_dispatch.py ; & $py _verify_probe\p3_slice_remap_edges.py
& $py _verify_probe\p4_valremap_plot.py ; & $py _verify_probe\p5_contract_edges.py
& $py _verify_probe\p6_perf_resume.py ; & $py _verify_probe\p7_decodes.py
& $py _verify_probe\p8_reinstall.py
& $py train.py
```

---

## 1. 数据流与 patch 点表（静态结论）

```
YOLO.train()
  └─ DetectionTrainer._build_train_pipeline()
       ├─ build_yolo_dataset()  ── build.py:264  `dataset = YOLODataset`   ← ★patch A: 模块级名字重绑
       │     └─ InstalledYOLODataset(OnlinePoolDataset, YOLODataset).__init__
       │           └─ super() → YOLODataset.build_transforms()  ── dataset.py:317 `v8_transforms(...)` ← ★patch B
       │                 └─ ultralytics_ooo.pool.augment_setup.v8_transforms
       │                       ├─ 把 ~67 个在线键从 hyp 镜像到 dataset（slice_*/ratio_*/blur_*/weather_*/
       │                       │   occlusion_*/compose_*/img_origin/*_ratio/close_aug_epoch/prefetch_factor…）
       │                       └─ 构造 OnlineSlice(p=slice_prob) 并挂到 dataset.slice_transform
       ├─ build_dataloader()    ── ★patch C: 单机 + shuffle → GroupedImageSampler
       ├─ get_validator() → DetectionValidator   ── ★patch D: __init__/update_metrics/finalize_metrics/
       │                                             gather_stats/get_dataloader（SliceValDataset）
       ├─ callbacks["on_train_epoch_start"] += _ooo_set_epoch  ── ★patch E（marker 幂等）
       └─ BaseTrainer       ── ★patch F: check_resume / resume_training
                            ── ★patch G: validate / save_model / final_eval (+ plot_results)
       └─ BaseDataset.get_img_files  ── ★patch H: fraction 归零守卫

样本尺度：BaseDataset.__getitem__ → transforms(OnlinePoolDataset.get_image_and_label(index))
  段布局（唯一真源 _segment_lengths / _mask_specs）：
    [base: n_per*K_slice) [origin: N·img_origin) [ratio: K_ratio) [blur: 2*K_blur)
    [compose: K_compose)  [weather: K_weather)   [occlusion: K_occlusion)
  解码：_load_image_cached（per-worker LRU 16 帧 / 256 MiB）→ 各分支 → _finalize_label
  记账：_touch_buffer(expanded_index) → dataset.buffer → Mosaic.get_indexes 抽混样伙伴
```

上游符号存在性（逐一核对，非猜测）：

| 符号 | 位置 | 存在 |
|---|---|---|
| `build.YOLODataset`（模块级） | `data/build.py:264` | ✓ |
| `dataset.v8_transforms`（模块级，dataset.py:32 导入 + :317 调用） | `data/dataset.py:32,317` | ✓ |
| `build_dataloader` / `RANK` / `get_torch_device_backend` / `seed_worker` | `data/build.py:40,42,230,318` | ✓ |
| `BaseTrainer.train_loader`（**无** `train_dataloader`） | `engine/trainer.py:286` | ✓ |
| `BaseTrainer.{validate,save_model,final_eval,check_resume,resume_training}` | `trainer.py:859,712,941,960,1063` | ✓ |
| `LetterBox` 的 `ratio_pad` 组合、`scale_boxes(..., ratio_pad=)` | `augment.py:1849-1855`、`detect/val.py:449-459` | ✓ |

---

## 2. 问题清单

### 🔴 严重

#### S1 零侵入契约不成立：`dataset.buffer`（mosaic 混样池）记账语义与上游不同

**问题描述**：不传任何 ooo 开关时，安装后仍与上游**不等价**。

**触发条件**：`install()` + 未设置任何 `slice_*` / `*_keep`（即"看起来完全惰性"的配置）。因为 `img_origin` 默认 `True`，全池 N 个槽全部走 `_build_origin_sample`。

**证据**：

1. 上游 `load_image` 只在**解码未命中**时写 buffer，命中直接 `return self.ims[i], ...`：
   `ultralytics/data/base.py:230 if im is None:` … `:270 self.buffer.append(i)` … `:278 return self.ims[i], ...`
   本包 `_build_origin_sample` **无条件**写：
   `ultralytics_ooo/pool/dataset.py:1402 self._touch_buffer(index)`（定义见 `:889-898`）
2. 对照实验（`_verify_probe/p1b_buffer_cause.py`，同种子逐样本采样 8 张图）：

   | 采样点 | 上游 `buffer_before` | 安装后 `buffer_before` |
   |---|---|---|
   | i=1 | `[0]` | `[0,0,0,0]` |
   | i=4 | `[0,1,2,3]` | `[1,0,0,3,2,0,1]` |
   | i=7 | `[0,1,2,3,4,5,6]` | `[5,4,5,6,0,0,6]` |

   `Mosaic.get_indexes()` 正是从这里抽伙伴（`augment.py:497-498`）。
3. 内容级后果（`p1_zero_intrusion.py`，`random.seed(1000+i)` 固定）：8 个样本 **7 个 sha256 不同**，且 `n_box` 已变（i=3 上游 4 / 安装后 3；i=6 上游 2 / 安装后 1）。
4. 旁证：`self.ims` 常驻帧数 上游 **7** / 安装后 **0**（origin 段走 `_load_image_cached`，`load_image` 的 memoise 分支永不执行）。
5. **根因确证**：把 `_build_origin_sample` 换成"仅未命中才写 buffer"的等价实现后（`p1b` 的 `installed_fixed`），8/8 摘要与上游**完全一致**。
6. 既有测试的断言是假的：`tests/test_ooo_branches.py:690 assert ds._extended_pool_on() is False, "no segment extends the pool -> stock buffer bookkeeping"` —— 该 flag 只影响 `load_image` 内部分支，而该路径根本没被走到。

**影响**：
- 文档/installer 的"无开关即逐字节上游"承诺不成立；拿本包做的 A/B 实验与历史上游实验**不可比**。
- mosaic 伙伴池被同一张图的重复项挤占（上表 7 个槽只有 4 个不同图），等效改变类别/尺度分布。

**修复建议**：让 `_load_image_cached` 返回 miss 标志，仅 miss 时 `_touch_buffer(index)`；或在 origin/分支路径复用上游 `self.ims` 的驻留判定。修完后 `_verify_probe/p1b` 的 `installed_fixed` 模式即为验收基线（要求 8/8 digest 与上游一致）。

---

#### S2 `val_slice_dual_metric=True` 时 `results.png` 静默丢失

**问题描述**：`_patch_plot_results` 把 `plot_results` 重定向到临时 CSV，而上游把图保存到 `Path(file).parent`，于是图落进临时目录并被删除。

**证据**：
- 包装代码：`ultralytics_ooo/pool/dual.py:58-65`
  ```python
  with tempfile.TemporaryDirectory(prefix="ooo_plot_") as tmpdir:
      tmp = Path(tmpdir) / "results.csv"
      df.select(keep).write_csv(tmp)
      return _orig(file=str(tmp), on_plot=on_plot)     # ← file 在临时目录
  ```
- 上游落盘位置：`ultralytics/utils/plotting.py:1095 fname = save_dir / "results.png"`，`save_dir = Path(file).parent`（`:1064`）
- A/B 实验（`_verify_probe/p4_valremap_plot.py` §E）：

  | 调用 | `results.png` |
  |---|---|
  | 打了 dual 包装的 `plot_results(file=<真实 results.csv>)` | **不存在**（目录里仍只有 `results.csv`） |
  | 手工剥掉 `whole_*` 列后调上游原函数 | 存在 |

- 真实 run 旁证：`runs/exp-4/` 有 `BoxF1_curve.png / BoxPR_curve.png / BoxP_curve.png / BoxR_curve.png / confusion_matrix*.png`，**唯独没有 `results.png`**（`runs/exp`、`runs/exp-3` 同样缺失）。

**影响**：每次双口径训练都缺训练曲线图，且无任何告警。

**修复建议**：把过滤后的 CSV 写到**真实 save_dir 下**的临时名（如 `results_no_whole.csv`）再传给它，事后删除；或直接自己 `savefig` 到 `Path(file).parent / "results.png"`。

---

### 🟠 中等

#### M1 `cache='ram'` 被静默旁路（且 `slice_prob=0` 时不降级、不告警）

- 证据：`_verify_probe/p5_contract_edges.py` §H —— 安装后 `cache='ram'`、`ims` 已预载 4 帧，一次全量 pass 仍发生 **4 次真实解码**（上游为 0）。
- 原因：训练侧所有样本都走 `_load_image_cached`（`dataset.py:1431`），它不读 `self.ims`；且 `self.ims` 存的是 **imgsz 缩略帧**，语义上确实无法复用。
- 旁路告警只在 `cache=='ram' and slice_prob>0` 触发（`dataset.py:203-208`），所以默认 `slice_prob=0` 时用户付了 RAM 却拿不到加速且无提示。
- 建议：`cache='ram'` 且扩展池开启时告警"RAM 缓存对训练样本池无效"；或实现原始分辨率 RAM 缓存。

#### M2 预烘焙 `.npy` 对训练池完全失效

- `dataset.py:1394` 起 origin 段与全部分支都绕过 `load_image`，因而绕过 `*.npy` 磁盘缓存（文档已承认，但影响面比自述更大：`img_origin=True` 时**所有**训练样本都走这条路）。
- 建议：至少在检测到 `npy_files` 存在时给出一次性提示。

#### M3 双口径把验证开销放大 (n_tiles + n_val)/n_val 倍

- 结构上必然：`validate()` 先跑切片主通道（`_orig_validate`），再 `_run_whole` 跑整图第二通道（`dual.py:133-156`）。
- `train.py` 实测（`_verify_out/train_run.log`）：每个 epoch 打印两张验证表（0.755/0.231 与 0.176/0.070），耗时 0.7s + 0.7s。
- 本配置 `val_slice_ratio=0.5` + `val_slice_all_tiles=True` → 6 tile + 3 整图 = **9** vs 上游 3 → **3×**；若 `val_slice_ratio=1.0` 则 12+3=15 → **5×**。
- 建议：文档明确标出倍数；或在 dual 模式下对验证集切分比例设上限。

#### M4 在线退化的工作分辨率过高（真实瓶颈所在）

- 冷缓存整 epoch 的分支耗时归因（`_verify_probe/p6_perf_resume.py`，25 槽池，imgsz=320）：

  | 分支 | 秒 | 占 epoch |
  |---|---|---|
  | `_build_weather_sample` | 0.197 | **29.9 %** |
  | `_build_occlusion_sample` | 0.157 | **23.8 %** |
  | `_build_blur_sample` | 0.143 | **21.7 %** |
  | `_build_ratio_sample` | 0.053 | 8.0 % |
  | `_base_slice`（切片本体） | 0.005 | **0.7 %** |
  | `_build_compose_sample` | 0.002 | 0.2 % |
  | epoch 合计 | 0.660 s / 25 样本 | — |

- 原因：`degrade_max_side=0` → auto = `2*imgsz = 640`，而最终目标是 320 —— 退化算子在 **4 倍像素**上跑完再缩到 1/4。
- 建议：把 auto 从 `2*imgsz` 收到 ≈`1.25*imgsz`（或按算子区分：weather-noise 最吃面积）。这条收益远大于继续优化切片路径。

#### M5 banker's rounding 的告警文案在 `ratio*count == 0.5` 时是错的

- 证据（`_verify_probe/p3_slice_remap_edges.py` §D）：`round(0.5) == 0`、`round(1.5) == 2`；1000 点扫描 K 对 x **单调无回退**；500 组 (count, ratio) 中 446 组落在精确 .5 上，其中 12 组被 banker's 规则向下取。
- 但 `dataset.py:474-478` 的告警建议是"Raise `{ratio_attr}` so that `{ratio_attr} x {count} >= 0.5`" —— 在恰好 0.5 时不成立。
- 真实后果（`p5` §G）：4 张图的数据集上 `compose_ratio=0.5` → 组数 `ceil(4/4)=1` → `round(0.5)=0` → compose 段长 **0**，一条合成样本都不产生。
- 另注：`_ratio_K`（布局）与抽签（`_rebuild_epoch_masks`）用**同一表达式** `int(round(x*count))`，所以池长与选择集不会漂移 —— 这点是对的。
- 建议：文案改为 `> 0.5`；并对 `0 < x*count < 1` 追加一句"该比例在 N 张图上恒为 0 槽"。

---

### 🟡 轻微

| 编号 | 问题 | 证据 | 建议 |
|---|---|---|---|
| L1 | `_build_occlusion_sample` 的 cls 过滤无长度校验：`cls.reshape(-1,1)[keep]` 在 `len(cls) != len(bboxes)` 时抛 `IndexError`（segments 路径有告警保护，cls 路径没有） | `dataset.py:1742` vs `:1751-1755` | 与 segments 一致：长度不符时告警并按位置对齐 |
| ~~L2~~ | ~~`_save_annotated` 的 `_save_state[branch]` 去重集合与 `OnlineSlice._saved_keys` 跨 epoch 只增不减~~ **本条已更正为误报** | 两处去重键分别是 `(branch, tier, index)` 与 `(img_index, k)`（`augment_setup.py:320,351`），按数据集规模有界（前者 ≤ `2 x 池长`，后者 ≤ `4 x N`），不是无界增长 | 无需修改；保留更正以说明"外部验证的结论同样需要被验证" |
| L3 | resume 的 `patience` 修复只重写 ckpt 内保存的 `train_args`；本轮 `EarlyStopping` 在 `resume_training` **之前**（`trainer.py:417` vs `:418`）就按 `self.args.patience` 构造，所以本轮早停不受该修复影响 | `_verify_probe/p6` §resume（`train_args.patience 3 → 7` 已改写，但 stopper 已建） | 日志文案改成"已修补 checkpoint 元数据（供下次续训读取）" |
| L4 | `grouped_sample_units` 由 `_ensure_epoch_sel()` 用 `epochs=None`（种子串 `…:-1`）构建，与 trainer 首次 `set_epoch(0, epochs)` 的抽签**不同** | `dataset.py:754-758`、`362` | 只影响 locality 提示（置换性质仍成立），可在文档补充 |
| L5 | epoch 0 每个 worker（本机 workers=0 故不触发）的前 ≤15 个样本使用"全选哨兵"而非真实抽签；文档只写了"至多用上一轮掩码"，未覆盖"第一轮没有上一轮" | `dataset.py:806-812` | 在 worker 首次同步前先做一次强制同步 |

---

## 3. 验证通过项（逐项给证据，均为本机实测）

1. **索引分流与段边界**（`p2.json`，4 套配置 × 全索引枚举）：0 异常；`sum(_segment_lengths()) == len(ds)`；`grouped_sample_units()` 是 `0..n-1` 的置换；`GroupedImageSampler.__iter__` 也是置换；`(branch,k,long)` 组合全部命中且越界为 0（`_base_slot` 的 `index//n_per / index%n_per` 与 `_tile_units` 两条路径都对）。
   - `train_py_exact`：`len=25`，段 `[4,0,4,8,1,4,4]`，base 4 槽解析为 `(img,k)=[(0,0),(4,1),(1,0),(3,0)]` —— 4 张互不相同的图，target-tile 调度生效。
   - `all_ratio_1_no_origin`（`slice_all_tiles=True`）：base=32=4×8，`(img,k)` = `(0,0),(0,1),(0,2),(0,3),(1,0)…` 与 `index//4, index%4` 完全一致。
2. **增强后 bboxes 与 cls 长度一致**：P2 中每个样本都校验 `len(bboxes)==len(cls)`、`len(segments)==len(bboxes)`、框尺寸为正、中心 ∈[0,1]，**0 例违例**（唯一告警是我探针自身把 CHW 张量前两维当成 H/W，已修正后重跑）。
3. **切片坐标重映射无 off-by-one / 越界**（`p3.json` §A）：
   - 4 个 tile 的像素内容与独立计算的 `img[y0:y1, x0:x1]` **逐像素 `array_equal` 全 True**；
   - 标签为子图归一化 xywh 时 max|diff| ≤ **4.0e-8**（float32 精度）；
   - `slice_background_ratio=0` 时空片正确回退**整图**而非空片（`quota_zero_falls_back_to_original = True`）。
4. **验证侧坐标重映射正确**（`p4.json` §B）：走真实 `SliceValDataset → LetterBox → Format` 产出，再用上游 `scale_boxes` 逆变换 + `_remap_boxes` 回原图坐标，与独立计算的"原框裁剪到 tile"对比，**max err = 1.1e-4 px**（12 个 tile 全覆盖，含空 tile）。`ratio_pad` 形如 `((0.2778,0.2778),(0,70))`，与 `LetterBox` 的 `gain`/`left,top` 组合一致。
5. **双口径确实用关切片的 loader**（代码 + 实测双证）：
   - `dual.py:104 vargs.val_slice_enable = False` → `valslice.py:363-378` 的 `sliced=False` 分支不包装 `SliceValDataset`，直接 `build_dataset(mode="val")`；
   - `finally` 恢复 `v.dataloader`（`dual.py:120-122`），保证下一轮主通道仍是切片 loader；
   - 实测 `runs/exp-4/results.csv` 同 epoch 同时存在 `metrics/mAP50(B)=0.755` 与 `whole_metrics/metrics/mAP50(B)=0.17625`，且日志打印两张独立验证表。
6. **`best_whole.pt` 由 whole fitness 驱动**：`validate()` 用显式 `_whole_improved` 标志（`dual.py:150-155`），`save_model()` 据此复制 `last.pt` 字节（`:176-179`），避免了"浮点相等"在并列时误覆盖。实测两个文件均产出并被 `final_eval` strip（日志 `Optimizer stripped from …\last_whole.pt / best_whole.pt`）。
7. **`resume_extend_epochs`**（`p6.json` §resume + 代码核对）：
   - 守卫 1：`extend <= finished` → `ValueError: resume_extend_epochs=2 必须大于 checkpoint 已完成轮数 5`；
   - 守卫 2：无法识别已完成轮数 → `ValueError: 无法从 Checkpoint 中检测已完成的 Epoch 数`；
   - 成功路径实测：`train_args/args` 的 `epochs 5→12`、`patience 3→7` 均被改写；`ckpt["epoch"]` 从 `-1` 回滚到 `finished-1=4`；`self.epochs = self.args.epochs = extend`；`_setup_scheduler()` 调用 1 次。
   - LR：`_setup_scheduler` 用 `self.epochs` 重建 `lr_lambda`（`trainer.py:253-256`），`_setup_train:419` 再把 `scheduler.last_epoch = start_epoch-1`，因此新总轮数下的 LR 曲线正确。
   - **是否覆盖用户 `epochs`**：是 —— `resume_extend_epochs > 0` 时 `epochs` 被改写为新总轮数；不传 `resume_extend_epochs` 时 `check_resume` 会用 ckpt 里的 `epochs` 覆盖用户传的 `epochs`（上游 whitelist 不含 `epochs`），这是一个需要注意的上游固有行为。
8. **`close_aug_epoch` 边界**（`p3.json` §C，可证伪：直接给算子打计数器）：
   - `epochs=2, close=1`：epoch0 非关闭（blur 68 / weather 24 / occlusion 29 / ratio_pad 33 / slice 39 次调用）；epoch1 关闭且**全部算子调用数为 0**；
   - `close=2`/`close=3`（≥epochs）→ 所有 epoch 关闭；`close=0` → 从不关闭；`epochs=4, close=1` → epoch2 非关闭、epoch3 关闭。
9. **配置一致性**：真实 train.py 日志 `augment masks @ epoch 0: slice 4/8 | ratio 4/8 | blur 4/8 | compose 1/2 | weather 4/8 | occlusion 4/8` —— 六条 ratio 全部按 0.5 生效，**不存在"配置说 10% 实际 100%"**。默认值与 `train.py` 开关一致（`img_origin=False`、`slice_prob=True`、`slice_ratio=0.5`、`slice_all_tiles=False`、`slice_target_tiles=True`、`close_aug_epoch=1`、`val_slice_dual_metric=True`）。
10. **性能面**：
    - 分组采样确实减少解码（冷缓存，配对 A/B，`p7.json`）：`slice_raw_cache_size=4` 时 grouped 中位 **10** 次 vs shuffled **27.5** 次；cache=16/32 时两者都降到 **8 = N**（本 mini 集整集可驻留，符合项目文档自述的"pool 不大于缓存时 grouping 无可观测收益"）。
    - raw LRU 命中率：真实 run **91.8%**（98 reads），探针 **94.0%**（399 reads），与文档 0.90–0.95 一致。
    - 内存：`workers=0`（CPU 强制，`trainer.py:164-165`），`degrade_max_side` 生效使峰值可控；本机 8 张 960×540 图未构成压力。**大规模/多 worker 内存结论本次无法外推**。
11. **零侵入契约（包结构部分）**：`import ultralytics_ooo.core` 与 `import ultralytics_ooo` 之后 `sys.modules` 里**没有** `ultralytics*`（`p5.json` §F，子进程验证）。核心 `core/` 只依赖 numpy/cv2/stdlib，成立。
12. **`install()` 幂等**（`p8.json`）：purge `sys.modules` 后再次 `install()` → `on_train_epoch_start` 里仍只有 **1** 个带 `_ooo_epoch_callback` 标记的回调；对 stub trainer 跑一遍 callback，`set_epoch` 恰好被调用 **1** 次。marker 机制（`installer.py:105-107`）有效。
13. **patch 目标符号真实存在**（见 §1 表），`tools/ooo_compat_check.py` 的 A/B/C/D 四组 63 项全过。
14. **既有测试**：`tests/test_ooo_*.py` 198 项全过（5.8 s）。

---

## 4. 总体结论

**能否用于生产训练**
- **核心数据管线逻辑是可信的**：索引分流、段边界、切片几何/标签重映射、验证侧坐标往返、池长恒定、双口径产物、`close_aug_epoch` 边界、`resume_extend_epochs` 守卫与元数据修补，都拿到了本机可复现的正面证据。
- **但我不建议在"与上游等价/可插拔"这一前提下直接上生产**：S1 使"无开关即惰性"的承诺失效且实测改变 mosaic 混样分布，S2 每次双口径训练静默丢 `results.png`。两者都属"不报错但行为/产物变了"的静默类缺陷。
- 若必须现在投入生产，最小动作是：修 S1（唯一会影响训练分布的一项）+ 修 S2（产物完整性），其余按 M1–M5 排期。

**最大风险点**
1. **S1**：mosaic 伙伴池语义偏移，直接改变增强分布；会让"本包 vs 上游"的对照实验结论不可信。
2. **零侵入设计的固有风险**（本包已自认，但风险等级仍然最高）：`build.YOLODataset` / `dataset.v8_transforms` / `trainer.train_loader` 这类**模块级名字重绑**，在上游改名/挪模块时**不抛异常、不留 traceback**，只是功能不再发生。`tools/ooo_compat_check.py` 是当前唯一有效兜底，请在每次升级上游后作为准入动作。
3. **M4**：真正的时间花在本包自己新增的退化算子上，而当前公共认知里的"瓶颈"（切片/解码）只占 0.7%。

**值得优化处（按收益排序）**
1. `degrade_max_side` 默认 auto 从 `2*imgsz` 收到 ≈`1.25*imgsz`（M4，退化三算子合计 ~75% 分支耗时，收益随像素面积平方式下降）。
2. S1 的 buffer 记账修正（正确性优先）。
3. `cache='ram'` 语义对齐或显式告警（M1）。
4. 双口径验证成本（M3）：文档标注倍数，或对 `val_slice_ratio` 设上限。
5. 二次验证的第二遍 `v(self)` 会重建 `init_metrics` 并重跑 `print_results`（日志里每 epoch 两张表）；若只想要指标，可考虑复用第一遍的 loader 对象以减少 `build_dataset` 的标签扫描（当前已缓存 loader，成本主要在推理本身）。

**性能瓶颈所在（实测归因）**
- 训练侧样本构造：**在线退化算子**（weather 29.9% / occlusion 23.8% / blur 21.7%），其次是上游 `Mosaic`+`RandomPerspective`（占剩余 44%），**切片本体仅 0.7%**。
- 解码：`slice_raw_cache_size=16` + 分组采样下命中率 91.8–94.0%，解码已非瓶颈；把 `slice_raw_cache_size` 降到 4 会让解码次数从 10 升到 ~27.5（×2.75）。
- 验证侧：`val_slice_enable + all_tiles + dual_metric` 使单轮验证成本约为上游的 **3×**（本配置）。
- 算力仍是第一杠杆（沿用项目既有实测：yolo26n imgsz=640 时 CPU/GPU 单步 1853.9 ms vs 49.1 ms，37.7×）；本报告的所有 CPU 占比结论只适用于"设备为 CPU"的语境。

---

## 5. 本次未覆盖 / 结论不可外推的部分（诚实标注）

- **多 worker（>0）路径**：本机 CPU 被上游强制 `workers=0`（`trainer.py:164-165`），因此 `_sync_epoch_masks` 的共享内存同步、每 worker 独立 LRU 命中率、`_sync_every=16` 的摊销误差都**未在真实多进程下实测**（L5 只是静态推导）。GPU 或显式 `workers>0` 的机器上应补测。
- **`resume_extend_epochs` 的端到端实跑**：只做了 stub 级单元验证 + 完整代码路径核对（含 LR 重建与 `scheduler.last_epoch` 复位），未跑"真实续训到更长轮数"的完整训练。
- **大规模数据集**：全部动态实验基于 8 张训练图 / 3 张验证图。任何与"数据集规模"耦合的结论（解码次数、LRU 容量是否够、内存随池增长）都不能从这里外推；`p7` 里 cache=16 时 grouped 与 shuffled 无差异正是规模不足的直接表现。
- **GPU / AMP / DDP / rect / obb / segment / pose**：未测。`rect=True` 与在线增强互斥、`use_obb=True` 同理，代码里有门（`augment_setup.py:832`），但运行时行为未验证。
- **`mosaic_save_*` 四个键**：确认为 no-op 且有一次告警（`augment_setup._warn_mosaic_save_is_a_noop`），未实测告警触发。
- **内存上限**：本机 7.9 GB 且常态只剩 0.4–0.7 GB，未能构造 `batch`/worker 数扫描来确定内存拐点。
