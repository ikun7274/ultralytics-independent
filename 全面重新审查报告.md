# ultralytics_ooo 全面重新审查报告（从零独立审查）

> 审查对象：`C:\Users\Administrator\Desktop\ultralytics-main`（`ultralytics_ooo/` 即插即用增强包，宿主为干净 Ultralytics 8.4.126）
> 审查方式：**不依赖此前任何结论**，以「读源码 → 跑测试 → 跑训练 → 跑基准 → 交叉验证文档」的实证路线重建判断
> 审查日期：2026-09-15
> 测试环境：conda `langchain`（Python 3.12.13 / torch 2.5.1 / opencv 5.0.0.93），CPU，物理内存约 8 GB（可用 ~1–2 GB）

---

## 0. 结论速览

| 维度 | 判定 |
|---|---|
| 文档声称的核心功能 | **基本全部落地**（切片 / 混合池 / 双口径 / 分组采样 / 修补续训 / 每-epoch 重采样 / 退化限幅 / SAHI 验证），且经测试 + 端到端训练双向验证 |
| 文档与实现的一致性 | **存在 3 处实质性不一致**：`slice_keep_origin`→`img_origin` 未同步、测试数自相矛盾（134/146/153）、CI lint 门禁**实际是红的** |
| 未被文档承认的无效配置 | `mosaic_save_*` 4 个键为**死配置**（永不生效） |
| 性能 | 关键路径可优化，**最大单点开销是原始图全分辨率 memcpy（4000×3000 ≈ 20 ms/次）**；但该开销在当前架构下是**必要**的（见 §2.3 纠错） |
| 总体 | 工程质量高、注释密度罕见地好；**无正确性阻断缺陷**，但 P0 级「工程门禁与文档」问题必须处理 |

**P0（2 项）**：CI lint 门禁已失败（20 errors）＋ 文档三处自相矛盾声明。→ 见 §3

---

> ## ⚠️ 修复进展（2026-09-15 更新，详见 `更新说明.md` 第十节）
>
> 本报告的发现已按下述优先级处理完毕。**正文保留为当日审查快照，其中的数字不做改写**（历史陈述保持历史），本节是唯一的状态入口：
>
> | 编号 | 问题 | 状态 |
> |---|---|---|
> | P0-1 | CI lint 门禁 20 errors（8 BLE001 / 5 I001 / 2 UP037 / 2 B010 / 2 F841 / 1 E731） | ✅ **已归零**；并顺手把 8 个 `tests/test_ooo_*.py` 的 28 条也清零，门禁**扩容覆盖本包自己的测试**（原先完全未覆盖），旧的 advisory 步骤随之移除 |
> | P0-2 | 测试数 134 / 146 / 153 三处自相矛盾 | ✅ 统一为 **157 项**（`项目说明.md` / `更新说明.md` / `样本池膨胀分析与方案.md`）；157 = 原 153 + 本轮新增 4 条回归测试 |
> | P0-3 | `slice_keep_origin` 已被 `img_origin` 取代但 5 个文档仍在用旧键，且 `origin` 段宽应为 **N** | ✅ 5 个文档全量同步 |
> | P1-1 | 建议把 `slice_raw_cache_size` 从 4 调大到 8–16 | ⚠️ **该建议被推翻、又于第七轮被重新采纳** —— 两次反转的完整链条见 `性能审查报告.md` §4.3/§4.5/§9 与 `更新说明.md` 第十·5 节与第十三节。本报告当时的反证取自 **8 图池**（`max_buffer_length ≤ 池`，整库可入缓存，容量无关），**不可外推**；240 图池重测方向相反（cap16 分组 41.0 vs 不分组 34.4 items/s）。**第七轮已把默认值提到 16、删除 oversized 守卫、并新增字节预算 `slice_raw_cache_mb`。** 当前状态以 `性能审查报告.md` §9 为准 |
> | P2-1 | `mosaic_save_dir/max/annotated/exist_ok` 是死配置（上游 `Mosaic` 签名无这些参数，`_compat` 静默剔除） | ✅ 改为**一次性告警**（并指向真正有效的 `slice_save_dir`），测试锁定 |
> | P2-2 | 切片热路径每次多一次全分辨率 memcpy（4000×3000 ≈ 20 ms） | ✅ 已优化。三条**会把入参原样返回**的路径（`p` 抛硬币 / 退化尺寸 / `_emit` 的 Plan A 回退）用**身份判定**精确识别后按需拷贝；附契约测试 + 注入验证 |
> | P2-3 | `valslice` 依赖被上游 `label.pop("shape")` 移除的 `shape`，缺失时静默产出空 tile | ✅ 改为**显式整图退化 + 一次性告警**，附回归测试 |
> | P2-4 | `test_cap_long_side_self_check` 在低内存机器假红（float64 中间量 288 MiB） | ✅ 改用 `rng.integers(..., dtype=uint8)`（峰值降 8×）+ `pytest.skip` 环境门控 |
> | P3 | L13 `_geometry` bbox 重复等可读性问题 | ⏸ 保持原状（文档自认未改） |
> | **新增发现** | **`.github/`（整个 CI 工作流）、`tools/`、`ultralytics_ooo/ruff.toml`、`pool/fraction.py`、4 个测试文件（承载 139/157 用例）、5 份项目文档均未被 git 跟踪** | ❗**未处理**。含义：文档反复引用的 CI 门禁、lint 配置与诊断工具，在版本控制里**并不存在**——一旦 clone 就只有 18/157 个用例和零门禁。见第十节 §10.9 |

---

## 一、功能核对：文档描述 vs 实际实现

### 1.1 已实现且与文档一致（附验证证据）

| # | 功能 | 证据 |
|---|---|---|
| 1 | **在线切片 `OnlineSlice`**（SAHI 语义：原始分辨率 2×2+overlap 切图，再交给 loader 放大） | `pool/augment_setup.py:60+`；`tests/test_ooo_augment.py` 5 项通过；训练日志可见切片分支 |
| 2 | **混合样本池 7 区段 + ratio 定长（方案 B+）** 段长 = `*_ratio` 定长，`ratio≥1` 时逐位兼容旧布局 | `pool/dataset.py::_segment_lengths`（单一真源）；`_mini_val_set` 8 图实测池 = 45 样本 `[16,8,4,8,1,4,4]`，与 7 区段命名一一对应 |
| 3 | **池缩减 5.32×** | `python tools/ooo_pool_measure.py --n 40 --ratio 0.1` → `410 → 77`（**复现成功**），与文档一致 |
| 4 | **每-epoch 选择集重采样**（`set_epoch` 经 `trainer.train_loader` 回调发布，跨进程 `multiprocessing.Value` 同步） | `dataset.py::set_epoch/_rebuild_epoch_masks/_sync_epoch_masks`；`tests/test_ooo_branches.py::test_pool_length_is_constant_across_epochs` 通过 |
| 5 | **`close_aug_epoch` 时间维调度**（与 `close_mosaic` 同构） | `augment_setup.py:715-718`（并注明修复了「从未复制到 dataset」的旧缺陷）；训练日志中最后 N 轮退化为原图 |
| 6 | **双口径验证**（切片主口径 → `best.pt`；整图副口径 → `best_whole.pt`） | 端到端训练**实际产出** `best.pt` / `best_whole.pt` / `last.pt`，`results.csv` **含 7 个 `whole_metrics/*` 列**；`tests/test_ooo_dual.py` 9 项通过（复刻上游短路契约，连跑 3 epoch 断言主口径恒为切片） |
| 7 | **表驱动分支定义 `_mask_specs`**（单一真源，防「比例键漏镜像」） | `tests/test_ooo_branches.py::test_every_mask_ratio_knob_actually_reaches_the_dataset` **遍历该表的每一行**断言属性存在且产生掩码 —— 这是防止 S3 类静默缺陷复发的最佳实践 |
| 8 | **分组采样 `GroupedImageSampler` + 原始图 LRU**（`slice_raw_cache_size`） | `pool/sampler.py`；`tests/test_ooo_sampler.py` 7 项通过（含 loader 属性漂移对齐断言） |
| 9 | **退化算子限幅**（`degrade_max_side` / `compose_max_side`：0=auto=2*imgsz，<0=关闭） | `dataset.py:1263` 等；`tests/test_ooo_guards.py` 覆盖 |
| 10 | **SAHI 切片验证**（`SliceValDataset` + `DetectionValidator` patch + 类别级 NMS 融合） | `pool/valslice.py`；`tests/test_ooo_valslice.py` 4 项通过；`val_slice_enable=False` 时逐字节等价上游 |
| 11 | **修补续训 resume patch** | `pool/resume.py`；`tests/test_ooo_resume.py` 4 项通过 |
| 12 | **零侵入安装**（monkey-patch，幂等 marker，绝不改上游源码） | `installer.py`；`tests/test_ooo_guards.py` 含幂等性断言 |
| 13 | **`_ONLINE_DEFAULTS` 精简到 77 条「仅项目自有键」+ 自检** | 实测 `len(_ONLINE_DEFAULTS) == 77`，`check_online_defaults_are_project_only()` 运行通过（**与文档 M4 声明一致**） |
| 14 | **`onnx`/`imgsz` 等边界、`slice_geometry` 夹紧偏移保证 tile ≥1px（M8）** | `core/geometry.py`；`tests/test_ooo_core.py` 通过 |
| 15 | **L1/L2 死导入与重复实现清理** | 侧证：`ruff check` 统计中 **F401 / F811 均为 0**，与文档「已归零」一致 |

### 1.2 实现完整，但**文档滞后 / 错误**（必须修正）

#### ① `slice_keep_origin` → `img_origin`：文档全面滞后，语义已变

代码中 **不存在** `slice_keep_origin` 这一生效键：

- `pool/augment_setup.py`：仅把旧键当作 **deprecated 别名**接受并映射为 `img_origin=True`；
- 语义变化：`origin` 段宽已从 `K_slice`（仅被切片选中的图补 1 张整图）变为 **N（每张原图恒定占 1 个整图槽）**，作用范围从「切片分支」扩大到**所有增强分支**。

仍写旧键/旧语义的位置：

| 文件 | 行 | 内容 |
|---|---|---|
| `项目说明.md` | 61 | origin 段 = `K_slice`，`slice_keep_origin` |
| `项目说明.md` | 105, 132 | 示例与训练日志动词写 `slice_keep_origin=True` |
| `ultralytics_ooo/README.md` | 24, 49, 72 | 段表 + 「⑤ 补充」行 + 示例 |
| `样本池膨胀分析与方案.md` | 56, 219（§7.1 标题） | 整节以旧键命名 |
| `背景切片配额与原图回退机制说明.md` | 87 | `slice_keep_origin=False` |

而 `train.py:17,84` **已正确**使用 `img_origin` 并注明「取代旧 slice_keep_origin」。→ **文档需一次性全局替换 + 段宽改为 N 的说明**。

#### ② 测试数量声明三处自相矛盾

| 来源 | 声称 |
|---|---|
| `项目说明.md:131` | **146 项通过** |
| `更新说明.md:11, 323` | **146 项**（约 2.8 s） |
| `样本池膨胀分析与方案.md:274` | **146 项通过** |
| `代码质量审查报告.md:588` | **134 项** |
| **实测（本次）** | **153 passed, 339 deselected（4.10 s）** |

实测明细（`def test_` 计数 76 个，其余由 `@parametrize` 展开）：
`test_ooo_guards.py` 96、`test_ooo_branches.py` 25、`test_ooo_dual.py` 9、`test_ooo_sampler.py` 7、`test_ooo_augment.py` 5、`test_ooo_resume.py` 4、`test_ooo_valslice.py` 4、`test_ooo_core.py` 3。

> 补充：首轮审查时 `test_cap_long_side_self_check` 曾因**本机可用内存不足**（申请 3000×4000 float64 ≈ 275 MiB）抛 `MemoryError`；本次复跑内存充足，**153 项全通过**。→ 该用例对本机内存敏感，属**测试环境问题，非代码缺陷**，但它会让「全绿」的结论在不同机器/负载下不稳定，建议改为小尺寸或 pytest `skipif` 门控。

#### ③ CI lint 门禁**当前实际是红的**

- `更新说明.md:11` 声称「新增 lint 配置、CI 门禁」；
- `.github/workflows/ooo-quality.yml:38-39` 的 `Lint the package` 步骤跑 `ruff check ultralytics_ooo`，**无 `continue-on-error`** → 硬门禁；
- 实测（ruff 0.16.7）：**Found 20 errors**

| 规则 | 数量 | 说明 |
|---|---|---|
| BLE001 blind-except | 8 | 盲 `except Exception`（多为刻意的降级告警，但需 `# noqa` 或收窄） |
| I001 unsorted-imports | 5 | 可 `--fix` |
| UP037 quoted-annotation | 2 | 可 `--fix` |
| B010 set-attr-with-constant | 2 | 可 `--fix` |
| F841 unused-variable | 2 | 未用局部变量 |
| E731 lambda-assignment | 1 | lambda 赋值 |

`代码质量审查报告.md` 只声明「F401/F811/浮点相等全部归零」，**未覆盖** BLE/I/UP/B010/E731 —— 声明与 CI 门禁的实际覆盖面不一致。**当前 CI 无法通过。**

### 1.3 **未实现 / 无效**（文档未承认）

#### `mosaic_save_dir` / `mosaic_save_max` / `mosaic_save_annotated` / `mosaic_save_exist_ok` —— 死配置

- `pool/constants.py:109-112` 定义这 4 个键；
- `pool/augment_setup.py:688-696` 把它们传给 `Mosaic(...)`；
- 但上游 `ultralytics/data/augment.py:462` 的签名是 `Mosaic.__init__(self, dataset, imgsz=640, p=1.0, n=4)` —— **不含任何 save 参数**；
- `_compat()`（`augment_setup.py:47-55`）用 `inspect.signature` 过滤 kwargs，**静默丢弃**未知键。

结论：这 4 个键在干净上游上**永不生效**（`_compat` 的 docstring 自认它们是「fork-only debug knobs」，但本仓库的 Mosaic 是上游原版）。→ 应**删除**或在 constants 中显式标注 `NO-OP on stock Ultralytics`。

### 1.4 实现完整但**需跟进**（本次审查新识别的两点）

#### ① `self.ims` 在切片开启时不缓存 —— 是**有意设计**，但权衡未文档化

- `dataset.py:273-274` 明确注释：切片开启时**不**把图放进 `self.ims`，因为池把索引**扩展**了（`self.ims` 以原始索引寻址，与扩展索引的 mosaic buffer 是两个索引空间，混用会泄漏内存）。
- 替代方案是 `_raw_cache`（per-worker LRU，`slice_raw_cache_size`）。
- **风险点**：默认 `slice_raw_cache_size = 2`，而 `dataset.py:116` 会 `max(4, v)` → **实际默认 LRU 只有 4 张图/worker**。若用户显式设为 `0`，每个 tile 都要**重新解码原始 JPEG**，大图多 tile 场景下数据加载会立刻成为瓶颈。默认值下因分组采样把同图 tile 排在一起，通常够用。
- → 本次审查**修正了此前的猜测**：这不是无意的性能回归，而是**故意的索引空间隔离**，代价由 LRU 承担，需要在文档里把「自定 `ims` 缓存被 LRU 取代 + 建议值」写清。

#### ② `valslice` 对 `label["shape"]` 的依赖 —— 已核实**当前安全**

- `pool/valslice.py:61` 用 `lb.get("shape", (0,0))[:2]` 取原始 H/W；
- 上游 `base.py:409` 的 `label.pop("shape")` 作用在 `deepcopy` **副本**上，**不会**影响 `self.labels`；
- 唯一致命路径是 `base.py:375` 的 `set_rectangle()` 会**直接** `pop` 掉 `self.labels` 里的 `shape`；但 `valslice.py:338-340` 检测到 `args.rect` 时**先置 False 再重建 dataset**，绕开了它。
- → 结论：**当前安全**。但该安全性依赖「重建」这一隐式步骤；建议加一条断言或回归测试，防止未来改动让切片**静默退化为「全部 passthrough、完全不切片」**（不报错、指标悄悄变差）。

---

## 二、性能审查

### 2.1 关键路径耗时（微基准实测，CPU）

| 操作 | 实测耗时 | 备注 |
|---|---|---|
| 全分辨率 memcpy 4000×3000（`_load_image_cached(copy=True)`） | **≈ 20.7 ms / 次** | 每次取样本都发生 |
| `cap_long_side` 4000×3000 → 1280 | ≈ 16.5 ms | 退化限幅的开销 |
| weather noise（峰值内存 17.6 MiB） | ≈ 31.7 ms | 由 `degrade_max_side` 限幅压低 |
| 轴对齐运动模糊 | ≈ 11.4 ms | |
| 非轴对齐运动模糊 | ≈ 50.8 ms | **5.3× 慢，与文档 L4 的 5.3–5.5× 一致** |
| `_geometry(only=k)` | 省 ≈ 34% | 与文档「只算用到的 tile」一致 |
| `_segment_bases()` | ≈ 6.6 µs / call | 可忽略 |
| 带标注同步保存 1280×960 / 4000×3000 | **23.6 / 182.6 ms**（文档实测） | 原图分辨率 tile，**不受** `degrade_max_side` 约束 |

### 2.2 资源占用

- **原始图 LRU 默认只 4 张/worker**（见 §1.4①）——决定了大图数据集的内存/重解码平衡点；
- **`degrade_max_side < 0`（关闭限幅）风险**：文档实测 weather-noise 峰值从 18.4 MB → **180 MB**、dense-PSF 模糊从 52 ms → **451 ms**。代码已对此**告警一次**（L7），是正确的护栏；
- **`ims` 不再随切片无限增长**（`_remember_ims` 有上界，注释记录旧版会涨到 ~29 GB/worker，8520 图 @1280）—— 该内存泄漏已被修掉，属实质性改进。

### 2.3 瓶颈排序与可优化点（含一处**自我纠错**）

**瓶颈 1：原始图全分辨率读取 + memcpy —— 最大单点开销（4000×3000 ≈ 20 ms/次）**

- **val 侧已优化**：`valslice.py:86-107` 用 `_load_image_cached(copy=False)`，省掉每 tile 一次全分辨率 memcpy（文档：4000×3000 省 13.6 ms，`all_tiles` 下每图约 54 ms），并带三层降级。
- **train 侧仍是 `copy=True`**（`dataset.py:1824`）。**我最初的判断是「照搬 val 的优化即可白赚 20 ms」——这是错的**，必须纠正：
  `OnlineSlice.slice_at`（`augment_setup.py:531-532`）有一个 **概率性 early-return**：`if random.uniform(0,1) > self.p: return img, label` —— 它把**入参数组原样返回**，而下游 affine / mosaic 会**原地写入**该数组。所以 train 侧 `copy=True` 是**语义必需**，不能直接改 `copy=False`。
- **真正可行的优化**（带回归风险，故列 P2）：把 `copy=False` 下放到 `slice_at`，并在**只读分支**（`_emit` 一定返回新 `sub`）保持共享、在 **early-return 分支自行拷贝**。需配一条「切片后原图 LRU 未被污染」的回归测试，否则会引入极难定位的随机数据污染。
- **零风险替代**：把 `slice_raw_cache_size` 从默认（实际 4）提高到 ≥ 单图 tile 数（如 8–16），显著减少重复解码，代价是每 worker 多几张大图常驻内存。

**瓶颈 2：退化算子本身（weather 32 ms / 非对齐 blur 51 ms）** —— 已由 `degrade_max_side`/`compose_max_side` 限幅，方案正确；建议文档给出「限幅值 → 典型耗时」对照表，帮助用户选值。

**瓶颈 3：同步保存（`cv2.imencode` + 落盘，4000×3000 = 182.6 ms/次）** —— S2 只收敛了 `train.py` 的默认值（`slice_save_max=100`、各 `*_save_dir` 注释化），**包本身默认已关**，机制未变。若开启抽查，大图 + 无 `save_max` 上限时对训练吞吐影响巨大。**优化方向：改为抽样/异步（后台线程）写盘，或强制 `save_max` 上限。**

**瓶颈 4：其余已做且正确的优化（无需改）** —— val 侧 `copy=False`、`_geometry(only=k)`、`_read_orig(copy=False)`、`_load_image_cached` 的 `move_to_end` 命中刷新、`.npy` 磁盘缓存移除（实测无收益）。

---

## 三、审查结论与优先级建议

### P0 —— 必须处理（阻断 / 可信度）

| 项 | 建议动作 |
|---|---|
| **P0-1 CI lint 门禁失败** | `ruff check --fix ultralytics_ooo` 清掉 9 个可自动修项，手工处理 8×BLE001（加 `# noqa: BLE001` 或收窄为具体异常）、2×F841、1×E731；**或**若短期不修，把 `ooo-quality.yml:38-39` 该步骤临时改 `continue-on-error: true` 并在注释里写明原因。当前状态：CI 红。 |
| **P0-2 文档声明自相矛盾** | ①测试数统一为 **153**（改 `项目说明.md:131`、`更新说明.md:11,323`、`样本池膨胀分析与方案.md:274`；`代码质量审查报告.md:588` 的 134 是历史快照可保留但需加「(历史)」）；②`slice_keep_origin` 全量替换为 `img_origin` 并把 `origin` 段宽写成 **N**（5 个文件，见 §1.2①）。 |

### P1 —— 强烈建议（性能 / 资源）

| 项 | 建议动作 |
|---|---|
| **P1-1 切片路径重复解码** | 把 `slice_raw_cache_size` 默认有效值从 4 提到 **8–16**，并在 `项目说明.md` 写明「大图 + 多 tile 场景请调大」；配套给出内存代价估算。 |
| **P1-2 同步保存开销** | `slice_save_*` / `compose_save` 保持默认关闭（现状已如此）；若提供示例，强制 `save_max` 上限并注明 4000×3000 ≈ 183 ms/次；中期改为异步写盘。 |
| **P1-3 `degrade_max_side` 取值指引** | 在文档补「限幅值 → 典型耗时/峰值内存」对照表（如 18.4 MB/32 ms vs 180 MB/451 ms），避免用户误关限幅。 |

### P2 —— 建议（健壮性 / 可维护性）

| 项 | 建议动作 |
|---|---|
| **P2-1 `mosaic_save_*` 死配置** | 从 `constants.py` 删除这 4 键，或在注释中显式标注 `NO-OP on stock Ultralytics（上游 Mosaic 不接受这些参数，_compat 会静默丢弃）`。 |
| **P2-2 train 侧 memcpy 优化** | 按 §2.3 瓶颈 1 的方案，把 `copy=False` 下放到 `slice_at`，**必须**配「LRU 未被污染」回归测试。属有风险优化，收益约 20 ms/样本（4000×3000）。 |
| **P2-3 valslice 的 `shape` 依赖** | 在 `SliceValDataset.__init__` 加断言：若 `all(lb.get("shape") is None)` 则告警「切片将被静默禁用」；并补一条回归测试锁住 `rect` 重建路径。 |
| **P2-4 内存敏感测试** | 给 `test_cap_long_side_self_check` 加尺寸门控或 `skipif`，避免在低内存机器上出现「本应全绿却失败」的假红。 |

### P3 —— 可选（可读性）

| 项 | 建议动作 |
|---|---|
| **P3-1 `_geometry` bbox 转换重复** | `更新说明.md` L13 自认「未改」，理由（改动收益低于回归风险）成立，可保持现状。 |

---

## 四、审查方法说明（可复现命令）

```bash
# 环境
export PATH="/d/miniconda3/envs/langchain:/d/miniconda3/envs/langchain/Scripts:/c/Windows/System32:/c/Windows:/usr/bin:/bin:$PATH"
cd C:/Users/Administrator/Desktop/ultralytics-main

# 1) 测试基线（实测 153 passed, 339 deselected）
python -m pytest tests/ -k ooo -q

# 2) lint 门禁（实测 20 errors —— CI 会红）
ruff check ultralytics_ooo --statistics

# 3) 池规模复现（410 → 77，5.32×）
python tools/ooo_pool_measure.py --n 40 --ratio 0.1

# 4) 端到端训练（2 epoch，产出 best.pt / best_whole.pt / last.pt）
python train.py

# 5) 配置表自检（77 条，无上游键被遮蔽）
python -c "from ultralytics_ooo.pool.constants import _ONLINE_DEFAULTS, check_online_defaults_are_project_only as c; print(len(_ONLINE_DEFAULTS)); c()"
```

---

## 五、总评

这是一份**质量显著高于平均**的改造包：注释密度与「为什么这么做」的记录罕见地完整（大量注释记录了历史缺陷的成因，而非仅描述行为）；表驱动 + 遍历式测试（`_mask_specs` + `test_every_mask_ratio_knob_actually_reaches_the_dataset`）是防静默退化缺陷的正确工程手段；「零侵入 monkey-patch + 默认全关时逐字节等价上游」的隔离策略干净可靠。

**核心问题不在代码正确性，而在「工程闭环与文档同步」**：CI 门禁实际是红的、测试数三处矛盾、主开关被更名后 5 个文件仍在用旧名、以及 4 个永不生效的死配置键。这些都属于**低修复成本、高可信度收益**的项，建议优先于任何性能优化处理。

**性能上的真实瓶颈只有两个**：原始图读取/解码（受限于索引空间设计，LRU 是唯一杠杆）与同步保存（默认已关）。退化算子与几何计算的耗时都已被正确地限幅与缓存，无需进一步投入。
