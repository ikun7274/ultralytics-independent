# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Global configuration YAML with settings and hyperparameters for YOLO training, validation, prediction and export
# For documentation see https://docs.ultralytics.com/usage/cfg

task: detect # (str) YOLO task, i.e. detect, segment, semantic, depth, classify, pose, obb
mode: train # (str) YOLO mode, i.e. train, val, predict, export, track, benchmark

# Train settings -------------------------------------------------------------------------------------------------------
model: # (str, optional) path to model file, i.e. yolo26n.pt or yolo26n.yaml
data: # (str, optional) path to dataset file or classification directory/name
epochs: 100 # (int) number of epochs to train for
time: # (float, optional) max hours to train; overrides epochs if set
patience: 100 # (int) early stop after N epochs without val improvement
batch: 16 # (int | float) batch size as int (e.g. 16), or float 0.0–1.0 for AutoBatch fraction of GPU memory
imgsz: 640 # (int | list) train/val use int (square); predict/export may use [h,w]
save: True # (bool) save train checkpoints and predict results
save_period: -1 # (int) save checkpoint every N epochs; disabled if < 1
cache: False # (bool | str) cache images in RAM (True/'ram') or on 'disk' to speed dataloading; False disables
device: # (int | str | list) device: 0/[0,1] CUDA, 'npu:0' Ascend, 'xpu:0' Intel, 'cpu'/'mps', -1/[-1,-1] auto GPUs
workers: 8 # (int) dataloader workers (per RANK if DDP)
prefetch_factor: 2 # (int) 每个 worker 预取的批次数; PyTorch 默认即为 2; 在线增强开启时降到 1 可减少约一半在途内存(相比改源码, 现可直接配置)
ims_cache_frames:
  0 # (int) 每 worker 的 self.ims 整图缓存帧数上限: 0=自动(取 min(上游公式, ims_cache_mb 预算)), >0=显式帧数, <0=沿用上游公式 min(ni, batch*8, 1000)-1
  # 该缓存把 load_image 的整图件留在内存里, 省掉重复 imread; 一帧 = imgsz*imgsz*channels, 而上游公式只随 batch 增长,
  # 于是 batch=64/imgsz=1280 是 511 帧≈2.3GB/worker、imgsz=1920 是 5.3GB/worker(还要叠加原图 LRU)
  # 默认档取 min(...) 保证"只会更省内存, 绝不会比上游更费"; 代价只是超预算的帧要重新解码
  # 本键不影响增广语义与样本内容: 未命中即重新解码, 结果与命中时逐位相同(有测试按内容钉住)
ims_cache_mb:
  1024 # (int) ims_cache_frames=0(自动)时的每 worker 内存预算(MiB); <=0=视为无预算(回退上游公式); 仅在自动档生效(显式帧数档不读它)
  # 1GiB 是"不打扰常见配置"与"封顶病态配置"的折中: imgsz<=640 且 batch<=64 时上游值本就在 1GiB 以内(511 帧=599MiB),
  # 故自动档对它们逐帧无影响; 只有 imgsz>=1280 或 batch>=128 才开始生效(1280 封到 218 帧=1.0GB, 1920 封到 97 帧=1.0GB)
  # 实测(n=2000, mosaic=1.0, 上游 511 帧): 帧数 511→218 时每样本 imread 由 0.94 升到 2.15, 即用 ~1.2 次重解码换 ~344MiB/worker;
  # 若源图是 4000x3000(单次解码 ~203ms)而内存充裕, 应调大本值或直接 ims_cache_frames=-1
project: # (str, optional) project name for results root
name: # (str, optional) experiment name or export hardware target
exist_ok: False # (bool) overwrite existing 'project/name' if True
pretrained: True # (bool | str) use pretrained weights (bool) or load weights from path (str)
cls_remap: True # (bool) when fine-tuning across datasets, remap pretrained cls head rows by class-name match
optimizer: auto # (str) optimizer: SGD, MuSGD, Adam, Adamax, AdamW, NAdam, RAdam, RMSProp, or auto
verbose: True # (bool) print verbose logs
seed: 0 # (int) random seed for reproducibility
deterministic: True # (bool) enable deterministic ops; reproducible but may be slower
single_cls: False # (bool) treat all classes as a single class
rect: False # (bool) rectangular batches for train; rectangular batching for val when mode='val'
cos_lr: False # (bool) cosine learning rate scheduler
close_mosaic: 10 # (int) disable mosaic augmentation for final N epochs (0 to keep enabled)
resume: False # (bool) resume training from last checkpoint in the run dir
resume_extend_epochs: 0 # (int, 0=关闭) resume 续训自动延长: 设为 >0 时, 若 checkpoint 已完成轮数 < 该值, 自动修补 ckpt 元数据(epochs/patience)并从旧停点续训到该轮数, 无需离线跑 fix_checkpoint 脚本; 若该值 <= 已完成轮数则报错
amp: True # (bool | str) train precision: True/'fp16' (AMP check), 'bf16', or False/'fp32'; AMP uses FP16 train validation
fraction: 1.0 # (float | int | list) train ratio/count or split values; 1 = all, integer >1 = count, test 0 = none
profile: False # (bool) profile ONNX/TensorRT speeds during training for loggers
freeze: # (int | list, optional) freeze first N layers (int), or specific layer indices or module names like "23.cv2" (list)
multi_scale: 0.0 # (float) multi-scale range as a fraction of imgsz; sizes are rounded to stride multiples
compile: False # (bool | str) enable torch.compile() backend='inductor'; True="default", False=off, or "default|reduce-overhead|max-autotune-no-cudagraphs"
channels_last: # (bool, optional) auto-enable for CUDA training on PyTorch 1.11+ and supported x86 CPU inference

# Segmentation
overlap_mask: True # (bool) merge instance masks into one mask during training (segment only)
mask_ratio: 4 # (int) mask downsample ratio (segment only)

# Classification
dropout: 0.0 # (float) dropout for classification head (classify only)

# Val/Test settings ----------------------------------------------------------------------------------------------------
val: True # (bool) run validation/testing during training
split: val # (str) dataset split to evaluate: 'val', 'test' or 'train'
save_json: False # (bool) save COCO JSON (detect/segment/pose), compute detect size metrics, or save semantic PNG masks
conf: # (float, optional) confidence threshold; defaults: predict=0.25, val=0.001
iou: 0.7 # (float) IoU threshold used for NMS
max_det: 300 # (int) maximum number of detections per image
quantize: # (int|str, optional) precision: predict/val use 16/fp16 for FP16 or 32/fp32/None for FP32; export also supports 8/int8 and w8a8|w16a16|w8a16|w8a32; replaces deprecated half/int8 args
dnn: False # (bool) use OpenCV DNN for ONNX inference
plots: True # (bool) save plots and images during train/val
end2end: # (bool, optional) whether to use end2end head (YOLO26, YOLOv10) for predict/val/export

# Predict settings -----------------------------------------------------------------------------------------------------
source: # (str, optional) path/dir/URL/stream for images or videos; e.g. 'ultralytics/assets' or '0' for webcam
vid_stride: 1 # (int) read every Nth frame for video sources
stream_buffer: False # (bool) True buffers all frames; False keeps the most recent frame for low-latency streams
visualize: False # (bool) save class activation heatmaps (predict) or visualize TP/FP/FN confusion (val)
augment: False # (bool) apply test-time augmentation during prediction
agnostic_nms: False # (bool) class-agnostic NMS
classes: # (int | list[int], optional) filter by class id(s), e.g. 0 or [0,2,3]
retina_masks: False # (bool) use high-resolution segmentation masks (segment)
embed: # (list[int], optional) return feature embeddings from given layer indices

# Visualize settings ---------------------------------------------------------------------------------------------------
show: False # (bool) show images/videos in a window if supported
save_frames: False # (bool) save individual frames from video predictions
save_txt: False # (bool) save results as .txt files (xywh format)
save_conf: False # (bool) save confidence scores with results
save_crop: False # (bool) save cropped prediction regions to files
show_labels: True # (bool) draw class labels on images, e.g. 'person'
show_conf: True # (bool) draw confidence values on images, e.g. '0.99'
show_boxes: True # (bool) draw bounding boxes on images
line_width: # (int, optional) line width of boxes; auto-scales with image size if not set

# Export settings ------------------------------------------------------------------------------------------------------
format: torchscript # (str) target format, e.g. torchscript|onnx|openvino|engine|coreml|saved_model|pb|edgetpu|paddle|mnn|ncnn|imx|rknn|executorch|axelera|deepx|qnn|litert|hailo|ascend
keras: False # (bool) TF SavedModel only (format=saved_model); enable Keras layers during export
optimize: False # (bool) DEEPX only; higher compiler optimization (slower compile, faster inference)
dynamic: False # (bool) dynamic shapes for torchscript, onnx, openvino, engine, coreml; enable variable image sizes
simplify: True # (bool) supported ONNX-based exports (see export table); simplify intermediate ONNX graph
opset: # (int, optional) supported ONNX-based exports (see export table); intermediate ONNX opset version
workspace: # (float, optional) engine (TensorRT) only; workspace size in GiB, e.g. 4
nms: False # (bool) fuse NMS into exported model when backend supports; if True, conf/iou/agnostic_nms apply

# Hyperparameters ------------------------------------------------------------------------------------------------------
lr0: 0.01 # (float) initial learning rate (SGD=1e-2, Adam/AdamW=1e-3)
lrf: 0.01 # (float) final LR fraction; final LR = lr0 * lrf
momentum: 0.937 # (float) SGD momentum or Adam beta1
weight_decay: 0.0005 # (float) weight decay (L2 regularization)
warmup_epochs: 3.0 # (float) warmup epochs (fractions allowed)
warmup_momentum: 0.8 # (float) initial momentum during warmup
warmup_bias_lr: 0.1 # (float) bias learning rate during warmup
distill_model: # (str, optional) path to teacher model for knowledge distillation
dis: 6.0 # (float) distillation loss weight
box: 7.5 # (float) box loss gain
cls: 0.5 # (float) classification loss gain
cls_pw: 0.0 # (float) class weights power for handling class imbalance (0.0=disable, 1.0=full inverse frequency)
dfl: 1.5 # (float) box distance loss gain (DFL when reg_max > 1, L1 on DFL-free YOLO26)
pose: 12.0 # (float) pose loss gain (pose tasks)
kobj: 1.0 # (float) keypoint objectness loss gain (pose tasks)
rle: 1.0 # (float) rle loss gain (pose tasks)
angle: 1.0 # (float) oriented angle loss gain (obb tasks)
dlog: 1.0 # (float) depth log (SILog) loss gain (depth tasks)
dgrad: 0.5 # (float) depth gradient loss gain (depth tasks)
dlam: 1.0 # (float) depth SILog variance focus: 1.0=scale-invariant, 0.0=log-RMSE (depth tasks)
nbs: 64 # (int) nominal batch size used for loss normalization
hsv_h: 0.015 # (float) HSV hue augmentation fraction
hsv_s: 0.7 # (float) HSV saturation augmentation fraction
hsv_v: 0.4 # (float) HSV value (brightness) augmentation fraction
degrees: 0.0 # (float) rotation degrees (+/-)
translate: 0.1 # (float) translation fraction (+/-)
scale: 0.5 # (float | tuple) scale gain (+/-) as float, or explicit (min, max) tuple
shear: 0.0 # (float) shear degrees (+/-)
perspective: 0.0 # (float) perspective fraction (0–0.001 typical)
flipud: 0.0 # (float) vertical flip probability
fliplr: 0.5 # (float) horizontal flip probability
bgr: 0.0 # (float) RGB↔BGR channel swap probability
mosaic: 1.0 # (float) mosaic augmentation probability

# ---------- SAHI 在线切片 (slice_*): 把大图在线切成 2x2 重叠子图 ----------
slice_prob: 0.0 # (float) 在线切片概率 [0,1]; 0=关闭切片; 纯放大时建议配合 mosaic=0
slice_ratio:
  1.0 # (float) 每个 epoch 随机选 round(x*N) 张原图走在线切片(原图级: 同一原图 4 片同生共死), 其余整图进池;
  # 1.0=纯切片(默认), <1.0 精确混入整图(缓解过拟合+降内存), 0=全整图(等效关闭切片); 每 epoch 重新随机
slice_raw_cache_size: 2 # (int) 在线增强 worker 内原图 LRU 缓存大小 (以原图数计); 0=关闭; 切片+原图+比例+模糊+合成会让同一张原图被 imread 多达 8~9 次, LRU 把解码降到 ~1 次/原图; 实际下限为 4 (合成分支一次要读 4 张原图)
slice_grouped_sampler:
  True # (bool) 分组采样: 同一张原图的子样本连续(单元内轮转)采样, 让上面的 LRU 真正命中; False=上游全局 shuffle
  # 关闭后同图子样本在池中平均相隔约 6.5N 个样本, 远超 LRU 容量 -> 几乎全 miss, 每张原图被重复解码 8~9 次(实测 4000x3000 单次解码 203ms vs 命中拷贝 21ms)
  # 代价: mosaic 的"近期样本池"收窄为当前单元(<=4 张原图) -> 每个 mosaic 仍是 4 张不同原图, 但同一单元内组合会重复; 需完全恢复上游 mosaic 随机性时设 False
  # 命中率每个 epoch 打印一次(LOG "raw-image LRU ... hit on N reads last epoch"), 便于确认是否生效
slice_overlap_ratio: 0.2 # (float) 相邻切片重叠比例 [0,1); 例: 原图4000x3000、重叠0.2 -> 切片2400x1800
slice_min_tile_area_ratio: 0.005 # (float) 切片块面积下界 [0,1]: 切片面积 < 原图x该值 的切片丢弃 (过小切片信息量低)
slice_min_box_retain_ratio: 0.4 # (float) 目标框保留下界 [0,1]: 被边界切到的目标, 其在切片内可见面积占原框比例 < 该值则丢弃
slice_background_ratio:
  -1 # (float) 背景切片数量 = 正样本切片数 x 该值; 负值(如-1)=全部背景都保留; 0=不要背景
  # 注意: slice_all_tiles=True下被拒的背景切片会退回原图(不再裁空片)
slice_all_tiles: False # (bool) 每张原图的 4 片子图全部参与训练 (数据集 x4, 每 epoch 全部子图都训练); False=每图随机取 1 片
slice_center_constraint: False # (bool) 目标唯一归属: 每个目标只分配给"中心所在"切片 (防同一目标被切两半重复出现在多个切片)
slice_min_center_retain_ratio: 0.6 # (float) 配合 center_constraint: 目标中心不在本片时, 若本片内可见面积占比 >= 该值仍保留; 1.0=严格只留中心片
slice_full_box_only: False # (bool) 目标必须完整落在切片内才保留; 被边界切开即过滤 (绝不保留残缺框); 开启时优先于 center_constraint
slice_center_bias: False # (bool) 目标感知切缝: 切缝按本图目标中心分布微移, 落在目标最稀疏区间, 减少目标被劈碎; 切缝偏移不改变切片数量/尺寸/重叠语义, 与 center_constraint 互补建议同开; 与验证侧固定网格无关
slice_bias_margin: 0.25 # (float) 切缝偏移窗口: 切缝只在 [margin, 1-margin] 区间内微移, 保证 tile 不过小
slice_bias_jitter: 0.05 # (float) 切缝抖动幅度 (相对原图边长): 每张原图每 epoch 只抖一次, 同一图同一 epoch 的 4 片共用同一张网格, 跨 epoch 才变化; 防切缝固定而过拟合; 0=关闭
# ---- 切片结果保存 (用于人工检查切片是否正确) ----
slice_save_dir: # (str) 切片图保存目录; 空=不保存
slice_save_max: 0 # (int) 默认保存限额: 同时作用于 [切片, ratio, blur, compose] 四类; 0=不限. 单独细调见 slice_save_max_{tile,ratio,blur,compose}
slice_save_annotated: True # (bool) 保存时是否在图上画标注框+类别
slice_save_exist_ok: True # (bool) 目录已存在时是否允许继续写入; False=抛错防覆盖
# 各分支独立保存限流 — 单独覆盖某分支时不互相挤占预算;
#       设了具体值即生效(0=不限), 未设置(None)回退到 slice_save_max.
slice_save_max_tile: # (int, optional) 切片图保存上限; 0=不限; 未设=回退 slice_save_max
slice_save_max_blur: # (int, optional) 模糊图保存上限; 0=不限; 未设=回退 slice_save_max
slice_save_max_ratio: # (int, optional) 比例填充图保存上限; 0=不限; 未设=回退 slice_save_max
slice_save_max_compose: # (int, optional) 合成图保存上限; 0=不限; 未设=回退 slice_save_max
# ---- 样本池: 每张原图额外保留 1 张未切片原图 (提供全图上下文) ----
slice_keep_origin: False # (bool, 独立开关, 需 slice_all_tiles=True) 每张原图额外保留 1 张未切片原图进样本池 (独立区段 +N, 不受 slice_prob 影响) -> 样本池 +N, 共同进入 mosaic

# ---------- 验证侧在线切片评估 (val_slice_*): 验证集按 2x2 重叠子图推理 + 坐标还原 + NMS 融合 ----------
# 训练侧已在线切片, 验证侧整图直推会因小目标被降采样而低估切片训练的收益. 开启后验证阶段把验证图切成
# 与训练一致的子图, 每子图独立推理, 子图框还原到原图坐标, 跨切片重复框 NMS 融合后与原图 GT 算 mAP (SAHI 评估).
# 默认全关 = 验证完全等价原生整图验证, 指标与上游/论文/其它工具链同口径.
# !! 开启后 metrics/mAP* 变为"切片推理口径"(由它驱动 fitness/早停/best.pt), 与整图口径不可直接比较 !!
# 成本: all_tiles=True 时每张验证图为 5 次 imgsz 前向(4 片 + 双口径整图), 验证耗时约 ×5.
val_slice_enable: False # (bool, 总开关) 验证侧切片评估; False=回归原生整图验证 (完全等价原版, 默认)
val_slice_all_tiles: False # (bool) True=每张验证图全部 2x2 子图都推理 (与训练侧 slice_all_tiles 对齐); False=每图随机 1 片 (快速验证)
val_slice_ratio: 1.0 # (float) 每轮验证随机选 round(x*N_val) 张验证图走切片, 其余整图直通(1片=整图); 1.0=全部切片; 0=全部整图(等效关闭)
val_slice_overlap_ratio: 0.2 # (float) 验证侧切片重叠比例 [0,1), 建议与训练侧 slice_overlap_ratio 一致
val_slice_nms_iou: 0.5 # (float) 跨切片重复框 NMS 融合 IoU 阈值
val_slice_dual_metric: False # (bool) 双口径: 先跑切片验证(主, 驱动 fitness/早停/best.pt), 再跑整图验证(参考), 输出 whole_* 指标并保存 best_whole.pt/last_whole.pt

# ---------- 在线增强共享参数 (blur / weather / occlusion / ratio_pad) ----------
# degrade_max_side: 退化分支的"工作分辨率上限". 这四类算子都是像素级操作(O(像素数)), 而样本随后
#   立刻被缩放到 imgsz, 在原始传感器分辨率上执行纯属浪费 —— 4000x3000 帧实测三类算子合计 11.5x 可避免
#   开销(单次 weather=noise 峰值达 824MB). 先降到该上限再退化, 并按比例缩放像素型参数(PSF 长度/失焦σ/雨线长度),
#   缩放到 imgsz 后的结果近似等价(实测 mean|diff|=2.11, corr=0.9982).
#   0=自动=2×imgsz (保留 2 倍过采样, 保证缩放时仍是降采样); >0=手动上限(像素, 长边); <0=关闭(原始分辨率退化, 旧行为).
#   切片分支不受影响: 切片必须在原分辨率上执行(小目标放大是语义要求, 而非浪费).
degrade_max_side: 0 # (int, 0=自动=2×imgsz, 默认开启) 退化分支最长边像素上限; >0=手动; <0=关闭
degrade_resample: "linear" # (str, area|linear) 退化分支限幅缩放的重采样核: area=抗锯齿更好但慢(4000x3000 单次 ~21.5ms), linear=快 3 倍但画质略软(同场景 ~7.8ms); 两者几何完全一致, 仅滤波不同

# ---------- 在线合成 (compose_*): 每 4 张原图拼 1 张 2x2 大图, 提供更大范围多目标上下文 ----------
compose_keep: False # (bool, 独立开关, 不依赖切片/keep_origin) 每 4 张原图额外合成 1 张 2x2 拼接大图进样本池 -> 样本池 +ceil(N/4)
compose_ratio: 1.0 # (float, 需 compose_keep) 每 epoch 随机选 round(x*ceil(N/4)) 组做合成(组级), 未选中组退回组内第 1 张原图(整图直通, len 恒定); 1.0=全量合成
compose_save: False # (bool, 默认关闭=不保存中间图) 需 compose_keep + 指定目录(compose_save_dir, 或回退 slice_save_dir/compose/): 保存合成大图供人工检查 (按 slice_save_annotated 画框, slice_save_max 限数量)
compose_save_dir: "" # (str, 可选, 需 compose_keep + compose_save) 合成大图自定义保存目录 (独立于 slice_save_dir); 空=默认 slice_save_dir/compose/
compose_max_side:
  0 # (int, 0=自动=2×imgsz, 默认开启) 合成 2x2 大图的最长边像素上限; 语义与 degrade_max_side 对齐: 0=自动=2×imgsz, >0=手动上限(如 2560), <0=关闭(全分辨率拼接)
  # 限幅在"拼接前": 每张原图先降到 上限/2 再分配画布, 因此画布本身不超过该上限。旧实现是"按原始分辨率拼完再整体降采样",
  # 实测 4 张 4000x3000(imgsz=640) 下耗时 92.6ms→11.9ms(7.8×)、峰值内存 141MB→7MB(20×); 标签为归一化坐标, 与像素缩放无关。
  # 注意: 该分支的限幅用 INTER_LINEAR 而非退化分支的 INTER_AREA —— 6.25 倍非整数抽样下后者走通用加权盒路径, 实测 40ms/张 vs 1.7ms/张。

# ---------- 在线比例调整 (ratio_pad_*): 在线加边框统一宽高比 (change_image_resolution 工具在线版) ----------
ratio_pad_keep: False # (bool, 独立开关, 不依赖切片/keep_origin) 每张原图额外生成 1 张"加边框对齐宽高比"的图进样本池 -> 样本池 +N
ratio_pad_ratio: 1.0 # (float, 需 ratio_pad_keep) 每 epoch 随机选 round(x*N) 张原图做比例调整(原图级), 未选中直接整图; 1.0=全量
ratio_pad_target: auto # (str: auto/4:3/16:9, 需 ratio_pad_keep) auto=4:3与16:9双向对齐, 其他比例转到最近的4:3或16:9; 4:3或16:9=把所有图统一到该比例
ratio_pad_color: black # (str: black/gray/white, 需 ratio_pad_keep) 加边框的颜色; gray=114,114,114 (与 YOLO letterbox 一致)
ratio_pad_save_dir: "" # (str, 可选, 需 ratio_pad_keep) 比例调整图保存目录 (画框/限数量/跨 epoch 去重); 空=不保存 (默认)

# ---------- 在线运动模糊 (blur_*): 每张原图生成 2 张模糊副本(短+长), 模拟无人机运动失焦, 标签不变 ----------
blur_keep: False # (bool, 独立开关, 不依赖切片/keep_origin) 每张原图额外生成 2 张运动模糊图(短+长)进样本池 -> 样本池 +2N
blur_ratio: 1.0 # (float, 需 blur_keep) 每 epoch 随机选 round(x*N) 张原图做运动模糊(原图级, 短+长同命运), 未选中整图x2; 1.0=全量
blur_short_len_min: 5 # (int|float) 短模糊(轻度, 无失焦) 的运动模糊长度下限(像素)
blur_short_len_max: 12 # (int|float) 短模糊(轻度, 无失焦) 的运动模糊长度上限(像素)
blur_long_len_min: 20 # (int|float) 长模糊(重度) 的运动模糊长度下限(像素)
blur_long_len_max: 35 # (int|float) 长模糊(重度) 的运动模糊长度上限(像素)
blur_long_defocus_sigma: 1.0 # (float) 长模糊的失焦高斯 σ 上限 [0,该值], 每张随机取; 0=长模糊不加失焦
blur_axis_aligned:
  True # (bool) 拖影方向限制为像面轴对齐(水平/垂直, 二选一随机), 而非 [0,180) 任意角; 适用于相机安装固定、运动方向在像面上恒定映射到水平或垂直的场景(车载/航拍沿航迹)
  # 这类域里"轴对齐"是更贴合的建模而非近似: 实测轴对齐的 PSF 精确等于均匀箱式, 故走 cv2.blur 的 O(1) 行程和路径, 与旧 filter2D 逐位相同(242 组 max|diff|=0), 两档合计 28.1→15.4 ms/图(1.82×)
  # 代价: 拖影方向由任意角收窄为 0°/90° 两个方向(增广分布真的变了)。需要任意角时设 False 做 A/B
blur_save_dir: "" # (str, 可选, 需 blur_keep) 模糊图保存目录 (画框/限数量/按图+档位跨 epoch 去重); 空=不保存 (默认)

# ---------- 在线气象退化 (weather_*): 每张原图生成 1 张雨/雾/噪声退化图, 提升恶劣天气鲁棒性, 标签不变 ----------
weather_keep: False # (bool, 独立开关, 不依赖切片/keep_origin) 每张原图额外生成 1 张气象退化图(类型随机)进样本池 -> 样本池 +N
weather_ratio: 0.5 # (float, 需 weather_keep) 每 epoch 随机选 round(x*N) 张原图做气象退化, 未选中整图直通; 0.3~0.6 推荐, 1.0=全量
weather_types: "rain,haze,noise" # (str) 参与随机抽样的退化类型, 逗号分隔 (可子集如 "rain,haze"); 为空时回退 haze
weather_rain_density: 0.15 # (float) 雨线密度 = 雨线数量 / max(h,w), 越大雨越密
weather_rain_length: 15.0 # (float) 雨线长度上限(像素), 每根随机取 0.5~1.0 倍
weather_haze_beta: 0.4 # (float) 雾浓度 [0,1), 越大雾越浓 (大气散射 I=J*(1-β)+A*β)
weather_noise_std: 15.0 # (float) 高斯噪声标准差(每通道独立), 模拟传感器/弱光噪点
weather_save_dir: "" # (str, 可选, 需 weather_keep) 气象退化图保存目录 (画框/限数量/按图+类型跨 epoch 去重); 空=不保存 (默认)
slice_save_max_weather: # (int|None, 可选) 气象退化图保存数量上限; 不填回退 slice_save_max; 0=不限
# ---------- 在线遮挡模拟 (occlusion_*): 语义遮挡块 (树冠/电线/阴影), 标签不变, 独立开关 + epoch 比例 ----------
occlusion_keep: False # (bool) 总开关: 每张被选中原图额外生成 1 张遮挡图进样本池 (区段 +N); 与 blur/weather 同构
occlusion_ratio: 0.5 # (float) epoch 级比例: 被选中的原图才生成遮挡图 (复用 set_epoch 掩码); 0.3~0.6 推荐
occlusion_types: rect,stripe # (str) 遮挡类型, 逗号分隔; rect=随机矩形(树冠/阴影), stripe=细长条带(电线/枝干/云影)
occlusion_blocks: 1 # (int) 每图遮挡块数 (1~3)
occlusion_size_ratio: 0.1 # (float) 单块面积上限(相对原图面积), 防目标被完全盖住
occlusion_color: auto # (str) auto=采样图像深色分位均值(融入场景); 或 black/gray 固定色
occlusion_max_cover: 0.95 # (float) 目标被遮挡面积占比 >= 该值则从标签剔除 (完全被盖住的目标=纯噪声); 1.0=标签永不变
occlusion_save_dir: "" # (str, 可选, 需 occlusion_keep) 遮挡图保存目录; 空=不保存 (默认)
slice_save_max_occlusion: # (int|None, 可选) 遮挡图保存数量上限; 不填回退 slice_save_max; 0=不限

# ---------- 训练后期关闭在线增强 (close_aug_epoch): 与 close_mosaic 同构的"时间维衰减" ----------
# 训练最后 close_aug_epoch 个 epoch 把全部在线增强 (切片/合成/比例/模糊/气象退化) 关闭, 各区段退回原图直通 (位置保留、
# 内容替换, len 恒定, 与方案A同构), 让模型在真实样本分布上微调收敛, 缓解小数据集后期过拟合到增强分布.
# 0=关闭该调度 (默认, 完全向后兼容).
close_aug_epoch: 0 # (int, 0=关闭) 训练最后 N 个 epoch 关闭全部在线增强 (类似 close_mosaic 但作用于在线切片/合成/比例/模糊/气象退化); 0 不启用

mosaic_save_dir: "" # (str) Mosaic 画布图保存目录, 用于验证 mosaic 是否启用 (空=不保存)
mosaic_save_max: 0 # (int) 最多保存的 Mosaic 画布图数量; 0=不限
mosaic_save_annotated: True # (bool) 保存的 Mosaic 画布图是否画框+类别
mosaic_save_exist_ok: True # (bool) mosaic_save_dir 已存在时是否允许继续写入 (False 抛错防覆盖)
mixup: 0.0 # (float) MixUp augmentation probability
cutmix: 0.0 # (float) CutMix augmentation probability
copy_paste: 0.0 # (float) segment/obb copy-paste fraction of eligible objects
copy_paste_mode: flip # (str) copy-paste strategy for segment/obb: flip or mixup
auto_augment: randaugment # (str) classification auto augmentation policy: randaugment, autoaugment, augmix
erasing: 0.4 # (float) random erasing probability for classification (0.0–1.0)

# Custom config.yaml ---------------------------------------------------------------------------------------------------
cfg: # (str, optional) path to a config.yaml that overrides defaults

# Tracker settings ------------------------------------------------------------------------------------------------------
tracker: tracktrack.yaml # (str) tracker config: botsort.yaml, bytetrack.yaml, ocsort.yaml, deepocsort.yaml, fasttrack.yaml, tracktrack.yaml
